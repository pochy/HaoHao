// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: todos.sql

package db

import (
	"context"

	"github.com/google/uuid"
)

const createTodo = `-- name: CreateTodo :one
INSERT INTO todos (
    tenant_id,
    created_by_user_id,
    title
) VALUES (
    $1,
    $2,
    $3
)
RETURNING
    id,
    public_id,
    tenant_id,
    created_by_user_id,
    title,
    completed,
    created_at,
    updated_at
`

type CreateTodoParams struct {
	TenantID        int64  `json:"tenant_id"`
	CreatedByUserID int64  `json:"created_by_user_id"`
	Title           string `json:"title"`
}

func (q *Queries) CreateTodo(ctx context.Context, arg CreateTodoParams) (Todo, error) {
	row := q.db.QueryRow(ctx, createTodo, arg.TenantID, arg.CreatedByUserID, arg.Title)
	var i Todo
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.TenantID,
		&i.CreatedByUserID,
		&i.Title,
		&i.Completed,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteTodoByPublicIDForTenant = `-- name: DeleteTodoByPublicIDForTenant :execrows
DELETE FROM todos
WHERE public_id = $1
  AND tenant_id = $2
`

type DeleteTodoByPublicIDForTenantParams struct {
	PublicID uuid.UUID `json:"public_id"`
	TenantID int64     `json:"tenant_id"`
}

func (q *Queries) DeleteTodoByPublicIDForTenant(ctx context.Context, arg DeleteTodoByPublicIDForTenantParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteTodoByPublicIDForTenant, arg.PublicID, arg.TenantID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getTodoByPublicIDForTenant = `-- name: GetTodoByPublicIDForTenant :one
SELECT
    id,
    public_id,
    tenant_id,
    created_by_user_id,
    title,
    completed,
    created_at,
    updated_at
FROM todos
WHERE public_id = $1
  AND tenant_id = $2
LIMIT 1
`

type GetTodoByPublicIDForTenantParams struct {
	PublicID uuid.UUID `json:"public_id"`
	TenantID int64     `json:"tenant_id"`
}

func (q *Queries) GetTodoByPublicIDForTenant(ctx context.Context, arg GetTodoByPublicIDForTenantParams) (Todo, error) {
	row := q.db.QueryRow(ctx, getTodoByPublicIDForTenant, arg.PublicID, arg.TenantID)
	var i Todo
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.TenantID,
		&i.CreatedByUserID,
		&i.Title,
		&i.Completed,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listTodosByTenantID = `-- name: ListTodosByTenantID :many
SELECT
    id,
    public_id,
    tenant_id,
    created_by_user_id,
    title,
    completed,
    created_at,
    updated_at
FROM todos
WHERE tenant_id = $1
ORDER BY created_at DESC, id DESC
`

func (q *Queries) ListTodosByTenantID(ctx context.Context, tenantID int64) ([]Todo, error) {
	rows, err := q.db.Query(ctx, listTodosByTenantID, tenantID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Todo
	for rows.Next() {
		var i Todo
		if err := rows.Scan(
			&i.ID,
			&i.PublicID,
			&i.TenantID,
			&i.CreatedByUserID,
			&i.Title,
			&i.Completed,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateTodoByPublicIDForTenant = `-- name: UpdateTodoByPublicIDForTenant :one
UPDATE todos
SET
    title = $3,
    completed = $4,
    updated_at = now()
WHERE public_id = $1
  AND tenant_id = $2
RETURNING
    id,
    public_id,
    tenant_id,
    created_by_user_id,
    title,
    completed,
    created_at,
    updated_at
`

type UpdateTodoByPublicIDForTenantParams struct {
	PublicID  uuid.UUID `json:"public_id"`
	TenantID  int64     `json:"tenant_id"`
	Title     string    `json:"title"`
	Completed bool      `json:"completed"`
}

func (q *Queries) UpdateTodoByPublicIDForTenant(ctx context.Context, arg UpdateTodoByPublicIDForTenantParams) (Todo, error) {
	row := q.db.QueryRow(ctx, updateTodoByPublicIDForTenant,
		arg.PublicID,
		arg.TenantID,
		arg.Title,
		arg.Completed,
	)
	var i Todo
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.TenantID,
		&i.CreatedByUserID,
		&i.Title,
		&i.Completed,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
