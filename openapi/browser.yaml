components:
  schemas:
    AcceptDriveShareInvitationBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/AcceptDriveShareInvitationBody.json
          format: uri
          readOnly: true
          type: string
        acceptToken:
          examples:
            - token_demo_1234567890
          type: string
      required:
        - acceptToken
      type: object
    AcceptTenantInvitationRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/AcceptTenantInvitationRequestBody.json
          format: uri
          readOnly: true
          type: string
        token:
          examples:
            - token_demo_1234567890
          type: string
      required:
        - token
      type: object
    AddDriveGroupMemberBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/AddDriveGroupMemberBody.json
          format: uri
          readOnly: true
          type: string
        userPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      required:
        - userPublicId
      type: object
    AuthSettingsBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/AuthSettingsBody.json
          format: uri
          readOnly: true
          type: string
        localPasswordLoginEnabled:
          examples:
            - true
          type: boolean
        mode:
          examples:
            - local
          type: string
        zitadel:
          $ref: "#/components/schemas/ZitadelSettingsBody"
          examples:
            - example
      required:
        - mode
        - localPasswordLoginEnabled
      type: object
    Cookie:
      additionalProperties: false
      properties:
        Domain:
          examples:
            - example
          type: string
        Expires:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        HttpOnly:
          examples:
            - true
          type: boolean
        MaxAge:
          examples:
            - 1
          format: int64
          type: integer
        Name:
          examples:
            - Acme
          type: string
        Partitioned:
          examples:
            - true
          type: boolean
        Path:
          examples:
            - example
          type: string
        Quoted:
          examples:
            - true
          type: boolean
        Raw:
          examples:
            - example
          type: string
        RawExpires:
          examples:
            - example
          type: string
        SameSite:
          examples:
            - 1
          format: int64
          type: integer
        Secure:
          examples:
            - true
          type: boolean
        Unparsed:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        Value:
          examples:
            - example
          type: string
      required:
        - Name
        - Value
        - Quoted
        - Path
        - Domain
        - Expires
        - RawExpires
        - MaxAge
        - Secure
        - HttpOnly
        - SameSite
        - Partitioned
        - Raw
        - Unparsed
      type: object
    CreateCustomerSignalBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateCustomerSignalBody.json
          format: uri
          readOnly: true
          type: string
        body:
          examples:
            - Customer asked for monthly report export.
          maxLength: 4000
          type: string
        customerName:
          examples:
            - Acme
          maxLength: 120
          minLength: 1
          type: string
        priority:
          enum:
            - low
            - medium
            - high
            - urgent
          examples:
            - medium
          type: string
        source:
          description: record の発生元または permission の由来です。
          enum:
            - support
            - sales
            - customer_success
            - research
            - internal
            - other
          examples:
            - support
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - new
            - triaged
            - planned
            - closed
          examples:
            - new
          type: string
        title:
          examples:
            - Export CSV from reports
          maxLength: 200
          minLength: 1
          type: string
      required:
        - customerName
        - title
      type: object
    CreateDriveFolderBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateDriveFolderBody.json
          format: uri
          readOnly: true
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 255
          type: string
        parentFolderPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        workspacePublicId:
          description: 対象 Drive workspace の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - name
      type: object
    CreateDriveGroupBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateDriveGroupBody.json
          format: uri
          readOnly: true
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          maxLength: 2000
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 255
          type: string
      required:
        - name
      type: object
    CreateDriveShareBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateDriveShareBody.json
          format: uri
          readOnly: true
          type: string
        role:
          description: resource に対して付与する role または tenant role code です。
          enum:
            - owner
            - editor
            - viewer
          examples:
            - owner
          type: string
        subjectPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        subjectType:
          enum:
            - user
            - group
          examples:
            - user
          type: string
      required:
        - subjectType
        - subjectPublicId
        - role
      type: object
    CreateDriveShareInvitationBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateDriveShareInvitationBody.json
          format: uri
          readOnly: true
          type: string
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        inviteeEmail:
          description: user または invitee の email address です。
          examples:
            - demo@example.com
          format: email
          type: string
        inviteeUserPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        role:
          description: resource に対して付与する role または tenant role code です。
          enum:
            - owner
            - editor
            - viewer
          examples:
            - owner
          type: string
      required:
        - inviteeEmail
        - role
      type: object
    CreateDriveShareLinkBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateDriveShareLinkBody.json
          format: uri
          readOnly: true
          type: string
        canDownload:
          description: share link または permission で download を許可するかを表します。
          examples:
            - true
          type: boolean
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        password:
          description: share link 保護などに使う secret value です。response には返しません。
          examples:
            - share-demo-passphrase
          maxLength: 256
          type: string
        role:
          description: resource に対して付与する role または tenant role code です。
          enum:
            - viewer
            - editor
          examples:
            - viewer
          type: string
      required:
        - canDownload
      type: object
    CreateDriveWorkspaceBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateDriveWorkspaceBody.json
          format: uri
          readOnly: true
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 255
          type: string
        policyOverride:
          additionalProperties: {}
          examples:
            - {}
          type: object
        storageQuotaBytes:
          examples:
            - 1048576
          format: int64
          type: integer
      required:
        - name
      type: object
    CreateTenantDataExportRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateTenantDataExportRequestBody.json
          format: uri
          readOnly: true
          type: string
        format:
          enum:
            - json
            - csv
          examples:
            - json
          type: string
      type: object
    CreateTenantInvitationRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateTenantInvitationRequestBody.json
          format: uri
          readOnly: true
          type: string
        email:
          description: user または invitee の email address です。
          examples:
            - demo@example.com
          format: email
          type: string
        roleCodes:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - - viewer
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - email
      type: object
    CreateTodoBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateTodoBody.json
          format: uri
          readOnly: true
          type: string
        title:
          examples:
            - Follow up with customer
          maxLength: 200
          type: string
      required:
        - title
      type: object
    CustomerSignalBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CustomerSignalBody.json
          format: uri
          readOnly: true
          type: string
        body:
          examples:
            - Customer asked for monthly report export.
          maxLength: 4000
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        customerName:
          examples:
            - Acme
          maxLength: 120
          minLength: 1
          type: string
        priority:
          enum:
            - low
            - medium
            - high
            - urgent
          examples:
            - medium
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        source:
          description: record の発生元または permission の由来です。
          enum:
            - support
            - sales
            - customer_success
            - research
            - internal
            - other
          examples:
            - support
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - new
            - triaged
            - planned
            - closed
          examples:
            - new
          type: string
        title:
          examples:
            - Export CSV from reports
          maxLength: 200
          minLength: 1
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - customerName
        - title
        - body
        - source
        - priority
        - status
        - createdAt
        - updatedAt
      type: object
    CustomerSignalImportJobBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CustomerSignalImportJobBody.json
          format: uri
          readOnly: true
          type: string
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorFileObjectId:
          examples:
            - 1
          format: int64
          type: integer
        errorSummary:
          examples:
            - example
          type: string
        inputFileObjectId:
          examples:
            - 1
          format: int64
          type: integer
        insertedRows:
          examples:
            - 1
          format: int32
          type: integer
        invalidRows:
          examples:
            - 1
          format: int32
          type: integer
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        totalRows:
          examples:
            - 1
          format: int32
          type: integer
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        validRows:
          examples:
            - 1
          format: int32
          type: integer
        validateOnly:
          examples:
            - true
          type: boolean
      required:
        - publicId
        - status
        - validateOnly
        - inputFileObjectId
        - totalRows
        - validRows
        - invalidRows
        - insertedRows
        - createdAt
        - updatedAt
      type: object
    CustomerSignalImportListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CustomerSignalImportListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/CustomerSignalImportJobBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    CustomerSignalImportRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CustomerSignalImportRequestBody.json
          format: uri
          readOnly: true
          type: string
        inputFilePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        validateOnly:
          examples:
            - true
          type: boolean
      required:
        - inputFilePublicId
      type: object
    CustomerSignalListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CustomerSignalListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/CustomerSignalBody"
          type:
            - array
            - "null"
        nextCursor:
          examples:
            - example
          type: string
      required:
        - items
      type: object
    CustomerSignalSavedFilterBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CustomerSignalSavedFilterBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        filters:
          additionalProperties: {}
          examples:
            - {}
          type: object
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        query:
          examples:
            - roadmap
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - name
        - query
        - filters
        - createdAt
        - updatedAt
      type: object
    CustomerSignalSavedFilterListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CustomerSignalSavedFilterListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/CustomerSignalSavedFilterBody"
          type:
            - array
            - "null"
        nextCursor:
          examples:
            - example
          type: string
      required:
        - items
      type: object
    CustomerSignalSavedFilterRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CustomerSignalSavedFilterRequestBody.json
          format: uri
          readOnly: true
          type: string
        filters:
          additionalProperties: {}
          examples:
            - {}
          type: object
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 120
          minLength: 1
          type: string
        query:
          examples:
            - roadmap
          maxLength: 200
          type: string
      required:
        - name
      type: object
    DataPipelineBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineBody.json
          format: uri
          readOnly: true
          type: string
        archivedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        disabledScheduleCount:
          examples:
            - 3
          format: int64
          type: integer
        enabledScheduleCount:
          examples:
            - 3
          format: int64
          type: integer
        latestRunAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        latestRunPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        latestRunStatus:
          examples:
            - active
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Customer cleansing
          type: string
        nextRunAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        publishedVersionId:
          examples:
            - 1
          format: int64
          type: integer
        scheduleState:
          enum:
            - enabled
            - disabled
            - none
          examples:
            - enabled
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - draft
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - name
        - description
        - status
        - createdAt
        - updatedAt
      type: object
    DataPipelineCreateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineCreateBody.json
          format: uri
          readOnly: true
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          maxLength: 2000
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 160
          type: string
      required:
        - name
      type: object
    DataPipelineDetailBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineDetailBody.json
          format: uri
          readOnly: true
          type: string
        pipeline:
          $ref: "#/components/schemas/DataPipelineBody"
          examples:
            - example
        publishedVersion:
          $ref: "#/components/schemas/DataPipelineVersionBody"
          examples:
            - example
        runs:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineRunBody"
          type:
            - array
            - "null"
        schedules:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineScheduleBody"
          type:
            - array
            - "null"
        versions:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineVersionBody"
          type:
            - array
            - "null"
      required:
        - pipeline
        - versions
        - runs
        - schedules
      type: object
    DataPipelineDraftPreviewRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineDraftPreviewRequestBody.json
          format: uri
          readOnly: true
          type: string
        graph:
          $ref: "#/components/schemas/DataPipelineGraph"
          examples:
            - example
        limit:
          description: pagination で返す最大件数です。
          examples:
            - 50
          format: int32
          maximum: 1000
          minimum: 1
          type: integer
        nodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      required:
        - graph
      type: object
    DataPipelineDraftValidationRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineDraftValidationRequestBody.json
          format: uri
          readOnly: true
          type: string
        graph:
          $ref: "#/components/schemas/DataPipelineGraph"
          examples:
            - example
      required:
        - graph
      type: object
    DataPipelineEdge:
      additionalProperties: false
      properties:
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        source:
          description: record の発生元または permission の由来です。
          examples:
            - support
          type: string
        target:
          examples:
            - example
          type: string
      required:
        - source
        - target
      type: object
    DataPipelineGraph:
      additionalProperties: false
      properties:
        edges:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineEdge"
          type:
            - array
            - "null"
        nodes:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineNode"
          type:
            - array
            - "null"
      required:
        - nodes
        - edges
      type: object
    DataPipelineGraphValidationBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineGraphValidationBody.json
          format: uri
          readOnly: true
          type: string
        nodeWarnings:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineNodeWarningBody"
          type:
            - array
            - "null"
        outputSchemas:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineOutputSchemaBody"
          type:
            - array
            - "null"
        validationSummary:
          $ref: "#/components/schemas/DataPipelineValidationSummary"
          examples:
            - example
      required:
        - validationSummary
        - outputSchemas
        - nodeWarnings
      type: object
    DataPipelineListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineBody"
          type:
            - array
            - "null"
        nextCursor:
          examples:
            - example
          type: string
      required:
        - items
      type: object
    DataPipelineNode:
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/DataPipelineNodeData"
          examples:
            - example
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        position:
          additionalProperties:
            format: double
            type: number
          examples:
            - {}
          type: object
        type:
          examples:
            - example
          type: string
      required:
        - id
        - data
      type: object
    DataPipelineNodeData:
      additionalProperties: false
      properties:
        config:
          additionalProperties: {}
          examples:
            - {}
          type: object
        label:
          examples:
            - example
          type: string
        stepType:
          examples:
            - example
          type: string
      required:
        - stepType
      type: object
    DataPipelineNodeWarningBody:
      additionalProperties: false
      properties:
        code:
          examples:
            - example
          type: string
        columns:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        configKeys:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        message:
          examples:
            - example
          type: string
        nodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        severity:
          examples:
            - example
          type: string
        stepType:
          examples:
            - example
          type: string
      required:
        - nodeId
        - stepType
        - code
        - severity
        - message
        - columns
      type: object
    DataPipelineOutputSchemaBody:
      additionalProperties: false
      properties:
        columns:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        nodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        stepType:
          examples:
            - example
          type: string
        warnings:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - nodeId
        - stepType
        - columns
      type: object
    DataPipelinePreviewBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelinePreviewBody.json
          format: uri
          readOnly: true
          type: string
        columns:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        nodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        outputSchemas:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineOutputSchemaBody"
          type:
            - array
            - "null"
        previewRows:
          examples:
            - - {}
          items:
            additionalProperties: {}
            type: object
          type:
            - array
            - "null"
        stepType:
          examples:
            - example
          type: string
      required:
        - nodeId
        - stepType
        - columns
        - previewRows
      type: object
    DataPipelinePreviewRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelinePreviewRequestBody.json
          format: uri
          readOnly: true
          type: string
        limit:
          description: pagination で返す最大件数です。
          examples:
            - 50
          format: int32
          maximum: 1000
          minimum: 1
          type: integer
        nodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      type: object
    DataPipelineReviewCommentBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineReviewCommentBody.json
          format: uri
          readOnly: true
          type: string
        authorUserId:
          examples:
            - 1
          format: int64
          type: integer
        body:
          examples:
            - Customer asked for monthly report export.
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - publicId
        - body
        - createdAt
      type: object
    DataPipelineReviewItemBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineReviewItemBody.json
          format: uri
          readOnly: true
          type: string
        assignedToUserId:
          examples:
            - 1
          format: int64
          type: integer
        comments:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineReviewCommentBody"
          type:
            - array
            - "null"
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdByUserId:
          examples:
            - 1
          format: int64
          type: integer
        decidedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        decisionComment:
          examples:
            - example
          type: string
        nodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        pipelineName:
          examples:
            - Acme
          type: string
        pipelinePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        queue:
          examples:
            - example
          type: string
        reason:
          examples:
            - - {}
          items:
            additionalProperties: {}
            type: object
          type:
            - array
            - "null"
        runId:
          examples:
            - 1
          format: int64
          type: integer
        runPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        sourceFingerprint:
          examples:
            - support
          type: string
        sourceSnapshot:
          additionalProperties: {}
          examples:
            - {}
          type: object
        status:
          description: 現在の lifecycle status です。
          enum:
            - open
            - approved
            - rejected
            - needs_changes
            - closed
          examples:
            - open
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        updatedByUserId:
          examples:
            - 1
          format: int64
          type: integer
        versionId:
          examples:
            - 1
          format: int64
          type: integer
      required:
        - publicId
        - versionId
        - runId
        - nodeId
        - queue
        - status
        - reason
        - sourceSnapshot
        - sourceFingerprint
        - createdAt
        - updatedAt
      type: object
    DataPipelineReviewItemCommentWriteBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineReviewItemCommentWriteBody.json
          format: uri
          readOnly: true
          type: string
        body:
          examples:
            - Customer asked for monthly report export.
          maxLength: 4000
          type: string
      required:
        - body
      type: object
    DataPipelineReviewItemListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineReviewItemListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineReviewItemBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DataPipelineReviewItemTransitionBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineReviewItemTransitionBody.json
          format: uri
          readOnly: true
          type: string
        comment:
          examples:
            - example
          maxLength: 4000
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - open
            - approved
            - rejected
            - needs_changes
            - closed
          examples:
            - open
          type: string
      required:
        - status
      type: object
    DataPipelineRunBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineRunBody.json
          format: uri
          readOnly: true
          type: string
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorSummary:
          examples:
            - example
          type: string
        outputWorkTableId:
          examples:
            - 1
          format: int64
          type: integer
        outputs:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineRunOutputBody"
          type:
            - array
            - "null"
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        rowCount:
          examples:
            - 3
          format: int64
          type: integer
        scheduleId:
          examples:
            - 1
          format: int64
          type: integer
        startedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - pending
          type: string
        steps:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineRunStepBody"
          type:
            - array
            - "null"
        triggerKind:
          examples:
            - manual
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        versionId:
          examples:
            - 1
          format: int64
          type: integer
      required:
        - publicId
        - versionId
        - triggerKind
        - status
        - rowCount
        - createdAt
        - updatedAt
        - steps
        - outputs
      type: object
    DataPipelineRunListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineRunListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineRunBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DataPipelineRunOutputBody:
      additionalProperties: false
      properties:
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorSummary:
          examples:
            - example
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        nodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        outputWorkTableId:
          examples:
            - 1
          format: int64
          type: integer
        rowCount:
          examples:
            - 3
          format: int64
          type: integer
        startedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - nodeId
        - status
        - rowCount
        - metadata
        - createdAt
        - updatedAt
      type: object
    DataPipelineRunStepBody:
      additionalProperties: false
      properties:
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorSample:
          examples:
            - - {}
          items:
            additionalProperties: {}
            type: object
          type:
            - array
            - "null"
        errorSummary:
          examples:
            - example
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        nodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        rowCount:
          examples:
            - 3
          format: int64
          type: integer
        startedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        stepType:
          examples:
            - example
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - nodeId
        - stepType
        - status
        - rowCount
        - errorSample
        - metadata
        - createdAt
        - updatedAt
      type: object
    DataPipelineScheduleBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineScheduleBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        enabled:
          examples:
            - true
          type: boolean
        frequency:
          enum:
            - daily
            - weekly
            - monthly
          examples:
            - daily
          type: string
        lastErrorSummary:
          examples:
            - example
          type: string
        lastRunAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        lastRunId:
          examples:
            - 1
          format: int64
          type: integer
        lastStatus:
          examples:
            - active
          type: string
        monthDay:
          examples:
            - 1
          format: int32
          maximum: 28
          minimum: 1
          type: integer
        nextRunAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        runTime:
          examples:
            - "03:00"
          type: string
        timezone:
          examples:
            - Asia/Tokyo
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        versionId:
          examples:
            - 1
          format: int64
          type: integer
        weekday:
          examples:
            - 1
          format: int32
          maximum: 7
          minimum: 1
          type: integer
      required:
        - publicId
        - versionId
        - frequency
        - timezone
        - runTime
        - enabled
        - nextRunAt
        - createdAt
        - updatedAt
      type: object
    DataPipelineScheduleListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineScheduleListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DataPipelineScheduleBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DataPipelineScheduleWriteBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineScheduleWriteBody.json
          format: uri
          readOnly: true
          type: string
        enabled:
          examples:
            - true
          type: boolean
        frequency:
          enum:
            - daily
            - weekly
            - monthly
          examples:
            - daily
          type: string
        monthDay:
          examples:
            - 1
          format: int32
          maximum: 28
          minimum: 1
          type: integer
        runTime:
          examples:
            - example
          type: string
        timezone:
          examples:
            - example
          type: string
        weekday:
          examples:
            - 1
          format: int32
          maximum: 7
          minimum: 1
          type: integer
      type: object
    DataPipelineUpdateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineUpdateBody.json
          format: uri
          readOnly: true
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          maxLength: 2000
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 160
          type: string
      required:
        - name
      type: object
    DataPipelineValidationSummary:
      additionalProperties: false
      properties:
        errors:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        valid:
          examples:
            - true
          type: boolean
      required:
        - valid
        - errors
      type: object
    DataPipelineVersionBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineVersionBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        graph:
          $ref: "#/components/schemas/DataPipelineGraph"
          examples:
            - example
        pipelineId:
          examples:
            - 1
          format: int64
          type: integer
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        publishedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - draft
          type: string
        validationSummary:
          $ref: "#/components/schemas/DataPipelineValidationSummary"
          examples:
            - example
        versionNumber:
          examples:
            - 1
          format: int32
          type: integer
      required:
        - publicId
        - pipelineId
        - versionNumber
        - status
        - graph
        - validationSummary
        - createdAt
      type: object
    DataPipelineVersionSaveBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DataPipelineVersionSaveBody.json
          format: uri
          readOnly: true
          type: string
        graph:
          $ref: "#/components/schemas/DataPipelineGraph"
          examples:
            - example
      required:
        - graph
      type: object
    DatasetBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetBody.json
          format: uri
          readOnly: true
          type: string
        byteSize:
          examples:
            - 104857600
          format: int64
          type: integer
        columns:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetColumnBody"
          type:
            - array
            - "null"
        contentType:
          examples:
            - text/csv
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorSummary:
          examples:
            - example
          type: string
        importJob:
          $ref: "#/components/schemas/DatasetImportJobBody"
          examples:
            - example
        importedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        latestSyncJob:
          $ref: "#/components/schemas/DatasetSyncJobBody"
          examples:
            - example
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Customers
          type: string
        originalFilename:
          examples:
            - customers.csv
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        rawDatabase:
          examples:
            - hh_t_1_raw
          type: string
        rawTable:
          examples:
            - ds_018f2f05c6c97a49b32d04f4dd84ef4a
          type: string
        rowCount:
          examples:
            - 10000000
          format: int64
          type: integer
        sourceFileObjectId:
          examples:
            - 1
          format: int64
          type: integer
        sourceKind:
          examples:
            - file
          type: string
        sourceWorkTableDatabase:
          examples:
            - support
          type: string
        sourceWorkTableId:
          examples:
            - 1
          format: int64
          type: integer
        sourceWorkTableName:
          examples:
            - support
          type: string
        sourceWorkTablePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        sourceWorkTableStatus:
          examples:
            - active
          type: string
        sourceWorkTableTable:
          examples:
            - support
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - ready
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        workDatabase:
          examples:
            - hh_t_1_work
          type: string
      required:
        - publicId
        - sourceKind
        - name
        - originalFilename
        - contentType
        - byteSize
        - rawDatabase
        - rawTable
        - workDatabase
        - status
        - rowCount
        - columns
        - createdAt
        - updatedAt
      type: object
    DatasetColumnBody:
      additionalProperties: false
      properties:
        clickHouseType:
          examples:
            - Nullable(String)
          type: string
        columnName:
          examples:
            - customer_id
          type: string
        ordinal:
          examples:
            - 1
          format: int32
          type: integer
        originalName:
          examples:
            - Customer ID
          type: string
      required:
        - ordinal
        - originalName
        - columnName
        - clickHouseType
      type: object
    DatasetCreateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetCreateBody.json
          format: uri
          readOnly: true
          type: string
        driveFilePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 160
          type: string
      required:
        - driveFilePublicId
      type: object
    DatasetGoldPublicationBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetGoldPublicationBody.json
          format: uri
          readOnly: true
          type: string
        archivedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Monthly sales mart
          type: string
        goldDatabase:
          examples:
            - hh_t_1_gold
          type: string
        goldTable:
          examples:
            - gm_monthly_sales
          type: string
        latestPublishRun:
          $ref: "#/components/schemas/DatasetGoldPublishRunBody"
          examples:
            - example
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        publishedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        refreshPolicy:
          enum:
            - manual
          examples:
            - manual
          type: string
        rowCount:
          examples:
            - 1000
          format: int64
          type: integer
        schemaSummary:
          additionalProperties: {}
          examples:
            - {}
          type: object
        sourceScd2Summary:
          $ref: "#/components/schemas/DatasetWorkTableSCD2SummaryBody"
          examples:
            - support
        sourceWorkTablePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - pending
            - active
            - failed
            - unpublished
            - archived
          examples:
            - active
          type: string
        totalBytes:
          examples:
            - 1048576
          format: int64
          type: integer
        unpublishedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - displayName
        - goldDatabase
        - goldTable
        - status
        - rowCount
        - totalBytes
        - refreshPolicy
        - createdAt
        - updatedAt
      type: object
    DatasetGoldPublicationCreateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetGoldPublicationCreateBody.json
          format: uri
          readOnly: true
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Monthly sales mart
          type: string
        goldTable:
          examples:
            - gm_monthly_sales
          type: string
      type: object
    DatasetGoldPublicationListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetGoldPublicationListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetGoldPublicationBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DatasetGoldPublicationPreviewBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetGoldPublicationPreviewBody.json
          format: uri
          readOnly: true
          type: string
        columns:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        database:
          examples:
            - hh_t_1_gold
          type: string
        previewRows:
          examples:
            - - {}
          items:
            additionalProperties: {}
            type: object
          type:
            - array
            - "null"
        table:
          examples:
            - gm_monthly_sales
          type: string
      required:
        - database
        - table
        - columns
        - previewRows
      type: object
    DatasetGoldPublishRunBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetGoldPublishRunBody.json
          format: uri
          readOnly: true
          type: string
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorSummary:
          examples:
            - example
          type: string
        goldDatabase:
          examples:
            - hh_t_1_gold
          type: string
        goldTable:
          examples:
            - gm_monthly_sales
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        publicationPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        rowCount:
          examples:
            - 1000
          format: int64
          type: integer
        schemaSummary:
          additionalProperties: {}
          examples:
            - {}
          type: object
        sourceWorkTablePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        startedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - pending
            - processing
            - completed
            - failed
          examples:
            - completed
          type: string
        totalBytes:
          examples:
            - 1048576
          format: int64
          type: integer
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - status
        - goldDatabase
        - goldTable
        - rowCount
        - totalBytes
        - createdAt
        - updatedAt
      type: object
    DatasetGoldPublishRunListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetGoldPublishRunListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetGoldPublishRunBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DatasetImportErrorBody:
      additionalProperties: false
      properties:
        error:
          examples:
            - expected 5 columns, got 4
          type: string
        raw:
          examples:
            - example
          type: string
        rowNumber:
          examples:
            - 10
          format: int64
          type: integer
      required:
        - rowNumber
        - error
      type: object
    DatasetImportJobBody:
      additionalProperties: false
      properties:
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorSample:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetImportErrorBody"
          type:
            - array
            - "null"
        errorSummary:
          examples:
            - example
          type: string
        invalidRows:
          examples:
            - 10
          format: int64
          type: integer
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - processing
          type: string
        totalRows:
          examples:
            - 10000000
          format: int64
          type: integer
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        validRows:
          examples:
            - 9999990
          format: int64
          type: integer
      required:
        - publicId
        - status
        - totalRows
        - validRows
        - invalidRows
        - errorSample
        - createdAt
        - updatedAt
      type: object
    DatasetLineageBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetLineageBody.json
          format: uri
          readOnly: true
          type: string
        edges:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetLineageEdgeBody"
          type:
            - array
            - "null"
        nodes:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetLineageNodeBody"
          type:
            - array
            - "null"
        root:
          $ref: "#/components/schemas/DatasetLineageNodeBody"
          examples:
            - example
        timeline:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetLineageTimelineItemBody"
          type:
            - array
            - "null"
      required:
        - root
        - nodes
        - edges
        - timeline
      type: object
    DatasetLineageChangeSetBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetLineageChangeSetBody.json
          format: uri
          readOnly: true
          type: string
        archivedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        publishedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        queryJobPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        rejectedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        rootResourcePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        rootResourceType:
          examples:
            - support
          type: string
        sourceKind:
          enum:
            - parser
            - manual
          examples:
            - parser
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - draft
            - published
            - rejected
            - archived
          examples:
            - draft
          type: string
        title:
          examples:
            - Export CSV from reports
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - rootResourceType
        - sourceKind
        - status
        - title
        - description
        - createdAt
        - updatedAt
      type: object
    DatasetLineageChangeSetCreateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetLineageChangeSetCreateBody.json
          format: uri
          readOnly: true
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          maxLength: 2000
          type: string
        rootResourcePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        rootResourceType:
          examples:
            - dataset_query_job
          type: string
        sourceKind:
          enum:
            - manual
            - parser
          examples:
            - manual
          type: string
        title:
          examples:
            - Export CSV from reports
          maxLength: 160
          type: string
      type: object
    DatasetLineageChangeSetGraphBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetLineageChangeSetGraphBody.json
          format: uri
          readOnly: true
          type: string
        changeSet:
          $ref: "#/components/schemas/DatasetLineageChangeSetBody"
          examples:
            - example
        edges:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetLineageEdgeBody"
          type:
            - array
            - "null"
        nodes:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetLineageNodeBody"
          type:
            - array
            - "null"
      required:
        - changeSet
        - nodes
        - edges
      type: object
    DatasetLineageChangeSetListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetLineageChangeSetListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetLineageChangeSetBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DatasetLineageEdgeBody:
      additionalProperties: false
      properties:
        confidence:
          enum:
            - metadata
            - parser_exact
            - parser_partial
            - manual
          examples:
            - metadata
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        editable:
          examples:
            - true
          type: boolean
        expression:
          examples:
            - example
          type: string
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        label:
          examples:
            - example
          type: string
        relationType:
          enum:
            - query_input
            - query_created_work_table
            - source_dataset
            - promoted_dataset
            - work_table_export
            - export_schedule
            - scheduled_export_run
            - dataset_sync_source
            - dataset_sync_target
            - column_derives
            - manual_dependency
          examples:
            - source_dataset
          type: string
        sourceKind:
          enum:
            - metadata
            - parser
            - manual
          examples:
            - metadata
          type: string
        sourceNodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        targetNodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      required:
        - id
        - sourceNodeId
        - targetNodeId
        - relationType
        - confidence
        - editable
      type: object
    DatasetLineageEdgeWriteBody:
      additionalProperties: false
      properties:
        confidence:
          enum:
            - parser_exact
            - parser_partial
            - manual
          examples:
            - parser_exact
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        expression:
          examples:
            - example
          type: string
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        label:
          examples:
            - example
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        relationType:
          examples:
            - example
          type: string
        sourceKind:
          enum:
            - parser
            - manual
          examples:
            - parser
          type: string
        sourceNodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        targetNodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      required:
        - sourceNodeId
        - targetNodeId
      type: object
    DatasetLineageGraphSaveBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetLineageGraphSaveBody.json
          format: uri
          readOnly: true
          type: string
        edges:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetLineageEdgeWriteBody"
          type:
            - array
            - "null"
        nodes:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetLineageNodeWriteBody"
          type:
            - array
            - "null"
      required:
        - nodes
        - edges
      type: object
    DatasetLineageNodeBody:
      additionalProperties: false
      properties:
        columnName:
          examples:
            - customer_id
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Sales
          type: string
        editable:
          examples:
            - true
          type: boolean
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - dataset:018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        nodeKind:
          enum:
            - resource
            - column
            - custom
          examples:
            - resource
          type: string
        position:
          $ref: "#/components/schemas/DatasetLineagePositionBody"
          examples:
            - example
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        resourceType:
          description: "`file` や `folder` など、対象 resource の種類です。"
          enum:
            - dataset
            - dataset_query_job
            - dataset_work_table
            - dataset_work_table_export
            - dataset_work_table_export_schedule
            - dataset_sync_job
            - custom
          examples:
            - dataset
          type: string
        sourceKind:
          enum:
            - metadata
            - parser
            - manual
          examples:
            - metadata
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - ready
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - id
        - resourceType
        - displayName
        - editable
      type: object
    DatasetLineageNodeWriteBody:
      additionalProperties: false
      properties:
        columnName:
          examples:
            - Acme
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        nodeKind:
          enum:
            - resource
            - column
            - custom
          examples:
            - resource
          type: string
        position:
          $ref: "#/components/schemas/DatasetLineagePositionBody"
          examples:
            - example
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        resourceType:
          description: "`file` や `folder` など、対象 resource の種類です。"
          examples:
            - support
          type: string
        sourceKind:
          enum:
            - parser
            - manual
          examples:
            - parser
          type: string
      required:
        - id
      type: object
    DatasetLineageParseRunBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetLineageParseRunBody.json
          format: uri
          readOnly: true
          type: string
        changeSetPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        columnEdgeCount:
          examples:
            - 3
          format: int32
          type: integer
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorSummary:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        queryJobPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - processing
            - completed
            - failed
          examples:
            - processing
          type: string
        tableRefCount:
          examples:
            - 3
          format: int32
          type: integer
      required:
        - publicId
        - queryJobPublicId
        - status
        - tableRefCount
        - columnEdgeCount
        - createdAt
      type: object
    DatasetLineageParseRunListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetLineageParseRunListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetLineageParseRunBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DatasetLineagePositionBody:
      additionalProperties: false
      properties:
        x:
          examples:
            - 120
          format: double
          type: number
        "y":
          examples:
            - 240
          format: double
          type: number
      required:
        - x
        - "y"
      type: object
    DatasetLineageTimelineItemBody:
      additionalProperties: false
      properties:
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        nodeId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        occurredAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        relationType:
          enum:
            - query_input
            - query_created_work_table
            - source_dataset
            - promoted_dataset
            - work_table_export
            - export_schedule
            - scheduled_export_run
            - dataset_sync_source
            - dataset_sync_target
          examples:
            - query_created_work_table
          type: string
        resourceType:
          description: "`file` や `folder` など、対象 resource の種類です。"
          enum:
            - dataset
            - dataset_query_job
            - dataset_work_table
            - dataset_work_table_export
            - dataset_work_table_export_schedule
            - dataset_sync_job
          examples:
            - dataset_work_table
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - completed
          type: string
      required:
        - id
        - nodeId
        - resourceType
        - relationType
      type: object
    DatasetListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DatasetQueryCreateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetQueryCreateBody.json
          format: uri
          readOnly: true
          type: string
        statement:
          examples:
            - SELECT count() FROM hh_t_1_raw.ds_abc
          type: string
      required:
        - statement
      type: object
    DatasetQueryJobBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetQueryJobBody.json
          format: uri
          readOnly: true
          type: string
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        durationMs:
          examples:
            - 120
          format: int64
          type: integer
        errorSummary:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        resultColumns:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        resultRows:
          examples:
            - - {}
          items:
            additionalProperties: {}
            type: object
          type:
            - array
            - "null"
        rowCount:
          examples:
            - 1000
          format: int32
          type: integer
        statement:
          examples:
            - example
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - completed
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - statement
        - status
        - resultColumns
        - resultRows
        - rowCount
        - durationMs
        - createdAt
        - updatedAt
      type: object
    DatasetQueryListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetQueryListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetQueryJobBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DatasetRowsPageBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetRowsPageBody.json
          format: uri
          readOnly: true
          type: string
        columns:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        hasMore:
          examples:
            - true
          type: boolean
        nextCursor:
          examples:
            - 1
          format: int64
          type: integer
        rows:
          examples:
            - - {}
          items:
            additionalProperties: {}
            type: object
          type:
            - array
            - "null"
      required:
        - columns
        - rows
        - hasMore
      type: object
    DatasetSourceFileBody:
      additionalProperties: false
      properties:
        byteSize:
          examples:
            - 104857600
          format: int64
          type: integer
        contentType:
          examples:
            - text/csv
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        originalFilename:
          examples:
            - customers.csv
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        sha256Hex:
          examples:
            - 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - originalFilename
        - contentType
        - byteSize
        - sha256Hex
        - updatedAt
        - createdAt
      type: object
    DatasetSourceFileListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetSourceFileListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetSourceFileBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DatasetSyncJobBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetSyncJobBody.json
          format: uri
          readOnly: true
          type: string
        cleanupErrorSummary:
          examples:
            - example
          type: string
        cleanupStatus:
          examples:
            - active
          type: string
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorSummary:
          examples:
            - example
          type: string
        mode:
          enum:
            - full_refresh
          examples:
            - full_refresh
          type: string
        newRawDatabase:
          examples:
            - hh_t_1_raw
          type: string
        newRawTable:
          examples:
            - ds_new
          type: string
        oldRawDatabase:
          examples:
            - hh_t_1_raw
          type: string
        oldRawTable:
          examples:
            - ds_old
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        rowCount:
          examples:
            - 100000
          format: int64
          type: integer
        startedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - pending
            - processing
            - completed
            - failed
          examples:
            - processing
          type: string
        totalBytes:
          examples:
            - 1048576
          format: int64
          type: integer
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - mode
        - status
        - oldRawDatabase
        - oldRawTable
        - newRawDatabase
        - newRawTable
        - rowCount
        - totalBytes
        - createdAt
        - updatedAt
      type: object
    DatasetSyncJobCreateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetSyncJobCreateBody.json
          format: uri
          readOnly: true
          type: string
        mode:
          enum:
            - full_refresh
          examples:
            - full_refresh
          type: string
      type: object
    DatasetSyncJobListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetSyncJobListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetSyncJobBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DatasetWorkTableBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableBody.json
          format: uri
          readOnly: true
          type: string
        columns:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetWorkTableColumnBody"
          type:
            - array
            - "null"
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        database:
          examples:
            - hh_t_1_work
          type: string
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - hai_category_summary
          type: string
        droppedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        engine:
          examples:
            - MergeTree
          type: string
        managed:
          examples:
            - true
          type: boolean
        originDatasetName:
          examples:
            - Acme
          type: string
        originDatasetPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        table:
          examples:
            - hai_category_summary
          type: string
        totalBytes:
          examples:
            - 1048576
          format: int64
          type: integer
        totalRows:
          examples:
            - 1000
          format: int64
          type: integer
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - database
        - table
        - displayName
        - status
        - managed
        - engine
        - totalRows
        - totalBytes
        - createdAt
        - updatedAt
      type: object
    DatasetWorkTableColumnBody:
      additionalProperties: false
      properties:
        clickHouseType:
          examples:
            - Nullable(String)
          type: string
        columnName:
          examples:
            - category
          type: string
        ordinal:
          examples:
            - 1
          format: int32
          type: integer
      required:
        - ordinal
        - columnName
        - clickHouseType
      type: object
    DatasetWorkTableExportBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableExportBody.json
          format: uri
          readOnly: true
          type: string
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorSummary:
          examples:
            - example
          type: string
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        fileObjectId:
          examples:
            - 1
          format: int64
          type: integer
        format:
          examples:
            - csv
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        schedulePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        scheduledFor:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        source:
          description: record の発生元または permission の由来です。
          enum:
            - manual
            - scheduled
          examples:
            - manual
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - processing
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        workTableId:
          examples:
            - 1
          format: int64
          type: integer
      required:
        - publicId
        - workTableId
        - format
        - status
        - source
        - expiresAt
        - createdAt
        - updatedAt
      type: object
    DatasetWorkTableExportCreateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableExportCreateBody.json
          format: uri
          readOnly: true
          type: string
        format:
          enum:
            - csv
            - json
            - parquet
          examples:
            - csv
          type: string
      type: object
    DatasetWorkTableExportListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableExportListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetWorkTableExportBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DatasetWorkTableExportScheduleBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableExportScheduleBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        enabled:
          examples:
            - true
          type: boolean
        format:
          enum:
            - csv
            - json
            - parquet
          examples:
            - csv
          type: string
        frequency:
          enum:
            - daily
            - weekly
            - monthly
          examples:
            - daily
          type: string
        lastErrorSummary:
          examples:
            - example
          type: string
        lastRunAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        lastStatus:
          examples:
            - active
          type: string
        monthDay:
          examples:
            - 1
          format: int32
          maximum: 28
          minimum: 1
          type: integer
        nextRunAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        retentionDays:
          examples:
            - 7
          format: int32
          maximum: 365
          minimum: 1
          type: integer
        runTime:
          examples:
            - "03:00"
          type: string
        timezone:
          examples:
            - Asia/Tokyo
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        weekday:
          examples:
            - 1
          format: int32
          maximum: 7
          minimum: 1
          type: integer
        workTableId:
          examples:
            - 1
          format: int64
          type: integer
      required:
        - publicId
        - workTableId
        - format
        - frequency
        - timezone
        - runTime
        - retentionDays
        - enabled
        - nextRunAt
        - createdAt
        - updatedAt
      type: object
    DatasetWorkTableExportScheduleCreateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableExportScheduleCreateBody.json
          format: uri
          readOnly: true
          type: string
        format:
          enum:
            - csv
            - json
            - parquet
          examples:
            - csv
          type: string
        frequency:
          enum:
            - daily
            - weekly
            - monthly
          examples:
            - daily
          type: string
        monthDay:
          examples:
            - 1
          format: int32
          maximum: 28
          minimum: 1
          type: integer
        retentionDays:
          examples:
            - 7
          format: int32
          maximum: 365
          minimum: 1
          type: integer
        runTime:
          examples:
            - "03:00"
          type: string
        timezone:
          examples:
            - Asia/Tokyo
          type: string
        weekday:
          examples:
            - 1
          format: int32
          maximum: 7
          minimum: 1
          type: integer
      type: object
    DatasetWorkTableExportScheduleListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableExportScheduleListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetWorkTableExportScheduleBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DatasetWorkTableExportScheduleUpdateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableExportScheduleUpdateBody.json
          format: uri
          readOnly: true
          type: string
        enabled:
          examples:
            - true
          type: boolean
        format:
          enum:
            - csv
            - json
            - parquet
          examples:
            - csv
          type: string
        frequency:
          enum:
            - daily
            - weekly
            - monthly
          examples:
            - daily
          type: string
        monthDay:
          examples:
            - 1
          format: int32
          maximum: 28
          minimum: 1
          type: integer
        retentionDays:
          examples:
            - 7
          format: int32
          maximum: 365
          minimum: 1
          type: integer
        runTime:
          examples:
            - "03:00"
          type: string
        timezone:
          examples:
            - Asia/Tokyo
          type: string
        weekday:
          examples:
            - 1
          format: int32
          maximum: 7
          minimum: 1
          type: integer
      type: object
    DatasetWorkTableLinkBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableLinkBody.json
          format: uri
          readOnly: true
          type: string
        datasetPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - datasetPublicId
      type: object
    DatasetWorkTableListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DatasetWorkTableBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DatasetWorkTablePreviewBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTablePreviewBody.json
          format: uri
          readOnly: true
          type: string
        columns:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        database:
          examples:
            - hh_t_1_work
          type: string
        previewRows:
          examples:
            - - {}
          items:
            additionalProperties: {}
            type: object
          type:
            - array
            - "null"
        scd2Summary:
          $ref: "#/components/schemas/DatasetWorkTableSCD2SummaryBody"
          examples:
            - example
        table:
          examples:
            - hai_category_summary
          type: string
      required:
        - database
        - table
        - columns
        - previewRows
      type: object
    DatasetWorkTablePromoteBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTablePromoteBody.json
          format: uri
          readOnly: true
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 160
          type: string
      type: object
    DatasetWorkTableRegisterBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableRegisterBody.json
          format: uri
          readOnly: true
          type: string
        database:
          examples:
            - example
          maxLength: 128
          type: string
        datasetPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 160
          type: string
        table:
          examples:
            - example
          maxLength: 256
          type: string
      required:
        - database
        - table
      type: object
    DatasetWorkTableRenameBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableRenameBody.json
          format: uri
          readOnly: true
          type: string
        table:
          examples:
            - example
          maxLength: 256
          type: string
      required:
        - table
      type: object
    DatasetWorkTableSCD2HistoryBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DatasetWorkTableSCD2HistoryBody.json
          format: uri
          readOnly: true
          type: string
        columns:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        database:
          examples:
            - hh_t_1_work
          type: string
        historyRows:
          examples:
            - - {}
          items:
            additionalProperties: {}
            type: object
          type:
            - array
            - "null"
        keyColumn:
          examples:
            - id
          type: string
        keyValue:
          examples:
            - SKU-001
          type: string
        table:
          examples:
            - hai_category_snapshot
          type: string
      required:
        - database
        - table
        - keyColumn
        - keyValue
        - columns
        - historyRows
      type: object
    DatasetWorkTableSCD2SummaryBody:
      additionalProperties: false
      properties:
        currentRows:
          examples:
            - 4
          format: int64
          type: integer
        detected:
          examples:
            - true
          type: boolean
        earliestValidAt:
          examples:
            - "2026-05-01 00:00:00"
          type: string
        historyRows:
          examples:
            - 8
          format: int64
          type: integer
        keyColumn:
          examples:
            - id
          type: string
        keyCount:
          examples:
            - 4
          format: int64
          type: integer
        latestValidAt:
          examples:
            - "2026-05-03 00:00:00"
          type: string
        totalRows:
          examples:
            - 12
          format: int64
          type: integer
      required:
        - detected
        - totalRows
        - currentRows
        - historyRows
        - keyCount
      type: object
    DriveAIClassificationBody:
      additionalProperties: false
      properties:
        confidence:
          examples:
            - 42.5
          format: double
          type: number
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        label:
          examples:
            - example
          type: string
        provider:
          examples:
            - example
          type: string
      required:
        - label
        - confidence
        - provider
        - createdAt
      type: object
    DriveAIClassificationsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveAIClassificationsOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveAIClassificationBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DriveAIJobBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveAIJobBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        jobType:
          examples:
            - example
          type: string
        provider:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - filePublicId
        - jobType
        - provider
        - status
        - createdAt
      type: object
    DriveAIJobCreateInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveAIJobCreateInputBody.json
          format: uri
          readOnly: true
          type: string
        jobType:
          enum:
            - summary
            - classification
          examples:
            - summary
          type: string
      required:
        - jobType
      type: object
    DriveAISummaryBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveAISummaryBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        provider:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        summaryText:
          examples:
            - example
          type: string
      required:
        - publicId
        - filePublicId
        - summaryText
        - provider
        - createdAt
      type: object
    DriveActivityBody:
      additionalProperties: false
      properties:
        action:
          examples:
            - example
          type: string
        actorDisplayName:
          examples:
            - Acme
          type: string
        actorUserPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourceType:
          description: "`file` や `folder` など、対象 resource の種類です。"
          examples:
            - support
          type: string
      required:
        - publicId
        - resourceType
        - action
        - createdAt
      type: object
    DriveActivityListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveActivityListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveActivityBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DriveArchiveBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveArchiveBody.json
          format: uri
          readOnly: true
          type: string
        filename:
          examples:
            - roadmap.md
          maxLength: 255
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveArchiveItemBody"
          maxItems: 100
          minItems: 1
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DriveArchiveItemBody:
      additionalProperties: false
      properties:
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        type:
          enum:
            - file
            - folder
          examples:
            - file
          type: string
      required:
        - type
        - publicId
      type: object
    DriveCleanRoomBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveCleanRoomBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - name
        - status
        - createdAt
      type: object
    DriveCleanRoomCreateInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveCleanRoomCreateInputBody.json
          format: uri
          readOnly: true
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 255
          type: string
      required:
        - name
      type: object
    DriveCleanRoomDatasetBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveCleanRoomDatasetBody.json
          format: uri
          readOnly: true
          type: string
        cleanRoomPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        sourceFilePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - cleanRoomPublicId
        - sourceFilePublicId
        - status
        - createdAt
      type: object
    DriveCleanRoomDatasetInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveCleanRoomDatasetInputBody.json
          format: uri
          readOnly: true
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - filePublicId
      type: object
    DriveCleanRoomExportBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveCleanRoomExportBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        deniedReason:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - status
        - createdAt
      type: object
    DriveCleanRoomExportInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveCleanRoomExportInputBody.json
          format: uri
          readOnly: true
          type: string
        rawDatasetExport:
          examples:
            - true
          type: boolean
      required:
        - rawDatasetExport
      type: object
    DriveCleanRoomParticipantInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveCleanRoomParticipantInputBody.json
          format: uri
          readOnly: true
          type: string
        role:
          description: resource に対して付与する role または tenant role code です。
          enum:
            - participant
            - reviewer
            - owner
          examples:
            - participant
          type: string
        userPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - userPublicId
        - role
      type: object
    DriveCopyBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveCopyBody.json
          format: uri
          readOnly: true
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 255
          type: string
        parentFolderPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      type: object
    DriveDeviceBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveDeviceBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        deviceName:
          examples:
            - Acme
          type: string
        lastSeenAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        platform:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        remoteWipeRequired:
          examples:
            - true
          type: boolean
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        token:
          examples:
            - token_demo_1234567890
          type: string
      required:
        - publicId
        - deviceName
        - platform
        - status
        - remoteWipeRequired
        - createdAt
      type: object
    DriveDeviceRevokeInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveDeviceRevokeInputBody.json
          format: uri
          readOnly: true
          type: string
        reason:
          examples:
            - example
          type: string
      type: object
    DriveDocumentManifest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveDocumentManifest.json
          format: uri
          readOnly: true
          type: string
        documentType:
          examples:
            - example
          type: string
        file:
          $ref: "#/components/schemas/DriveDocumentManifestFile"
          examples:
            - example
        generatedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        manifest:
          additionalProperties: {}
          examples:
            - {}
          type: object
        parserVersion:
          examples:
            - example
          type: string
        reason:
          examples:
            - example
          type: string
        stale:
          examples:
            - true
          type: boolean
      required:
        - file
        - documentType
        - manifest
        - generatedAt
        - parserVersion
        - stale
      type: object
    DriveDocumentManifestFile:
      additionalProperties: false
      properties:
        byteSize:
          examples:
            - 1048576
          format: int64
          type: integer
        contentType:
          examples:
            - text/markdown
          type: string
        originalFilename:
          examples:
            - roadmap.md
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        sha256Hex:
          examples:
            - 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
          type: string
      required:
        - publicId
        - originalFilename
        - contentType
        - byteSize
        - sha256Hex
      type: object
    DriveE2EEEnvelopeBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveE2EEEnvelopeBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        fileKeyPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        recipientUserId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        wrapAlgorithm:
          examples:
            - example
          type: string
        wrappedFileKey:
          examples:
            - example
          type: string
      required:
        - fileKeyPublicId
        - recipientUserId
        - wrappedFileKey
        - wrapAlgorithm
        - createdAt
      type: object
    DriveE2EEEnvelopeCreateInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveE2EEEnvelopeCreateInputBody.json
          format: uri
          readOnly: true
          type: string
        recipientUserPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        wrapAlgorithm:
          examples:
            - example
          type: string
        wrappedFileKey:
          examples:
            - example
          type: string
      required:
        - recipientUserPublicId
        - wrappedFileKey
      type: object
    DriveE2EEFileKeyBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveE2EEFileKeyBody.json
          format: uri
          readOnly: true
          type: string
        algorithm:
          examples:
            - example
          type: string
        ciphertextSha256:
          examples:
            - 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        keyVersion:
          examples:
            - 1
          format: int64
          type: integer
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      required:
        - publicId
        - filePublicId
        - keyVersion
        - algorithm
        - ciphertextSha256
        - createdAt
      type: object
    DriveE2EEFileKeyCreateInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveE2EEFileKeyCreateInputBody.json
          format: uri
          readOnly: true
          type: string
        algorithm:
          examples:
            - example
          type: string
        ciphertextSha256:
          examples:
            - 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        wrapAlgorithm:
          examples:
            - example
          type: string
        wrappedFileKey:
          examples:
            - example
          type: string
      required:
        - wrappedFileKey
      type: object
    DriveE2EEUserKeyBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveE2EEUserKeyBody.json
          format: uri
          readOnly: true
          type: string
        algorithm:
          examples:
            - example
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        userPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      required:
        - publicId
        - userPublicId
        - algorithm
        - status
        - createdAt
      type: object
    DriveE2EEUserKeyCreateInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveE2EEUserKeyCreateInputBody.json
          format: uri
          readOnly: true
          type: string
        algorithm:
          examples:
            - example
          type: string
        publicKey:
          additionalProperties: {}
          examples:
            - {}
          type: object
      type: object
    DriveEDiscoveryConnectionBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveEDiscoveryConnectionBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        provider:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - provider
        - status
        - createdAt
      type: object
    DriveEDiscoveryConnectionInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveEDiscoveryConnectionInputBody.json
          format: uri
          readOnly: true
          type: string
        provider:
          examples:
            - example
          type: string
      type: object
    DriveEDiscoveryExportBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveEDiscoveryExportBody.json
          format: uri
          readOnly: true
          type: string
        casePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        itemCount:
          examples:
            - 3
          format: int64
          type: integer
        manifestHash:
          examples:
            - example
          type: string
        providerExportId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - casePublicId
        - status
        - itemCount
        - createdAt
      type: object
    DriveEDiscoveryExportInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveEDiscoveryExportInputBody.json
          format: uri
          readOnly: true
          type: string
        casePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        connectionPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - connectionPublicId
        - casePublicId
      type: object
    DriveEditSaveBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveEditSaveBody.json
          format: uri
          readOnly: true
          type: string
        content:
          examples:
            - example
          maxLength: 262144
          type: string
        contentType:
          examples:
            - text/markdown
          type: string
        expectedRevision:
          examples:
            - 1
          format: int64
          type: integer
        filename:
          examples:
            - roadmap.md
          maxLength: 255
          type: string
      required:
        - content
        - expectedRevision
      type: object
    DriveEditSaveOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveEditSaveOutputBody.json
          format: uri
          readOnly: true
          type: string
        conflict:
          examples:
            - true
          type: boolean
        file:
          $ref: "#/components/schemas/DriveFileBody"
          examples:
            - example
        revision:
          examples:
            - 1
          format: int64
          type: integer
      required:
        - file
        - revision
        - conflict
      type: object
    DriveEditSessionBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveEditSessionBody.json
          format: uri
          readOnly: true
          type: string
        baseRevision:
          examples:
            - 1
          format: int64
          type: integer
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        provider:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - filePublicId
        - status
        - baseRevision
        - provider
        - expiresAt
        - createdAt
      type: object
    DriveFileBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveFileBody.json
          format: uri
          readOnly: true
          type: string
        byteSize:
          examples:
            - 1048576
          format: int64
          type: integer
        contentType:
          examples:
            - text/markdown
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        deletedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        dlpBlocked:
          examples:
            - false
          type: boolean
        inheritanceEnabled:
          examples:
            - true
          type: boolean
        lockReason:
          examples:
            - example
          type: string
        locked:
          examples:
            - true
          type: boolean
        lockedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        originalFilename:
          examples:
            - spec.md
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        scanStatus:
          examples:
            - active
          type: string
        sha256Hex:
          examples:
            - 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        workspacePublicId:
          description: 対象 Drive workspace の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      required:
        - publicId
        - originalFilename
        - contentType
        - byteSize
        - sha256Hex
        - status
        - scanStatus
        - dlpBlocked
        - inheritanceEnabled
        - locked
        - createdAt
        - updatedAt
      type: object
    DriveFolderBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveFolderBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        deletedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        inheritanceEnabled:
          examples:
            - true
          type: boolean
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Project
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        workspacePublicId:
          description: 対象 Drive workspace の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      required:
        - publicId
        - name
        - inheritanceEnabled
        - createdAt
        - updatedAt
      type: object
    DriveFolderTreeBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveFolderTreeBody.json
          format: uri
          readOnly: true
          type: string
        ownedRoots:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveFolderTreeNodeBody"
          type:
            - array
            - "null"
        sharedRoots:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveFolderTreeNodeBody"
          type:
            - array
            - "null"
      required:
        - ownedRoots
        - sharedRoots
      type: object
    DriveFolderTreeNodeBody:
      additionalProperties: false
      properties:
        children:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveFolderTreeNodeBody"
          type:
            - array
            - "null"
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      required:
        - publicId
        - name
        - children
      type: object
    DriveGatewayBindInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveGatewayBindInputBody.json
          format: uri
          readOnly: true
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - filePublicId
      type: object
    DriveGatewayBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveGatewayBody.json
          format: uri
          readOnly: true
          type: string
        certificateFingerprint:
          examples:
            - example
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        endpointUrl:
          examples:
            - https://example.com/webhooks/haohao
          type: string
        lastSeenAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - name
        - status
        - endpointUrl
        - certificateFingerprint
        - createdAt
      type: object
    DriveGatewayInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveGatewayInputBody.json
          format: uri
          readOnly: true
          type: string
        certificateFingerprint:
          examples:
            - example
          type: string
        endpointUrl:
          examples:
            - https://example.com/webhooks/haohao
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
      type: object
    DriveGatewayObjectBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveGatewayObjectBody.json
          format: uri
          readOnly: true
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        gatewayPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        manifestHash:
          examples:
            - example
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - gatewayPublicId
        - filePublicId
        - manifestHash
        - status
      type: object
    DriveGatewayStatusInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveGatewayStatusInputBody.json
          format: uri
          readOnly: true
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - active
            - disabled
            - disconnected
          examples:
            - active
          type: string
      required:
        - status
      type: object
    DriveGroupBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveGroupBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        members:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - name
        - description
        - createdAt
        - updatedAt
      type: object
    DriveGroupListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveGroupListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveGroupBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DriveHSMBindInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveHSMBindInputBody.json
          format: uri
          readOnly: true
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        keyPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - filePublicId
        - keyPublicId
      type: object
    DriveHSMDeploymentBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveHSMDeploymentBody.json
          format: uri
          readOnly: true
          type: string
        attestationHash:
          examples:
            - example
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        endpointUrl:
          examples:
            - https://example.com/webhooks/haohao
          type: string
        healthStatus:
          examples:
            - active
          type: string
        keyPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        keyStatus:
          examples:
            - active
          type: string
        provider:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - provider
        - endpointUrl
        - status
        - healthStatus
        - keyPublicId
        - keyStatus
        - createdAt
      type: object
    DriveHSMDeploymentInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveHSMDeploymentInputBody.json
          format: uri
          readOnly: true
          type: string
        endpointUrl:
          examples:
            - https://example.com/webhooks/haohao
          type: string
        provider:
          examples:
            - example
          type: string
      type: object
    DriveHSMKeyStatusInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveHSMKeyStatusInputBody.json
          format: uri
          readOnly: true
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - active
            - disabled
            - destroyed
            - unavailable
          examples:
            - active
          type: string
      required:
        - status
      type: object
    DriveIndexRebuildBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveIndexRebuildBody.json
          format: uri
          readOnly: true
          type: string
        failed:
          examples:
            - 1
          format: int64
          type: integer
        indexed:
          examples:
            - 1
          format: int64
          type: integer
        skipped:
          examples:
            - 1
          format: int64
          type: integer
      required:
        - indexed
        - skipped
        - failed
      type: object
    DriveInheritanceBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveInheritanceBody.json
          format: uri
          readOnly: true
          type: string
        enabled:
          examples:
            - true
          type: boolean
      required:
        - enabled
      type: object
    DriveItemBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveItemBody.json
          format: uri
          readOnly: true
          type: string
        file:
          $ref: "#/components/schemas/DriveFileBody"
          examples:
            - example
        folder:
          $ref: "#/components/schemas/DriveFolderBody"
          examples:
            - example
        ownedByMe:
          examples:
            - true
          type: boolean
        ownerDisplayName:
          examples:
            - Acme
          type: string
        ownerUserPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        shareRole:
          examples:
            - viewer
          type: string
        sharedWithMe:
          examples:
            - true
          type: boolean
        source:
          description: record の発生元または permission の由来です。
          examples:
            - upload
          type: string
        starredByMe:
          examples:
            - true
          type: boolean
        tags:
          description: 検索や分類に使う tag 配列です。
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        type:
          examples:
            - file
          type: string
      required:
        - type
        - ownedByMe
        - sharedWithMe
        - starredByMe
      type: object
    DriveItemListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveItemListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveItemBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DriveLegalCaseBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveLegalCaseBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - name
        - description
        - status
        - createdAt
      type: object
    DriveLegalCaseCreateInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveLegalCaseCreateInputBody.json
          format: uri
          readOnly: true
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          maxLength: 2000
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 255
          type: string
      required:
        - name
      type: object
    DriveLegalCaseFileInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveLegalCaseFileInputBody.json
          format: uri
          readOnly: true
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        reason:
          examples:
            - example
          maxLength: 2000
          type: string
      required:
        - filePublicId
      type: object
    DriveLegalExportBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveLegalExportBody.json
          format: uri
          readOnly: true
          type: string
        casePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - casePublicId
        - status
        - createdAt
      type: object
    DriveMarketplaceAppBody:
      additionalProperties: false
      properties:
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        publisherName:
          examples:
            - Acme
          type: string
        scopes:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        slug:
          examples:
            - example
          type: string
        version:
          examples:
            - example
          type: string
      required:
        - publicId
        - slug
        - name
        - publisherName
        - version
        - scopes
      type: object
    DriveMarketplaceAppsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveMarketplaceAppsOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveMarketplaceAppBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DriveMarketplaceInstallBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveMarketplaceInstallBody.json
          format: uri
          readOnly: true
          type: string
        appName:
          examples:
            - Acme
          type: string
        appSlug:
          examples:
            - example
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        scopes:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - appSlug
        - appName
        - status
        - scopes
        - createdAt
      type: object
    DriveMarketplaceInstallInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveMarketplaceInstallInputBody.json
          format: uri
          readOnly: true
          type: string
        appSlug:
          examples:
            - example
          type: string
      required:
        - appSlug
      type: object
    DriveMarketplaceScopeOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveMarketplaceScopeOutputBody.json
          format: uri
          readOnly: true
          type: string
        allowed:
          examples:
            - true
          type: boolean
      required:
        - allowed
      type: object
    DriveMobileOfflineOperationBody:
      additionalProperties: false
      properties:
        baseRevision:
          examples:
            - 1
          format: int64
          type: integer
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        operationId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        operationType:
          examples:
            - example
          type: string
        resourcePublicId:
          description: 対象 resource の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        resourceType:
          description: "`file` や `folder` など、対象 resource の種類です。"
          examples:
            - support
          type: string
      required:
        - operationType
        - resourceType
        - resourcePublicId
        - baseRevision
      type: object
    DriveMobileOfflineReplayInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveMobileOfflineReplayInputBody.json
          format: uri
          readOnly: true
          type: string
        operations:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveMobileOfflineOperationBody"
          type:
            - array
            - "null"
      required:
        - operations
      type: object
    DriveMobileOfflineReplayOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveMobileOfflineReplayOutputBody.json
          format: uri
          readOnly: true
          type: string
        applied:
          examples:
            - 1
          format: int64
          type: integer
        conflicted:
          examples:
            - 1
          format: int64
          type: integer
        denied:
          examples:
            - 1
          format: int64
          type: integer
      required:
        - applied
        - denied
        - conflicted
      type: object
    DriveOCRJobBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveOCRJobBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        engine:
          examples:
            - example
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - publicId
        - filePublicId
        - engine
        - status
        - createdAt
      type: object
    DriveOCROutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveOCROutputBody.json
          format: uri
          readOnly: true
          type: string
        pages:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveOCRPageBody"
          type:
            - array
            - "null"
        run:
          $ref: "#/components/schemas/DriveOCRRunBody"
          examples:
            - example
      required:
        - run
        - pages
      type: object
    DriveOCRPageBody:
      additionalProperties: false
      properties:
        averageConfidence:
          examples:
            - 42.5
          format: double
          type: number
        boxesJson:
          examples:
            - example
        layoutJson:
          examples:
            - example
        pageNumber:
          examples:
            - 1
          format: int64
          type: integer
        rawText:
          examples:
            - example
          type: string
      required:
        - pageNumber
        - rawText
      type: object
    DriveOCRRunBody:
      additionalProperties: false
      properties:
        averageConfidence:
          examples:
            - 42.5
          format: double
          type: number
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        engine:
          examples:
            - example
          type: string
        errorCode:
          examples:
            - example
          type: string
        errorMessage:
          examples:
            - example
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        languages:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        pageCount:
          examples:
            - 3
          format: int64
          type: integer
        processedPageCount:
          examples:
            - 3
          format: int64
          type: integer
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        reason:
          examples:
            - example
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        structuredExtractor:
          examples:
            - example
          type: string
      required:
        - publicId
        - filePublicId
        - engine
        - languages
        - structuredExtractor
        - status
        - reason
        - pageCount
        - processedPageCount
        - createdAt
      type: object
    DriveOfficeSessionBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveOfficeSessionBody.json
          format: uri
          readOnly: true
          type: string
        accessLevel:
          examples:
            - example
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        launchUrl:
          examples:
            - https://example.com/webhooks/haohao
          type: string
        provider:
          examples:
            - example
          type: string
        providerSessionId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      required:
        - publicId
        - filePublicId
        - provider
        - providerSessionId
        - accessLevel
        - launchUrl
        - expiresAt
        - createdAt
      type: object
    DriveOfficeSessionCreateInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveOfficeSessionCreateInputBody.json
          format: uri
          readOnly: true
          type: string
        accessLevel:
          enum:
            - view
            - edit
          examples:
            - view
          type: string
      type: object
    DriveOfficeWebhookInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveOfficeWebhookInputBody.json
          format: uri
          readOnly: true
          type: string
        checksum:
          examples:
            - example
          type: string
        providerEventId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        providerFileId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        revision:
          examples:
            - example
          type: string
      required:
        - providerEventId
        - providerFileId
        - revision
      type: object
    DriveOfficeWebhookOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveOfficeWebhookOutputBody.json
          format: uri
          readOnly: true
          type: string
        providerEventId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        providerFileId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        result:
          examples:
            - example
          type: string
        revision:
          examples:
            - example
          type: string
      required:
        - providerEventId
        - providerFileId
        - revision
        - result
      type: object
    DriveOwnerTransferBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveOwnerTransferBody.json
          format: uri
          readOnly: true
          type: string
        newOwnerUserPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        revokePreviousOwnerAccess:
          examples:
            - true
          type: boolean
      required:
        - newOwnerUserPublicId
        - revokePreviousOwnerAccess
      type: object
    DrivePermissionBody:
      additionalProperties: false
      properties:
        canDownload:
          description: share link または permission で download を許可するかを表します。
          examples:
            - true
          type: boolean
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        inheritedFromId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        kind:
          examples:
            - share
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        role:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - viewer
          type: string
        source:
          description: record の発生元または permission の由来です。
          examples:
            - direct
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        subjectId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        subjectType:
          examples:
            - user
          type: string
      required:
        - source
        - kind
        - role
        - createdAt
      type: object
    DrivePermissionsBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DrivePermissionsBody.json
          format: uri
          readOnly: true
          type: string
        direct:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DrivePermissionBody"
          type:
            - array
            - "null"
        inherited:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DrivePermissionBody"
          type:
            - array
            - "null"
      required:
        - direct
        - inherited
      type: object
    DriveProductExtractionItemBody:
      additionalProperties: false
      properties:
        attributes:
          additionalProperties: {}
          examples:
            - {}
          type: object
        availability:
          additionalProperties: {}
          examples:
            - {}
          type: object
        brand:
          examples:
            - example
          type: string
        category:
          examples:
            - example
          type: string
        confidence:
          examples:
            - 42.5
          format: double
          type: number
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        evidence:
          examples:
            - - {}
          items:
            additionalProperties: {}
            type: object
          type:
            - array
            - "null"
        itemType:
          examples:
            - example
          type: string
        janCode:
          examples:
            - example
          type: string
        manufacturer:
          examples:
            - example
          type: string
        model:
          examples:
            - example
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        price:
          additionalProperties: {}
          examples:
            - {}
          type: object
        promotion:
          additionalProperties: {}
          examples:
            - {}
          type: object
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        sku:
          examples:
            - example
          type: string
        sourceText:
          examples:
            - support
          type: string
      required:
        - publicId
        - itemType
        - name
        - price
        - promotion
        - availability
        - sourceText
        - evidence
        - attributes
        - createdAt
      type: object
    DriveProductExtractionJobBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveProductExtractionJobBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        extractor:
          examples:
            - example
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        itemCount:
          examples:
            - 3
          format: int64
          type: integer
        ocrRunPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - filePublicId
        - ocrRunPublicId
        - extractor
        - status
        - itemCount
        - createdAt
      type: object
    DriveProductExtractionsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveProductExtractionsOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveProductExtractionItemBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DriveRAGAnswerBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveRAGAnswerBody.json
          format: uri
          readOnly: true
          type: string
        answer:
          examples:
            - example
          type: string
        blocked:
          examples:
            - false
          type: boolean
        citations:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveRAGCitationBody"
          type:
            - array
            - "null"
        matches:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveRAGCitationBody"
          type:
            - array
            - "null"
        retrievalTrace:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveRAGRetrievalTraceBody"
          type:
            - array
            - "null"
      required:
        - answer
        - citations
        - matches
        - blocked
      type: object
    DriveRAGCitationBody:
      additionalProperties: false
      properties:
        citationId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        filePublicId:
          description: 対象 Drive file の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        filename:
          examples:
            - roadmap.md
          type: string
        resourceKind:
          enum:
            - drive_file
            - ocr_run
            - product_extraction
            - gold_table
          examples:
            - drive_file
          type: string
        resourcePublicId:
          description: 対象 resource の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        score:
          examples:
            - 42.5
          format: double
          type: number
        snippet:
          examples:
            - example
          type: string
      required:
        - citationId
        - resourceKind
        - resourcePublicId
        - filePublicId
        - filename
        - snippet
      type: object
    DriveRAGQueryBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveRAGQueryBody.json
          format: uri
          readOnly: true
          type: string
        limit:
          description: pagination で返す最大件数です。
          examples:
            - 50
          format: int32
          maximum: 20
          minimum: 1
          type: integer
        mode:
          enum:
            - keyword
            - semantic
            - hybrid
          examples:
            - keyword
          type: string
        query:
          examples:
            - roadmap
          maxLength: 1000
          minLength: 1
          type: string
      required:
        - query
      type: object
    DriveRAGRetrievalTraceBody:
      additionalProperties: false
      properties:
        intent:
          examples:
            - example
          type: string
        mergedCount:
          examples:
            - 3
          format: int64
          type: integer
        missingSignals:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        planSource:
          examples:
            - support
          type: string
        query:
          examples:
            - roadmap
          type: string
        resultCount:
          examples:
            - 3
          format: int64
          type: integer
        retry:
          examples:
            - true
          type: boolean
        retryReason:
          examples:
            - example
          type: string
        searchMode:
          enum:
            - keyword
            - semantic
            - hybrid
          examples:
            - keyword
          type: string
      required:
        - query
        - resultCount
        - mergedCount
        - searchMode
      type: object
    DriveRegisterDeviceBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveRegisterDeviceBody.json
          format: uri
          readOnly: true
          type: string
        deviceName:
          examples:
            - Acme
          maxLength: 255
          type: string
        platform:
          enum:
            - desktop
            - mobile
            - web
          examples:
            - desktop
          type: string
      required:
        - deviceName
      type: object
    DriveSearchResultBody:
      additionalProperties: false
      properties:
        indexedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        item:
          $ref: "#/components/schemas/DriveItemBody"
          examples:
            - example
        matches:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveSearchResultMatchBody"
          type:
            - array
            - "null"
        snippet:
          examples:
            - example
          type: string
      required:
        - item
      type: object
    DriveSearchResultMatchBody:
      additionalProperties: false
      properties:
        indexedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        layer:
          enum:
            - bronze
            - silver
            - gold
          examples:
            - bronze
          type: string
        medallionAssetPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        resourceKind:
          enum:
            - drive_file
            - ocr_run
            - product_extraction
            - gold_table
          examples:
            - drive_file
          type: string
        resourcePublicId:
          description: 対象 resource の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        snippet:
          examples:
            - example
          type: string
      required:
        - resourceKind
        - resourcePublicId
      type: object
    DriveSearchResultOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveSearchResultOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveSearchResultBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DriveShareBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveShareBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdByUserId:
          examples:
            - 1
          format: int64
          type: integer
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourcePublicId:
          description: 対象 resource の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourceType:
          description: "`file` や `folder` など、対象 resource の種類です。"
          examples:
            - file
          type: string
        role:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - viewer
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        subjectPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        subjectType:
          examples:
            - user
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - resourceType
        - resourcePublicId
        - subjectType
        - subjectPublicId
        - role
        - status
        - createdByUserId
        - createdAt
        - updatedAt
      type: object
    DriveShareInvitationBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveShareInvitationBody.json
          format: uri
          readOnly: true
          type: string
        acceptToken:
          examples:
            - token_demo_1234567890
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        inviteeEmailDomain:
          examples:
            - demo@example.com
          type: string
        maskedInviteeEmail:
          examples:
            - demo@example.com
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourcePublicId:
          description: 対象 resource の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourceType:
          description: "`file` や `folder` など、対象 resource の種類です。"
          examples:
            - support
          type: string
        role:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - viewer
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - resourceType
        - inviteeEmailDomain
        - role
        - status
        - expiresAt
        - createdAt
        - updatedAt
      type: object
    DriveShareInvitationListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveShareInvitationListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveShareInvitationBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DriveShareLinkBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveShareLinkBody.json
          format: uri
          readOnly: true
          type: string
        canDownload:
          description: share link または permission で download を許可するかを表します。
          examples:
            - true
          type: boolean
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        passwordRequired:
          examples:
            - true
          type: boolean
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourcePublicId:
          description: 対象 resource の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourceType:
          description: "`file` や `folder` など、対象 resource の種類です。"
          examples:
            - file
          type: string
        role:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - viewer
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        token:
          examples:
            - token_demo_1234567890
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - resourceType
        - resourcePublicId
        - role
        - canDownload
        - passwordRequired
        - expiresAt
        - status
        - createdAt
        - updatedAt
      type: object
    DriveShareTargetBody:
      additionalProperties: false
      properties:
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        secondary:
          examples:
            - example
          type: string
        type:
          examples:
            - user
          type: string
      required:
        - type
        - publicId
        - displayName
      type: object
    DriveShareTargetsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveShareTargetsOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveShareTargetBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DriveStorageUsageBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveStorageUsageBody.json
          format: uri
          readOnly: true
          type: string
        fileCount:
          examples:
            - 3
          format: int64
          type: integer
        quotaBytes:
          examples:
            - 1048576
          format: int64
          type: integer
        storageDriver:
          examples:
            - example
          type: string
        trashBytes:
          examples:
            - 1048576
          format: int64
          type: integer
        trashFileCount:
          examples:
            - 3
          format: int64
          type: integer
        usedBytes:
          examples:
            - 1048576
          format: int64
          type: integer
      required:
        - usedBytes
        - trashBytes
        - fileCount
        - trashFileCount
        - storageDriver
      type: object
    DriveSyncDeltaEventBody:
      additionalProperties: false
      properties:
        action:
          examples:
            - example
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 1
          format: int64
          type: integer
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        objectVersion:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourcePublicId:
          description: 対象 resource の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourceType:
          description: "`file` や `folder` など、対象 resource の種類です。"
          examples:
            - support
          type: string
      required:
        - id
        - publicId
        - resourceType
        - action
        - createdAt
      type: object
    DriveSyncDeltaOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveSyncDeltaOutputBody.json
          format: uri
          readOnly: true
          type: string
        cursor:
          examples:
            - example
          type: string
        deniedCount:
          examples:
            - 3
          format: int64
          type: integer
        events:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveSyncDeltaEventBody"
          type:
            - array
            - "null"
        fullResync:
          examples:
            - true
          type: boolean
        remoteWipe:
          examples:
            - true
          type: boolean
      required:
        - cursor
        - events
        - remoteWipe
        - fullResync
        - deniedCount
      type: object
    DriveWorkspaceBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveWorkspaceBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        policyOverride:
          additionalProperties: {}
          examples:
            - {}
          type: object
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        storageQuotaBytes:
          examples:
            - 1048576
          format: int64
          type: integer
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - name
        - createdAt
        - updatedAt
      type: object
    DriveWorkspaceListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveWorkspaceListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveWorkspaceBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    EntitlementBody:
      additionalProperties: false
      properties:
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        enabled:
          examples:
            - true
          type: boolean
        featureCode:
          examples:
            - example
          type: string
        limitValue:
          additionalProperties: {}
          examples:
            - {}
          type: object
        source:
          description: record の発生元または permission の由来です。
          examples:
            - support
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - featureCode
        - displayName
        - description
        - enabled
        - limitValue
        - source
        - updatedAt
      type: object
    EntitlementListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/EntitlementListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/EntitlementBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    EntitlementUpdateBody:
      additionalProperties: false
      properties:
        enabled:
          examples:
            - true
          type: boolean
        featureCode:
          examples:
            - example
          type: string
        limitValue:
          additionalProperties: {}
          examples:
            - {}
          type: object
      required:
        - featureCode
        - enabled
      type: object
    ErrorDetail:
      additionalProperties: false
      properties:
        location:
          description: Where the error occurred, e.g. 'body.items[3].tags' or 'path.thing-id'
          examples:
            - example
          type: string
        message:
          description: Error message text
          examples:
            - example
          type: string
        value:
          description: The value at the given location
          examples:
            - example
      type: object
    ErrorModel:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ErrorModel.json
          format: uri
          readOnly: true
          type: string
        detail:
          description: A human-readable explanation specific to this occurrence of the problem.
          examples:
            - Property foo is required but is missing.
          type: string
        errors:
          description: Optional list of individual error details
          examples:
            - - example
          items:
            $ref: "#/components/schemas/ErrorDetail"
          type:
            - array
            - "null"
        instance:
          description: A URI reference that identifies the specific occurrence of the problem.
          examples:
            - https://example.com/error-log/abc123
          format: uri
          type: string
        status:
          description: HTTP status code
          examples:
            - 400
          format: int64
          type: integer
        title:
          description: A short, human-readable summary of the problem type. This value should not change between occurrences of the error.
          examples:
            - Bad Request
          type: string
        type:
          default: about:blank
          description: A URI reference to human-readable documentation for the error.
          examples:
            - https://example.com/errors/example
          format: uri
          type: string
      type: object
    FileListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/FileListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/FileObjectBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    FileObjectBody:
      additionalProperties: false
      properties:
        attachedToId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        attachedToType:
          examples:
            - customer_signal
          type: string
        byteSize:
          examples:
            - 1048576
          format: int64
          type: integer
        contentType:
          examples:
            - text/plain
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        originalFilename:
          examples:
            - roadmap.md
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        purpose:
          examples:
            - attachment
          type: string
        sha256Hex:
          examples:
            - 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        tenantId:
          examples:
            - 1
          format: int64
          type: integer
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - tenantId
        - purpose
        - originalFilename
        - contentType
        - byteSize
        - sha256Hex
        - status
        - createdAt
        - updatedAt
      type: object
    IntegrationStatusBody:
      additionalProperties: false
      properties:
        connected:
          examples:
            - true
          type: boolean
        grantedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        lastErrorCode:
          examples:
            - invalid_grant
          type: string
        lastRefreshedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        provider:
          examples:
            - zitadel
          type: string
        resourceServer:
          examples:
            - zitadel
          type: string
        revokedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        scopes:
          examples:
            - - offline_access
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - resourceServer
        - provider
        - connected
      type: object
    ListIntegrationsBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListIntegrationsBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/IntegrationStatusBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    ListMachineClientsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListMachineClientsOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/MachineClientBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    ListTenantAdminTenantsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListTenantAdminTenantsOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantAdminTenantBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    ListTenantsBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListTenantsBody.json
          format: uri
          readOnly: true
          type: string
        activeTenant:
          $ref: "#/components/schemas/TenantBody"
          examples:
            - example
        defaultTenant:
          $ref: "#/components/schemas/TenantBody"
          examples:
            - example
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    LocalSearchIndexJobBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/LocalSearchIndexJobBody.json
          format: uri
          readOnly: true
          type: string
        attempts:
          examples:
            - 1
          format: int32
          type: integer
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        failedCount:
          examples:
            - 3
          format: int32
          type: integer
        indexedCount:
          examples:
            - 3
          format: int32
          type: integer
        lastError:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        reason:
          examples:
            - example
          type: string
        resourceId:
          examples:
            - 1
          format: int64
          type: integer
        resourceKind:
          enum:
            - drive_file
            - ocr_run
            - product_extraction
            - gold_table
            - schema_column
            - mapping_example
          examples:
            - drive_file
          type: string
        resourcePublicId:
          description: 対象 resource の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        skippedCount:
          examples:
            - 3
          format: int32
          type: integer
        startedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - queued
            - processing
            - completed
            - failed
            - skipped
          examples:
            - queued
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - reason
        - status
        - attempts
        - indexedCount
        - skippedCount
        - failedCount
        - createdAt
        - updatedAt
      type: object
    LocalSearchIndexJobListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/LocalSearchIndexJobListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/LocalSearchIndexJobBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    LoginInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/LoginInputBody.json
          format: uri
          readOnly: true
          type: string
        email:
          description: user または invitee の email address です。
          examples:
            - demo@example.com
          format: email
          type: string
        password:
          description: share link 保護などに使う secret value です。response には返しません。
          examples:
            - changeme123
          minLength: 8
          type: string
      required:
        - email
        - password
      type: object
    LogoutBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/LogoutBody.json
          format: uri
          readOnly: true
          type: string
        postLogoutURL:
          examples:
            - http://localhost:8081/oidc/v1/end_session?id_token_hint=...
          format: uri
          type: string
      type: object
    MachineClientBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/MachineClientBody.json
          format: uri
          readOnly: true
          type: string
        active:
          examples:
            - true
          type: boolean
        allowedScopes:
          examples:
            - - m2m:read
          items:
            type: string
          type:
            - array
            - "null"
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        defaultTenant:
          $ref: "#/components/schemas/TenantBody"
          examples:
            - example
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - nightly worker
          type: string
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 1
          format: int64
          type: integer
        provider:
          examples:
            - zitadel
          type: string
        providerClientId:
          examples:
            - "312345678901234567"
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - id
        - provider
        - providerClientId
        - displayName
        - active
        - createdAt
        - updatedAt
      type: object
    MachineClientRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/MachineClientRequestBody.json
          format: uri
          readOnly: true
          type: string
        active:
          examples:
            - true
          type: boolean
        allowedScopes:
          examples:
            - - m2m:read
          items:
            type: string
          type:
            - array
            - "null"
        defaultTenantId:
          examples:
            - 1
          format: int64
          type: integer
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - nightly worker
          type: string
        provider:
          examples:
            - zitadel
          type: string
        providerClientId:
          examples:
            - "312345678901234567"
          type: string
      required:
        - providerClientId
        - displayName
      type: object
    MarkAllNotificationsReadBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/MarkAllNotificationsReadBody.json
          format: uri
          readOnly: true
          type: string
        channel:
          enum:
            - in_app
            - email
          examples:
            - in_app
          type: string
        createdAfter:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        q:
          examples:
            - roadmap
          type: string
        readState:
          enum:
            - all
            - unread
            - read
          examples:
            - all
          type: string
      type: object
    MarkNotificationsReadBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/MarkNotificationsReadBody.json
          format: uri
          readOnly: true
          type: string
        publicIds:
          examples:
            - - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          items:
            type: string
          maxItems: 100
          minItems: 1
          type:
            - array
            - "null"
      required:
        - publicIds
      type: object
    MedallionAssetBody:
      additionalProperties: false
      properties:
        archivedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        byteSize:
          examples:
            - 1048576
          format: int64
          type: integer
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - customers.csv
          type: string
        layer:
          enum:
            - bronze
            - silver
            - gold
          examples:
            - bronze
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        resourceKind:
          examples:
            - drive_file
          type: string
        resourcePublicId:
          description: 対象 resource の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        rowCount:
          examples:
            - 1000
          format: int64
          type: integer
        schemaSummary:
          additionalProperties: {}
          examples:
            - {}
          type: object
        status:
          description: 現在の lifecycle status です。
          enum:
            - active
            - building
            - failed
            - skipped
            - archived
          examples:
            - active
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - layer
        - resourceKind
        - resourcePublicId
        - displayName
        - status
        - createdAt
        - updatedAt
      type: object
    MedallionAssetListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/MedallionAssetListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/MedallionAssetBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    MedallionCatalogBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/MedallionCatalogBody.json
          format: uri
          readOnly: true
          type: string
        asset:
          $ref: "#/components/schemas/MedallionAssetBody"
          examples:
            - example
        downstream:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/MedallionAssetBody"
          type:
            - array
            - "null"
        pipelineRuns:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/MedallionPipelineRunBody"
          type:
            - array
            - "null"
        upstream:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/MedallionAssetBody"
          type:
            - array
            - "null"
      required:
        - asset
        - upstream
        - downstream
        - pipelineRuns
      type: object
    MedallionPipelineRunBody:
      additionalProperties: false
      properties:
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorSummary:
          examples:
            - example
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        pipelineType:
          examples:
            - drive_ocr
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        retryable:
          examples:
            - true
          type: boolean
        runtime:
          examples:
            - clickhouse
          type: string
        sourceAssetPublicIds:
          examples:
            - - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          items:
            type: string
          type:
            - array
            - "null"
        sourceResourceKind:
          examples:
            - support
          type: string
        sourceResourcePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        startedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - pending
            - processing
            - completed
            - failed
            - skipped
          examples:
            - completed
          type: string
        targetAssetPublicIds:
          examples:
            - - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          items:
            type: string
          type:
            - array
            - "null"
        targetResourceKind:
          examples:
            - support
          type: string
        targetResourcePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        triggerKind:
          enum:
            - manual
            - upload
            - scheduled
            - system
            - read_repair
          examples:
            - manual
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - pipelineType
        - status
        - triggerKind
        - retryable
        - createdAt
        - updatedAt
        - sourceAssetPublicIds
        - targetAssetPublicIds
      type: object
    NotificationBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/NotificationBody.json
          format: uri
          readOnly: true
          type: string
        body:
          examples:
            - You have a new notification.
          type: string
        channel:
          examples:
            - in_app
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        readAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - queued
          type: string
        subject:
          examples:
            - Invitation
          type: string
        template:
          examples:
            - tenant_invitation
          type: string
        tenantId:
          examples:
            - 1
          format: int64
          type: integer
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - channel
        - template
        - subject
        - body
        - status
        - createdAt
        - updatedAt
      type: object
    NotificationBulkReadBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/NotificationBulkReadBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/NotificationBody"
          type:
            - array
            - "null"
        updatedCount:
          examples:
            - 3
          format: int64
          type: integer
      required:
        - items
        - updatedCount
      type: object
    NotificationListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/NotificationListBody.json
          format: uri
          readOnly: true
          type: string
        filteredCount:
          examples:
            - 3
          format: int64
          type: integer
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/NotificationBody"
          type:
            - array
            - "null"
        nextCursor:
          examples:
            - example
          type: string
        readCount:
          examples:
            - 3
          format: int64
          type: integer
        totalCount:
          examples:
            - 3
          format: int64
          type: integer
        unreadCount:
          examples:
            - 3
          format: int64
          type: integer
      required:
        - items
        - totalCount
        - filteredCount
        - unreadCount
        - readCount
      type: object
    NotificationReadAllBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/NotificationReadAllBody.json
          format: uri
          readOnly: true
          type: string
        updatedCount:
          examples:
            - 3
          format: int64
          type: integer
      required:
        - updatedCount
      type: object
    PublicDriveShareLinkOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/PublicDriveShareLinkOutputBody.json
          format: uri
          readOnly: true
          type: string
        file:
          $ref: "#/components/schemas/DriveFileBody"
          examples:
            - example
        folder:
          $ref: "#/components/schemas/DriveFolderBody"
          examples:
            - example
        link:
          $ref: "#/components/schemas/DriveShareLinkBody"
          examples:
            - example
      required:
        - link
      type: object
    RestoreDriveResourceBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/RestoreDriveResourceBody.json
          format: uri
          readOnly: true
          type: string
        parentFolderPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      type: object
    SchemaMappingCandidateBody:
      additionalProperties: false
      properties:
        acceptedEvidence:
          examples:
            - 1
          format: int64
          type: integer
        matchMethod:
          enum:
            - keyword
            - vector
            - hybrid
            - strict
          examples:
            - keyword
          type: string
        reason:
          examples:
            - example
          type: string
        rejectedEvidence:
          examples:
            - 1
          format: int64
          type: integer
        schemaColumnPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        score:
          examples:
            - 42.5
          format: double
          type: number
        snippet:
          examples:
            - example
          type: string
        targetColumn:
          examples:
            - example
          type: string
      required:
        - schemaColumnPublicId
        - targetColumn
        - score
        - matchMethod
        - reason
        - acceptedEvidence
        - rejectedEvidence
      type: object
    SchemaMappingCandidateColumnBody:
      additionalProperties: false
      properties:
        neighborColumns:
          examples:
            - - example
          items:
            type: string
          maxItems: 40
          type:
            - array
            - "null"
        sampleValues:
          examples:
            - - example
          items:
            type: string
          maxItems: 20
          type:
            - array
            - "null"
        sheetName:
          examples:
            - Acme
          maxLength: 240
          type: string
        sourceColumn:
          examples:
            - support
          maxLength: 240
          type: string
      required:
        - sourceColumn
      type: object
    SchemaMappingCandidateItemBody:
      additionalProperties: false
      properties:
        candidates:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/SchemaMappingCandidateBody"
          type:
            - array
            - "null"
        sourceColumn:
          examples:
            - support
          type: string
      required:
        - sourceColumn
        - candidates
      type: object
    SchemaMappingCandidateListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SchemaMappingCandidateListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/SchemaMappingCandidateItemBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    SchemaMappingCandidateRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SchemaMappingCandidateRequestBody.json
          format: uri
          readOnly: true
          type: string
        columns:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/SchemaMappingCandidateColumnBody"
          maxItems: 100
          minItems: 1
          type:
            - array
            - "null"
        domain:
          examples:
            - example
          maxLength: 120
          type: string
        limit:
          description: pagination で返す最大件数です。
          examples:
            - 50
          format: int32
          maximum: 10
          minimum: 1
          type: integer
        pipelinePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        schemaType:
          examples:
            - example
          maxLength: 120
          type: string
        versionPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - columns
      type: object
    SchemaMappingExampleBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SchemaMappingExampleBody.json
          format: uri
          readOnly: true
          type: string
        decision:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        schemaColumnPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        sharedScope:
          examples:
            - example
          type: string
        sourceColumn:
          examples:
            - support
          type: string
        targetColumn:
          examples:
            - example
          type: string
      required:
        - publicId
        - schemaColumnPublicId
        - sourceColumn
        - targetColumn
        - decision
        - sharedScope
      type: object
    SchemaMappingExampleWriteBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SchemaMappingExampleWriteBody.json
          format: uri
          readOnly: true
          type: string
        decision:
          enum:
            - accepted
            - rejected
          examples:
            - accepted
          type: string
        neighborColumns:
          examples:
            - - example
          items:
            type: string
          maxItems: 40
          type:
            - array
            - "null"
        pipelinePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        sampleValues:
          examples:
            - - example
          items:
            type: string
          maxItems: 20
          type:
            - array
            - "null"
        schemaColumnPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        sheetName:
          examples:
            - Acme
          maxLength: 240
          type: string
        sourceColumn:
          examples:
            - support
          maxLength: 240
          type: string
        versionPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - pipelinePublicId
        - schemaColumnPublicId
        - sourceColumn
        - decision
      type: object
    SelectTenantInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SelectTenantInputBody.json
          format: uri
          readOnly: true
          type: string
        tenantSlug:
          description: tenant を識別する slug です。
          examples:
            - acme
          type: string
      required:
        - tenantSlug
      type: object
    SelectTenantOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SelectTenantOutputBody.json
          format: uri
          readOnly: true
          type: string
        activeTenant:
          $ref: "#/components/schemas/TenantBody"
          examples:
            - example
      required:
        - activeTenant
      type: object
    SessionBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SessionBody.json
          format: uri
          readOnly: true
          type: string
        supportAccess:
          $ref: "#/components/schemas/SupportAccessBody"
          examples:
            - example
        user:
          $ref: "#/components/schemas/UserResponse"
          examples:
            - example
      required:
        - user
      type: object
    SetupTenantInvitationIdentityRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SetupTenantInvitationIdentityRequestBody.json
          format: uri
          readOnly: true
          type: string
        token:
          examples:
            - token_demo_1234567890
          type: string
      required:
        - token
      type: object
    StartSupportAccessBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/StartSupportAccessBody.json
          format: uri
          readOnly: true
          type: string
        durationMinutes:
          examples:
            - 1
          format: int64
          minimum: 1
          type: integer
        impersonatedUserPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        reason:
          examples:
            - example
          maxLength: 1000
          minLength: 8
          type: string
        tenantSlug:
          description: tenant を識別する slug です。
          examples:
            - acme
          maxLength: 64
          minLength: 3
          type: string
      required:
        - tenantSlug
        - impersonatedUserPublicId
        - reason
      type: object
    SupportAccessBody:
      additionalProperties: false
      properties:
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        impersonatedUserDisplayName:
          examples:
            - Acme
          type: string
        impersonatedUserEmail:
          examples:
            - demo@example.com
          format: email
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        reason:
          examples:
            - example
          type: string
        startedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        supportUserDisplayName:
          examples:
            - Acme
          type: string
        supportUserEmail:
          examples:
            - demo@example.com
          format: email
          type: string
        tenantDisplayName:
          examples:
            - Acme
          type: string
        tenantSlug:
          description: tenant を識別する slug です。
          examples:
            - acme
          type: string
      required:
        - publicId
        - supportUserEmail
        - supportUserDisplayName
        - impersonatedUserEmail
        - impersonatedUserDisplayName
        - tenantSlug
        - tenantDisplayName
        - reason
        - startedAt
        - expiresAt
      type: object
    SupportAccessOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SupportAccessOutputBody.json
          format: uri
          readOnly: true
          type: string
        access:
          $ref: "#/components/schemas/SupportAccessBody"
          examples:
            - example
        active:
          examples:
            - true
          type: boolean
      required:
        - active
      type: object
    SystemJobBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SystemJobBody.json
          format: uri
          readOnly: true
          type: string
        action:
          examples:
            - example
          type: string
        canStop:
          examples:
            - true
          type: boolean
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorMessage:
          examples:
            - example
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        outboxEventPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        requestedByDisplayName:
          examples:
            - Acme
          type: string
        requestedByEmail:
          examples:
            - demo@example.com
          type: string
        startedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        statusGroup:
          examples:
            - active
          type: string
        subjectPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        subjectType:
          examples:
            - example
          type: string
        title:
          examples:
            - Export CSV from reports
          type: string
        type:
          examples:
            - example
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - type
        - publicId
        - title
        - status
        - statusGroup
        - createdAt
        - updatedAt
        - metadata
        - canStop
      type: object
    SystemJobListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SystemJobListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/SystemJobBody"
          type:
            - array
            - "null"
        limit:
          description: pagination で返す最大件数です。
          examples:
            - 50
          format: int32
          type: integer
        offset:
          description: pagination の開始位置です。
          examples:
            - 0
          format: int32
          type: integer
        total:
          examples:
            - 1
          format: int64
          type: integer
      required:
        - items
        - total
        - limit
        - offset
      type: object
    TenantAdminDataAccessGrantBody:
      additionalProperties: false
      properties:
        action:
          examples:
            - example
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        subjectGroupName:
          examples:
            - Acme
          type: string
        subjectGroupPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        subjectType:
          enum:
            - user
            - group
          examples:
            - user
          type: string
        subjectUserEmail:
          examples:
            - demo@example.com
          type: string
        subjectUserName:
          examples:
            - Acme
          type: string
        subjectUserPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - subjectType
        - action
        - createdAt
      type: object
    TenantAdminDataAccessGroupBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDataAccessGroupBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdByUserId:
          examples:
            - 1
          format: int64
          type: integer
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        members:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantAdminDataAccessMemberBody"
          type:
            - array
            - "null"
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        systemKey:
          examples:
            - example
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - name
        - description
        - createdAt
        - updatedAt
      type: object
    TenantAdminDataAccessGroupCreateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDataAccessGroupCreateBody.json
          format: uri
          readOnly: true
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          maxLength: 2000
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 160
          type: string
      required:
        - name
      type: object
    TenantAdminDataAccessGroupListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDataAccessGroupListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantAdminDataAccessGroupBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    TenantAdminDataAccessGroupUpdateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDataAccessGroupUpdateBody.json
          format: uri
          readOnly: true
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          maxLength: 2000
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 160
          type: string
      required:
        - name
      type: object
    TenantAdminDataAccessMemberBody:
      additionalProperties: false
      properties:
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        email:
          description: user または invitee の email address です。
          examples:
            - demo@example.com
          type: string
        userPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - userPublicId
        - email
        - displayName
        - createdAt
      type: object
    TenantAdminDataAccessMemberWriteBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDataAccessMemberWriteBody.json
          format: uri
          readOnly: true
          type: string
        userPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - userPublicId
      type: object
    TenantAdminDataAccessPermissionListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDataAccessPermissionListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantAdminDataAccessGrantBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    TenantAdminDataAccessPermissionWriteBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDataAccessPermissionWriteBody.json
          format: uri
          readOnly: true
          type: string
        actions:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        subjectPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        subjectType:
          enum:
            - user
            - group
          examples:
            - user
          type: string
      required:
        - subjectType
        - subjectPublicId
        - actions
      type: object
    TenantAdminDriveAdminContentSessionBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveAdminContentSessionBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        reasonCategory:
          examples:
            - example
          type: string
      required:
        - publicId
        - reasonCategory
        - expiresAt
        - createdAt
      type: object
    TenantAdminDriveAdminContentSessionInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveAdminContentSessionInputBody.json
          format: uri
          readOnly: true
          type: string
        reason:
          examples:
            - example
          maxLength: 2000
          type: string
        reasonCategory:
          enum:
            - manual
            - incident
            - legal
            - security
          examples:
            - manual
          type: string
      required:
        - reason
      type: object
    TenantAdminDriveAuditEventBody:
      additionalProperties: false
      properties:
        action:
          examples:
            - example
          type: string
        actorType:
          examples:
            - example
          type: string
        metadata:
          additionalProperties: {}
          examples:
            - {}
          type: object
        occurredAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        targetId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        targetType:
          examples:
            - example
          type: string
      required:
        - publicId
        - actorType
        - action
        - targetType
        - targetId
        - metadata
        - occurredAt
      type: object
    TenantAdminDriveAuditOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveAuditOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantAdminDriveAuditEventBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    TenantAdminDriveEncryptionPolicyBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveEncryptionPolicyBody.json
          format: uri
          readOnly: true
          type: string
        keyPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        keyStatus:
          examples:
            - active
          type: string
        maskedKeyRef:
          examples:
            - example
          type: string
        mode:
          examples:
            - example
          type: string
        provider:
          examples:
            - example
          type: string
        scope:
          examples:
            - example
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - mode
        - scope
        - keyStatus
      type: object
    TenantAdminDriveEncryptionPolicyInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveEncryptionPolicyInputBody.json
          format: uri
          readOnly: true
          type: string
        keyRef:
          examples:
            - example
          type: string
        mode:
          enum:
            - service_managed
            - tenant_managed
          examples:
            - service_managed
          type: string
        provider:
          examples:
            - example
          type: string
      required:
        - mode
      type: object
    TenantAdminDriveInvitationsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveInvitationsOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveShareInvitationBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    TenantAdminDriveKMSKeyStatusInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveKMSKeyStatusInputBody.json
          format: uri
          readOnly: true
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - active
            - disabled
            - unavailable
            - deleted
          examples:
            - active
          type: string
      required:
        - status
      type: object
    TenantAdminDriveOCRDependencyBody:
      additionalProperties: false
      properties:
        available:
          examples:
            - true
          type: boolean
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        version:
          examples:
            - example
          type: string
      required:
        - name
        - available
      type: object
    TenantAdminDriveOCRLMStudioBody:
      additionalProperties: false
      properties:
        configured:
          examples:
            - true
          type: boolean
        modelAvailable:
          examples:
            - true
          type: boolean
        reachable:
          examples:
            - true
          type: boolean
      required:
        - configured
        - reachable
        - modelAvailable
      type: object
    TenantAdminDriveOCRLocalCommandBody:
      additionalProperties: false
      properties:
        available:
          examples:
            - true
          type: boolean
        command:
          examples:
            - example
          type: string
        configured:
          examples:
            - true
          type: boolean
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        version:
          examples:
            - example
          type: string
      required:
        - name
        - command
        - configured
        - available
      type: object
    TenantAdminDriveOCROllamaBody:
      additionalProperties: false
      properties:
        configured:
          examples:
            - true
          type: boolean
        modelAvailable:
          examples:
            - true
          type: boolean
        reachable:
          examples:
            - true
          type: boolean
      required:
        - configured
        - reachable
        - modelAvailable
      type: object
    TenantAdminDriveOCRStatusBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveOCRStatusBody.json
          format: uri
          readOnly: true
          type: string
        dependencies:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantAdminDriveOCRDependencyBody"
          type:
            - array
            - "null"
        enabled:
          examples:
            - true
          type: boolean
        lmStudio:
          $ref: "#/components/schemas/TenantAdminDriveOCRLMStudioBody"
          examples:
            - example
        localCommands:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantAdminDriveOCRLocalCommandBody"
          type:
            - array
            - "null"
        ocrEngine:
          examples:
            - example
          type: string
        ollama:
          $ref: "#/components/schemas/TenantAdminDriveOCROllamaBody"
          examples:
            - example
        statusCounts:
          additionalProperties:
            format: int64
            type: integer
          examples:
            - {}
          type: object
        structuredExtractor:
          examples:
            - example
          type: string
      required:
        - enabled
        - ocrEngine
        - structuredExtractor
        - dependencies
        - ollama
        - lmStudio
        - localCommands
        - statusCounts
      type: object
    TenantAdminDriveOperationsHealthBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveOperationsHealthBody.json
          format: uri
          readOnly: true
          type: string
        checkedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        missingWorkspaceCount:
          examples:
            - 3
          format: int64
          type: integer
        openfgaDriftCount:
          examples:
            - 3
          format: int64
          type: integer
        storageMissingCount:
          examples:
            - 3
          format: int64
          type: integer
        storageOrphanCheckState:
          examples:
            - example
          type: string
        tenantId:
          examples:
            - 1
          format: int64
          type: integer
        workspaceCount:
          examples:
            - 3
          format: int64
          type: integer
      required:
        - tenantId
        - workspaceCount
        - missingWorkspaceCount
        - openfgaDriftCount
        - storageMissingCount
        - storageOrphanCheckState
        - checkedAt
      type: object
    TenantAdminDriveResidencyPolicyBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveResidencyPolicyBody.json
          format: uri
          readOnly: true
          type: string
        allowedRegions:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        backupRegion:
          examples:
            - example
          type: string
        indexRegion:
          examples:
            - example
          type: string
        primaryRegion:
          examples:
            - example
          type: string
        replicationMode:
          examples:
            - example
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - primaryRegion
        - allowedRegions
        - replicationMode
        - indexRegion
        - backupRegion
        - status
      type: object
    TenantAdminDriveShareLinkStateBody:
      additionalProperties: false
      properties:
        canDownload:
          description: share link または permission で download を許可するかを表します。
          examples:
            - true
          type: boolean
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        passwordRequired:
          examples:
            - true
          type: boolean
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourceName:
          examples:
            - support
          type: string
        resourcePublicId:
          description: 対象 resource の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourceType:
          description: "`file` や `folder` など、対象 resource の種類です。"
          examples:
            - support
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - resourceType
        - resourcePublicId
        - resourceName
        - canDownload
        - passwordRequired
        - status
        - expiresAt
        - createdAt
        - updatedAt
      type: object
    TenantAdminDriveShareLinksOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveShareLinksOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantAdminDriveShareLinkStateBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    TenantAdminDriveShareStateBody:
      additionalProperties: false
      properties:
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourceName:
          examples:
            - support
          type: string
        resourcePublicId:
          description: 対象 resource の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourceType:
          description: "`file` や `folder` など、対象 resource の種類です。"
          examples:
            - support
          type: string
        role:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - viewer
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        subjectPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        subjectType:
          examples:
            - example
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - resourceType
        - resourcePublicId
        - resourceName
        - subjectType
        - subjectPublicId
        - role
        - status
        - createdAt
        - updatedAt
      type: object
    TenantAdminDriveSharesOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveSharesOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantAdminDriveShareStateBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    TenantAdminDriveSyncItemBody:
      additionalProperties: false
      properties:
        action:
          examples:
            - example
          type: string
        error:
          examples:
            - example
          type: string
        kind:
          examples:
            - example
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
      required:
        - kind
        - publicId
        - status
        - action
      type: object
    TenantAdminDriveSyncOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminDriveSyncOutputBody.json
          format: uri
          readOnly: true
          type: string
        dryRun:
          examples:
            - true
          type: boolean
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantAdminDriveSyncItemBody"
          type:
            - array
            - "null"
      required:
        - dryRun
        - items
      type: object
    TenantAdminMembershipBody:
      additionalProperties: false
      properties:
        deactivated:
          examples:
            - false
          type: boolean
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Demo User
          type: string
        email:
          description: user または invitee の email address です。
          examples:
            - demo@example.com
          format: email
          type: string
        roles:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - - viewer
          items:
            $ref: "#/components/schemas/TenantAdminRoleBindingBody"
          type:
            - array
            - "null"
        userPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - userPublicId
        - email
        - displayName
        - deactivated
        - roles
      type: object
    TenantAdminMembershipRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminMembershipRequestBody.json
          format: uri
          readOnly: true
          type: string
        roleCode:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - todo_user
          type: string
        userEmail:
          description: user または invitee の email address です。
          examples:
            - demo@example.com
          format: email
          type: string
      required:
        - userEmail
        - roleCode
      type: object
    TenantAdminRoleBindingBody:
      additionalProperties: false
      properties:
        active:
          examples:
            - true
          type: boolean
        roleCode:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - todo_user
          type: string
        source:
          description: record の発生元または permission の由来です。
          examples:
            - local_override
          type: string
      required:
        - roleCode
        - source
        - active
      type: object
    TenantAdminSchemaMappingExampleListItemBody:
      additionalProperties: false
      properties:
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        decidedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        decision:
          examples:
            - example
          type: string
        domain:
          examples:
            - example
          type: string
        neighborColumns:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        pipelineName:
          examples:
            - Acme
          type: string
        pipelinePublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        sampleValues:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        schemaColumnPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        schemaType:
          examples:
            - example
          type: string
        searchDocumentMaterialized:
          examples:
            - true
          type: boolean
        sharedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        sharedScope:
          examples:
            - example
          type: string
        sheetName:
          examples:
            - Acme
          type: string
        sourceColumn:
          examples:
            - support
          type: string
        targetColumn:
          examples:
            - example
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - pipelinePublicId
        - pipelineName
        - schemaColumnPublicId
        - domain
        - schemaType
        - sourceColumn
        - sampleValues
        - neighborColumns
        - targetColumn
        - decision
        - sharedScope
        - searchDocumentMaterialized
        - decidedAt
        - createdAt
        - updatedAt
      type: object
    TenantAdminSchemaMappingExampleListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminSchemaMappingExampleListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantAdminSchemaMappingExampleListItemBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    TenantAdminSchemaMappingExampleSharingInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminSchemaMappingExampleSharingInputBody.json
          format: uri
          readOnly: true
          type: string
        sharedScope:
          enum:
            - private
            - tenant
          examples:
            - private
          type: string
      required:
        - sharedScope
      type: object
    TenantAdminSchemaMappingSearchDocumentsRebuildInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminSchemaMappingSearchDocumentsRebuildInputBody.json
          format: uri
          readOnly: true
          type: string
        limit:
          description: pagination で返す最大件数です。
          examples:
            - 50
          format: int32
          maximum: 1000
          minimum: 1
          type: integer
      type: object
    TenantAdminSchemaMappingSearchDocumentsRebuildOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminSchemaMappingSearchDocumentsRebuildOutputBody.json
          format: uri
          readOnly: true
          type: string
        indexed:
          examples:
            - 1
          format: int64
          type: integer
        mappingExamplesIndexed:
          examples:
            - 1
          format: int64
          type: integer
        schemaColumnsIndexed:
          examples:
            - 1
          format: int64
          type: integer
      required:
        - indexed
        - schemaColumnsIndexed
        - mappingExamplesIndexed
      type: object
    TenantAdminTenantBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminTenantBody.json
          format: uri
          readOnly: true
          type: string
        active:
          examples:
            - true
          type: boolean
        activeMemberCount:
          examples:
            - 3
          format: int64
          type: integer
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 1
          format: int64
          type: integer
        slug:
          examples:
            - acme
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - id
        - slug
        - displayName
        - active
        - activeMemberCount
        - createdAt
        - updatedAt
      type: object
    TenantAdminTenantDetailBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminTenantDetailBody.json
          format: uri
          readOnly: true
          type: string
        memberships:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantAdminMembershipBody"
          type:
            - array
            - "null"
        tenant:
          $ref: "#/components/schemas/TenantAdminTenantBody"
          examples:
            - example
      required:
        - tenant
        - memberships
      type: object
    TenantAdminTenantRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantAdminTenantRequestBody.json
          format: uri
          readOnly: true
          type: string
        active:
          examples:
            - true
          type: boolean
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 120
          minLength: 1
          type: string
        slug:
          examples:
            - acme
          maxLength: 64
          minLength: 3
          type: string
      required:
        - displayName
      type: object
    TenantBody:
      additionalProperties: false
      properties:
        default:
          examples:
            - true
          type: boolean
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - acme
          type: string
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 1
          format: int64
          type: integer
        roles:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - - todo_user
          items:
            type: string
          type:
            - array
            - "null"
        selected:
          examples:
            - true
          type: boolean
        slug:
          examples:
            - acme
          type: string
      required:
        - id
        - slug
        - displayName
        - default
        - selected
      type: object
    TenantDataExportBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantDataExportBody.json
          format: uri
          readOnly: true
          type: string
        completedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        errorSummary:
          examples:
            - example
          type: string
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        fileObjectId:
          examples:
            - 1
          format: int64
          type: integer
        format:
          examples:
            - json
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - processing
          type: string
        tenantId:
          examples:
            - 1
          format: int64
          type: integer
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - tenantId
        - format
        - status
        - expiresAt
        - createdAt
        - updatedAt
      type: object
    TenantDataExportListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantDataExportListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantDataExportBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    TenantInvitationBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantInvitationBody.json
          format: uri
          readOnly: true
          type: string
        acceptUrl:
          examples:
            - https://example.com/webhooks/haohao
          format: uri
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        identitySetupDeliveryMode:
          examples:
            - return_code
          type: string
        identitySetupEmailCode:
          examples:
            - demo@example.com
          type: string
        identitySetupInviteCode:
          examples:
            - example
          type: string
        identitySetupLoginUrl:
          examples:
            - https://example.com/webhooks/haohao
          format: uri
          type: string
        identitySetupUserId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        inviteeEmailNormalized:
          description: user または invitee の email address です。
          examples:
            - demo@example.com
          format: email
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        roleCodes:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - - viewer
          items:
            type: string
          type:
            - array
            - "null"
        status:
          description: 現在の lifecycle status です。
          examples:
            - pending
          type: string
        tenantDisplayName:
          examples:
            - Acme
          type: string
        tenantId:
          examples:
            - 1
          format: int64
          type: integer
        tenantSlug:
          description: tenant を識別する slug です。
          examples:
            - acme
          type: string
        token:
          examples:
            - token_demo_1234567890
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - tenantId
        - inviteeEmailNormalized
        - roleCodes
        - status
        - expiresAt
        - createdAt
        - updatedAt
      type: object
    TenantInvitationListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantInvitationListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantInvitationBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    TenantSettingsBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantSettingsBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        features:
          additionalProperties: {}
          examples:
            - {}
          type: object
        fileQuotaBytes:
          examples:
            - 1048576
          format: int64
          type: integer
        notificationsEnabled:
          examples:
            - true
          type: boolean
        rateLimitBrowserApiPerMinute:
          examples:
            - 50
          format: int32
          type: integer
        rateLimitExternalApiPerMinute:
          examples:
            - 50
          format: int32
          type: integer
        rateLimitLoginPerMinute:
          examples:
            - 50
          format: int32
          type: integer
        tenantId:
          examples:
            - 1
          format: int64
          type: integer
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - tenantId
        - fileQuotaBytes
        - notificationsEnabled
        - features
        - createdAt
        - updatedAt
      type: object
    TenantSettingsRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TenantSettingsRequestBody.json
          format: uri
          readOnly: true
          type: string
        features:
          additionalProperties: {}
          examples:
            - {}
          type: object
        fileQuotaBytes:
          examples:
            - 1048576
          format: int64
          type: integer
        notificationsEnabled:
          examples:
            - true
          type: boolean
        rateLimitBrowserApiPerMinute:
          examples:
            - 50
          format: int32
          type: integer
        rateLimitExternalApiPerMinute:
          examples:
            - 50
          format: int32
          type: integer
        rateLimitLoginPerMinute:
          examples:
            - 50
          format: int32
          type: integer
      required:
        - fileQuotaBytes
        - notificationsEnabled
      type: object
    TodoBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TodoBody.json
          format: uri
          readOnly: true
          type: string
        completed:
          examples:
            - false
          type: boolean
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        title:
          examples:
            - Follow up with customer
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - title
        - completed
        - createdAt
        - updatedAt
      type: object
    TodoListBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TodoListBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TodoBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    UpdateCustomerSignalBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/UpdateCustomerSignalBody.json
          format: uri
          readOnly: true
          type: string
        body:
          examples:
            - Customer asked for monthly report export.
          maxLength: 4000
          type: string
        customerName:
          examples:
            - Acme
          maxLength: 120
          minLength: 1
          type: string
        priority:
          enum:
            - low
            - medium
            - high
            - urgent
          examples:
            - medium
          type: string
        source:
          description: record の発生元または permission の由来です。
          enum:
            - support
            - sales
            - customer_success
            - research
            - internal
            - other
          examples:
            - support
          type: string
        status:
          description: 現在の lifecycle status です。
          enum:
            - new
            - triaged
            - planned
            - closed
          examples:
            - triaged
          type: string
        title:
          examples:
            - Export CSV from reports
          maxLength: 200
          minLength: 1
          type: string
      type: object
    UpdateDriveFileBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/UpdateDriveFileBody.json
          format: uri
          readOnly: true
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          maxLength: 4000
          type: string
        originalFilename:
          examples:
            - roadmap.md
          maxLength: 255
          type: string
        parentFolderPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        tags:
          description: 検索や分類に使う tag 配列です。
          examples:
            - - example
          items:
            type: string
          type: array
      type: object
    UpdateDriveFolderBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/UpdateDriveFolderBody.json
          format: uri
          readOnly: true
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          maxLength: 4000
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 255
          type: string
        parentFolderPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        tags:
          description: 検索や分類に使う tag 配列です。
          examples:
            - - example
          items:
            type: string
          type: array
      type: object
    UpdateDriveShareBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/UpdateDriveShareBody.json
          format: uri
          readOnly: true
          type: string
        role:
          description: resource に対して付与する role または tenant role code です。
          enum:
            - editor
            - viewer
          examples:
            - editor
          type: string
      required:
        - role
      type: object
    UpdateDriveShareLinkBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/UpdateDriveShareLinkBody.json
          format: uri
          readOnly: true
          type: string
        canDownload:
          description: share link または permission で download を許可するかを表します。
          examples:
            - true
          type: boolean
        expiresAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      type: object
    UpdateEntitlementsInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/UpdateEntitlementsInputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/EntitlementUpdateBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    UpdateTodoBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/UpdateTodoBody.json
          format: uri
          readOnly: true
          type: string
        completed:
          examples:
            - true
          type: boolean
        title:
          examples:
            - Follow up with customer
          maxLength: 200
          type: string
      type: object
    UserResponse:
      additionalProperties: false
      properties:
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Demo User
          type: string
        email:
          description: user または invitee の email address です。
          examples:
            - demo@example.com
          format: email
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - publicId
        - email
        - displayName
      type: object
    VerifyIntegrationBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/VerifyIntegrationBody.json
          format: uri
          readOnly: true
          type: string
        accessExpiresAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        connected:
          examples:
            - true
          type: boolean
        refreshedAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        resourceServer:
          examples:
            - zitadel
          type: string
        scopes:
          examples:
            - - offline_access
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - resourceServer
        - connected
      type: object
    WebhookDeliveryBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/WebhookDeliveryBody.json
          format: uri
          readOnly: true
          type: string
        attemptCount:
          examples:
            - 3
          format: int32
          type: integer
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        deliveredAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        eventType:
          examples:
            - example
          type: string
        lastError:
          examples:
            - example
          type: string
        lastHttpStatus:
          examples:
            - 1
          format: int32
          type: integer
        maxAttempts:
          examples:
            - 1
          format: int32
          type: integer
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        responsePreview:
          examples:
            - example
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - eventType
        - status
        - attemptCount
        - maxAttempts
        - createdAt
        - updatedAt
      type: object
    WebhookDeliveryListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/WebhookDeliveryListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/WebhookDeliveryBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    WebhookEndpointBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/WebhookEndpointBody.json
          format: uri
          readOnly: true
          type: string
        active:
          examples:
            - true
          type: boolean
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        eventTypes:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        lastDeliveryAt:
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        secret:
          examples:
            - example
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        url:
          description: callback または external service の URL です。
          examples:
            - https://example.com/webhooks/haohao
          format: uri
          type: string
      required:
        - publicId
        - name
        - url
        - eventTypes
        - active
        - createdAt
        - updatedAt
      type: object
    WebhookEndpointRequestBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/WebhookEndpointRequestBody.json
          format: uri
          readOnly: true
          type: string
        active:
          examples:
            - true
          type: boolean
        eventTypes:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 120
          minLength: 1
          type: string
        url:
          description: callback または external service の URL です。
          examples:
            - https://example.com/webhooks/haohao
          format: uri
          type: string
      required:
        - name
        - url
        - eventTypes
      type: object
    WebhookListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/WebhookListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/WebhookEndpointBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    ZitadelSettingsBody:
      additionalProperties: false
      properties:
        clientId:
          examples:
            - "312345678901234567"
          type: string
        issuer:
          examples:
            - http://localhost:8081
          format: uri
          type: string
      required:
        - issuer
        - clientId
      type: object
  securitySchemes:
    cookieAuth:
      in: cookie
      name: SESSION_ID
      type: apiKey
info:
  description: |-
    HaoHao API は browser session API、tenant 管理 API、Drive API、external integration API をまとめた OpenAPI document です。

    ## 認証

    - browser API は `SESSION_ID` Cookie を使います。状態を変更する request では `X-CSRF-Token` も送ってください。
    - external / M2M / SCIM API は Bearer token を使います。必要な audience、scope、role は環境設定で制御されます。
    - tenant-scoped browser API は active tenant を前提にします。必要に応じて tenant select API で active tenant を切り替えます。

    ## Request の基本

    - UUID は OpenAPI 上では `format: uuid` として表します。
    - timestamp は RFC3339 UTC の `date-time` です。
    - list API の `limit` / `offset` は pagination に使います。
    - `Idempotency-Key` がある mutation API は、同じ key と同じ body の retry に対して保存済み response を返します。

    ## Error response

    error は Problem Details 形式です。validation error、認証エラー、認可エラー、not found、conflict、rate limit、server error は `application/problem+json` の `default` response として表します。
  title: HaoHao API
  version: 0.1.0
openapi: 3.1.0
paths:
  /api/office/webhooks/{provider}:
    post:
      description: |-
        Drive Office provider webhook を受け取る。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: acceptDriveOfficeWebhook
      parameters:
        - description: path 内の `provider` を指定します。
          example: example
          in: path
          name: provider
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  checksum: example
                  providerEventId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  providerFileId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  revision: example
            schema:
              $ref: "#/components/schemas/DriveOfficeWebhookInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    providerEventId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    providerFileId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    result: example
                    revision: example
              schema:
                $ref: "#/components/schemas/DriveOfficeWebhookOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      summary: Drive Office provider webhook を受け取る
      tags:
        - Drive Collaboration & Sync
  /api/public/drive/share-links/{token}:
    get:
      description: |-
        public Drive share link metadata を返す。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
      operationId: getPublicDriveShareLink
      parameters:
        - description: path 内の `token` を指定します。
          example: token_demo_1234567890
          in: path
          name: token
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    file:
                      $schema: https://example.com/schemas/DriveFileBody.json
                      byteSize: 1048576
                      contentType: text/markdown
                      createdAt: "2026-01-15T09:30:00Z"
                      deletedAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      dlpBlocked: false
                      inheritanceEnabled: true
                      lockReason: example
                      locked: true
                      lockedAt: "2026-01-15T09:30:00Z"
                      metadata: {}
                      originalFilename: spec.md
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      scanStatus: active
                      sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                      status: active
                      updatedAt: "2026-01-15T09:30:00Z"
                      workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    folder:
                      $schema: https://example.com/schemas/DriveFolderBody.json
                      createdAt: "2026-01-15T09:30:00Z"
                      deletedAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      inheritanceEnabled: true
                      name: Project
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      updatedAt: "2026-01-15T09:30:00Z"
                      workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    link:
                      $schema: https://example.com/schemas/DriveShareLinkBody.json
                      canDownload: true
                      createdAt: "2026-01-15T09:30:00Z"
                      expiresAt: "2026-01-15T09:30:00Z"
                      passwordRequired: true
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      resourceType: file
                      role: viewer
                      status: active
                      token: token_demo_1234567890
                      updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/PublicDriveShareLinkOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      summary: public Drive share link metadata を返す
      tags:
        - Drive Sharing & Permissions
  /api/public/drive/share-links/{token}/children:
    get:
      description: |-
        public Drive folder share link children を返す。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
      operationId: listPublicDriveShareLinkChildren
      parameters:
        - description: path 内の `token` を指定します。
          example: token_demo_1234567890
          in: path
          name: token
          required: true
          schema:
            type: string
        - description: request cookie `DRIVE_SHARE_LINK_VERIFICATION` です。
          example: example
          in: cookie
          name: DRIVE_SHARE_LINK_VERIFICATION
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveItemListOutputBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/DriveItemListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      summary: public Drive folder share link children を返す
      tags:
        - Drive Sharing & Permissions
  /api/v1/admin/tenants:
    get:
      description: |-
        tenant admin 用の tenant 一覧を返す。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantAdminTenants
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - active: true
                        activeMemberCount: 3
                        createdAt: "2026-01-15T09:30:00Z"
                        displayName: Acme
                        id: 1
                        slug: acme
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/ListTenantAdminTenantsOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant admin 用の tenant 一覧を返す
      tags:
        - Tenant Administration
    post:
      description: |-
        tenant を作成する。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createTenantAdminTenant
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  active: true
                  displayName: Acme
                  slug: acme
            schema:
              $ref: "#/components/schemas/TenantAdminTenantRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    active: true
                    activeMemberCount: 3
                    createdAt: "2026-01-15T09:30:00Z"
                    displayName: Acme
                    id: 1
                    slug: acme
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminTenantBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant を作成する
      tags:
        - Tenant Administration
  /api/v1/admin/tenants/{tenantSlug}:
    delete:
      description: |-
        tenant を無効化する。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deactivateTenantAdminTenant
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant を無効化する
      tags:
        - Tenant Administration
    get:
      description: |-
        tenant detail と membership を返す。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getTenantAdminTenant
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    memberships:
                      - deactivated: false
                        displayName: Demo User
                        email: demo@example.com
                        roles: []
                        userPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    tenant:
                      $schema: https://example.com/schemas/TenantAdminTenantBody.json
                      active: true
                      activeMemberCount: 3
                      createdAt: "2026-01-15T09:30:00Z"
                      displayName: Acme
                      id: 1
                      slug: acme
                      updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminTenantDetailBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant detail と membership を返す
      tags:
        - Tenant Administration
    put:
      description: |-
        tenant を更新する。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateTenantAdminTenant
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  active: true
                  displayName: Acme
                  slug: acme
            schema:
              $ref: "#/components/schemas/TenantAdminTenantRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantAdminTenantBody.json
                    active: true
                    activeMemberCount: 3
                    createdAt: "2026-01-15T09:30:00Z"
                    displayName: Acme
                    id: 1
                    slug: acme
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminTenantBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant を更新する
      tags:
        - Tenant Administration
  /api/v1/admin/tenants/{tenantSlug}/data-access/groups:
    get:
      description: |-
        GET /api/v1/admin/tenants/{tenantSlug}/data-access/groups。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantAdminDataAccessGroups
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            maximum: 200
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - createdAt: "2026-01-15T09:30:00Z"
                        createdByUserId: 1
                        description: Quarterly planning documents.
                        members: []
                        name: Acme
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        systemKey: example
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminDataAccessGroupListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      tags:
        - Tenant Administration
    post:
      description: |-
        POST /api/v1/admin/tenants/{tenantSlug}/data-access/groups。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createTenantAdminDataAccessGroup
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  description: Quarterly planning documents.
                  name: Acme
            schema:
              $ref: "#/components/schemas/TenantAdminDataAccessGroupCreateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    createdByUserId: 1
                    description: Quarterly planning documents.
                    members:
                      - example
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    systemKey: example
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminDataAccessGroupBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      tags:
        - Tenant Administration
  /api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}:
    get:
      description: |-
        GET /api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getTenantAdminDataAccessGroup
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            type: string
        - description: path 内の `groupPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: groupPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantAdminDataAccessGroupBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    createdByUserId: 1
                    description: Quarterly planning documents.
                    members:
                      - example
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    systemKey: example
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminDataAccessGroupBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      tags:
        - Tenant Administration
    patch:
      description: |-
        PATCH /api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateTenantAdminDataAccessGroup
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            type: string
        - description: path 内の `groupPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: groupPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  description: Quarterly planning documents.
                  name: Acme
            schema:
              $ref: "#/components/schemas/TenantAdminDataAccessGroupUpdateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantAdminDataAccessGroupBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    createdByUserId: 1
                    description: Quarterly planning documents.
                    members:
                      - example
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    systemKey: example
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminDataAccessGroupBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      tags:
        - Tenant Administration
  /api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}/members:
    post:
      description: |-
        POST /api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}/members。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: addTenantAdminDataAccessGroupMember
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            type: string
        - description: path 内の `groupPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: groupPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  userPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/TenantAdminDataAccessMemberWriteBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      tags:
        - Tenant Administration
  /api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}/members/{userPublicId}:
    delete:
      description: |-
        DELETE /api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}/members/{userPublicId}。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: removeTenantAdminDataAccessGroupMember
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            type: string
        - description: path 内の `groupPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: groupPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: path 内の `userPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: userPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      tags:
        - Tenant Administration
  /api/v1/admin/tenants/{tenantSlug}/data-access/scope/permissions:
    get:
      description: |-
        GET /api/v1/admin/tenants/{tenantSlug}/data-access/scope/permissions。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantAdminDataAccessScopePermissions
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantAdminDataAccessPermissionListOutputBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/TenantAdminDataAccessPermissionListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      tags:
        - Tenant Administration
    put:
      description: |-
        PUT /api/v1/admin/tenants/{tenantSlug}/data-access/scope/permissions。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: putTenantAdminDataAccessScopePermissions
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  actions:
                    - example
                  subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  subjectType: user
            schema:
              $ref: "#/components/schemas/TenantAdminDataAccessPermissionWriteBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      tags:
        - Tenant Administration
  /api/v1/admin/tenants/{tenantSlug}/data-access/{resourceType}/{resourcePublicId}/permissions:
    get:
      description: |-
        GET /api/v1/admin/tenants/{tenantSlug}/data-access/{resourceType}/{resourcePublicId}/permissions。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantAdminDataAccessResourcePermissions
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            type: string
        - description: path 内の `resourceType` を指定します。
          example: dataset
          in: path
          name: resourceType
          required: true
          schema:
            enum:
              - dataset
              - work_table
              - data_pipeline
            type: string
        - description: path 内の `resourcePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: resourcePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - action: example
                        createdAt: "2026-01-15T09:30:00Z"
                        subjectGroupName: Acme
                        subjectGroupPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        subjectType: user
                        subjectUserEmail: demo@example.com
                        subjectUserName: Acme
                        subjectUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/TenantAdminDataAccessPermissionListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      tags:
        - Tenant Administration
    put:
      description: |-
        PUT /api/v1/admin/tenants/{tenantSlug}/data-access/{resourceType}/{resourcePublicId}/permissions。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: putTenantAdminDataAccessResourcePermissions
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            type: string
        - description: path 内の `resourceType` を指定します。
          example: dataset
          in: path
          name: resourceType
          required: true
          schema:
            enum:
              - dataset
              - work_table
              - data_pipeline
            type: string
        - description: path 内の `resourcePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: resourcePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  actions:
                    - example
                  subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  subjectType: user
            schema:
              $ref: "#/components/schemas/TenantAdminDataAccessPermissionWriteBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      tags:
        - Tenant Administration
  /api/v1/admin/tenants/{tenantSlug}/data-pipelines/schema-mapping/examples:
    get:
      description: |-
        tenant admin 用 schema mapping example 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantAdminSchemaMappingExamples
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: 検索語または filter query です。
          example: roadmap
          explode: false
          in: query
          name: q
          schema:
            maxLength: 240
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `sharedScope` です。
          example: private
          explode: false
          in: query
          name: sharedScope
          schema:
            enum:
              - private
              - tenant
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `decision` です。
          example: accepted
          explode: false
          in: query
          name: decision
          schema:
            enum:
              - accepted
              - rejected
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            format: int32
            maximum: 200
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - createdAt: "2026-01-15T09:30:00Z"
                        decidedAt: "2026-01-15T09:30:00Z"
                        decision: example
                        domain: example
                        neighborColumns:
                          - example
                        pipelineName: Acme
                        pipelinePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        sampleValues:
                          - example
                        schemaColumnPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        schemaType: example
                        searchDocumentMaterialized: true
                        sharedAt: "2026-01-15T09:30:00Z"
                        sharedScope: example
                        sheetName: Acme
                        sourceColumn: support
                        targetColumn: example
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminSchemaMappingExampleListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant admin 用 schema mapping example 一覧を返す
      tags:
        - Data & Datasets
  /api/v1/admin/tenants/{tenantSlug}/data-pipelines/schema-mapping/examples/{mappingExamplePublicId}/sharing:
    patch:
      description: |-
        schema mapping example の tenant 共有を切り替える。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateTenantAdminSchemaMappingExampleSharing
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `mappingExamplePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: mappingExamplePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  sharedScope: private
            schema:
              $ref: "#/components/schemas/TenantAdminSchemaMappingExampleSharingInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/SchemaMappingExampleBody.json
                    decision: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    schemaColumnPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    sharedScope: example
                    sourceColumn: support
                    targetColumn: example
              schema:
                $ref: "#/components/schemas/SchemaMappingExampleBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: schema mapping example の tenant 共有を切り替える
      tags:
        - Data & Datasets
  /api/v1/admin/tenants/{tenantSlug}/data-pipelines/schema-mapping/search-documents/rebuild:
    post:
      description: |-
        schema mapping search documents を rebuild する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: rebuildTenantAdminSchemaMappingSearchDocuments
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  limit: 50
            schema:
              $ref: "#/components/schemas/TenantAdminSchemaMappingSearchDocumentsRebuildInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    indexed: 1
                    mappingExamplesIndexed: 1
                    schemaColumnsIndexed: 1
              schema:
                $ref: "#/components/schemas/TenantAdminSchemaMappingSearchDocumentsRebuildOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: schema mapping search documents を rebuild する
      tags:
        - Data & Datasets
  /api/v1/admin/tenants/{tenantSlug}/drive/audit-events:
    get:
      description: |-
        tenant admin 用 Drive audit events を返す。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantAdminDriveAuditEvents
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - action: example
                        actorType: example
                        metadata: {}
                        occurredAt: "2026-01-15T09:30:00Z"
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        targetId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        targetType: example
              schema:
                $ref: "#/components/schemas/TenantAdminDriveAuditOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant admin 用 Drive audit events を返す
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/clean-rooms:
    post:
      description: |-
        Drive clean room を作成する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveCleanRoom
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  name: Acme
            schema:
              $ref: "#/components/schemas/DriveCleanRoomCreateInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveCleanRoomBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive clean room を作成する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/clean-rooms/{roomPublicId}/datasets:
    post:
      description: |-
        Drive clean room dataset を投入する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: submitDriveCleanRoomDataset
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `roomPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: roomPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/DriveCleanRoomDatasetInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    cleanRoomPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    createdAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    sourceFilePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveCleanRoomDatasetBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive clean room dataset を投入する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/clean-rooms/{roomPublicId}/exports:
    post:
      description: |-
        Drive clean room export を要求する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: requestDriveCleanRoomExport
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `roomPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: roomPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  rawDatasetExport: true
            schema:
              $ref: "#/components/schemas/DriveCleanRoomExportInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    deniedReason: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveCleanRoomExportBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive clean room export を要求する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/clean-rooms/{roomPublicId}/participants:
    post:
      description: |-
        Drive clean room participant を追加する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: addDriveCleanRoomParticipant
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `roomPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: roomPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  role: participant
                  userPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/DriveCleanRoomParticipantInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive clean room participant を追加する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/content-access-sessions:
    post:
      description: |-
        Drive admin content break-glass session を開始する。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: startTenantAdminDriveContentAccessSession
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  reason: example
                  reasonCategory: manual
            schema:
              $ref: "#/components/schemas/TenantAdminDriveAdminContentSessionInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    reasonCategory: example
              schema:
                $ref: "#/components/schemas/TenantAdminDriveAdminContentSessionBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive admin content break-glass session を開始する
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/content-access-sessions/current:
    delete:
      description: |-
        Drive admin content break-glass session を終了する。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: endTenantAdminDriveContentAccessSession
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive admin content break-glass session を終了する
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/ediscovery/connections:
    post:
      description: |-
        Drive eDiscovery provider connection を作成する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveEDiscoveryConnection
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  provider: example
            schema:
              $ref: "#/components/schemas/DriveEDiscoveryConnectionInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    provider: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveEDiscoveryConnectionBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive eDiscovery provider connection を作成する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/ediscovery/exports:
    post:
      description: |-
        Drive eDiscovery export を request する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: requestDriveEDiscoveryExport
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  casePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  connectionPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/DriveEDiscoveryExportInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    casePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    createdAt: "2026-01-15T09:30:00Z"
                    itemCount: 3
                    manifestHash: example
                    providerExportId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveEDiscoveryExportBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive eDiscovery export を request する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/ediscovery/exports/{exportPublicId}/approve:
    post:
      description: |-
        Drive eDiscovery export を approve する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: approveDriveEDiscoveryExport
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `exportPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: exportPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveEDiscoveryExportBody.json
                    casePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    createdAt: "2026-01-15T09:30:00Z"
                    itemCount: 3
                    manifestHash: example
                    providerExportId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveEDiscoveryExportBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive eDiscovery export を approve する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/files/{filePublicId}/metadata:
    get:
      description: |-
        break-glass session 中に Drive file metadata を返す。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getTenantAdminDriveFileMetadata
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    byteSize: 1048576
                    contentType: text/markdown
                    createdAt: "2026-01-15T09:30:00Z"
                    deletedAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    dlpBlocked: false
                    inheritanceEnabled: true
                    lockReason: example
                    locked: true
                    lockedAt: "2026-01-15T09:30:00Z"
                    metadata: {}
                    originalFilename: spec.md
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    scanStatus: active
                    sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                    status: active
                    updatedAt: "2026-01-15T09:30:00Z"
                    workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveFileBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: break-glass session 中に Drive file metadata を返す
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/gateways:
    post:
      description: |-
        Drive on-prem gateway を登録する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: registerDriveGateway
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  certificateFingerprint: example
                  endpointUrl: https://example.com/webhooks/haohao
                  name: Acme
            schema:
              $ref: "#/components/schemas/DriveGatewayInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    certificateFingerprint: example
                    createdAt: "2026-01-15T09:30:00Z"
                    endpointUrl: https://example.com/webhooks/haohao
                    lastSeenAt: "2026-01-15T09:30:00Z"
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveGatewayBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive on-prem gateway を登録する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/gateways/{gatewayPublicId}:
    patch:
      description: |-
        Drive gateway status を更新する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDriveGatewayStatus
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `gatewayPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: gatewayPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  status: active
            schema:
              $ref: "#/components/schemas/DriveGatewayStatusInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveGatewayBody.json
                    certificateFingerprint: example
                    createdAt: "2026-01-15T09:30:00Z"
                    endpointUrl: https://example.com/webhooks/haohao
                    lastSeenAt: "2026-01-15T09:30:00Z"
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveGatewayBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive gateway status を更新する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/gateways/{gatewayPublicId}/objects:
    post:
      description: |-
        Drive file を gateway object として bind する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: bindDriveGatewayFile
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `gatewayPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: gatewayPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/DriveGatewayBindInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    gatewayPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    manifestHash: example
                    status: active
              schema:
                $ref: "#/components/schemas/DriveGatewayObjectBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file を gateway object として bind する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/hsm/bindings:
    post:
      description: |-
        Drive HSM key を file に bind する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: bindDriveHSMKey
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  keyPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/DriveHSMBindInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive HSM key を file に bind する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/hsm/deployments:
    post:
      description: |-
        Drive HSM deployment を作成する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveHSMDeployment
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  endpointUrl: https://example.com/webhooks/haohao
                  provider: example
            schema:
              $ref: "#/components/schemas/DriveHSMDeploymentInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    attestationHash: example
                    createdAt: "2026-01-15T09:30:00Z"
                    endpointUrl: https://example.com/webhooks/haohao
                    healthStatus: active
                    keyPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    keyStatus: active
                    provider: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveHSMDeploymentBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive HSM deployment を作成する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/hsm/keys/{keyPublicId}:
    patch:
      description: |-
        Drive HSM key status を更新する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDriveHSMKeyStatus
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `keyPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: keyPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  status: active
            schema:
              $ref: "#/components/schemas/DriveHSMKeyStatusInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveHSMDeploymentBody.json
                    attestationHash: example
                    createdAt: "2026-01-15T09:30:00Z"
                    endpointUrl: https://example.com/webhooks/haohao
                    healthStatus: active
                    keyPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    keyStatus: active
                    provider: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveHSMDeploymentBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive HSM key status を更新する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/invitations:
    get:
      description: |-
        tenant admin 用 Drive invitations を返す。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantAdminDriveInvitations
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - acceptToken: token_demo_1234567890
                        createdAt: "2026-01-15T09:30:00Z"
                        expiresAt: "2026-01-15T09:30:00Z"
                        inviteeEmailDomain: demo@example.com
                        maskedInviteeEmail: demo@example.com
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceType: support
                        role: viewer
                        status: active
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminDriveInvitationsOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant admin 用 Drive invitations を返す
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/legal/cases:
    post:
      description: |-
        Drive legal case を作成する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveLegalCase
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  description: Quarterly planning documents.
                  name: Acme
            schema:
              $ref: "#/components/schemas/DriveLegalCaseCreateInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveLegalCaseBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive legal case を作成する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/legal/cases/{casePublicId}/exports:
    post:
      description: |-
        Drive legal export を作成する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: createDriveLegalExport
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `casePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: casePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    casePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    createdAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveLegalExportBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive legal export を作成する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/legal/cases/{casePublicId}/files:
    post:
      description: |-
        Drive legal case に file hold を追加する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: addDriveLegalCaseFile
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `casePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: casePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  reason: example
            schema:
              $ref: "#/components/schemas/DriveLegalCaseFileInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive legal case に file hold を追加する
      tags:
        - Drive Security & Compliance
  /api/v1/admin/tenants/{tenantSlug}/drive/marketplace/installations:
    post:
      description: |-
        Drive marketplace app install を request する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: installDriveMarketplaceApp
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  appSlug: example
            schema:
              $ref: "#/components/schemas/DriveMarketplaceInstallInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    appName: Acme
                    appSlug: example
                    createdAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    scopes:
                      - example
                    status: active
              schema:
                $ref: "#/components/schemas/DriveMarketplaceInstallBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive marketplace app install を request する
      tags:
        - Platform Integrations
  /api/v1/admin/tenants/{tenantSlug}/drive/marketplace/installations/{installationPublicId}:
    delete:
      description: |-
        Drive marketplace app install を uninstall する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: uninstallDriveMarketplaceInstallation
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `installationPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: installationPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive marketplace app install を uninstall する
      tags:
        - Platform Integrations
  /api/v1/admin/tenants/{tenantSlug}/drive/marketplace/installations/{installationPublicId}/approve:
    post:
      description: |-
        Drive marketplace app install を approve する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: approveDriveMarketplaceInstallation
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `installationPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: installationPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveMarketplaceInstallBody.json
                    appName: Acme
                    appSlug: example
                    createdAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    scopes:
                      - example
                    status: active
              schema:
                $ref: "#/components/schemas/DriveMarketplaceInstallBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive marketplace app install を approve する
      tags:
        - Platform Integrations
  /api/v1/admin/tenants/{tenantSlug}/drive/marketplace/installations/{installationPublicId}/scope-check:
    get:
      description: |-
        Drive marketplace scope と OpenFGA permission を確認する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: checkDriveMarketplaceScope
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `installationPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: installationPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `scope` です。
          example: example
          explode: false
          in: query
          name: scope
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          explode: false
          in: query
          name: filePublicId
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    allowed: true
              schema:
                $ref: "#/components/schemas/DriveMarketplaceScopeOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive marketplace scope と OpenFGA permission を確認する
      tags:
        - Platform Integrations
  /api/v1/admin/tenants/{tenantSlug}/drive/ocr/status:
    get:
      description: |-
        tenant admin 用 Drive OCR runtime status を返す。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getTenantAdminDriveOCRStatus
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    dependencies:
                      - available: true
                        name: Acme
                        version: example
                    enabled: true
                    lmStudio:
                      configured: true
                      modelAvailable: true
                      reachable: true
                    localCommands:
                      - available: true
                        command: example
                        configured: true
                        name: Acme
                        version: example
                    ocrEngine: example
                    ollama:
                      configured: true
                      modelAvailable: true
                      reachable: true
                    statusCounts: {}
                    structuredExtractor: example
              schema:
                $ref: "#/components/schemas/TenantAdminDriveOCRStatusBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant admin 用 Drive OCR runtime status を返す
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/openfga-sync/drift:
    get:
      description: |-
        Drive OpenFGA drift dry-run を返す。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getTenantAdminDriveOpenFGADrift
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    dryRun: true
                    items:
                      - action: example
                        error: example
                        kind: example
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        status: active
              schema:
                $ref: "#/components/schemas/TenantAdminDriveSyncOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive OpenFGA drift dry-run を返す
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/openfga-sync/repair:
    post:
      description: |-
        Drive OpenFGA pending sync を修復する。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: repairTenantAdminDriveOpenFGASync
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantAdminDriveSyncOutputBody.json
                    dryRun: true
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/TenantAdminDriveSyncOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive OpenFGA pending sync を修復する
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/operations/drift-check:
    post:
      description: |-
        Drive OpenFGA drift dry-run を operation endpoint から実行する。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: checkTenantAdminDriveOperationsDrift
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantAdminDriveSyncOutputBody.json
                    dryRun: true
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/TenantAdminDriveSyncOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive OpenFGA drift dry-run を operation endpoint から実行する
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/operations/health:
    get:
      description: |-
        Drive / OpenFGA / storage operations health を返す。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getTenantAdminDriveOperationsHealth
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    checkedAt: "2026-01-15T09:30:00Z"
                    missingWorkspaceCount: 3
                    openfgaDriftCount: 3
                    storageMissingCount: 3
                    storageOrphanCheckState: example
                    tenantId: 1
                    workspaceCount: 3
              schema:
                $ref: "#/components/schemas/TenantAdminDriveOperationsHealthBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive / OpenFGA / storage operations health を返す
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/operations/repair:
    post:
      description: |-
        Drive OpenFGA pending sync repair を operation endpoint から実行する。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: repairTenantAdminDriveOperations
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantAdminDriveSyncOutputBody.json
                    dryRun: true
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/TenantAdminDriveSyncOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive OpenFGA pending sync repair を operation endpoint から実行する
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/residency-policy:
    get:
      description: |-
        Drive residency policy を返す。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getTenantAdminDriveResidencyPolicy
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    allowedRegions:
                      - example
                    backupRegion: example
                    indexRegion: example
                    primaryRegion: example
                    replicationMode: example
                    status: active
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminDriveResidencyPolicyBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive residency policy を返す
      tags:
        - Drive Admin & Governance
    put:
      description: |-
        Drive residency policy を更新する。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateTenantAdminDriveResidencyPolicy
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  allowedRegions:
                    - example
                  backupRegion: example
                  indexRegion: example
                  primaryRegion: example
                  replicationMode: example
                  status: active
            schema:
              $ref: "#/components/schemas/TenantAdminDriveResidencyPolicyBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantAdminDriveResidencyPolicyBody.json
                    allowedRegions:
                      - example
                    backupRegion: example
                    indexRegion: example
                    primaryRegion: example
                    replicationMode: example
                    status: active
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminDriveResidencyPolicyBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive residency policy を更新する
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/search/index/rebuild:
    post:
      description: |-
        Drive search index を rebuild する。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: rebuildTenantAdminDriveSearchIndex
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    failed: 1
                    indexed: 1
                    skipped: 1
              schema:
                $ref: "#/components/schemas/DriveIndexRebuildBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive search index を rebuild する
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/search/local-index/jobs:
    get:
      description: |-
        Drive local search index job 一覧を返す。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantAdminDriveLocalSearchIndexJobs
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `status` です。
          example: queued
          explode: false
          in: query
          name: status
          schema:
            enum:
              - queued
              - processing
              - completed
              - failed
              - skipped
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int32
            maximum: 200
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - $schema: https://example.com/schemas/LocalSearchIndexJobBody.json
                        attempts: 1
                        completedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        failedCount: 3
                        indexedCount: 3
                        lastError: example
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        reason: example
                        resourceId: 1
                        resourceKind: drive_file
                        resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        skippedCount: 3
                        startedAt: "2026-01-15T09:30:00Z"
                        status: queued
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/LocalSearchIndexJobListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive local search index job 一覧を返す
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/search/local-index/rebuilds:
    post:
      description: |-
        Drive local search index rebuild を enqueue する。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: createTenantAdminDriveLocalSearchRebuild
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    attempts: 1
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    failedCount: 3
                    indexedCount: 3
                    lastError: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    reason: example
                    resourceId: 1
                    resourceKind: drive_file
                    resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    skippedCount: 3
                    startedAt: "2026-01-15T09:30:00Z"
                    status: queued
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/LocalSearchIndexJobBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive local search index rebuild を enqueue する
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/security/encryption-policy:
    get:
      description: |-
        Drive encryption policy を返す。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getTenantAdminDriveEncryptionPolicy
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    keyPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    keyStatus: active
                    maskedKeyRef: example
                    mode: example
                    provider: example
                    scope: example
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminDriveEncryptionPolicyBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive encryption policy を返す
      tags:
        - Drive Admin & Governance
    put:
      description: |-
        Drive encryption policy を更新する。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateTenantAdminDriveEncryptionPolicy
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  keyRef: example
                  mode: service_managed
                  provider: example
            schema:
              $ref: "#/components/schemas/TenantAdminDriveEncryptionPolicyInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantAdminDriveEncryptionPolicyBody.json
                    keyPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    keyStatus: active
                    maskedKeyRef: example
                    mode: example
                    provider: example
                    scope: example
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminDriveEncryptionPolicyBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive encryption policy を更新する
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/security/kms-keys/{keyPublicId}:
    patch:
      description: |-
        Drive KMS key status を更新する。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateTenantAdminDriveKmsKeyStatus
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `keyPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: keyPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  status: active
            schema:
              $ref: "#/components/schemas/TenantAdminDriveKMSKeyStatusInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantAdminDriveEncryptionPolicyBody.json
                    keyPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    keyStatus: active
                    maskedKeyRef: example
                    mode: example
                    provider: example
                    scope: example
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminDriveEncryptionPolicyBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive KMS key status を更新する
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/share-approvals:
    get:
      description: |-
        tenant admin 用 Drive share approvals を返す。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantAdminDriveShareApprovals
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantAdminDriveInvitationsOutputBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/TenantAdminDriveInvitationsOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant admin 用 Drive share approvals を返す
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/share-approvals/{invitationPublicId}/approve:
    post:
      description: |-
        Drive external share approval を承認する。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: approveTenantAdminDriveShareApproval
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `invitationPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: invitationPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive external share approval を承認する
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/share-approvals/{invitationPublicId}/reject:
    post:
      description: |-
        Drive external share approval を拒否する。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: rejectTenantAdminDriveShareApproval
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `invitationPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: invitationPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive external share approval を拒否する
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/share-links:
    get:
      description: |-
        tenant admin 用 Drive share link state を返す。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantAdminDriveShareLinks
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - canDownload: true
                        createdAt: "2026-01-15T09:30:00Z"
                        expiresAt: "2026-01-15T09:30:00Z"
                        passwordRequired: true
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceName: support
                        resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceType: support
                        status: active
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminDriveShareLinksOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant admin 用 Drive share link state を返す
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/drive/shares:
    get:
      description: |-
        tenant admin 用 Drive share state を返す。

        tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantAdminDriveShares
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - createdAt: "2026-01-15T09:30:00Z"
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceName: support
                        resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceType: support
                        role: viewer
                        status: active
                        subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        subjectType: example
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantAdminDriveSharesOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant admin 用 Drive share state を返す
      tags:
        - Drive Admin & Governance
  /api/v1/admin/tenants/{tenantSlug}/entitlements:
    get:
      description: |-
        tenant entitlements を返す。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantEntitlements
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - description: Quarterly planning documents.
                        displayName: Acme
                        enabled: true
                        featureCode: example
                        limitValue: {}
                        source: support
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/EntitlementListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant entitlements を返す
      tags:
        - Platform Integrations
    put:
      description: |-
        tenant entitlements を更新する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateTenantEntitlements
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  items:
                    - enabled: true
                      featureCode: example
                      limitValue: {}
            schema:
              $ref: "#/components/schemas/UpdateEntitlementsInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/EntitlementListOutputBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/EntitlementListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant entitlements を更新する
      tags:
        - Platform Integrations
  /api/v1/admin/tenants/{tenantSlug}/exports:
    get:
      description: |-
        tenant data export 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantDataExports
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - completedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        errorSummary: example
                        expiresAt: "2026-01-15T09:30:00Z"
                        fileObjectId: 1
                        format: json
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        status: processing
                        tenantId: 1
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantDataExportListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant data export 一覧を返す
      tags:
        - Data & Datasets
    post:
      description: |-
        tenant data export を request する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createTenantDataExport
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: 同じ request の retry を安全に扱うための任意 key です。同じ key は同じ method/path/body にだけ再利用できます。
          example: req_20260115_customer_signal_001
          in: header
          name: Idempotency-Key
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  format: json
            schema:
              $ref: "#/components/schemas/CreateTenantDataExportRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    errorSummary: example
                    expiresAt: "2026-01-15T09:30:00Z"
                    fileObjectId: 1
                    format: json
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: processing
                    tenantId: 1
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantDataExportBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant data export を request する
      tags:
        - Data & Datasets
  /api/v1/admin/tenants/{tenantSlug}/exports/{exportPublicId}:
    get:
      description: |-
        tenant data export detail を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getTenantDataExport
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `exportPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: exportPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantDataExportBody.json
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    errorSummary: example
                    expiresAt: "2026-01-15T09:30:00Z"
                    fileObjectId: 1
                    format: json
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: processing
                    tenantId: 1
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantDataExportBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant data export detail を返す
      tags:
        - Data & Datasets
  /api/v1/admin/tenants/{tenantSlug}/exports/{exportPublicId}/download:
    get:
      description: |-
        ready tenant data export を download する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: downloadTenantDataExport
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `exportPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: exportPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value: example
              schema:
                contentEncoding: base64
                type: string
          description: 操作に成功し、response body に結果を返します。
          headers:
            Content-Disposition:
              schema:
                type: string
            Content-Type:
              schema:
                type: string
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: ready tenant data export を download する
      tags:
        - Data & Datasets
  /api/v1/admin/tenants/{tenantSlug}/imports:
    get:
      description: |-
        Customer Signals import job 一覧を返す。

        customer signal の作成、検索、更新、import workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listCustomerSignalImports
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - completedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        errorFileObjectId: 1
                        errorSummary: example
                        inputFileObjectId: 1
                        insertedRows: 1
                        invalidRows: 1
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        status: active
                        totalRows: 1
                        updatedAt: "2026-01-15T09:30:00Z"
                        validRows: 1
                        validateOnly: true
              schema:
                $ref: "#/components/schemas/CustomerSignalImportListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Customer Signals import job 一覧を返す
      tags:
        - Customer Signals
    post:
      description: |-
        Customer Signals CSV import job を作成する。

        customer signal の作成、検索、更新、import workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createCustomerSignalImport
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  inputFilePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  validateOnly: true
            schema:
              $ref: "#/components/schemas/CustomerSignalImportRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    errorFileObjectId: 1
                    errorSummary: example
                    inputFileObjectId: 1
                    insertedRows: 1
                    invalidRows: 1
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    totalRows: 1
                    updatedAt: "2026-01-15T09:30:00Z"
                    validRows: 1
                    validateOnly: true
              schema:
                $ref: "#/components/schemas/CustomerSignalImportJobBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Customer Signals CSV import job を作成する
      tags:
        - Customer Signals
  /api/v1/admin/tenants/{tenantSlug}/imports/{importPublicId}:
    get:
      description: |-
        Customer Signals import job detail を返す。

        customer signal の作成、検索、更新、import workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getCustomerSignalImport
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `importPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: importPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/CustomerSignalImportJobBody.json
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    errorFileObjectId: 1
                    errorSummary: example
                    inputFileObjectId: 1
                    insertedRows: 1
                    invalidRows: 1
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    totalRows: 1
                    updatedAt: "2026-01-15T09:30:00Z"
                    validRows: 1
                    validateOnly: true
              schema:
                $ref: "#/components/schemas/CustomerSignalImportJobBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Customer Signals import job detail を返す
      tags:
        - Customer Signals
  /api/v1/admin/tenants/{tenantSlug}/invitations:
    get:
      description: |-
        tenant invitation 一覧を返す。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenantInvitations
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - acceptUrl: https://example.com/webhooks/haohao
                        createdAt: "2026-01-15T09:30:00Z"
                        expiresAt: "2026-01-15T09:30:00Z"
                        identitySetupDeliveryMode: return_code
                        identitySetupEmailCode: demo@example.com
                        identitySetupInviteCode: example
                        identitySetupLoginUrl: https://example.com/webhooks/haohao
                        identitySetupUserId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        inviteeEmailNormalized: demo@example.com
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        roleCodes:
                          - viewer
                        status: pending
                        tenantDisplayName: Acme
                        tenantId: 1
                        tenantSlug: acme
                        token: token_demo_1234567890
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantInvitationListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant invitation 一覧を返す
      tags:
        - Tenant Workspace
    post:
      description: |-
        tenant に user を招待します。email と role codes を指定すると、招待 URL を含む invitation record が作成されます。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createTenantInvitation
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: 同じ request の retry を安全に扱うための任意 key です。同じ key は同じ method/path/body にだけ再利用できます。
          example: req_20260115_customer_signal_001
          in: header
          name: Idempotency-Key
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  email: demo@example.com
                  roleCodes:
                    - todo_user
                    - drive_user
            schema:
              $ref: "#/components/schemas/CreateTenantInvitationRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    acceptUrl: https://example.com/webhooks/haohao
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    identitySetupDeliveryMode: return_code
                    identitySetupEmailCode: demo@example.com
                    identitySetupInviteCode: example
                    identitySetupLoginUrl: https://example.com/webhooks/haohao
                    identitySetupUserId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    inviteeEmailNormalized: demo@example.com
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    roleCodes:
                      - viewer
                    status: pending
                    tenantDisplayName: Acme
                    tenantId: 1
                    tenantSlug: acme
                    token: token_demo_1234567890
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantInvitationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant invitation を作成する
      tags:
        - Tenant Workspace
  /api/v1/admin/tenants/{tenantSlug}/invitations/{invitationPublicId}:
    delete:
      description: |-
        tenant invitation を revoke する。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: revokeTenantInvitation
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `invitationPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: invitationPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant invitation を revoke する
      tags:
        - Tenant Workspace
  /api/v1/admin/tenants/{tenantSlug}/invitations/{invitationPublicId}/provision-identity:
    post:
      description: |-
        pending tenant invitation の Zitadel identity 初期化メールを送る。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: provisionTenantInvitationIdentity
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `invitationPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: invitationPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantInvitationBody.json
                    acceptUrl: https://example.com/webhooks/haohao
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    identitySetupDeliveryMode: return_code
                    identitySetupEmailCode: demo@example.com
                    identitySetupInviteCode: example
                    identitySetupLoginUrl: https://example.com/webhooks/haohao
                    identitySetupUserId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    inviteeEmailNormalized: demo@example.com
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    roleCodes:
                      - viewer
                    status: pending
                    tenantDisplayName: Acme
                    tenantId: 1
                    tenantSlug: acme
                    token: token_demo_1234567890
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantInvitationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: pending tenant invitation の Zitadel identity 初期化メールを送る
      tags:
        - Tenant Workspace
  /api/v1/admin/tenants/{tenantSlug}/memberships:
    post:
      description: |-
        tenant local role を付与する。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: grantTenantAdminRole
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  roleCode: todo_user
                  userEmail: demo@example.com
            schema:
              $ref: "#/components/schemas/TenantAdminMembershipRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant local role を付与する
      tags:
        - Tenant Administration
  /api/v1/admin/tenants/{tenantSlug}/memberships/{userPublicId}/roles/{roleCode}:
    delete:
      description: |-
        tenant local role を無効化する。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: revokeTenantAdminRole
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `userPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: userPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: path 内の `roleCode` を指定します。
          example: viewer
          in: path
          name: roleCode
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant local role を無効化する
      tags:
        - Tenant Administration
  /api/v1/admin/tenants/{tenantSlug}/settings:
    get:
      description: |-
        tenant settings を返す。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getTenantSettings
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    features: {}
                    fileQuotaBytes: 1048576
                    notificationsEnabled: true
                    rateLimitBrowserApiPerMinute: 50
                    rateLimitExternalApiPerMinute: 50
                    rateLimitLoginPerMinute: 50
                    tenantId: 1
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantSettingsBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant settings を返す
      tags:
        - Tenant Workspace
    put:
      description: |-
        tenant settings を更新する。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateTenantSettings
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  features: {}
                  fileQuotaBytes: 1048576
                  notificationsEnabled: true
                  rateLimitBrowserApiPerMinute: 50
                  rateLimitExternalApiPerMinute: 50
                  rateLimitLoginPerMinute: 50
            schema:
              $ref: "#/components/schemas/TenantSettingsRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantSettingsBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    features: {}
                    fileQuotaBytes: 1048576
                    notificationsEnabled: true
                    rateLimitBrowserApiPerMinute: 50
                    rateLimitExternalApiPerMinute: 50
                    rateLimitLoginPerMinute: 50
                    tenantId: 1
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantSettingsBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant settings を更新する
      tags:
        - Tenant Workspace
  /api/v1/admin/tenants/{tenantSlug}/webhooks:
    get:
      description: |-
        webhook endpoint 一覧を返す。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listWebhooks
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - active: true
                        createdAt: "2026-01-15T09:30:00Z"
                        eventTypes:
                          - example
                        lastDeliveryAt: "2026-01-15T09:30:00Z"
                        name: Acme
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        secret: example
                        updatedAt: "2026-01-15T09:30:00Z"
                        url: https://example.com/webhooks/haohao
              schema:
                $ref: "#/components/schemas/WebhookListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: webhook endpoint 一覧を返す
      tags:
        - Platform Integrations
    post:
      description: |-
        tenant admin API で webhook endpoint を登録します。作成後の delivery には署名 header が付き、secret は rotate API で更新できます。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createWebhook
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  active: true
                  eventTypes:
                    - customer_signal.created
                  name: Customer signal sink
                  url: https://example.com/webhooks/haohao
            schema:
              $ref: "#/components/schemas/WebhookEndpointRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    active: true
                    createdAt: "2026-01-15T09:30:00Z"
                    eventTypes:
                      - example
                    lastDeliveryAt: "2026-01-15T09:30:00Z"
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    secret: example
                    updatedAt: "2026-01-15T09:30:00Z"
                    url: https://example.com/webhooks/haohao
              schema:
                $ref: "#/components/schemas/WebhookEndpointBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: webhook endpoint を作成する
      tags:
        - Platform Integrations
  /api/v1/admin/tenants/{tenantSlug}/webhooks/{webhookPublicId}:
    delete:
      description: |-
        webhook endpoint を削除する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteWebhook
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `webhookPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: webhookPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: webhook endpoint を削除する
      tags:
        - Platform Integrations
    get:
      description: |-
        webhook endpoint detail を返す。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getWebhook
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `webhookPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: webhookPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/WebhookEndpointBody.json
                    active: true
                    createdAt: "2026-01-15T09:30:00Z"
                    eventTypes:
                      - example
                    lastDeliveryAt: "2026-01-15T09:30:00Z"
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    secret: example
                    updatedAt: "2026-01-15T09:30:00Z"
                    url: https://example.com/webhooks/haohao
              schema:
                $ref: "#/components/schemas/WebhookEndpointBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: webhook endpoint detail を返す
      tags:
        - Platform Integrations
    put:
      description: |-
        webhook endpoint を更新する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateWebhook
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `webhookPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: webhookPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  active: true
                  eventTypes:
                    - example
                  name: Acme
                  url: https://example.com/webhooks/haohao
            schema:
              $ref: "#/components/schemas/WebhookEndpointRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/WebhookEndpointBody.json
                    active: true
                    createdAt: "2026-01-15T09:30:00Z"
                    eventTypes:
                      - example
                    lastDeliveryAt: "2026-01-15T09:30:00Z"
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    secret: example
                    updatedAt: "2026-01-15T09:30:00Z"
                    url: https://example.com/webhooks/haohao
              schema:
                $ref: "#/components/schemas/WebhookEndpointBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: webhook endpoint を更新する
      tags:
        - Platform Integrations
  /api/v1/admin/tenants/{tenantSlug}/webhooks/{webhookPublicId}/deliveries:
    get:
      description: |-
        webhook delivery log を返す。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listWebhookDeliveries
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `webhookPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: webhookPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - attemptCount: 3
                        createdAt: "2026-01-15T09:30:00Z"
                        deliveredAt: "2026-01-15T09:30:00Z"
                        eventType: example
                        lastError: example
                        lastHttpStatus: 1
                        maxAttempts: 1
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        responsePreview: example
                        status: active
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/WebhookDeliveryListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: webhook delivery log を返す
      tags:
        - Platform Integrations
  /api/v1/admin/tenants/{tenantSlug}/webhooks/{webhookPublicId}/deliveries/{deliveryPublicId}/retry:
    post:
      description: |-
        webhook delivery を retry する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: retryWebhookDelivery
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `webhookPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: webhookPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: path 内の `deliveryPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: deliveryPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    attemptCount: 3
                    createdAt: "2026-01-15T09:30:00Z"
                    deliveredAt: "2026-01-15T09:30:00Z"
                    eventType: example
                    lastError: example
                    lastHttpStatus: 1
                    maxAttempts: 1
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    responsePreview: example
                    status: active
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/WebhookDeliveryBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: webhook delivery を retry する
      tags:
        - Platform Integrations
  /api/v1/admin/tenants/{tenantSlug}/webhooks/{webhookPublicId}/rotate-secret:
    post:
      description: |-
        webhook secret を rotate する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: rotateWebhookSecret
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: path
          name: tenantSlug
          required: true
          schema:
            maxLength: 64
            minLength: 3
            type: string
        - description: path 内の `webhookPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: webhookPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/WebhookEndpointBody.json
                    active: true
                    createdAt: "2026-01-15T09:30:00Z"
                    eventTypes:
                      - example
                    lastDeliveryAt: "2026-01-15T09:30:00Z"
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    secret: example
                    updatedAt: "2026-01-15T09:30:00Z"
                    url: https://example.com/webhooks/haohao
              schema:
                $ref: "#/components/schemas/WebhookEndpointBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: webhook secret を rotate する
      tags:
        - Platform Integrations
  /api/v1/auth/callback:
    get:
      description: |-
        OIDC callback を完了する。

        login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。
      operationId: finishOIDCLogin
      parameters:
        - description: 結果の絞り込みや pagination に使う query parameter `code` です。
          example: example
          explode: false
          in: query
          name: code
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `state` です。
          example: example
          explode: false
          in: query
          name: state
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `error` です。
          example: example
          explode: false
          in: query
          name: error
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `error_description` です。
          example: Quarterly planning documents.
          explode: false
          in: query
          name: error_description
          schema:
            type: string
      responses:
        "302":
          description: 操作に成功した場合の response です。
          headers:
            Location:
              schema:
                type: string
            Set-Cookie:
              schema:
                type: string
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      summary: OIDC callback を完了する
      tags:
        - Auth & Session
  /api/v1/auth/login:
    get:
      description: |-
        OIDC login を開始する。

        login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。
      operationId: startOIDCLogin
      parameters:
        - description: 結果の絞り込みや pagination に使う query parameter `returnTo` です。
          example: example
          explode: false
          in: query
          name: returnTo
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `loginHint` です。
          example: example
          explode: false
          in: query
          name: loginHint
          schema:
            type: string
      responses:
        "302":
          description: 操作に成功した場合の response です。
          headers:
            Location:
              schema:
                type: string
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      summary: OIDC login を開始する
      tags:
        - Auth & Session
  /api/v1/auth/settings:
    get:
      description: |-
        現在の認証モード設定を返す。

        login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。
      operationId: getAuthSettings
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    localPasswordLoginEnabled: true
                    mode: local
                    zitadel:
                      clientId: "312345678901234567"
                      issuer: http://localhost:8081
              schema:
                $ref: "#/components/schemas/AuthSettingsBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      summary: 現在の認証モード設定を返す
      tags:
        - Auth & Session
  /api/v1/csrf:
    get:
      description: |-
        CSRF token を再発行する。

        login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getCSRF
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
          headers:
            Set-Cookie:
              schema:
                type: string
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: CSRF token を再発行する
      tags:
        - Auth & Session
  /api/v1/customer-signal-filters:
    get:
      description: |-
        Customer Signal saved filters を返す。

        customer signal の作成、検索、更新、import workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listCustomerSignalSavedFilters
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 検索語または filter query です。
          example: roadmap
          explode: false
          in: query
          name: q
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `status` です。
          example: active
          explode: false
          in: query
          name: status
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `priority` です。
          example: medium
          explode: false
          in: query
          name: priority
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `source` です。
          example: support
          explode: false
          in: query
          name: source
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `cursor` です。
          example: example
          explode: false
          in: query
          name: cursor
          schema:
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - createdAt: "2026-01-15T09:30:00Z"
                        filters: {}
                        name: Acme
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        query: roadmap
                        updatedAt: "2026-01-15T09:30:00Z"
                    nextCursor: example
              schema:
                $ref: "#/components/schemas/CustomerSignalSavedFilterListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Customer Signal saved filters を返す
      tags:
        - Customer Signals
    post:
      description: |-
        Customer Signal saved filter を作成する。

        customer signal の作成、検索、更新、import workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createCustomerSignalSavedFilter
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  filters: {}
                  name: Acme
                  query: roadmap
            schema:
              $ref: "#/components/schemas/CustomerSignalSavedFilterRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    filters: {}
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    query: roadmap
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/CustomerSignalSavedFilterBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Customer Signal saved filter を作成する
      tags:
        - Customer Signals
  /api/v1/customer-signal-filters/{filterPublicId}:
    delete:
      description: |-
        Customer Signal saved filter を削除する。

        customer signal の作成、検索、更新、import workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteCustomerSignalSavedFilter
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `filterPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filterPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Customer Signal saved filter を削除する
      tags:
        - Customer Signals
    put:
      description: |-
        Customer Signal saved filter を更新する。

        customer signal の作成、検索、更新、import workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateCustomerSignalSavedFilter
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `filterPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filterPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  filters: {}
                  name: Acme
                  query: roadmap
            schema:
              $ref: "#/components/schemas/CustomerSignalSavedFilterRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/CustomerSignalSavedFilterBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    filters: {}
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    query: roadmap
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/CustomerSignalSavedFilterBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Customer Signal saved filter を更新する
      tags:
        - Customer Signals
  /api/v1/customer-signals:
    get:
      description: |-
        active tenant の Customer Signals 一覧を返す。

        customer signal の作成、検索、更新、import workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listCustomerSignals
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 検索語または filter query です。
          example: roadmap
          explode: false
          in: query
          name: q
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `status` です。
          example: active
          explode: false
          in: query
          name: status
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `priority` です。
          example: medium
          explode: false
          in: query
          name: priority
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `source` です。
          example: support
          explode: false
          in: query
          name: source
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `cursor` です。
          example: example
          explode: false
          in: query
          name: cursor
          schema:
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - body: Customer asked for monthly report export.
                        createdAt: "2026-01-15T09:30:00Z"
                        customerName: Acme
                        priority: medium
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        source: support
                        status: new
                        title: Export CSV from reports
                        updatedAt: "2026-01-15T09:30:00Z"
                    nextCursor: example
              schema:
                $ref: "#/components/schemas/CustomerSignalListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の Customer Signals 一覧を返す
      tags:
        - Customer Signals
    post:
      description: |-
        active tenant に customer signal を作成します。重複送信を避けたい場合は `Idempotency-Key` を指定してください。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createCustomerSignal
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: 同じ request の retry を安全に扱うための任意 key です。同じ key は同じ method/path/body にだけ再利用できます。
          example: req_20260115_customer_signal_001
          in: header
          name: Idempotency-Key
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  body: Customer asked for monthly report export.
                  customerName: Acme
                  priority: medium
                  source: support
                  status: new
                  title: Export CSV from reports
            schema:
              $ref: "#/components/schemas/CreateCustomerSignalBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    body: Customer asked for monthly report export.
                    createdAt: "2026-01-15T09:30:00Z"
                    customerName: Acme
                    priority: medium
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    source: support
                    status: new
                    title: Export CSV from reports
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/CustomerSignalBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant に Customer Signal を作成する
      tags:
        - Customer Signals
  /api/v1/customer-signals/{signalPublicId}:
    delete:
      description: |-
        active tenant の Customer Signal を soft delete する。

        customer signal の作成、検索、更新、import workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteCustomerSignal
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `signalPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: signalPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の Customer Signal を soft delete する
      tags:
        - Customer Signals
    get:
      description: |-
        active tenant の Customer Signal detail を返す。

        customer signal の作成、検索、更新、import workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getCustomerSignal
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `signalPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: signalPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/CustomerSignalBody.json
                    body: Customer asked for monthly report export.
                    createdAt: "2026-01-15T09:30:00Z"
                    customerName: Acme
                    priority: medium
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    source: support
                    status: new
                    title: Export CSV from reports
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/CustomerSignalBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の Customer Signal detail を返す
      tags:
        - Customer Signals
    patch:
      description: |-
        active tenant の Customer Signal を更新する。

        customer signal の作成、検索、更新、import workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateCustomerSignal
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `signalPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: signalPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  body: Customer asked for monthly report export.
                  customerName: Acme
                  priority: medium
                  source: support
                  title: Export CSV from reports
            schema:
              $ref: "#/components/schemas/UpdateCustomerSignalBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/CustomerSignalBody.json
                    body: Customer asked for monthly report export.
                    createdAt: "2026-01-15T09:30:00Z"
                    customerName: Acme
                    priority: medium
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    source: support
                    status: new
                    title: Export CSV from reports
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/CustomerSignalBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の Customer Signal を更新する
      tags:
        - Customer Signals
  /api/v1/data-pipeline-review-items/{reviewItemPublicId}:
    get:
      description: |-
        data pipeline review item detail を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDataPipelineReviewItem
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `reviewItemPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: reviewItemPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    assignedToUserId: 1
                    comments:
                      - example
                    createdAt: "2026-01-15T09:30:00Z"
                    createdByUserId: 1
                    decidedAt: "2026-01-15T09:30:00Z"
                    decisionComment: example
                    nodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    pipelineName: Acme
                    pipelinePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    queue: example
                    reason:
                      - {}
                    runId: 1
                    runPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    sourceFingerprint: support
                    sourceSnapshot: {}
                    status: open
                    updatedAt: "2026-01-15T09:30:00Z"
                    updatedByUserId: 1
                    versionId: 1
              schema:
                $ref: "#/components/schemas/DataPipelineReviewItemBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline review item detail を返す
      tags:
        - Data & Datasets
  /api/v1/data-pipeline-review-items/{reviewItemPublicId}/comments:
    post:
      description: |-
        data pipeline review item に comment を追加する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: commentDataPipelineReviewItem
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `reviewItemPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: reviewItemPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  body: Customer asked for monthly report export.
            schema:
              $ref: "#/components/schemas/DataPipelineReviewItemCommentWriteBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    authorUserId: 1
                    body: Customer asked for monthly report export.
                    createdAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DataPipelineReviewCommentBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline review item に comment を追加する
      tags:
        - Data & Datasets
  /api/v1/data-pipeline-review-items/{reviewItemPublicId}/transition:
    post:
      description: |-
        data pipeline review item の status を更新する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: transitionDataPipelineReviewItem
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `reviewItemPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: reviewItemPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  comment: example
                  status: open
            schema:
              $ref: "#/components/schemas/DataPipelineReviewItemTransitionBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DataPipelineReviewItemBody.json
                    assignedToUserId: 1
                    comments:
                      - example
                    createdAt: "2026-01-15T09:30:00Z"
                    createdByUserId: 1
                    decidedAt: "2026-01-15T09:30:00Z"
                    decisionComment: example
                    nodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    pipelineName: Acme
                    pipelinePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    queue: example
                    reason:
                      - {}
                    runId: 1
                    runPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    sourceFingerprint: support
                    sourceSnapshot: {}
                    status: open
                    updatedAt: "2026-01-15T09:30:00Z"
                    updatedByUserId: 1
                    versionId: 1
              schema:
                $ref: "#/components/schemas/DataPipelineReviewItemBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline review item の status を更新する
      tags:
        - Data & Datasets
  /api/v1/data-pipeline-schedules/{schedulePublicId}:
    delete:
      description: |-
        data pipeline schedule を disable する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: disableDataPipelineSchedule
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `schedulePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: schedulePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DataPipelineScheduleBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    enabled: true
                    frequency: daily
                    lastErrorSummary: example
                    lastRunAt: "2026-01-15T09:30:00Z"
                    lastRunId: 1
                    lastStatus: active
                    monthDay: 1
                    nextRunAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    runTime: "03:00"
                    timezone: Asia/Tokyo
                    updatedAt: "2026-01-15T09:30:00Z"
                    versionId: 1
                    weekday: 1
              schema:
                $ref: "#/components/schemas/DataPipelineScheduleBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline schedule を disable する
      tags:
        - Data & Datasets
    patch:
      description: |-
        data pipeline schedule を更新する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDataPipelineSchedule
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `schedulePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: schedulePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  enabled: true
                  frequency: daily
                  monthDay: 1
                  runTime: example
                  timezone: example
                  weekday: 1
            schema:
              $ref: "#/components/schemas/DataPipelineScheduleWriteBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DataPipelineScheduleBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    enabled: true
                    frequency: daily
                    lastErrorSummary: example
                    lastRunAt: "2026-01-15T09:30:00Z"
                    lastRunId: 1
                    lastStatus: active
                    monthDay: 1
                    nextRunAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    runTime: "03:00"
                    timezone: Asia/Tokyo
                    updatedAt: "2026-01-15T09:30:00Z"
                    versionId: 1
                    weekday: 1
              schema:
                $ref: "#/components/schemas/DataPipelineScheduleBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline schedule を更新する
      tags:
        - Data & Datasets
  /api/v1/data-pipeline-versions/{versionPublicId}/preview:
    post:
      description: |-
        selected node まで preview する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: previewDataPipelineVersion
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `versionPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: versionPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  limit: 50
                  nodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/DataPipelinePreviewRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    columns:
                      - example
                    nodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    outputSchemas:
                      - columns:
                          - example
                        nodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        stepType: example
                        warnings:
                          - example
                    previewRows:
                      - {}
                    stepType: example
              schema:
                $ref: "#/components/schemas/DataPipelinePreviewBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: selected node まで preview する
      tags:
        - Data & Datasets
  /api/v1/data-pipeline-versions/{versionPublicId}/publish:
    post:
      description: |-
        data pipeline version を publish する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: publishDataPipelineVersion
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `versionPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: versionPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DataPipelineVersionBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    graph:
                      edges:
                        - example
                      nodes:
                        - example
                    pipelineId: 1
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publishedAt: "2026-01-15T09:30:00Z"
                    status: draft
                    validationSummary:
                      errors:
                        - example
                      valid: true
                    versionNumber: 1
              schema:
                $ref: "#/components/schemas/DataPipelineVersionBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline version を publish する
      tags:
        - Data & Datasets
  /api/v1/data-pipeline-versions/{versionPublicId}/runs:
    post:
      description: |-
        data pipeline manual run を request する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: createDataPipelineRun
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: 同じ request の retry を安全に扱うための任意 key です。同じ key は同じ method/path/body にだけ再利用できます。
          example: req_20260115_customer_signal_001
          in: header
          name: Idempotency-Key
          schema:
            type: string
        - description: path 内の `versionPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: versionPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    errorSummary: example
                    outputWorkTableId: 1
                    outputs:
                      - example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    rowCount: 3
                    scheduleId: 1
                    startedAt: "2026-01-15T09:30:00Z"
                    status: pending
                    steps:
                      - example
                    triggerKind: manual
                    updatedAt: "2026-01-15T09:30:00Z"
                    versionId: 1
              schema:
                $ref: "#/components/schemas/DataPipelineRunBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline manual run を request する
      tags:
        - Data & Datasets
  /api/v1/data-pipelines:
    get:
      description: |-
        active tenant の data pipeline 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDataPipelines
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 検索語または filter query です。
          example: roadmap
          explode: false
          in: query
          name: q
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `status` です。
          example: draft
          explode: false
          in: query
          name: status
          schema:
            enum:
              - draft
              - published
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `publication` です。
          example: all
          explode: false
          in: query
          name: publication
          schema:
            default: all
            enum:
              - all
              - published
              - unpublished
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `runStatus` です。
          example: pending
          explode: false
          in: query
          name: runStatus
          schema:
            enum:
              - pending
              - processing
              - completed
              - failed
              - skipped
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `scheduleState` です。
          example: all
          explode: false
          in: query
          name: scheduleState
          schema:
            default: all
            enum:
              - all
              - enabled
              - disabled
              - none
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `sort` です。
          example: updated_desc
          explode: false
          in: query
          name: sort
          schema:
            default: updated_desc
            enum:
              - updated_desc
              - updated_asc
              - created_desc
              - created_asc
              - name_asc
              - name_desc
              - latest_run_desc
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `cursor` です。
          example: example
          explode: false
          in: query
          name: cursor
          schema:
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 25
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - archivedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        description: Quarterly planning documents.
                        disabledScheduleCount: 3
                        enabledScheduleCount: 3
                        latestRunAt: "2026-01-15T09:30:00Z"
                        latestRunPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        latestRunStatus: active
                        name: Customer cleansing
                        nextRunAt: "2026-01-15T09:30:00Z"
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        publishedVersionId: 1
                        scheduleState: enabled
                        status: draft
                        updatedAt: "2026-01-15T09:30:00Z"
                    nextCursor: example
              schema:
                $ref: "#/components/schemas/DataPipelineListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の data pipeline 一覧を返す
      tags:
        - Data & Datasets
    post:
      description: |-
        active tenant に data pipeline を作成する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDataPipeline
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: 同じ request の retry を安全に扱うための任意 key です。同じ key は同じ method/path/body にだけ再利用できます。
          example: req_20260115_customer_signal_001
          in: header
          name: Idempotency-Key
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  description: Quarterly planning documents.
                  name: Acme
            schema:
              $ref: "#/components/schemas/DataPipelineCreateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    archivedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    disabledScheduleCount: 3
                    enabledScheduleCount: 3
                    latestRunAt: "2026-01-15T09:30:00Z"
                    latestRunPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    latestRunStatus: active
                    name: Customer cleansing
                    nextRunAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publishedVersionId: 1
                    scheduleState: enabled
                    status: draft
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DataPipelineBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant に data pipeline を作成する
      tags:
        - Data & Datasets
  /api/v1/data-pipelines/schema-mapping/candidates:
    post:
      description: |-
        schema mapping 候補を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: schemaMappingCandidates
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  columns:
                    - neighborColumns:
                        - example
                      sampleValues:
                        - example
                      sheetName: Acme
                      sourceColumn: support
                  domain: example
                  limit: 50
                  pipelinePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  schemaType: example
                  versionPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/SchemaMappingCandidateRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - candidates: []
                        sourceColumn: support
              schema:
                $ref: "#/components/schemas/SchemaMappingCandidateListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: schema mapping 候補を返す
      tags:
        - Data & Datasets
  /api/v1/data-pipelines/schema-mapping/examples:
    post:
      description: |-
        schema mapping の採用/却下履歴を記録する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: recordSchemaMappingExample
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  decision: accepted
                  neighborColumns:
                    - example
                  pipelinePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  sampleValues:
                    - example
                  schemaColumnPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  sheetName: Acme
                  sourceColumn: support
                  versionPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/SchemaMappingExampleWriteBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    decision: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    schemaColumnPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    sharedScope: example
                    sourceColumn: support
                    targetColumn: example
              schema:
                $ref: "#/components/schemas/SchemaMappingExampleBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: schema mapping の採用/却下履歴を記録する
      tags:
        - Data & Datasets
  /api/v1/data-pipelines/{pipelinePublicId}:
    get:
      description: |-
        data pipeline detail を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDataPipeline
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `pipelinePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: pipelinePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    pipeline:
                      $schema: https://example.com/schemas/DataPipelineBody.json
                      archivedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      disabledScheduleCount: 3
                      enabledScheduleCount: 3
                      latestRunAt: "2026-01-15T09:30:00Z"
                      latestRunPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      latestRunStatus: active
                      name: Customer cleansing
                      nextRunAt: "2026-01-15T09:30:00Z"
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      publishedVersionId: 1
                      scheduleState: enabled
                      status: draft
                      updatedAt: "2026-01-15T09:30:00Z"
                    publishedVersion:
                      createdAt: "2026-01-15T09:30:00Z"
                      graph:
                        edges: []
                        nodes: []
                      pipelineId: 1
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      publishedAt: "2026-01-15T09:30:00Z"
                      status: draft
                      validationSummary:
                        errors: []
                        valid: true
                      versionNumber: 1
                    runs:
                      - completedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        errorSummary: example
                        outputWorkTableId: 1
                        outputs: []
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        rowCount: 3
                        scheduleId: 1
                        startedAt: "2026-01-15T09:30:00Z"
                        status: pending
                        steps: []
                        triggerKind: manual
                        updatedAt: "2026-01-15T09:30:00Z"
                        versionId: 1
                    schedules:
                      - createdAt: "2026-01-15T09:30:00Z"
                        enabled: true
                        frequency: daily
                        lastErrorSummary: example
                        lastRunAt: "2026-01-15T09:30:00Z"
                        lastRunId: 1
                        lastStatus: active
                        monthDay: 1
                        nextRunAt: "2026-01-15T09:30:00Z"
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        runTime: "03:00"
                        timezone: Asia/Tokyo
                        updatedAt: "2026-01-15T09:30:00Z"
                        versionId: 1
                        weekday: 1
                    versions:
                      - createdAt: "2026-01-15T09:30:00Z"
                        graph: {}
                        pipelineId: 1
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        publishedAt: "2026-01-15T09:30:00Z"
                        status: draft
                        validationSummary: {}
                        versionNumber: 1
              schema:
                $ref: "#/components/schemas/DataPipelineDetailBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline detail を返す
      tags:
        - Data & Datasets
    patch:
      description: |-
        data pipeline の name / description を更新する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDataPipeline
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `pipelinePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: pipelinePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  description: Quarterly planning documents.
                  name: Acme
            schema:
              $ref: "#/components/schemas/DataPipelineUpdateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DataPipelineBody.json
                    archivedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    disabledScheduleCount: 3
                    enabledScheduleCount: 3
                    latestRunAt: "2026-01-15T09:30:00Z"
                    latestRunPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    latestRunStatus: active
                    name: Customer cleansing
                    nextRunAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publishedVersionId: 1
                    scheduleState: enabled
                    status: draft
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DataPipelineBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline の name / description を更新する
      tags:
        - Data & Datasets
  /api/v1/data-pipelines/{pipelinePublicId}/preview:
    post:
      description: |-
        未保存 draft graph の selected node まで preview する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: previewDataPipelineDraft
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `pipelinePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: pipelinePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  graph:
                    edges:
                      - example
                    nodes:
                      - example
                  limit: 50
                  nodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/DataPipelineDraftPreviewRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DataPipelinePreviewBody.json
                    columns:
                      - example
                    nodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    outputSchemas:
                      - example
                    previewRows:
                      - {}
                    stepType: example
              schema:
                $ref: "#/components/schemas/DataPipelinePreviewBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: 未保存 draft graph の selected node まで preview する
      tags:
        - Data & Datasets
  /api/v1/data-pipelines/{pipelinePublicId}/review-items:
    get:
      description: |-
        data pipeline review item 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDataPipelineReviewItems
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `pipelinePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: pipelinePublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `status` です。
          example: open
          explode: false
          in: query
          name: status
          schema:
            enum:
              - open
              - approved
              - rejected
              - needs_changes
              - closed
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 25
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - assignedToUserId: 1
                        comments: []
                        createdAt: "2026-01-15T09:30:00Z"
                        createdByUserId: 1
                        decidedAt: "2026-01-15T09:30:00Z"
                        decisionComment: example
                        nodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        pipelineName: Acme
                        pipelinePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        queue: example
                        reason:
                          - {}
                        runId: 1
                        runPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        sourceFingerprint: support
                        sourceSnapshot: {}
                        status: open
                        updatedAt: "2026-01-15T09:30:00Z"
                        updatedByUserId: 1
                        versionId: 1
              schema:
                $ref: "#/components/schemas/DataPipelineReviewItemListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline review item 一覧を返す
      tags:
        - Data & Datasets
  /api/v1/data-pipelines/{pipelinePublicId}/runs:
    get:
      description: |-
        data pipeline run history を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDataPipelineRuns
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `pipelinePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: pipelinePublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 25
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - completedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        errorSummary: example
                        outputWorkTableId: 1
                        outputs:
                          - example
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        rowCount: 3
                        scheduleId: 1
                        startedAt: "2026-01-15T09:30:00Z"
                        status: pending
                        steps:
                          - example
                        triggerKind: manual
                        updatedAt: "2026-01-15T09:30:00Z"
                        versionId: 1
              schema:
                $ref: "#/components/schemas/DataPipelineRunListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline run history を返す
      tags:
        - Data & Datasets
  /api/v1/data-pipelines/{pipelinePublicId}/schedules:
    get:
      description: |-
        data pipeline schedules を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDataPipelineSchedules
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `pipelinePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: pipelinePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - createdAt: "2026-01-15T09:30:00Z"
                        enabled: true
                        frequency: daily
                        lastErrorSummary: example
                        lastRunAt: "2026-01-15T09:30:00Z"
                        lastRunId: 1
                        lastStatus: active
                        monthDay: 1
                        nextRunAt: "2026-01-15T09:30:00Z"
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        runTime: "03:00"
                        timezone: Asia/Tokyo
                        updatedAt: "2026-01-15T09:30:00Z"
                        versionId: 1
                        weekday: 1
              schema:
                $ref: "#/components/schemas/DataPipelineScheduleListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline schedules を返す
      tags:
        - Data & Datasets
    post:
      description: |-
        data pipeline schedule を作成する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDataPipelineSchedule
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `pipelinePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: pipelinePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  enabled: true
                  frequency: daily
                  monthDay: 1
                  runTime: example
                  timezone: example
                  weekday: 1
            schema:
              $ref: "#/components/schemas/DataPipelineScheduleWriteBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    enabled: true
                    frequency: daily
                    lastErrorSummary: example
                    lastRunAt: "2026-01-15T09:30:00Z"
                    lastRunId: 1
                    lastStatus: active
                    monthDay: 1
                    nextRunAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    runTime: "03:00"
                    timezone: Asia/Tokyo
                    updatedAt: "2026-01-15T09:30:00Z"
                    versionId: 1
                    weekday: 1
              schema:
                $ref: "#/components/schemas/DataPipelineScheduleBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline schedule を作成する
      tags:
        - Data & Datasets
  /api/v1/data-pipelines/{pipelinePublicId}/validate:
    post:
      description: |-
        未保存 draft graph の schema と column warning を検証する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: validateDataPipelineDraft
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `pipelinePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: pipelinePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  graph:
                    edges:
                      - example
                    nodes:
                      - example
            schema:
              $ref: "#/components/schemas/DataPipelineDraftValidationRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    nodeWarnings:
                      - code: example
                        columns:
                          - example
                        configKeys:
                          - example
                        message: example
                        nodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        severity: example
                        stepType: example
                    outputSchemas:
                      - columns:
                          - example
                        nodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        stepType: example
                        warnings:
                          - example
                    validationSummary:
                      errors:
                        - example
                      valid: true
              schema:
                $ref: "#/components/schemas/DataPipelineGraphValidationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: 未保存 draft graph の schema と column warning を検証する
      tags:
        - Data & Datasets
  /api/v1/data-pipelines/{pipelinePublicId}/versions:
    post:
      description: |-
        data pipeline draft graph を保存する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: saveDataPipelineVersion
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `pipelinePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: pipelinePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  graph:
                    edges:
                      - example
                    nodes:
                      - example
            schema:
              $ref: "#/components/schemas/DataPipelineVersionSaveBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    graph:
                      edges:
                        - example
                      nodes:
                        - example
                    pipelineId: 1
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publishedAt: "2026-01-15T09:30:00Z"
                    status: draft
                    validationSummary:
                      errors:
                        - example
                      valid: true
                    versionNumber: 1
              schema:
                $ref: "#/components/schemas/DataPipelineVersionBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: data pipeline draft graph を保存する
      tags:
        - Data & Datasets
  /api/v1/dataset-query-jobs:
    get:
      description: |-
        active tenant の dataset query job 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetQueryJobs
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 25
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetQueryListBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/DatasetQueryListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の dataset query job 一覧を返す
      tags:
        - Data & Datasets
    post:
      description: |-
        dataset に対する SQL query job を作成します。query は tenant-scoped work table に限定して実行されます。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDatasetQueryJob
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  sql: select customer_name, count(*) as signal_count from customer_signals group by customer_name
            schema:
              $ref: "#/components/schemas/DatasetQueryCreateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetQueryJobBody.json
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    durationMs: 120
                    errorSummary: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resultColumns:
                      - example
                    resultRows:
                      - {}
                    rowCount: 1000
                    statement: example
                    status: completed
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetQueryJobBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の dataset SQL query job を作成する
      tags:
        - Data & Datasets
  /api/v1/dataset-query-jobs/{queryJobPublicId}:
    get:
      description: |-
        active tenant の dataset query job detail を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDatasetQueryJob
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `queryJobPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: queryJobPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetQueryJobBody.json
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    durationMs: 120
                    errorSummary: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resultColumns:
                      - example
                    resultRows:
                      - {}
                    rowCount: 1000
                    statement: example
                    status: completed
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetQueryJobBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の dataset query job detail を返す
      tags:
        - Data & Datasets
  /api/v1/dataset-query-jobs/{queryJobPublicId}/lineage:
    get:
      description: |-
        active tenant の dataset query job lineage graph を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDatasetQueryJobLineage
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `queryJobPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: queryJobPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `direction` です。
          example: upstream
          explode: false
          in: query
          name: direction
          schema:
            default: both
            enum:
              - upstream
              - downstream
              - both
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `depth` です。
          example: 1
          explode: false
          in: query
          name: depth
          schema:
            default: 1
            format: int32
            maximum: 2
            minimum: 1
            type: integer
        - description: 結果の絞り込みや pagination に使う query parameter `includeHistory` です。
          example: true
          explode: false
          in: query
          name: includeHistory
          schema:
            default: true
            type: boolean
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int32
            maximum: 100
            minimum: 1
            type: integer
        - description: 結果の絞り込みや pagination に使う query parameter `level` です。
          example: table
          explode: false
          in: query
          name: level
          schema:
            default: table
            enum:
              - table
              - column
              - both
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `sources` です。
          example: metadata,parser,manual
          explode: false
          in: query
          name: sources
          schema:
            examples:
              - metadata,parser,manual
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `includeDraft` です。
          example: true
          explode: false
          in: query
          name: includeDraft
          schema:
            default: false
            type: boolean
        - description: 結果の絞り込みや pagination に使う query parameter `changeSetPublicId` です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          explode: false
          in: query
          name: changeSetPublicId
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetLineageBody.json
                    edges:
                      - example
                    nodes:
                      - example
                    root:
                      columnName: customer_id
                      createdAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      displayName: Sales
                      editable: true
                      id: dataset:018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      metadata: {}
                      nodeKind: resource
                      position:
                        x: 120
                        "y": 240
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      resourceType: dataset
                      sourceKind: metadata
                      status: ready
                      updatedAt: "2026-01-15T09:30:00Z"
                    timeline:
                      - example
              schema:
                $ref: "#/components/schemas/DatasetLineageBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の dataset query job lineage graph を返す
      tags:
        - Data & Datasets
  /api/v1/dataset-query-jobs/{queryJobPublicId}/lineage/parse:
    post:
      description: |-
        dataset query job の SQL を parse して lineage draft を作成する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: parseDatasetQueryJobLineage
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `queryJobPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: queryJobPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    changeSetPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    columnEdgeCount: 3
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    errorSummary: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    queryJobPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: processing
                    tableRefCount: 3
              schema:
                $ref: "#/components/schemas/DatasetLineageParseRunBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: dataset query job の SQL を parse して lineage draft を作成する
      tags:
        - Data & Datasets
  /api/v1/dataset-query-jobs/{queryJobPublicId}/lineage/parse-runs:
    get:
      description: |-
        dataset query job の lineage parse run 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetQueryJobLineageParseRuns
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `queryJobPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: queryJobPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 25
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - $schema: https://example.com/schemas/DatasetLineageParseRunBody.json
                        changeSetPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        columnEdgeCount: 3
                        completedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        errorSummary: example
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        queryJobPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        status: processing
                        tableRefCount: 3
              schema:
                $ref: "#/components/schemas/DatasetLineageParseRunListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: dataset query job の lineage parse run 一覧を返す
      tags:
        - Data & Datasets
  /api/v1/dataset-source-files:
    get:
      description: |-
        Dataset に取り込める Drive CSV file 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetSourceFiles
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 検索語または filter query です。
          example: roadmap
          explode: false
          in: query
          name: q
          schema:
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            maximum: 200
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - byteSize: 104857600
                        contentType: text/csv
                        createdAt: "2026-01-15T09:30:00Z"
                        originalFilename: customers.csv
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetSourceFileListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Dataset に取り込める Drive CSV file 一覧を返す
      tags:
        - Data & Datasets
  /api/v1/dataset-work-table-export-schedules/{schedulePublicId}:
    patch:
      description: |-
        work table export schedule を更新する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDatasetWorkTableExportSchedule
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `schedulePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: schedulePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  enabled: true
                  format: csv
                  frequency: daily
                  monthDay: 1
                  retentionDays: 7
                  runTime: "03:00"
                  timezone: Asia/Tokyo
                  weekday: 1
            schema:
              $ref: "#/components/schemas/DatasetWorkTableExportScheduleUpdateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetWorkTableExportScheduleBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    enabled: true
                    format: csv
                    frequency: daily
                    lastErrorSummary: example
                    lastRunAt: "2026-01-15T09:30:00Z"
                    lastStatus: active
                    monthDay: 1
                    nextRunAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    retentionDays: 7
                    runTime: "03:00"
                    timezone: Asia/Tokyo
                    updatedAt: "2026-01-15T09:30:00Z"
                    weekday: 1
                    workTableId: 1
              schema:
                $ref: "#/components/schemas/DatasetWorkTableExportScheduleBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: work table export schedule を更新する
      tags:
        - Data & Datasets
  /api/v1/dataset-work-table-export-schedules/{schedulePublicId}/disable:
    post:
      description: |-
        work table export schedule を disable する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: disableDatasetWorkTableExportSchedule
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `schedulePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: schedulePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetWorkTableExportScheduleBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    enabled: true
                    format: csv
                    frequency: daily
                    lastErrorSummary: example
                    lastRunAt: "2026-01-15T09:30:00Z"
                    lastStatus: active
                    monthDay: 1
                    nextRunAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    retentionDays: 7
                    runTime: "03:00"
                    timezone: Asia/Tokyo
                    updatedAt: "2026-01-15T09:30:00Z"
                    weekday: 1
                    workTableId: 1
              schema:
                $ref: "#/components/schemas/DatasetWorkTableExportScheduleBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: work table export schedule を disable する
      tags:
        - Data & Datasets
  /api/v1/dataset-work-table-exports/{exportPublicId}:
    get:
      description: |-
        work table export detail を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDatasetWorkTableExport
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `exportPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: exportPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetWorkTableExportBody.json
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    errorSummary: example
                    expiresAt: "2026-01-15T09:30:00Z"
                    fileObjectId: 1
                    format: csv
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    schedulePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    scheduledFor: "2026-01-15T09:30:00Z"
                    source: manual
                    status: processing
                    updatedAt: "2026-01-15T09:30:00Z"
                    workTableId: 1
              schema:
                $ref: "#/components/schemas/DatasetWorkTableExportBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: work table export detail を返す
      tags:
        - Data & Datasets
  /api/v1/dataset-work-table-exports/{exportPublicId}/download:
    get:
      description: |-
        ready work table export を download する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: downloadDatasetWorkTableExport
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `exportPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: exportPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value: example
              schema:
                contentEncoding: base64
                type: string
          description: 操作に成功し、response body に結果を返します。
          headers:
            Content-Disposition:
              schema:
                type: string
            Content-Type:
              schema:
                type: string
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: ready work table export を download する
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables:
    get:
      description: |-
        active tenant の ClickHouse work table 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetWorkTables
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            maximum: 200
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - columns: []
                        createdAt: "2026-01-15T09:30:00Z"
                        database: hh_t_1_work
                        displayName: hai_category_summary
                        droppedAt: "2026-01-15T09:30:00Z"
                        engine: MergeTree
                        managed: true
                        originDatasetName: Acme
                        originDatasetPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        status: active
                        table: hai_category_summary
                        totalBytes: 1048576
                        totalRows: 1000
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetWorkTableListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の ClickHouse work table 一覧を返す
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/by-ref/{database}/{table}:
    get:
      description: |-
        active tenant の ClickHouse work table detail を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDatasetWorkTable
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `database` を指定します。
          example: example
          in: path
          name: database
          required: true
          schema:
            maxLength: 128
            type: string
        - description: path 内の `table` を指定します。
          example: example
          in: path
          name: table
          required: true
          schema:
            maxLength: 256
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetWorkTableBody.json
                    columns:
                      - example
                    createdAt: "2026-01-15T09:30:00Z"
                    database: hh_t_1_work
                    displayName: hai_category_summary
                    droppedAt: "2026-01-15T09:30:00Z"
                    engine: MergeTree
                    managed: true
                    originDatasetName: Acme
                    originDatasetPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    table: hai_category_summary
                    totalBytes: 1048576
                    totalRows: 1000
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetWorkTableBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の ClickHouse work table detail を返す
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/by-ref/{database}/{table}/preview:
    get:
      description: |-
        active tenant の ClickHouse work table preview rows を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDatasetWorkTablePreview
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `database` を指定します。
          example: example
          in: path
          name: database
          required: true
          schema:
            maxLength: 128
            type: string
        - description: path 内の `table` を指定します。
          example: example
          in: path
          name: table
          required: true
          schema:
            maxLength: 256
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            maximum: 1000
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetWorkTablePreviewBody.json
                    columns:
                      - example
                    database: hh_t_1_work
                    previewRows:
                      - {}
                    scd2Summary:
                      currentRows: 4
                      detected: true
                      earliestValidAt: "2026-05-01 00:00:00"
                      historyRows: 8
                      keyColumn: id
                      keyCount: 4
                      latestValidAt: "2026-05-03 00:00:00"
                      totalRows: 12
                    table: hai_category_summary
              schema:
                $ref: "#/components/schemas/DatasetWorkTablePreviewBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の ClickHouse work table preview rows を返す
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/register:
    post:
      description: |-
        active tenant の ClickHouse work table を管理レコード化する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: registerDatasetWorkTable
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  database: example
                  datasetPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  displayName: Acme
                  table: example
            schema:
              $ref: "#/components/schemas/DatasetWorkTableRegisterBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    columns:
                      - example
                    createdAt: "2026-01-15T09:30:00Z"
                    database: hh_t_1_work
                    displayName: hai_category_summary
                    droppedAt: "2026-01-15T09:30:00Z"
                    engine: MergeTree
                    managed: true
                    originDatasetName: Acme
                    originDatasetPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    table: hai_category_summary
                    totalBytes: 1048576
                    totalRows: 1000
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetWorkTableBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の ClickHouse work table を管理レコード化する
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/{workTablePublicId}:
    delete:
      description: |-
        managed work table を drop する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteDatasetWorkTable
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table を drop する
      tags:
        - Data & Datasets
    get:
      description: |-
        managed work table detail を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getManagedDatasetWorkTable
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetWorkTableBody.json
                    columns:
                      - example
                    createdAt: "2026-01-15T09:30:00Z"
                    database: hh_t_1_work
                    displayName: hai_category_summary
                    droppedAt: "2026-01-15T09:30:00Z"
                    engine: MergeTree
                    managed: true
                    originDatasetName: Acme
                    originDatasetPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    table: hai_category_summary
                    totalBytes: 1048576
                    totalRows: 1000
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetWorkTableBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table detail を返す
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/{workTablePublicId}/export-schedules:
    get:
      description: |-
        managed work table の export schedule 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetWorkTableExportSchedules
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - createdAt: "2026-01-15T09:30:00Z"
                        enabled: true
                        format: csv
                        frequency: daily
                        lastErrorSummary: example
                        lastRunAt: "2026-01-15T09:30:00Z"
                        lastStatus: active
                        monthDay: 1
                        nextRunAt: "2026-01-15T09:30:00Z"
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        retentionDays: 7
                        runTime: "03:00"
                        timezone: Asia/Tokyo
                        updatedAt: "2026-01-15T09:30:00Z"
                        weekday: 1
                        workTableId: 1
              schema:
                $ref: "#/components/schemas/DatasetWorkTableExportScheduleListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table の export schedule 一覧を返す
      tags:
        - Data & Datasets
    post:
      description: |-
        managed work table の export schedule を作成する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDatasetWorkTableExportSchedule
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  format: csv
                  frequency: daily
                  monthDay: 1
                  retentionDays: 7
                  runTime: "03:00"
                  timezone: Asia/Tokyo
                  weekday: 1
            schema:
              $ref: "#/components/schemas/DatasetWorkTableExportScheduleCreateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    enabled: true
                    format: csv
                    frequency: daily
                    lastErrorSummary: example
                    lastRunAt: "2026-01-15T09:30:00Z"
                    lastStatus: active
                    monthDay: 1
                    nextRunAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    retentionDays: 7
                    runTime: "03:00"
                    timezone: Asia/Tokyo
                    updatedAt: "2026-01-15T09:30:00Z"
                    weekday: 1
                    workTableId: 1
              schema:
                $ref: "#/components/schemas/DatasetWorkTableExportScheduleBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table の export schedule を作成する
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/{workTablePublicId}/exports:
    get:
      description: |-
        managed work table の export 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetWorkTableExports
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 25
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - $schema: https://example.com/schemas/DatasetWorkTableExportBody.json
                        completedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        errorSummary: example
                        expiresAt: "2026-01-15T09:30:00Z"
                        fileObjectId: 1
                        format: csv
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        schedulePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        scheduledFor: "2026-01-15T09:30:00Z"
                        source: manual
                        status: processing
                        updatedAt: "2026-01-15T09:30:00Z"
                        workTableId: 1
              schema:
                $ref: "#/components/schemas/DatasetWorkTableExportListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table の export 一覧を返す
      tags:
        - Data & Datasets
    post:
      description: |-
        managed work table の export を request する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDatasetWorkTableExport
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  format: csv
            schema:
              $ref: "#/components/schemas/DatasetWorkTableExportCreateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    errorSummary: example
                    expiresAt: "2026-01-15T09:30:00Z"
                    fileObjectId: 1
                    format: csv
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    schedulePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    scheduledFor: "2026-01-15T09:30:00Z"
                    source: manual
                    status: processing
                    updatedAt: "2026-01-15T09:30:00Z"
                    workTableId: 1
              schema:
                $ref: "#/components/schemas/DatasetWorkTableExportBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table の export を request する
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/{workTablePublicId}/gold-publications:
    post:
      description: |-
        managed work table を Gold data mart として publish する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDatasetGoldPublication
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          schema:
            type: string
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  description: Quarterly planning documents.
                  displayName: Monthly sales mart
                  goldTable: gm_monthly_sales
            schema:
              $ref: "#/components/schemas/DatasetGoldPublicationCreateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    archivedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    displayName: Monthly sales mart
                    goldDatabase: hh_t_1_gold
                    goldTable: gm_monthly_sales
                    latestPublishRun:
                      completedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      errorSummary: example
                      goldDatabase: hh_t_1_gold
                      goldTable: gm_monthly_sales
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      publicationPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rowCount: 1000
                      schemaSummary: {}
                      sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      startedAt: "2026-01-15T09:30:00Z"
                      status: completed
                      totalBytes: 1048576
                      updatedAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publishedAt: "2026-01-15T09:30:00Z"
                    refreshPolicy: manual
                    rowCount: 1000
                    schemaSummary: {}
                    sourceScd2Summary:
                      currentRows: 4
                      detected: true
                      earliestValidAt: "2026-05-01 00:00:00"
                      historyRows: 8
                      keyColumn: id
                      keyCount: 4
                      latestValidAt: "2026-05-03 00:00:00"
                      totalRows: 12
                    sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    totalBytes: 1048576
                    unpublishedAt: "2026-01-15T09:30:00Z"
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetGoldPublicationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table を Gold data mart として publish する
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/{workTablePublicId}/lineage:
    get:
      description: |-
        managed work table の lineage graph を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDatasetWorkTableLineage
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `direction` です。
          example: upstream
          explode: false
          in: query
          name: direction
          schema:
            default: both
            enum:
              - upstream
              - downstream
              - both
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `depth` です。
          example: 1
          explode: false
          in: query
          name: depth
          schema:
            default: 1
            format: int32
            maximum: 2
            minimum: 1
            type: integer
        - description: 結果の絞り込みや pagination に使う query parameter `includeHistory` です。
          example: true
          explode: false
          in: query
          name: includeHistory
          schema:
            default: true
            type: boolean
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int32
            maximum: 100
            minimum: 1
            type: integer
        - description: 結果の絞り込みや pagination に使う query parameter `level` です。
          example: table
          explode: false
          in: query
          name: level
          schema:
            default: table
            enum:
              - table
              - column
              - both
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `sources` です。
          example: metadata,parser,manual
          explode: false
          in: query
          name: sources
          schema:
            examples:
              - metadata,parser,manual
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `includeDraft` です。
          example: true
          explode: false
          in: query
          name: includeDraft
          schema:
            default: false
            type: boolean
        - description: 結果の絞り込みや pagination に使う query parameter `changeSetPublicId` です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          explode: false
          in: query
          name: changeSetPublicId
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    edges:
                      - confidence: metadata
                        createdAt: "2026-01-15T09:30:00Z"
                        description: Quarterly planning documents.
                        editable: true
                        expression: example
                        id: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        label: example
                        relationType: source_dataset
                        sourceKind: metadata
                        sourceNodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        targetNodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    nodes:
                      - columnName: customer_id
                        createdAt: "2026-01-15T09:30:00Z"
                        description: Quarterly planning documents.
                        displayName: Sales
                        editable: true
                        id: dataset:018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        metadata: {}
                        nodeKind: resource
                        position: {}
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceType: dataset
                        sourceKind: metadata
                        status: ready
                        updatedAt: "2026-01-15T09:30:00Z"
                    root:
                      columnName: customer_id
                      createdAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      displayName: Sales
                      editable: true
                      id: dataset:018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      metadata: {}
                      nodeKind: resource
                      position:
                        x: 120
                        "y": 240
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      resourceType: dataset
                      sourceKind: metadata
                      status: ready
                      updatedAt: "2026-01-15T09:30:00Z"
                    timeline:
                      - id: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        metadata: {}
                        nodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        occurredAt: "2026-01-15T09:30:00Z"
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        relationType: query_created_work_table
                        resourceType: dataset_work_table
                        status: completed
              schema:
                $ref: "#/components/schemas/DatasetLineageBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table の lineage graph を返す
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/{workTablePublicId}/link:
    post:
      description: |-
        managed work table を dataset に紐付ける。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: linkDatasetWorkTable
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  datasetPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/DatasetWorkTableLinkBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetWorkTableBody.json
                    columns:
                      - example
                    createdAt: "2026-01-15T09:30:00Z"
                    database: hh_t_1_work
                    displayName: hai_category_summary
                    droppedAt: "2026-01-15T09:30:00Z"
                    engine: MergeTree
                    managed: true
                    originDatasetName: Acme
                    originDatasetPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    table: hai_category_summary
                    totalBytes: 1048576
                    totalRows: 1000
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetWorkTableBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table を dataset に紐付ける
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/{workTablePublicId}/preview:
    get:
      description: |-
        managed work table preview rows を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getManagedDatasetWorkTablePreview
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            maximum: 1000
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    columns:
                      - example
                    database: hh_t_1_work
                    previewRows:
                      - {}
                    scd2Summary:
                      currentRows: 4
                      detected: true
                      earliestValidAt: "2026-05-01 00:00:00"
                      historyRows: 8
                      keyColumn: id
                      keyCount: 4
                      latestValidAt: "2026-05-03 00:00:00"
                      totalRows: 12
                    table: hai_category_summary
              schema:
                $ref: "#/components/schemas/DatasetWorkTablePreviewBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table preview rows を返す
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/{workTablePublicId}/promote:
    post:
      description: |-
        managed work table を dataset 化する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: promoteDatasetWorkTable
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  name: Acme
            schema:
              $ref: "#/components/schemas/DatasetWorkTablePromoteBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetBody.json
                    byteSize: 104857600
                    columns:
                      - example
                    contentType: text/csv
                    createdAt: "2026-01-15T09:30:00Z"
                    errorSummary: example
                    importJob:
                      completedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      errorSample:
                        - example
                      errorSummary: example
                      invalidRows: 10
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      status: processing
                      totalRows: 10000000
                      updatedAt: "2026-01-15T09:30:00Z"
                      validRows: 9999990
                    importedAt: "2026-01-15T09:30:00Z"
                    latestSyncJob:
                      cleanupErrorSummary: example
                      cleanupStatus: active
                      completedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      errorSummary: example
                      mode: full_refresh
                      newRawDatabase: hh_t_1_raw
                      newRawTable: ds_new
                      oldRawDatabase: hh_t_1_raw
                      oldRawTable: ds_old
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rowCount: 100000
                      startedAt: "2026-01-15T09:30:00Z"
                      status: processing
                      totalBytes: 1048576
                      updatedAt: "2026-01-15T09:30:00Z"
                    name: Customers
                    originalFilename: customers.csv
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    rawDatabase: hh_t_1_raw
                    rawTable: ds_018f2f05c6c97a49b32d04f4dd84ef4a
                    rowCount: 10000000
                    sourceFileObjectId: 1
                    sourceKind: file
                    sourceWorkTableDatabase: support
                    sourceWorkTableId: 1
                    sourceWorkTableName: support
                    sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    sourceWorkTableStatus: active
                    sourceWorkTableTable: support
                    status: ready
                    updatedAt: "2026-01-15T09:30:00Z"
                    workDatabase: hh_t_1_work
              schema:
                $ref: "#/components/schemas/DatasetBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table を dataset 化する
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/{workTablePublicId}/rename:
    post:
      description: |-
        managed work table を rename する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: renameDatasetWorkTable
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  table: example
            schema:
              $ref: "#/components/schemas/DatasetWorkTableRenameBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetWorkTableBody.json
                    columns:
                      - example
                    createdAt: "2026-01-15T09:30:00Z"
                    database: hh_t_1_work
                    displayName: hai_category_summary
                    droppedAt: "2026-01-15T09:30:00Z"
                    engine: MergeTree
                    managed: true
                    originDatasetName: Acme
                    originDatasetPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    table: hai_category_summary
                    totalBytes: 1048576
                    totalRows: 1000
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetWorkTableBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table を rename する
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/{workTablePublicId}/scd2-history:
    get:
      description: |-
        managed SCD2 work table の key history rows を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getManagedDatasetWorkTableSCD2History
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `key` です。
          example: example
          explode: false
          in: query
          name: key
          schema:
            maxLength: 512
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            maximum: 1000
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    columns:
                      - example
                    database: hh_t_1_work
                    historyRows:
                      - {}
                    keyColumn: id
                    keyValue: SKU-001
                    table: hai_category_snapshot
              schema:
                $ref: "#/components/schemas/DatasetWorkTableSCD2HistoryBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed SCD2 work table の key history rows を返す
      tags:
        - Data & Datasets
  /api/v1/dataset-work-tables/{workTablePublicId}/truncate:
    post:
      description: |-
        managed work table を truncate する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: truncateDatasetWorkTable
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `workTablePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workTablePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetWorkTableBody.json
                    columns:
                      - example
                    createdAt: "2026-01-15T09:30:00Z"
                    database: hh_t_1_work
                    displayName: hai_category_summary
                    droppedAt: "2026-01-15T09:30:00Z"
                    engine: MergeTree
                    managed: true
                    originDatasetName: Acme
                    originDatasetPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    table: hai_category_summary
                    totalBytes: 1048576
                    totalRows: 1000
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetWorkTableBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: managed work table を truncate する
      tags:
        - Data & Datasets
  /api/v1/datasets:
    get:
      description: |-
        active tenant の dataset 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasets
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            maximum: 200
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - $schema: https://example.com/schemas/DatasetBody.json
                        byteSize: 104857600
                        columns:
                          - example
                        contentType: text/csv
                        createdAt: "2026-01-15T09:30:00Z"
                        errorSummary: example
                        importJob: {}
                        importedAt: "2026-01-15T09:30:00Z"
                        latestSyncJob: {}
                        name: Customers
                        originalFilename: customers.csv
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        rawDatabase: hh_t_1_raw
                        rawTable: ds_018f2f05c6c97a49b32d04f4dd84ef4a
                        rowCount: 10000000
                        sourceFileObjectId: 1
                        sourceKind: file
                        sourceWorkTableDatabase: support
                        sourceWorkTableId: 1
                        sourceWorkTableName: support
                        sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        sourceWorkTableStatus: active
                        sourceWorkTableTable: support
                        status: ready
                        updatedAt: "2026-01-15T09:30:00Z"
                        workDatabase: hh_t_1_work
              schema:
                $ref: "#/components/schemas/DatasetListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の dataset 一覧を返す
      tags:
        - Data & Datasets
    post:
      description: |-
        Drive CSV file を dataset として登録します。取り込み対象の file は active tenant の Drive 内に存在し、dataset 作成権限が必要です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDataset
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  driveFilePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  name: Acme
            schema:
              $ref: "#/components/schemas/DatasetCreateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    byteSize: 104857600
                    columns:
                      - clickHouseType: Nullable(String)
                        columnName: customer_id
                        ordinal: 1
                        originalName: Customer ID
                    contentType: text/csv
                    createdAt: "2026-01-15T09:30:00Z"
                    errorSummary: example
                    importJob:
                      completedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      errorSample:
                        - {}
                      errorSummary: example
                      invalidRows: 10
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      status: processing
                      totalRows: 10000000
                      updatedAt: "2026-01-15T09:30:00Z"
                      validRows: 9999990
                    importedAt: "2026-01-15T09:30:00Z"
                    latestSyncJob:
                      cleanupErrorSummary: example
                      cleanupStatus: active
                      completedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      errorSummary: example
                      mode: full_refresh
                      newRawDatabase: hh_t_1_raw
                      newRawTable: ds_new
                      oldRawDatabase: hh_t_1_raw
                      oldRawTable: ds_old
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rowCount: 100000
                      startedAt: "2026-01-15T09:30:00Z"
                      status: processing
                      totalBytes: 1048576
                      updatedAt: "2026-01-15T09:30:00Z"
                    name: Customers
                    originalFilename: customers.csv
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    rawDatabase: hh_t_1_raw
                    rawTable: ds_018f2f05c6c97a49b32d04f4dd84ef4a
                    rowCount: 10000000
                    sourceFileObjectId: 1
                    sourceKind: file
                    sourceWorkTableDatabase: support
                    sourceWorkTableId: 1
                    sourceWorkTableName: support
                    sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    sourceWorkTableStatus: active
                    sourceWorkTableTable: support
                    status: ready
                    updatedAt: "2026-01-15T09:30:00Z"
                    workDatabase: hh_t_1_work
              schema:
                $ref: "#/components/schemas/DatasetBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive CSV file から active tenant の dataset を作成する
      tags:
        - Data & Datasets
  /api/v1/datasets/{datasetPublicId}:
    delete:
      description: |-
        active tenant の dataset を削除する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteDataset
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `datasetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: datasetPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の dataset を削除する
      tags:
        - Data & Datasets
    get:
      description: |-
        active tenant の dataset detail を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDataset
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `datasetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: datasetPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetBody.json
                    byteSize: 104857600
                    columns:
                      - example
                    contentType: text/csv
                    createdAt: "2026-01-15T09:30:00Z"
                    errorSummary: example
                    importJob:
                      completedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      errorSample:
                        - example
                      errorSummary: example
                      invalidRows: 10
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      status: processing
                      totalRows: 10000000
                      updatedAt: "2026-01-15T09:30:00Z"
                      validRows: 9999990
                    importedAt: "2026-01-15T09:30:00Z"
                    latestSyncJob:
                      cleanupErrorSummary: example
                      cleanupStatus: active
                      completedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      errorSummary: example
                      mode: full_refresh
                      newRawDatabase: hh_t_1_raw
                      newRawTable: ds_new
                      oldRawDatabase: hh_t_1_raw
                      oldRawTable: ds_old
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rowCount: 100000
                      startedAt: "2026-01-15T09:30:00Z"
                      status: processing
                      totalBytes: 1048576
                      updatedAt: "2026-01-15T09:30:00Z"
                    name: Customers
                    originalFilename: customers.csv
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    rawDatabase: hh_t_1_raw
                    rawTable: ds_018f2f05c6c97a49b32d04f4dd84ef4a
                    rowCount: 10000000
                    sourceFileObjectId: 1
                    sourceKind: file
                    sourceWorkTableDatabase: support
                    sourceWorkTableId: 1
                    sourceWorkTableName: support
                    sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    sourceWorkTableStatus: active
                    sourceWorkTableTable: support
                    status: ready
                    updatedAt: "2026-01-15T09:30:00Z"
                    workDatabase: hh_t_1_work
              schema:
                $ref: "#/components/schemas/DatasetBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の dataset detail を返す
      tags:
        - Data & Datasets
  /api/v1/datasets/{datasetPublicId}/lineage:
    get:
      description: |-
        active tenant の dataset lineage graph を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDatasetLineage
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `datasetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: datasetPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `direction` です。
          example: upstream
          explode: false
          in: query
          name: direction
          schema:
            default: both
            enum:
              - upstream
              - downstream
              - both
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `depth` です。
          example: 1
          explode: false
          in: query
          name: depth
          schema:
            default: 1
            format: int32
            maximum: 2
            minimum: 1
            type: integer
        - description: 結果の絞り込みや pagination に使う query parameter `includeHistory` です。
          example: true
          explode: false
          in: query
          name: includeHistory
          schema:
            default: true
            type: boolean
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int32
            maximum: 100
            minimum: 1
            type: integer
        - description: 結果の絞り込みや pagination に使う query parameter `level` です。
          example: table
          explode: false
          in: query
          name: level
          schema:
            default: table
            enum:
              - table
              - column
              - both
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `sources` です。
          example: metadata,parser,manual
          explode: false
          in: query
          name: sources
          schema:
            examples:
              - metadata,parser,manual
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `includeDraft` です。
          example: true
          explode: false
          in: query
          name: includeDraft
          schema:
            default: false
            type: boolean
        - description: 結果の絞り込みや pagination に使う query parameter `changeSetPublicId` です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          explode: false
          in: query
          name: changeSetPublicId
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetLineageBody.json
                    edges:
                      - example
                    nodes:
                      - example
                    root:
                      columnName: customer_id
                      createdAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      displayName: Sales
                      editable: true
                      id: dataset:018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      metadata: {}
                      nodeKind: resource
                      position:
                        x: 120
                        "y": 240
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      resourceType: dataset
                      sourceKind: metadata
                      status: ready
                      updatedAt: "2026-01-15T09:30:00Z"
                    timeline:
                      - example
              schema:
                $ref: "#/components/schemas/DatasetLineageBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の dataset lineage graph を返す
      tags:
        - Data & Datasets
  /api/v1/datasets/{datasetPublicId}/query-jobs:
    get:
      description: |-
        active tenant の dataset に紐づく query job 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetScopedQueryJobs
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `datasetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: datasetPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 25
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - $schema: https://example.com/schemas/DatasetQueryJobBody.json
                        completedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        durationMs: 120
                        errorSummary: example
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resultColumns:
                          - example
                        resultRows:
                          - {}
                        rowCount: 1000
                        statement: example
                        status: completed
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetQueryListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の dataset に紐づく query job 一覧を返す
      tags:
        - Data & Datasets
    post:
      description: |-
        active tenant の dataset に紐づく SQL query job を作成する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDatasetScopedQueryJob
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `datasetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: datasetPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  statement: SELECT count() FROM hh_t_1_raw.ds_abc
            schema:
              $ref: "#/components/schemas/DatasetQueryCreateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    durationMs: 120
                    errorSummary: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resultColumns:
                      - example
                    resultRows:
                      - {}
                    rowCount: 1000
                    statement: example
                    status: completed
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetQueryJobBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の dataset に紐づく SQL query job を作成する
      tags:
        - Data & Datasets
  /api/v1/datasets/{datasetPublicId}/rows:
    get:
      description: |-
        active tenant の dataset rows を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetRows
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `datasetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: datasetPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `cursor` です。
          example: 1
          explode: false
          in: query
          name: cursor
          schema:
            default: 0
            format: int64
            minimum: 0
            type: integer
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 250
            format: int32
            maximum: 1000
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    columns:
                      - example
                    hasMore: true
                    nextCursor: 1
                    rows:
                      - {}
              schema:
                $ref: "#/components/schemas/DatasetRowsPageBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の dataset rows を返す
      tags:
        - Data & Datasets
  /api/v1/datasets/{datasetPublicId}/syncs:
    get:
      description: |-
        dataset の sync history を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetSyncJobs
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `datasetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: datasetPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 25
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - $schema: https://example.com/schemas/DatasetSyncJobBody.json
                        cleanupErrorSummary: example
                        cleanupStatus: active
                        completedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        errorSummary: example
                        mode: full_refresh
                        newRawDatabase: hh_t_1_raw
                        newRawTable: ds_new
                        oldRawDatabase: hh_t_1_raw
                        oldRawTable: ds_old
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        rowCount: 100000
                        startedAt: "2026-01-15T09:30:00Z"
                        status: processing
                        totalBytes: 1048576
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetSyncJobListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: dataset の sync history を返す
      tags:
        - Data & Datasets
    post:
      description: |-
        work table 由来 dataset の再同期を request する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDatasetSyncJob
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `datasetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: datasetPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  mode: full_refresh
            schema:
              $ref: "#/components/schemas/DatasetSyncJobCreateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    cleanupErrorSummary: example
                    cleanupStatus: active
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    errorSummary: example
                    mode: full_refresh
                    newRawDatabase: hh_t_1_raw
                    newRawTable: ds_new
                    oldRawDatabase: hh_t_1_raw
                    oldRawTable: ds_old
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    rowCount: 100000
                    startedAt: "2026-01-15T09:30:00Z"
                    status: processing
                    totalBytes: 1048576
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetSyncJobBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: work table 由来 dataset の再同期を request する
      tags:
        - Data & Datasets
  /api/v1/datasets/{datasetPublicId}/work-tables:
    get:
      description: |-
        dataset に紐づく managed work table 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetScopedWorkTables
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `datasetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: datasetPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            maximum: 200
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetWorkTableListBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/DatasetWorkTableListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: dataset に紐づく managed work table 一覧を返す
      tags:
        - Data & Datasets
  /api/v1/drive-mobile/offline/replay:
    post:
      description: |-
        Drive mobile offline operation queue を replay する。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: replayDriveMobileOfflineOperations
      parameters:
        - description: request header `Authorization` です。
          example: example
          in: header
          name: Authorization
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  operations:
                    - baseRevision: 1
                      name: Acme
                      operationId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      operationType: example
                      resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      resourceType: support
            schema:
              $ref: "#/components/schemas/DriveMobileOfflineReplayInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    applied: 1
                    conflicted: 1
                    denied: 1
              schema:
                $ref: "#/components/schemas/DriveMobileOfflineReplayOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: Drive mobile offline operation queue を replay する
      tags:
        - Drive Collaboration & Sync
  /api/v1/drive-sync/delta:
    get:
      description: |-
        Drive sync delta を返す。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
      operationId: getDriveSyncDelta
      parameters:
        - description: request header `Authorization` です。
          example: example
          in: header
          name: Authorization
          required: true
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `cursor` です。
          example: example
          explode: false
          in: query
          name: cursor
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    cursor: example
                    deniedCount: 3
                    events:
                      - action: example
                        createdAt: "2026-01-15T09:30:00Z"
                        id: 1
                        metadata: {}
                        objectVersion: example
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceType: support
                    fullResync: true
                    remoteWipe: true
              schema:
                $ref: "#/components/schemas/DriveSyncDeltaOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: Drive sync delta を返す
      tags:
        - Drive Collaboration & Sync
  /api/v1/drive-sync/devices/register:
    post:
      description: |-
        Drive sync device を登録する。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: registerDriveSyncDevice
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  deviceName: Acme
                  platform: desktop
            schema:
              $ref: "#/components/schemas/DriveRegisterDeviceBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    deviceName: Acme
                    lastSeenAt: "2026-01-15T09:30:00Z"
                    platform: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    remoteWipeRequired: true
                    status: active
                    token: token_demo_1234567890
              schema:
                $ref: "#/components/schemas/DriveDeviceBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive sync device を登録する
      tags:
        - Drive Collaboration & Sync
  /api/v1/drive-sync/devices/{devicePublicId}/revoke:
    post:
      description: |-
        Drive sync device を revoke する。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: revokeDriveSyncDevice
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `devicePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: devicePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  reason: example
            schema:
              $ref: "#/components/schemas/DriveDeviceRevokeInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive sync device を revoke する
      tags:
        - Drive Collaboration & Sync
  /api/v1/drive/downloads/archive:
    post:
      description: |-
        Drive items を ZIP archive として download する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: downloadDriveArchive
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  filename: roadmap.md
                  items:
                    - publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      type: file
            schema:
              $ref: "#/components/schemas/DriveArchiveBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/zip:
              schema:
                contentMediaType: application/octet-stream
                format: binary
                type: string
          description: ZIP archive
          headers:
            Content-Disposition:
              schema:
                type: string
            Content-Type:
              schema:
                type: string
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive items を ZIP archive として download する
      tags:
        - Drive Files & Folders
  /api/v1/drive/e2ee/user-keys:
    post:
      description: |-
        Drive E2EE public key を登録する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveE2EEUserKey
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  algorithm: example
                  publicKey: {}
            schema:
              $ref: "#/components/schemas/DriveE2EEUserKeyCreateInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    algorithm: example
                    createdAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    userPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveE2EEUserKeyBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive E2EE public key を登録する
      tags:
        - Drive Security & Compliance
  /api/v1/drive/files/{filePublicId}:
    delete:
      description: |-
        Drive file を削除する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteDriveFile
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file を削除する
      tags:
        - Drive Files & Folders
    get:
      description: |-
        Drive file metadata を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDriveFile
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveFileBody.json
                    byteSize: 1048576
                    contentType: text/markdown
                    createdAt: "2026-01-15T09:30:00Z"
                    deletedAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    dlpBlocked: false
                    inheritanceEnabled: true
                    lockReason: example
                    locked: true
                    lockedAt: "2026-01-15T09:30:00Z"
                    metadata: {}
                    originalFilename: spec.md
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    scanStatus: active
                    sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                    status: active
                    updatedAt: "2026-01-15T09:30:00Z"
                    workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveFileBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file metadata を返す
      tags:
        - Drive Files & Folders
    patch:
      description: |-
        Drive file の filename、description、tags、parent folder を更新します。ファイル本体の差し替えではなく metadata と配置の更新です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDriveFile
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  description: Updated product roadmap.
                  metadataVersionComment: Moved into planning folder.
                  originalFilename: roadmap.md
                  parentFolderPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  tags:
                    - roadmap
                    - planning
            schema:
              $ref: "#/components/schemas/UpdateDriveFileBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveFileBody.json
                    byteSize: 1048576
                    contentType: text/markdown
                    createdAt: "2026-01-15T09:30:00Z"
                    deletedAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    dlpBlocked: false
                    inheritanceEnabled: true
                    lockReason: example
                    locked: true
                    lockedAt: "2026-01-15T09:30:00Z"
                    metadata: {}
                    originalFilename: spec.md
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    scanStatus: active
                    sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                    status: active
                    updatedAt: "2026-01-15T09:30:00Z"
                    workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveFileBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file metadata を更新する
      tags:
        - Drive Files & Folders
  /api/v1/drive/files/{filePublicId}/activity:
    get:
      description: |-
        Drive file activity を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveFileActivity
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - action: example
                        actorDisplayName: Acme
                        actorUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        createdAt: "2026-01-15T09:30:00Z"
                        metadata: {}
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceType: support
              schema:
                $ref: "#/components/schemas/DriveActivityListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file activity を返す
      tags:
        - Drive Files & Folders
  /api/v1/drive/files/{filePublicId}/ai/classifications:
    get:
      description: |-
        Drive AI classifications を返す。

        Drive file の OCR、product extraction、AI summary/classification に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveAIClassifications
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - confidence: 42.5
                        createdAt: "2026-01-15T09:30:00Z"
                        label: example
                        provider: example
              schema:
                $ref: "#/components/schemas/DriveAIClassificationsOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive AI classifications を返す
      tags:
        - Drive AI & OCR
  /api/v1/drive/files/{filePublicId}/ai/jobs:
    post:
      description: |-
        Drive file に対する AI processing job を作成します。summary や classification のような後続取得 API と組み合わせて使います。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveAIJob
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  jobType: summary
            schema:
              $ref: "#/components/schemas/DriveAIJobCreateInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    jobType: example
                    provider: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveAIJobBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive AI job を作成する
      tags:
        - Drive AI & OCR
  /api/v1/drive/files/{filePublicId}/ai/summary:
    get:
      description: |-
        Drive AI summary を返す。

        Drive file の OCR、product extraction、AI summary/classification に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDriveAISummary
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    provider: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    summaryText: example
              schema:
                $ref: "#/components/schemas/DriveAISummaryBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive AI summary を返す
      tags:
        - Drive AI & OCR
  /api/v1/drive/files/{filePublicId}/copy:
    post:
      description: |-
        Drive file を copy する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: copyDriveFile
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  name: Acme
                  parentFolderPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/DriveCopyBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    file:
                      $schema: https://example.com/schemas/DriveFileBody.json
                      byteSize: 1048576
                      contentType: text/markdown
                      createdAt: "2026-01-15T09:30:00Z"
                      deletedAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      dlpBlocked: false
                      inheritanceEnabled: true
                      lockReason: example
                      locked: true
                      lockedAt: "2026-01-15T09:30:00Z"
                      metadata: {}
                      originalFilename: spec.md
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      scanStatus: active
                      sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                      status: active
                      updatedAt: "2026-01-15T09:30:00Z"
                      workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    folder:
                      $schema: https://example.com/schemas/DriveFolderBody.json
                      createdAt: "2026-01-15T09:30:00Z"
                      deletedAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      inheritanceEnabled: true
                      name: Project
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      updatedAt: "2026-01-15T09:30:00Z"
                      workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    ownedByMe: true
                    ownerDisplayName: Acme
                    ownerUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    shareRole: viewer
                    sharedWithMe: true
                    source: upload
                    starredByMe: true
                    tags:
                      - example
                    type: file
              schema:
                $ref: "#/components/schemas/DriveItemBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file を copy する
      tags:
        - Drive Files & Folders
  /api/v1/drive/files/{filePublicId}/data-pipeline-review-items:
    get:
      description: |-
        Drive file に紐づく data pipeline review item 一覧を返す。

        Drive file の OCR、product extraction、AI summary/classification に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveFileDataPipelineReviewItems
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `status` です。
          example: open
          explode: false
          in: query
          name: status
          schema:
            enum:
              - open
              - approved
              - rejected
              - needs_changes
              - closed
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DataPipelineReviewItemListBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/DataPipelineReviewItemListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file に紐づく data pipeline review item 一覧を返す
      tags:
        - Drive AI & OCR
  /api/v1/drive/files/{filePublicId}/e2ee/envelope:
    get:
      description: |-
        Drive E2EE actor envelope を返す。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDriveE2EEEnvelope
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveE2EEEnvelopeBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    fileKeyPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    recipientUserId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    wrapAlgorithm: example
                    wrappedFileKey: example
              schema:
                $ref: "#/components/schemas/DriveE2EEEnvelopeBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive E2EE actor envelope を返す
      tags:
        - Drive Security & Compliance
  /api/v1/drive/files/{filePublicId}/e2ee/envelopes:
    post:
      description: |-
        Drive E2EE recipient envelope を作成する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveE2EEEnvelope
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  recipientUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  wrapAlgorithm: example
                  wrappedFileKey: example
            schema:
              $ref: "#/components/schemas/DriveE2EEEnvelopeCreateInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    fileKeyPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    recipientUserId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    wrapAlgorithm: example
                    wrappedFileKey: example
              schema:
                $ref: "#/components/schemas/DriveE2EEEnvelopeBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive E2EE recipient envelope を作成する
      tags:
        - Drive Security & Compliance
  /api/v1/drive/files/{filePublicId}/e2ee/envelopes/{recipientUserPublicId}:
    delete:
      description: |-
        Drive E2EE recipient envelope を revoke する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: revokeDriveE2EEEnvelope
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: path 内の `recipientUserPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: recipientUserPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive E2EE recipient envelope を revoke する
      tags:
        - Drive Security & Compliance
  /api/v1/drive/files/{filePublicId}/e2ee/keys:
    post:
      description: |-
        Drive E2EE file key metadata を作成する。

        Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveE2EEFileKey
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  algorithm: example
                  ciphertextSha256: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                  metadata: {}
                  wrapAlgorithm: example
                  wrappedFileKey: example
            schema:
              $ref: "#/components/schemas/DriveE2EEFileKeyCreateInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    algorithm: example
                    ciphertextSha256: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                    createdAt: "2026-01-15T09:30:00Z"
                    filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    keyVersion: 1
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveE2EEFileKeyBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive E2EE file key metadata を作成する
      tags:
        - Drive Security & Compliance
  /api/v1/drive/files/{filePublicId}/edit-sessions:
    post:
      description: |-
        Drive file edit session を開始する。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: startDriveEditSession
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    baseRevision: 1
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    provider: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveEditSessionBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file edit session を開始する
      tags:
        - Drive Collaboration & Sync
  /api/v1/drive/files/{filePublicId}/edit-sessions/{sessionPublicId}:
    delete:
      description: |-
        Drive edit session を終了する。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: endDriveEditSession
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: path 内の `sessionPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: sessionPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive edit session を終了する
      tags:
        - Drive Collaboration & Sync
  /api/v1/drive/files/{filePublicId}/edit-sessions/{sessionPublicId}/content:
    put:
      description: |-
        Drive edit session content を保存する。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: saveDriveEditSessionContent
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: path 内の `sessionPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: sessionPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  content: example
                  contentType: text/markdown
                  expectedRevision: 1
                  filename: roadmap.md
            schema:
              $ref: "#/components/schemas/DriveEditSaveBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    conflict: true
                    file:
                      $schema: https://example.com/schemas/DriveFileBody.json
                      byteSize: 1048576
                      contentType: text/markdown
                      createdAt: "2026-01-15T09:30:00Z"
                      deletedAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      dlpBlocked: false
                      inheritanceEnabled: true
                      lockReason: example
                      locked: true
                      lockedAt: "2026-01-15T09:30:00Z"
                      metadata: {}
                      originalFilename: spec.md
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      scanStatus: active
                      sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                      status: active
                      updatedAt: "2026-01-15T09:30:00Z"
                      workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    revision: 1
              schema:
                $ref: "#/components/schemas/DriveEditSaveOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive edit session content を保存する
      tags:
        - Drive Collaboration & Sync
  /api/v1/drive/files/{filePublicId}/edit-sessions/{sessionPublicId}/heartbeat:
    post:
      description: |-
        Drive file edit session lease を延長する。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: heartbeatDriveEditSession
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: path 内の `sessionPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: sessionPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveEditSessionBody.json
                    baseRevision: 1
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    provider: example
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveEditSessionBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file edit session lease を延長する
      tags:
        - Drive Collaboration & Sync
  /api/v1/drive/files/{filePublicId}/inheritance:
    patch:
      description: |-
        Drive file inheritance を更新する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDriveFileInheritance
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  enabled: true
            schema:
              $ref: "#/components/schemas/DriveInheritanceBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file inheritance を更新する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/files/{filePublicId}/invitations:
    post:
      description: |-
        Drive file external share invitation を作成する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveFileShareInvitation
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  expiresAt: "2026-01-15T09:30:00Z"
                  inviteeEmail: demo@example.com
                  inviteeUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  role: owner
            schema:
              $ref: "#/components/schemas/CreateDriveShareInvitationBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    acceptToken: token_demo_1234567890
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    inviteeEmailDomain: demo@example.com
                    maskedInviteeEmail: demo@example.com
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourceType: support
                    role: viewer
                    status: active
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveShareInvitationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file external share invitation を作成する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/files/{filePublicId}/manifest:
    get:
      description: |-
        Drive file document manifest を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDriveFileManifest
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    documentType: example
                    file:
                      byteSize: 1048576
                      contentType: text/markdown
                      originalFilename: roadmap.md
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                    generatedAt: "2026-01-15T09:30:00Z"
                    manifest: {}
                    parserVersion: example
                    reason: example
                    stale: true
              schema:
                $ref: "#/components/schemas/DriveDocumentManifest"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file document manifest を返す
      tags:
        - Drive Files & Folders
  /api/v1/drive/files/{filePublicId}/manifest/refresh:
    post:
      description: |-
        Drive file document manifest を再解析する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: refreshDriveFileManifest
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveDocumentManifest.json
                    documentType: example
                    file:
                      byteSize: 1048576
                      contentType: text/markdown
                      originalFilename: roadmap.md
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                    generatedAt: "2026-01-15T09:30:00Z"
                    manifest: {}
                    parserVersion: example
                    reason: example
                    stale: true
              schema:
                $ref: "#/components/schemas/DriveDocumentManifest"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file document manifest を再解析する
      tags:
        - Drive Files & Folders
  /api/v1/drive/files/{filePublicId}/ocr:
    get:
      description: |-
        Drive file OCR result を返す。

        Drive file の OCR、product extraction、AI summary/classification に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDriveOCR
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    pages:
                      - averageConfidence: 42.5
                        boxesJson: example
                        layoutJson: example
                        pageNumber: 1
                        rawText: example
                    run:
                      averageConfidence: 42.5
                      completedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      engine: example
                      errorCode: example
                      errorMessage: example
                      filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      languages:
                        - example
                      pageCount: 3
                      processedPageCount: 3
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      reason: example
                      status: active
                      structuredExtractor: example
              schema:
                $ref: "#/components/schemas/DriveOCROutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file OCR result を返す
      tags:
        - Drive AI & OCR
  /api/v1/drive/files/{filePublicId}/ocr/jobs:
    post:
      description: |-
        Drive file の OCR job を作成します。画像/PDF から抽出された text は OCR result API で取得します。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: createDriveOCRJob
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    engine: example
                    filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveOCRJobBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file OCR job を作成する
      tags:
        - Drive AI & OCR
  /api/v1/drive/files/{filePublicId}/office/sessions:
    post:
      description: |-
        Drive Office edit session を作成する。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveOfficeSession
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  accessLevel: view
            schema:
              $ref: "#/components/schemas/DriveOfficeSessionCreateInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    accessLevel: example
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    launchUrl: https://example.com/webhooks/haohao
                    provider: example
                    providerSessionId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveOfficeSessionBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive Office edit session を作成する
      tags:
        - Drive Collaboration & Sync
  /api/v1/drive/files/{filePublicId}/owner-transfer:
    post:
      description: |-
        Drive file owner を移譲する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: transferDriveFileOwner
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  newOwnerUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  revokePreviousOwnerAccess: true
            schema:
              $ref: "#/components/schemas/DriveOwnerTransferBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveItemBody.json
                    file:
                      $schema: https://example.com/schemas/DriveFileBody.json
                      byteSize: 1048576
                      contentType: text/markdown
                      createdAt: "2026-01-15T09:30:00Z"
                      deletedAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      dlpBlocked: false
                      inheritanceEnabled: true
                      lockReason: example
                      locked: true
                      lockedAt: "2026-01-15T09:30:00Z"
                      metadata: {}
                      originalFilename: spec.md
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      scanStatus: active
                      sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                      status: active
                      updatedAt: "2026-01-15T09:30:00Z"
                      workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    folder:
                      $schema: https://example.com/schemas/DriveFolderBody.json
                      createdAt: "2026-01-15T09:30:00Z"
                      deletedAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      inheritanceEnabled: true
                      name: Project
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      updatedAt: "2026-01-15T09:30:00Z"
                      workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    ownedByMe: true
                    ownerDisplayName: Acme
                    ownerUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    shareRole: viewer
                    sharedWithMe: true
                    source: upload
                    starredByMe: true
                    tags:
                      - example
                    type: file
              schema:
                $ref: "#/components/schemas/DriveItemBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file owner を移譲する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/files/{filePublicId}/permanent:
    delete:
      description: |-
        Drive file を完全削除する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: permanentlyDeleteDriveFile
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file を完全削除する
      tags:
        - Drive Files & Folders
  /api/v1/drive/files/{filePublicId}/permissions:
    get:
      description: |-
        Drive file permissions を返す。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDriveFilePermissions
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    direct:
                      - canDownload: true
                        createdAt: "2026-01-15T09:30:00Z"
                        expiresAt: "2026-01-15T09:30:00Z"
                        inheritedFromId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        kind: share
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        role: viewer
                        source: direct
                        status: active
                        subjectId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        subjectType: user
                    inherited:
                      - canDownload: true
                        createdAt: "2026-01-15T09:30:00Z"
                        expiresAt: "2026-01-15T09:30:00Z"
                        inheritedFromId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        kind: share
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        role: viewer
                        source: direct
                        status: active
                        subjectId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        subjectType: user
              schema:
                $ref: "#/components/schemas/DrivePermissionsBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file permissions を返す
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/files/{filePublicId}/product-extractions:
    get:
      description: |-
        Drive file product extraction items を返す。

        Drive file の OCR、product extraction、AI summary/classification に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveProductExtractions
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - attributes: {}
                        availability: {}
                        brand: example
                        category: example
                        confidence: 42.5
                        createdAt: "2026-01-15T09:30:00Z"
                        description: Quarterly planning documents.
                        evidence:
                          - {}
                        itemType: example
                        janCode: example
                        manufacturer: example
                        model: example
                        name: Acme
                        price: {}
                        promotion: {}
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        sku: example
                        sourceText: support
              schema:
                $ref: "#/components/schemas/DriveProductExtractionsOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file product extraction items を返す
      tags:
        - Drive AI & OCR
  /api/v1/drive/files/{filePublicId}/product-extractions/jobs:
    post:
      description: |-
        OCR 結果をもとに product extraction job を作成します。抽出結果は product extraction list API で確認します。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: createDriveProductExtractionJob
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    extractor: example
                    filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    itemCount: 3
                    ocrRunPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
              schema:
                $ref: "#/components/schemas/DriveProductExtractionJobBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file product extraction job を作成する
      tags:
        - Drive AI & OCR
  /api/v1/drive/files/{filePublicId}/restore:
    post:
      description: |-
        Drive file を trash から復元する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: restoreDriveFile
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  parentFolderPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/RestoreDriveResourceBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveFileBody.json
                    byteSize: 1048576
                    contentType: text/markdown
                    createdAt: "2026-01-15T09:30:00Z"
                    deletedAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    dlpBlocked: false
                    inheritanceEnabled: true
                    lockReason: example
                    locked: true
                    lockedAt: "2026-01-15T09:30:00Z"
                    metadata: {}
                    originalFilename: spec.md
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    scanStatus: active
                    sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                    status: active
                    updatedAt: "2026-01-15T09:30:00Z"
                    workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveFileBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file を trash から復元する
      tags:
        - Drive Files & Folders
  /api/v1/drive/files/{filePublicId}/share-links:
    post:
      description: |-
        Drive file に public share link を作成します。password や expiry を設定すると、link access の安全性を高められます。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveFileShareLink
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  canDownload: true
                  expiresAt: "2026-02-01T00:00:00Z"
                  password: share-demo-passphrase
                  role: viewer
            schema:
              $ref: "#/components/schemas/CreateDriveShareLinkBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    canDownload: true
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    passwordRequired: true
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourceType: file
                    role: viewer
                    status: active
                    token: token_demo_1234567890
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveShareLinkBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file share link を作成する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/files/{filePublicId}/shares:
    post:
      description: |-
        Drive file share を作成する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveFileShare
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  role: owner
                  subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  subjectType: user
            schema:
              $ref: "#/components/schemas/CreateDriveShareBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    createdByUserId: 1
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourceType: file
                    role: viewer
                    status: active
                    subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    subjectType: user
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveShareBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file share を作成する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/files/{filePublicId}/shares/{sharePublicId}:
    delete:
      description: |-
        Drive file share を解除する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteDriveFileShare
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
        - description: Drive share record の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: sharePublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file share を解除する
      tags:
        - Drive Sharing & Permissions
    patch:
      description: |-
        Drive file share role を更新する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDriveFileShare
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
        - description: Drive share record の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: sharePublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  role: editor
            schema:
              $ref: "#/components/schemas/UpdateDriveShareBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveShareBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    createdByUserId: 1
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourceType: file
                    role: viewer
                    status: active
                    subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    subjectType: user
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveShareBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file share role を更新する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/files/{filePublicId}/star:
    delete:
      description: |-
        Drive file の star を外す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: unstarDriveFile
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file の star を外す
      tags:
        - Drive Files & Folders
    post:
      description: |-
        Drive file に star を付ける。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: starDriveFile
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive file に star を付ける
      tags:
        - Drive Files & Folders
  /api/v1/drive/folder-tree:
    get:
      description: |-
        Drive folder tree を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDriveFolderTree
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 500
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    ownedRoots:
                      - children: []
                        name: Acme
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    sharedRoots:
                      - children: []
                        name: Acme
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveFolderTreeBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder tree を返す
      tags:
        - Drive Files & Folders
  /api/v1/drive/folders:
    post:
      description: |-
        Drive に folder を作成します。`parentFolderPublicId` を省略すると root 配下に作成し、`workspacePublicId` を指定すると workspace 配下で管理します。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveFolder
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  description: Quarterly planning documents.
                  name: Project Alpha
                  parentFolderPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  tags:
                    - planning
                    - finance
                  workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/CreateDriveFolderBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    deletedAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    inheritanceEnabled: true
                    name: Project
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    updatedAt: "2026-01-15T09:30:00Z"
                    workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveFolderBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder を作成する
      tags:
        - Drive Files & Folders
  /api/v1/drive/folders/{folderPublicId}:
    delete:
      description: |-
        Drive folder を削除する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteDriveFolder
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder を削除する
      tags:
        - Drive Files & Folders
    get:
      description: |-
        Drive folder detail を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDriveFolder
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveFolderBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    deletedAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    inheritanceEnabled: true
                    name: Project
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    updatedAt: "2026-01-15T09:30:00Z"
                    workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveFolderBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder detail を返す
      tags:
        - Drive Files & Folders
    patch:
      description: |-
        Drive folder を更新する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDriveFolder
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  description: Quarterly planning documents.
                  name: Acme
                  parentFolderPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  tags:
                    - example
            schema:
              $ref: "#/components/schemas/UpdateDriveFolderBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveFolderBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    deletedAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    inheritanceEnabled: true
                    name: Project
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    updatedAt: "2026-01-15T09:30:00Z"
                    workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveFolderBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder を更新する
      tags:
        - Drive Files & Folders
  /api/v1/drive/folders/{folderPublicId}/activity:
    get:
      description: |-
        Drive folder activity を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveFolderActivity
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveActivityListOutputBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/DriveActivityListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder activity を返す
      tags:
        - Drive Files & Folders
  /api/v1/drive/folders/{folderPublicId}/children:
    get:
      description: |-
        Drive folder children を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveFolderChildren
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
        - description: Drive workspace の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          explode: false
          in: query
          name: workspacePublicId
          schema:
            format: uuid
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - file: {}
                        folder: {}
                        ownedByMe: true
                        ownerDisplayName: Acme
                        ownerUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        shareRole: viewer
                        sharedWithMe: true
                        source: upload
                        starredByMe: true
                        tags:
                          - example
                        type: file
              schema:
                $ref: "#/components/schemas/DriveItemListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder children を返す
      tags:
        - Drive Files & Folders
  /api/v1/drive/folders/{folderPublicId}/copy:
    post:
      description: |-
        Drive folder を copy する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: copyDriveFolder
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  name: Acme
                  parentFolderPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/DriveCopyBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveItemBody.json
                    file:
                      $schema: https://example.com/schemas/DriveFileBody.json
                      byteSize: 1048576
                      contentType: text/markdown
                      createdAt: "2026-01-15T09:30:00Z"
                      deletedAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      dlpBlocked: false
                      inheritanceEnabled: true
                      lockReason: example
                      locked: true
                      lockedAt: "2026-01-15T09:30:00Z"
                      metadata: {}
                      originalFilename: spec.md
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      scanStatus: active
                      sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                      status: active
                      updatedAt: "2026-01-15T09:30:00Z"
                      workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    folder:
                      $schema: https://example.com/schemas/DriveFolderBody.json
                      createdAt: "2026-01-15T09:30:00Z"
                      deletedAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      inheritanceEnabled: true
                      name: Project
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      updatedAt: "2026-01-15T09:30:00Z"
                      workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    ownedByMe: true
                    ownerDisplayName: Acme
                    ownerUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    shareRole: viewer
                    sharedWithMe: true
                    source: upload
                    starredByMe: true
                    tags:
                      - example
                    type: file
              schema:
                $ref: "#/components/schemas/DriveItemBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder を copy する
      tags:
        - Drive Files & Folders
  /api/v1/drive/folders/{folderPublicId}/inheritance:
    patch:
      description: |-
        Drive folder inheritance を更新する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDriveFolderInheritance
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  enabled: true
            schema:
              $ref: "#/components/schemas/DriveInheritanceBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder inheritance を更新する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/folders/{folderPublicId}/invitations:
    post:
      description: |-
        Drive folder external share invitation を作成する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveFolderShareInvitation
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  expiresAt: "2026-01-15T09:30:00Z"
                  inviteeEmail: demo@example.com
                  inviteeUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  role: owner
            schema:
              $ref: "#/components/schemas/CreateDriveShareInvitationBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveShareInvitationBody.json
                    acceptToken: token_demo_1234567890
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    inviteeEmailDomain: demo@example.com
                    maskedInviteeEmail: demo@example.com
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourceType: support
                    role: viewer
                    status: active
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveShareInvitationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder external share invitation を作成する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/folders/{folderPublicId}/owner-transfer:
    post:
      description: |-
        Drive folder owner を移譲する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: transferDriveFolderOwner
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  newOwnerUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  revokePreviousOwnerAccess: true
            schema:
              $ref: "#/components/schemas/DriveOwnerTransferBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveItemBody.json
                    file:
                      $schema: https://example.com/schemas/DriveFileBody.json
                      byteSize: 1048576
                      contentType: text/markdown
                      createdAt: "2026-01-15T09:30:00Z"
                      deletedAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      dlpBlocked: false
                      inheritanceEnabled: true
                      lockReason: example
                      locked: true
                      lockedAt: "2026-01-15T09:30:00Z"
                      metadata: {}
                      originalFilename: spec.md
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      scanStatus: active
                      sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                      status: active
                      updatedAt: "2026-01-15T09:30:00Z"
                      workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    folder:
                      $schema: https://example.com/schemas/DriveFolderBody.json
                      createdAt: "2026-01-15T09:30:00Z"
                      deletedAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      inheritanceEnabled: true
                      name: Project
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      updatedAt: "2026-01-15T09:30:00Z"
                      workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    ownedByMe: true
                    ownerDisplayName: Acme
                    ownerUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    shareRole: viewer
                    sharedWithMe: true
                    source: upload
                    starredByMe: true
                    tags:
                      - example
                    type: file
              schema:
                $ref: "#/components/schemas/DriveItemBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder owner を移譲する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/folders/{folderPublicId}/permanent:
    delete:
      description: |-
        Drive folder を完全削除する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: permanentlyDeleteDriveFolder
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder を完全削除する
      tags:
        - Drive Files & Folders
  /api/v1/drive/folders/{folderPublicId}/permissions:
    get:
      description: |-
        Drive folder permissions を返す。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDriveFolderPermissions
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DrivePermissionsBody.json
                    direct:
                      - example
                    inherited:
                      - example
              schema:
                $ref: "#/components/schemas/DrivePermissionsBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder permissions を返す
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/folders/{folderPublicId}/restore:
    post:
      description: |-
        Drive folder を trash から復元する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: restoreDriveFolder
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  parentFolderPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/RestoreDriveResourceBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveFolderBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    deletedAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    inheritanceEnabled: true
                    name: Project
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    updatedAt: "2026-01-15T09:30:00Z"
                    workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveFolderBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder を trash から復元する
      tags:
        - Drive Files & Folders
  /api/v1/drive/folders/{folderPublicId}/share-links:
    post:
      description: |-
        Drive folder share link を作成する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveFolderShareLink
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  canDownload: true
                  expiresAt: "2026-01-15T09:30:00Z"
                  password: share-demo-passphrase
                  role: viewer
            schema:
              $ref: "#/components/schemas/CreateDriveShareLinkBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveShareLinkBody.json
                    canDownload: true
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    passwordRequired: true
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourceType: file
                    role: viewer
                    status: active
                    token: token_demo_1234567890
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveShareLinkBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder share link を作成する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/folders/{folderPublicId}/shares:
    post:
      description: |-
        Drive folder share を作成する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveFolderShare
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  role: owner
                  subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  subjectType: user
            schema:
              $ref: "#/components/schemas/CreateDriveShareBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveShareBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    createdByUserId: 1
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourceType: file
                    role: viewer
                    status: active
                    subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    subjectType: user
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveShareBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder share を作成する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/folders/{folderPublicId}/shares/{sharePublicId}:
    delete:
      description: |-
        Drive folder share を解除する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteDriveFolderShare
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
        - description: Drive share record の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: sharePublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder share を解除する
      tags:
        - Drive Sharing & Permissions
    patch:
      description: |-
        Drive folder share role を更新する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDriveFolderShare
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
        - description: Drive share record の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: sharePublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  role: editor
            schema:
              $ref: "#/components/schemas/UpdateDriveShareBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveShareBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    createdByUserId: 1
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourceType: file
                    role: viewer
                    status: active
                    subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    subjectType: user
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveShareBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder share role を更新する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/folders/{folderPublicId}/star:
    delete:
      description: |-
        Drive folder の star を外す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: unstarDriveFolder
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder の star を外す
      tags:
        - Drive Files & Folders
    post:
      description: |-
        Drive folder に star を付ける。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: starDriveFolder
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderPublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive folder に star を付ける
      tags:
        - Drive Files & Folders
  /api/v1/drive/groups:
    get:
      description: |-
        Drive groups を返す。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveGroups
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - createdAt: "2026-01-15T09:30:00Z"
                        description: Quarterly planning documents.
                        members:
                          - example
                        name: Acme
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveGroupListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive groups を返す
      tags:
        - Drive Sharing & Permissions
    post:
      description: |-
        Drive group を作成する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveGroup
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  description: Quarterly planning documents.
                  name: Acme
            schema:
              $ref: "#/components/schemas/CreateDriveGroupBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    members:
                      - example
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveGroupBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive group を作成する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/groups/{groupPublicId}:
    delete:
      description: |-
        Drive group を削除する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteDriveGroup
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `groupPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: groupPublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive group を削除する
      tags:
        - Drive Sharing & Permissions
    get:
      description: |-
        Drive group detail を返す。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDriveGroup
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `groupPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: groupPublicId
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveGroupBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    members:
                      - example
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveGroupBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive group detail を返す
      tags:
        - Drive Sharing & Permissions
    patch:
      description: |-
        Drive group を更新する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDriveGroup
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `groupPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: groupPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  description: Quarterly planning documents.
                  name: Acme
            schema:
              $ref: "#/components/schemas/CreateDriveGroupBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveGroupBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    members:
                      - example
                    name: Acme
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveGroupBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive group を更新する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/groups/{groupPublicId}/members:
    post:
      description: |-
        Drive group member を追加する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: addDriveGroupMember
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `groupPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: groupPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  userPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/AddDriveGroupMemberBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive group member を追加する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/groups/{groupPublicId}/members/{userPublicId}:
    delete:
      description: |-
        Drive group member を削除する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteDriveGroupMember
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `groupPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: groupPublicId
          required: true
          schema:
            type: string
        - description: path 内の `userPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: userPublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive group member を削除する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/invitations:
    get:
      description: |-
        ログイン user 宛の Drive invitation を返す。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveShareInvitations
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - $schema: https://example.com/schemas/DriveShareInvitationBody.json
                        acceptToken: token_demo_1234567890
                        createdAt: "2026-01-15T09:30:00Z"
                        expiresAt: "2026-01-15T09:30:00Z"
                        inviteeEmailDomain: demo@example.com
                        maskedInviteeEmail: demo@example.com
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceType: support
                        role: viewer
                        status: active
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveShareInvitationListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: ログイン user 宛の Drive invitation を返す
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/invitations/{invitationPublicId}/accept:
    post:
      description: |-
        Drive invitation を受諾する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: acceptDriveShareInvitation
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `invitationPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: invitationPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  acceptToken: token_demo_1234567890
            schema:
              $ref: "#/components/schemas/AcceptDriveShareInvitationBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveShareBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    createdByUserId: 1
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourceType: file
                    role: viewer
                    status: active
                    subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    subjectType: user
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveShareBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive invitation を受諾する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/invitations/{invitationPublicId}/revoke:
    post:
      description: |-
        Drive invitation を revoke する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: revokeDriveShareInvitation
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `invitationPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: invitationPublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive invitation を revoke する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/items:
    get:
      description: |-
        Drive item 一覧を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveItems
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Drive workspace の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          explode: false
          in: query
          name: workspacePublicId
          schema:
            format: uuid
            type: string
        - description: Drive folder の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          explode: false
          in: query
          name: folderPublicId
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `parentFolderPublicId` です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          explode: false
          in: query
          name: parentFolderPublicId
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `type` です。
          example: example
          explode: false
          in: query
          name: type
          schema:
            default: all
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `owner` です。
          example: example
          explode: false
          in: query
          name: owner
          schema:
            default: all
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `source` です。
          example: support
          explode: false
          in: query
          name: source
          schema:
            default: all
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `sort` です。
          example: example
          explode: false
          in: query
          name: sort
          schema:
            default: updated_at
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `direction` です。
          example: example
          explode: false
          in: query
          name: direction
          schema:
            default: desc
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveItemListOutputBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/DriveItemListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive item 一覧を返す
      tags:
        - Drive Files & Folders
  /api/v1/drive/marketplace/apps:
    get:
      description: |-
        Drive marketplace catalog を返す。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveMarketplaceApps
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - name: Acme
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        publisherName: Acme
                        scopes:
                          - example
                        slug: example
                        version: example
              schema:
                $ref: "#/components/schemas/DriveMarketplaceAppsOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive marketplace catalog を返す
      tags:
        - Platform Integrations
  /api/v1/drive/office/sessions/{sessionPublicId}:
    delete:
      description: |-
        Drive Office edit session を revoke する。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: revokeDriveOfficeSession
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `sessionPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: sessionPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive Office edit session を revoke する
      tags:
        - Drive Collaboration & Sync
  /api/v1/drive/rag/query:
    post:
      description: |-
        Drive content を permission-filtered RAG で回答する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: queryDriveRAG
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  limit: 50
                  mode: keyword
                  query: roadmap
            schema:
              $ref: "#/components/schemas/DriveRAGQueryBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    answer: example
                    blocked: false
                    citations:
                      - citationId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        filename: roadmap.md
                        resourceKind: drive_file
                        resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        score: 42.5
                        snippet: example
                    matches:
                      - citationId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        filePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        filename: roadmap.md
                        resourceKind: drive_file
                        resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        score: 42.5
                        snippet: example
                    retrievalTrace:
                      - intent: example
                        mergedCount: 3
                        missingSignals:
                          - example
                        planSource: support
                        query: roadmap
                        resultCount: 3
                        retry: true
                        retryReason: example
                        searchMode: keyword
              schema:
                $ref: "#/components/schemas/DriveRAGAnswerBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive content を permission-filtered RAG で回答する
      tags:
        - Drive Files & Folders
  /api/v1/drive/recent:
    get:
      description: |-
        Recent Drive item を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveRecent
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveItemListOutputBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/DriveItemListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Recent Drive item を返す
      tags:
        - Drive Files & Folders
  /api/v1/drive/search:
    get:
      description: |-
        Drive item を検索する。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: searchDriveItems
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 検索語または filter query です。
          example: roadmap
          explode: false
          in: query
          name: q
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `mode` です。
          example: keyword
          explode: false
          in: query
          name: mode
          schema:
            default: keyword
            enum:
              - keyword
              - semantic
              - hybrid
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `contentType` です。
          example: text/markdown
          explode: false
          in: query
          name: contentType
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `type` です。
          example: example
          explode: false
          in: query
          name: type
          schema:
            default: all
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `owner` です。
          example: example
          explode: false
          in: query
          name: owner
          schema:
            default: all
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `source` です。
          example: support
          explode: false
          in: query
          name: source
          schema:
            default: all
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `sort` です。
          example: example
          explode: false
          in: query
          name: sort
          schema:
            default: updated_at
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `direction` です。
          example: example
          explode: false
          in: query
          name: direction
          schema:
            default: desc
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveItemListOutputBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/DriveItemListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive item を検索する
      tags:
        - Drive Files & Folders
  /api/v1/drive/search/documents:
    get:
      description: |-
        Drive content index 付き検索結果を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: searchDriveDocuments
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 検索語または filter query です。
          example: roadmap
          explode: false
          in: query
          name: q
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `mode` です。
          example: keyword
          explode: false
          in: query
          name: mode
          schema:
            default: keyword
            enum:
              - keyword
              - semantic
              - hybrid
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `contentType` です。
          example: text/markdown
          explode: false
          in: query
          name: contentType
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `type` です。
          example: example
          explode: false
          in: query
          name: type
          schema:
            default: all
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `owner` です。
          example: example
          explode: false
          in: query
          name: owner
          schema:
            default: all
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `source` です。
          example: support
          explode: false
          in: query
          name: source
          schema:
            default: all
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `sort` です。
          example: example
          explode: false
          in: query
          name: sort
          schema:
            default: updated_at
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `direction` です。
          example: example
          explode: false
          in: query
          name: direction
          schema:
            default: desc
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - indexedAt: "2026-01-15T09:30:00Z"
                        item: {}
                        matches: []
                        snippet: example
              schema:
                $ref: "#/components/schemas/DriveSearchResultOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive content index 付き検索結果を返す
      tags:
        - Drive Files & Folders
  /api/v1/drive/share-links/{shareLinkPublicId}:
    delete:
      description: |-
        Drive share link を無効化する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteDriveShareLink
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `shareLinkPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: shareLinkPublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive share link を無効化する
      tags:
        - Drive Sharing & Permissions
    patch:
      description: |-
        Drive share link を更新する。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDriveShareLink
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `shareLinkPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: shareLinkPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  canDownload: true
                  expiresAt: "2026-01-15T09:30:00Z"
            schema:
              $ref: "#/components/schemas/UpdateDriveShareLinkBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveShareLinkBody.json
                    canDownload: true
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    passwordRequired: true
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourceType: file
                    role: viewer
                    status: active
                    token: token_demo_1234567890
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveShareLinkBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive share link を更新する
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/share-targets:
    get:
      description: |-
        Drive share target candidates を返す。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveShareTargets
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 検索語または filter query です。
          example: roadmap
          explode: false
          in: query
          name: q
          schema:
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 20
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - displayName: Acme
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        secondary: example
                        type: user
              schema:
                $ref: "#/components/schemas/DriveShareTargetsOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive share target candidates を返す
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/shared-with-me:
    get:
      description: |-
        自分に共有された Drive item を返す。

        Drive resource の共有、権限、share link、招待、group を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveSharedWithMe
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveItemListOutputBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/DriveItemListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: 自分に共有された Drive item を返す
      tags:
        - Drive Sharing & Permissions
  /api/v1/drive/starred:
    get:
      description: |-
        Starred Drive item を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveStarred
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveItemListOutputBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/DriveItemListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Starred Drive item を返す
      tags:
        - Drive Files & Folders
  /api/v1/drive/storage:
    get:
      description: |-
        Drive storage usage を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDriveStorageUsage
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    fileCount: 3
                    quotaBytes: 1048576
                    storageDriver: example
                    trashBytes: 1048576
                    trashFileCount: 3
                    usedBytes: 1048576
              schema:
                $ref: "#/components/schemas/DriveStorageUsageBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive storage usage を返す
      tags:
        - Drive Files & Folders
  /api/v1/drive/trash:
    get:
      description: |-
        Drive trash item 一覧を返す。

        Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveTrashItems
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveItemListOutputBody.json
                    items:
                      - example
              schema:
                $ref: "#/components/schemas/DriveItemListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive trash item 一覧を返す
      tags:
        - Drive Files & Folders
  /api/v1/drive/workspaces:
    get:
      description: |-
        Drive workspace 一覧を返す。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDriveWorkspaces
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - createdAt: "2026-01-15T09:30:00Z"
                        name: Acme
                        policyOverride: {}
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        storageQuotaBytes: 1048576
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveWorkspaceListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive workspace 一覧を返す
      tags:
        - Drive Collaboration & Sync
    post:
      description: |-
        Drive workspace を作成する。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDriveWorkspace
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  name: Acme
                  policyOverride: {}
                  storageQuotaBytes: 1048576
            schema:
              $ref: "#/components/schemas/CreateDriveWorkspaceBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    name: Acme
                    policyOverride: {}
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    storageQuotaBytes: 1048576
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveWorkspaceBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive workspace を作成する
      tags:
        - Drive Collaboration & Sync
  /api/v1/drive/workspaces/{workspacePublicId}:
    delete:
      description: |-
        Drive workspace を削除する。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteDriveWorkspace
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive workspace の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workspacePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive workspace を削除する
      tags:
        - Drive Collaboration & Sync
    patch:
      description: |-
        Drive workspace を更新する。

        Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDriveWorkspace
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive workspace の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: workspacePublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  name: Acme
                  policyOverride: {}
                  storageQuotaBytes: 1048576
            schema:
              $ref: "#/components/schemas/CreateDriveWorkspaceBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DriveWorkspaceBody.json
                    createdAt: "2026-01-15T09:30:00Z"
                    name: Acme
                    policyOverride: {}
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    storageQuotaBytes: 1048576
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveWorkspaceBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Drive workspace を更新する
      tags:
        - Drive Collaboration & Sync
  /api/v1/files:
    get:
      description: |-
        active tenant の file metadata を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listFiles
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 結果の絞り込みや pagination に使う query parameter `attachedToType` です。
          example: example
          explode: false
          in: query
          name: attachedToType
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `attachedToId` です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          explode: false
          in: query
          name: attachedToId
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - attachedToId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        attachedToType: customer_signal
                        byteSize: 1048576
                        contentType: text/plain
                        createdAt: "2026-01-15T09:30:00Z"
                        originalFilename: roadmap.md
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        purpose: attachment
                        sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                        status: active
                        tenantId: 1
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/FileListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の file metadata を返す
      tags:
        - Data & Datasets
  /api/v1/files/{filePublicId}:
    delete:
      description: |-
        active tenant の file を soft delete する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteFile
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: Drive file の public UUID です。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: filePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の file を soft delete する
      tags:
        - Data & Datasets
  /api/v1/gold-publications:
    get:
      description: |-
        active tenant の Gold data mart 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetGoldPublications
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - $schema: https://example.com/schemas/DatasetGoldPublicationBody.json
                        archivedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        description: Quarterly planning documents.
                        displayName: Monthly sales mart
                        goldDatabase: hh_t_1_gold
                        goldTable: gm_monthly_sales
                        latestPublishRun: {}
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        publishedAt: "2026-01-15T09:30:00Z"
                        refreshPolicy: manual
                        rowCount: 1000
                        schemaSummary: {}
                        sourceScd2Summary: {}
                        sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        status: active
                        totalBytes: 1048576
                        unpublishedAt: "2026-01-15T09:30:00Z"
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetGoldPublicationListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の Gold data mart 一覧を返す
      tags:
        - Data & Datasets
  /api/v1/gold-publications/{goldPublicId}:
    get:
      description: |-
        Gold data mart detail を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDatasetGoldPublication
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `goldPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: goldPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetGoldPublicationBody.json
                    archivedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    displayName: Monthly sales mart
                    goldDatabase: hh_t_1_gold
                    goldTable: gm_monthly_sales
                    latestPublishRun:
                      completedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      errorSummary: example
                      goldDatabase: hh_t_1_gold
                      goldTable: gm_monthly_sales
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      publicationPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rowCount: 1000
                      schemaSummary: {}
                      sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      startedAt: "2026-01-15T09:30:00Z"
                      status: completed
                      totalBytes: 1048576
                      updatedAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publishedAt: "2026-01-15T09:30:00Z"
                    refreshPolicy: manual
                    rowCount: 1000
                    schemaSummary: {}
                    sourceScd2Summary:
                      currentRows: 4
                      detected: true
                      earliestValidAt: "2026-05-01 00:00:00"
                      historyRows: 8
                      keyColumn: id
                      keyCount: 4
                      latestValidAt: "2026-05-03 00:00:00"
                      totalRows: 12
                    sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    totalBytes: 1048576
                    unpublishedAt: "2026-01-15T09:30:00Z"
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetGoldPublicationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Gold data mart detail を返す
      tags:
        - Data & Datasets
  /api/v1/gold-publications/{goldPublicId}/archive:
    post:
      description: |-
        Gold data mart を archive する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: archiveDatasetGoldPublication
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          schema:
            type: string
        - description: path 内の `goldPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: goldPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetGoldPublicationBody.json
                    archivedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    displayName: Monthly sales mart
                    goldDatabase: hh_t_1_gold
                    goldTable: gm_monthly_sales
                    latestPublishRun:
                      $schema: https://example.com/schemas/DatasetGoldPublishRunBody.json
                      completedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      errorSummary: example
                      goldDatabase: hh_t_1_gold
                      goldTable: gm_monthly_sales
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      publicationPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rowCount: 1000
                      schemaSummary: {}
                      sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      startedAt: "2026-01-15T09:30:00Z"
                      status: completed
                      totalBytes: 1048576
                      updatedAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publishedAt: "2026-01-15T09:30:00Z"
                    refreshPolicy: manual
                    rowCount: 1000
                    schemaSummary: {}
                    sourceScd2Summary:
                      currentRows: 4
                      detected: true
                      earliestValidAt: "2026-05-01 00:00:00"
                      historyRows: 8
                      keyColumn: id
                      keyCount: 4
                      latestValidAt: "2026-05-03 00:00:00"
                      totalRows: 12
                    sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    totalBytes: 1048576
                    unpublishedAt: "2026-01-15T09:30:00Z"
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetGoldPublicationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Gold data mart を archive する
      tags:
        - Data & Datasets
  /api/v1/gold-publications/{goldPublicId}/preview:
    get:
      description: |-
        Gold data mart preview rows を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: previewDatasetGoldPublication
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `goldPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: goldPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    columns:
                      - example
                    database: hh_t_1_gold
                    previewRows:
                      - {}
                    table: gm_monthly_sales
              schema:
                $ref: "#/components/schemas/DatasetGoldPublicationPreviewBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Gold data mart preview rows を返す
      tags:
        - Data & Datasets
  /api/v1/gold-publications/{goldPublicId}/publish-runs:
    get:
      description: |-
        Gold data mart publish history を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetGoldPublishRuns
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `goldPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: goldPublicId
          required: true
          schema:
            format: uuid
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - completedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        errorSummary: example
                        goldDatabase: hh_t_1_gold
                        goldTable: gm_monthly_sales
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        publicationPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        rowCount: 1000
                        schemaSummary: {}
                        sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        startedAt: "2026-01-15T09:30:00Z"
                        status: completed
                        totalBytes: 1048576
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetGoldPublishRunListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Gold data mart publish history を返す
      tags:
        - Data & Datasets
  /api/v1/gold-publications/{goldPublicId}/refresh:
    post:
      description: |-
        Gold data mart の full publish refresh を request する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: refreshDatasetGoldPublication
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          schema:
            type: string
        - description: path 内の `goldPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: goldPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    errorSummary: example
                    goldDatabase: hh_t_1_gold
                    goldTable: gm_monthly_sales
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicationPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    rowCount: 1000
                    schemaSummary: {}
                    sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    startedAt: "2026-01-15T09:30:00Z"
                    status: completed
                    totalBytes: 1048576
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetGoldPublishRunBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Gold data mart の full publish refresh を request する
      tags:
        - Data & Datasets
  /api/v1/gold-publications/{goldPublicId}/unpublish:
    post:
      description: |-
        Gold data mart を unpublish する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: unpublishDatasetGoldPublication
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          schema:
            type: string
        - description: path 内の `goldPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: goldPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetGoldPublicationBody.json
                    archivedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    displayName: Monthly sales mart
                    goldDatabase: hh_t_1_gold
                    goldTable: gm_monthly_sales
                    latestPublishRun:
                      $schema: https://example.com/schemas/DatasetGoldPublishRunBody.json
                      completedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      errorSummary: example
                      goldDatabase: hh_t_1_gold
                      goldTable: gm_monthly_sales
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      publicationPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rowCount: 1000
                      schemaSummary: {}
                      sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      startedAt: "2026-01-15T09:30:00Z"
                      status: completed
                      totalBytes: 1048576
                      updatedAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publishedAt: "2026-01-15T09:30:00Z"
                    refreshPolicy: manual
                    rowCount: 1000
                    schemaSummary: {}
                    sourceScd2Summary:
                      currentRows: 4
                      detected: true
                      earliestValidAt: "2026-05-01 00:00:00"
                      historyRows: 8
                      keyColumn: id
                      keyCount: 4
                      latestValidAt: "2026-05-03 00:00:00"
                      totalRows: 12
                    sourceWorkTablePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    status: active
                    totalBytes: 1048576
                    unpublishedAt: "2026-01-15T09:30:00Z"
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetGoldPublicationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Gold data mart を unpublish する
      tags:
        - Data & Datasets
  /api/v1/integrations:
    get:
      description: |-
        downstream integration の接続状態を返す。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listIntegrations
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - connected: true
                        grantedAt: "2026-01-15T09:30:00Z"
                        lastErrorCode: invalid_grant
                        lastRefreshedAt: "2026-01-15T09:30:00Z"
                        provider: zitadel
                        resourceServer: zitadel
                        revokedAt: "2026-01-15T09:30:00Z"
                        scopes:
                          - offline_access
              schema:
                $ref: "#/components/schemas/ListIntegrationsBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: downstream integration の接続状態を返す
      tags:
        - Platform Integrations
  /api/v1/integrations/{resourceServer}/callback:
    get:
      description: |-
        downstream integration consent callback を完了する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: finishIntegrationConnect
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `resourceServer` を指定します。
          example: zitadel
          in: path
          name: resourceServer
          required: true
          schema:
            examples:
              - zitadel
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `code` です。
          example: example
          explode: false
          in: query
          name: code
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `state` です。
          example: example
          explode: false
          in: query
          name: state
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `error` です。
          example: example
          explode: false
          in: query
          name: error
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `error_description` です。
          example: Quarterly planning documents.
          explode: false
          in: query
          name: error_description
          schema:
            type: string
      responses:
        "302":
          description: 操作に成功した場合の response です。
          headers:
            Location:
              schema:
                type: string
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: downstream integration consent callback を完了する
      tags:
        - Platform Integrations
  /api/v1/integrations/{resourceServer}/connect:
    get:
      description: |-
        downstream integration consent を開始する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: connectIntegration
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `resourceServer` を指定します。
          example: zitadel
          in: path
          name: resourceServer
          required: true
          schema:
            examples:
              - zitadel
            type: string
      responses:
        "302":
          description: 操作に成功した場合の response です。
          headers:
            Location:
              schema:
                type: string
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: downstream integration consent を開始する
      tags:
        - Platform Integrations
  /api/v1/integrations/{resourceServer}/grant:
    delete:
      description: |-
        downstream integration grant を削除する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteIntegrationGrant
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `resourceServer` を指定します。
          example: zitadel
          in: path
          name: resourceServer
          required: true
          schema:
            examples:
              - zitadel
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: downstream integration grant を削除する
      tags:
        - Platform Integrations
  /api/v1/integrations/{resourceServer}/verify:
    post:
      description: |-
        downstream access token を backend 内で取得できるか検証する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: verifyIntegrationAccess
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `resourceServer` を指定します。
          example: zitadel
          in: path
          name: resourceServer
          required: true
          schema:
            examples:
              - zitadel
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    accessExpiresAt: "2026-01-15T09:30:00Z"
                    connected: true
                    refreshedAt: "2026-01-15T09:30:00Z"
                    resourceServer: zitadel
                    scopes:
                      - offline_access
              schema:
                $ref: "#/components/schemas/VerifyIntegrationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: downstream access token を backend 内で取得できるか検証する
      tags:
        - Platform Integrations
  /api/v1/invitations/accept:
    post:
      description: |-
        tenant invitation を accept する。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: acceptTenantInvitation
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: 同じ request の retry を安全に扱うための任意 key です。同じ key は同じ method/path/body にだけ再利用できます。
          example: req_20260115_customer_signal_001
          in: header
          name: Idempotency-Key
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  token: token_demo_1234567890
            schema:
              $ref: "#/components/schemas/AcceptTenantInvitationRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantInvitationBody.json
                    acceptUrl: https://example.com/webhooks/haohao
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    identitySetupDeliveryMode: return_code
                    identitySetupEmailCode: demo@example.com
                    identitySetupInviteCode: example
                    identitySetupLoginUrl: https://example.com/webhooks/haohao
                    identitySetupUserId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    inviteeEmailNormalized: demo@example.com
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    roleCodes:
                      - viewer
                    status: pending
                    tenantDisplayName: Acme
                    tenantId: 1
                    tenantSlug: acme
                    token: token_demo_1234567890
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantInvitationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: tenant invitation を accept する
      tags:
        - Tenant Workspace
  /api/v1/invitations/resolve:
    get:
      description: |-
        tenant invitation token の公開情報を返す。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。
      operationId: resolveTenantInvitation
      parameters:
        - description: 結果の絞り込みや pagination に使う query parameter `token` です。
          example: token_demo_1234567890
          explode: false
          in: query
          name: token
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantInvitationBody.json
                    acceptUrl: https://example.com/webhooks/haohao
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    identitySetupDeliveryMode: return_code
                    identitySetupEmailCode: demo@example.com
                    identitySetupInviteCode: example
                    identitySetupLoginUrl: https://example.com/webhooks/haohao
                    identitySetupUserId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    inviteeEmailNormalized: demo@example.com
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    roleCodes:
                      - viewer
                    status: pending
                    tenantDisplayName: Acme
                    tenantId: 1
                    tenantSlug: acme
                    token: token_demo_1234567890
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantInvitationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      summary: tenant invitation token の公開情報を返す
      tags:
        - Tenant Workspace
  /api/v1/invitations/{invitationPublicId}/setup-identity:
    post:
      description: |-
        tenant invitation の Zitadel identity 初期設定を開始する。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: setupTenantInvitationIdentity
      parameters:
        - description: path 内の `invitationPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: invitationPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  token: token_demo_1234567890
            schema:
              $ref: "#/components/schemas/SetupTenantInvitationIdentityRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TenantInvitationBody.json
                    acceptUrl: https://example.com/webhooks/haohao
                    createdAt: "2026-01-15T09:30:00Z"
                    expiresAt: "2026-01-15T09:30:00Z"
                    identitySetupDeliveryMode: return_code
                    identitySetupEmailCode: demo@example.com
                    identitySetupInviteCode: example
                    identitySetupLoginUrl: https://example.com/webhooks/haohao
                    identitySetupUserId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    inviteeEmailNormalized: demo@example.com
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    roleCodes:
                      - viewer
                    status: pending
                    tenantDisplayName: Acme
                    tenantId: 1
                    tenantSlug: acme
                    token: token_demo_1234567890
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TenantInvitationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      summary: tenant invitation の Zitadel identity 初期設定を開始する
      tags:
        - Tenant Workspace
  /api/v1/jobs:
    get:
      description: |-
        system jobs を検索する。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listSystemJobs
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 検索語または filter query です。
          example: roadmap
          explode: false
          in: query
          name: query
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `type` です。
          example: example
          explode: false
          in: query
          name: type
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `status` です。
          example: active
          explode: false
          in: query
          name: status
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `statusGroup` です。
          example: active
          explode: false
          in: query
          name: statusGroup
          schema:
            enum:
              - active
              - terminal
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 25
            format: int32
            maximum: 100
            minimum: 1
            type: integer
        - description: pagination の開始位置です。`limit` と組み合わせて使います。
          example: 0
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            format: int32
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - action: example
                        canStop: true
                        completedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        errorMessage: example
                        metadata: {}
                        outboxEventPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        requestedByDisplayName: Acme
                        requestedByEmail: demo@example.com
                        startedAt: "2026-01-15T09:30:00Z"
                        status: active
                        statusGroup: active
                        subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        subjectType: example
                        title: Export CSV from reports
                        type: example
                        updatedAt: "2026-01-15T09:30:00Z"
                    limit: 50
                    offset: 0
                    total: 1
              schema:
                $ref: "#/components/schemas/SystemJobListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: system jobs を検索する
      tags:
        - Tenant Administration
  /api/v1/jobs/{jobType}/{jobPublicId}:
    get:
      description: |-
        system job detail を返す。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getSystemJob
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          schema:
            type: string
        - description: path 内の `jobType` を指定します。
          example: example
          in: path
          name: jobType
          required: true
          schema:
            type: string
        - description: path 内の `jobPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: jobPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    action: example
                    canStop: true
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    errorMessage: example
                    metadata: {}
                    outboxEventPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    requestedByDisplayName: Acme
                    requestedByEmail: demo@example.com
                    startedAt: "2026-01-15T09:30:00Z"
                    status: active
                    statusGroup: active
                    subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    subjectType: example
                    title: Export CSV from reports
                    type: example
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/SystemJobBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: system job detail を返す
      tags:
        - Tenant Administration
  /api/v1/jobs/{jobType}/{jobPublicId}/stop:
    post:
      description: |-
        system job を手動停止する。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: stopSystemJob
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          schema:
            type: string
        - description: path 内の `jobType` を指定します。
          example: example
          in: path
          name: jobType
          required: true
          schema:
            type: string
        - description: path 内の `jobPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: jobPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/SystemJobBody.json
                    action: example
                    canStop: true
                    completedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    errorMessage: example
                    metadata: {}
                    outboxEventPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    requestedByDisplayName: Acme
                    requestedByEmail: demo@example.com
                    startedAt: "2026-01-15T09:30:00Z"
                    status: active
                    statusGroup: active
                    subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    subjectType: example
                    title: Export CSV from reports
                    type: example
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/SystemJobBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: system job を手動停止する
      tags:
        - Tenant Administration
  /api/v1/lineage/change-sets:
    get:
      description: |-
        lineage change set 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listDatasetLineageChangeSets
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 結果の絞り込みや pagination に使う query parameter `status` です。
          example: draft
          explode: false
          in: query
          name: status
          schema:
            enum:
              - draft
              - published
              - rejected
              - archived
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - $schema: https://example.com/schemas/DatasetLineageChangeSetBody.json
                        archivedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        description: Quarterly planning documents.
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        publishedAt: "2026-01-15T09:30:00Z"
                        queryJobPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        rejectedAt: "2026-01-15T09:30:00Z"
                        rootResourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        rootResourceType: support
                        sourceKind: parser
                        status: draft
                        title: Export CSV from reports
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetLineageChangeSetListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: lineage change set 一覧を返す
      tags:
        - Data & Datasets
    post:
      description: |-
        lineage change set draft を作成する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createDatasetLineageChangeSet
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  description: Quarterly planning documents.
                  rootResourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  rootResourceType: dataset_query_job
                  sourceKind: manual
                  title: Export CSV from reports
            schema:
              $ref: "#/components/schemas/DatasetLineageChangeSetCreateBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    archivedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publishedAt: "2026-01-15T09:30:00Z"
                    queryJobPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    rejectedAt: "2026-01-15T09:30:00Z"
                    rootResourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    rootResourceType: support
                    sourceKind: parser
                    status: draft
                    title: Export CSV from reports
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetLineageChangeSetBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: lineage change set draft を作成する
      tags:
        - Data & Datasets
  /api/v1/lineage/change-sets/{changeSetPublicId}:
    get:
      description: |-
        lineage change set graph を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getDatasetLineageChangeSet
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `changeSetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: changeSetPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    changeSet:
                      $schema: https://example.com/schemas/DatasetLineageChangeSetBody.json
                      archivedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      publishedAt: "2026-01-15T09:30:00Z"
                      queryJobPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rejectedAt: "2026-01-15T09:30:00Z"
                      rootResourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rootResourceType: support
                      sourceKind: parser
                      status: draft
                      title: Export CSV from reports
                      updatedAt: "2026-01-15T09:30:00Z"
                    edges:
                      - confidence: metadata
                        createdAt: "2026-01-15T09:30:00Z"
                        description: Quarterly planning documents.
                        editable: true
                        expression: example
                        id: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        label: example
                        relationType: source_dataset
                        sourceKind: metadata
                        sourceNodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        targetNodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    nodes:
                      - columnName: customer_id
                        createdAt: "2026-01-15T09:30:00Z"
                        description: Quarterly planning documents.
                        displayName: Sales
                        editable: true
                        id: dataset:018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        metadata: {}
                        nodeKind: resource
                        position: {}
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceType: dataset
                        sourceKind: metadata
                        status: ready
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetLineageChangeSetGraphBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: lineage change set graph を返す
      tags:
        - Data & Datasets
  /api/v1/lineage/change-sets/{changeSetPublicId}/graph:
    put:
      description: |-
        lineage change set draft graph を保存する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateDatasetLineageChangeSetGraph
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `changeSetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: changeSetPublicId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  edges:
                    - confidence: parser_exact
                      description: Quarterly planning documents.
                      expression: example
                      label: example
                      metadata: {}
                      relationType: example
                      sourceKind: parser
                      sourceNodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      targetNodeId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  nodes:
                    - columnName: Acme
                      description: Quarterly planning documents.
                      displayName: Acme
                      id: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      metadata: {}
                      nodeKind: resource
                      position: {}
                      resourceType: support
                      sourceKind: parser
            schema:
              $ref: "#/components/schemas/DatasetLineageGraphSaveBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetLineageChangeSetGraphBody.json
                    changeSet:
                      $schema: https://example.com/schemas/DatasetLineageChangeSetBody.json
                      archivedAt: "2026-01-15T09:30:00Z"
                      createdAt: "2026-01-15T09:30:00Z"
                      description: Quarterly planning documents.
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      publishedAt: "2026-01-15T09:30:00Z"
                      queryJobPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rejectedAt: "2026-01-15T09:30:00Z"
                      rootResourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rootResourceType: support
                      sourceKind: parser
                      status: draft
                      title: Export CSV from reports
                      updatedAt: "2026-01-15T09:30:00Z"
                    edges:
                      - example
                    nodes:
                      - example
              schema:
                $ref: "#/components/schemas/DatasetLineageChangeSetGraphBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: lineage change set draft graph を保存する
      tags:
        - Data & Datasets
  /api/v1/lineage/change-sets/{changeSetPublicId}/publish:
    post:
      description: |-
        lineage change set draft を publish する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: publishDatasetLineageChangeSet
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `changeSetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: changeSetPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetLineageChangeSetBody.json
                    archivedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publishedAt: "2026-01-15T09:30:00Z"
                    queryJobPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    rejectedAt: "2026-01-15T09:30:00Z"
                    rootResourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    rootResourceType: support
                    sourceKind: parser
                    status: draft
                    title: Export CSV from reports
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetLineageChangeSetBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: lineage change set draft を publish する
      tags:
        - Data & Datasets
  /api/v1/lineage/change-sets/{changeSetPublicId}/reject:
    post:
      description: |-
        lineage change set draft を reject する。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: rejectDatasetLineageChangeSet
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `changeSetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: changeSetPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/DatasetLineageChangeSetBody.json
                    archivedAt: "2026-01-15T09:30:00Z"
                    createdAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    publishedAt: "2026-01-15T09:30:00Z"
                    queryJobPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    rejectedAt: "2026-01-15T09:30:00Z"
                    rootResourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    rootResourceType: support
                    sourceKind: parser
                    status: draft
                    title: Export CSV from reports
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DatasetLineageChangeSetBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: lineage change set draft を reject する
      tags:
        - Data & Datasets
  /api/v1/login:
    post:
      description: |-
        local password login が有効な環境で、email/password を検証して Cookie session を発行します。成功時は `SESSION_ID` Cookie が返り、以後の browser API はその Cookie と必要に応じて `X-CSRF-Token` を送ります。

        ### 使い方

        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: login
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  email: demo@example.com
                  password: password
            schema:
              $ref: "#/components/schemas/LoginInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/SessionBody.json
                    supportAccess:
                      expiresAt: "2026-01-15T09:30:00Z"
                      impersonatedUserDisplayName: Acme
                      impersonatedUserEmail: demo@example.com
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      reason: example
                      startedAt: "2026-01-15T09:30:00Z"
                      supportUserDisplayName: Acme
                      supportUserEmail: demo@example.com
                      tenantDisplayName: Acme
                      tenantSlug: acme
                    user:
                      displayName: Demo User
                      email: demo@example.com
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/SessionBody"
          description: 操作に成功し、response body に結果を返します。
          headers:
            Set-Cookie:
              schema:
                type: string
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      summary: ログインして Cookie セッションを払い出す
      tags:
        - Auth & Session
  /api/v1/logout:
    post:
      description: |-
        セッションを破棄する。

        login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: logout
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    postLogoutURL: http://localhost:8081/oidc/v1/end_session?id_token_hint=...
              schema:
                $ref: "#/components/schemas/LogoutBody"
          description: 操作に成功し、response body に結果を返します。
          headers:
            Set-Cookie:
              schema:
                type: string
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: セッションを破棄する
      tags:
        - Auth & Session
  /api/v1/machine-clients:
    get:
      description: |-
        machine client を list する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listMachineClients
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - active: true
                        allowedScopes:
                          - m2m:read
                        createdAt: "2026-01-15T09:30:00Z"
                        defaultTenant: {}
                        displayName: nightly worker
                        id: 1
                        provider: zitadel
                        providerClientId: "312345678901234567"
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/ListMachineClientsOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: machine client を list する
      tags:
        - Platform Integrations
    post:
      description: |-
        machine client を作成する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createMachineClient
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  active: true
                  allowedScopes:
                    - m2m:read
                  defaultTenantId: 1
                  displayName: nightly worker
                  provider: zitadel
                  providerClientId: "312345678901234567"
            schema:
              $ref: "#/components/schemas/MachineClientRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    active: true
                    allowedScopes:
                      - m2m:read
                    createdAt: "2026-01-15T09:30:00Z"
                    defaultTenant:
                      default: true
                      displayName: acme
                      id: 1
                      roles:
                        - todo_user
                      selected: true
                      slug: acme
                    displayName: nightly worker
                    id: 1
                    provider: zitadel
                    providerClientId: "312345678901234567"
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/MachineClientBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: machine client を作成する
      tags:
        - Platform Integrations
  /api/v1/machine-clients/{id}:
    delete:
      description: |-
        machine client を無効化する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteMachineClient
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `id` を指定します。
          example: 1
          in: path
          name: id
          required: true
          schema:
            format: int64
            minimum: 1
            type: integer
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: machine client を無効化する
      tags:
        - Platform Integrations
    get:
      description: |-
        machine client を取得する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getMachineClient
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `id` を指定します。
          example: 1
          in: path
          name: id
          required: true
          schema:
            format: int64
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/MachineClientBody.json
                    active: true
                    allowedScopes:
                      - m2m:read
                    createdAt: "2026-01-15T09:30:00Z"
                    defaultTenant:
                      default: true
                      displayName: acme
                      id: 1
                      roles:
                        - todo_user
                      selected: true
                      slug: acme
                    displayName: nightly worker
                    id: 1
                    provider: zitadel
                    providerClientId: "312345678901234567"
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/MachineClientBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: machine client を取得する
      tags:
        - Platform Integrations
    put:
      description: |-
        machine client を更新する。

        integration、machine client、entitlement、webhook の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateMachineClient
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `id` を指定します。
          example: 1
          in: path
          name: id
          required: true
          schema:
            format: int64
            minimum: 1
            type: integer
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  active: true
                  allowedScopes:
                    - m2m:read
                  defaultTenantId: 1
                  displayName: nightly worker
                  provider: zitadel
                  providerClientId: "312345678901234567"
            schema:
              $ref: "#/components/schemas/MachineClientRequestBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/MachineClientBody.json
                    active: true
                    allowedScopes:
                      - m2m:read
                    createdAt: "2026-01-15T09:30:00Z"
                    defaultTenant:
                      default: true
                      displayName: acme
                      id: 1
                      roles:
                        - todo_user
                      selected: true
                      slug: acme
                    displayName: nightly worker
                    id: 1
                    provider: zitadel
                    providerClientId: "312345678901234567"
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/MachineClientBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: machine client を更新する
      tags:
        - Platform Integrations
  /api/v1/medallion/assets:
    get:
      description: |-
        active tenant の Medallion catalog asset 一覧を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listMedallionAssets
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 検索語または filter query です。
          example: roadmap
          explode: false
          in: query
          name: q
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `layer` です。
          example: bronze
          explode: false
          in: query
          name: layer
          schema:
            enum:
              - bronze
              - silver
              - gold
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `resourceKind` です。
          example: drive_file
          explode: false
          in: query
          name: resourceKind
          schema:
            enum:
              - drive_file
              - dataset
              - work_table
              - ocr_run
              - product_extraction
              - gold_table
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            format: int32
            maximum: 200
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - archivedAt: "2026-01-15T09:30:00Z"
                        byteSize: 1048576
                        createdAt: "2026-01-15T09:30:00Z"
                        displayName: customers.csv
                        layer: bronze
                        metadata: {}
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceKind: drive_file
                        resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        rowCount: 1000
                        schemaSummary: {}
                        status: active
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/MedallionAssetListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の Medallion catalog asset 一覧を返す
      tags:
        - Data & Datasets
  /api/v1/medallion/assets/{assetPublicId}:
    get:
      description: |-
        Medallion catalog asset detail を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getMedallionAsset
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `assetPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: assetPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    asset:
                      archivedAt: "2026-01-15T09:30:00Z"
                      byteSize: 1048576
                      createdAt: "2026-01-15T09:30:00Z"
                      displayName: customers.csv
                      layer: bronze
                      metadata: {}
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      resourceKind: drive_file
                      resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rowCount: 1000
                      schemaSummary: {}
                      status: active
                      updatedAt: "2026-01-15T09:30:00Z"
                    downstream:
                      - archivedAt: "2026-01-15T09:30:00Z"
                        byteSize: 1048576
                        createdAt: "2026-01-15T09:30:00Z"
                        displayName: customers.csv
                        layer: bronze
                        metadata: {}
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceKind: drive_file
                        resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        rowCount: 1000
                        schemaSummary: {}
                        status: active
                        updatedAt: "2026-01-15T09:30:00Z"
                    pipelineRuns:
                      - completedAt: "2026-01-15T09:30:00Z"
                        createdAt: "2026-01-15T09:30:00Z"
                        errorSummary: example
                        metadata: {}
                        pipelineType: drive_ocr
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        retryable: true
                        runtime: clickhouse
                        sourceAssetPublicIds:
                          - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        sourceResourceKind: support
                        sourceResourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        startedAt: "2026-01-15T09:30:00Z"
                        status: completed
                        targetAssetPublicIds:
                          - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        targetResourceKind: support
                        targetResourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        triggerKind: manual
                        updatedAt: "2026-01-15T09:30:00Z"
                    upstream:
                      - archivedAt: "2026-01-15T09:30:00Z"
                        byteSize: 1048576
                        createdAt: "2026-01-15T09:30:00Z"
                        displayName: customers.csv
                        layer: bronze
                        metadata: {}
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        resourceKind: drive_file
                        resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        rowCount: 1000
                        schemaSummary: {}
                        status: active
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/MedallionCatalogBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: Medallion catalog asset detail を返す
      tags:
        - Data & Datasets
  /api/v1/medallion/resources/{resourceKind}/{resourcePublicId}:
    get:
      description: |-
        resource public ID から Medallion catalog summary を返す。

        file metadata、dataset、work table、query job、export の管理に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getMedallionResourceCatalog
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: path 内の `resourceKind` を指定します。
          example: drive_file
          in: path
          name: resourceKind
          required: true
          schema:
            enum:
              - drive_file
              - dataset
              - work_table
              - ocr_run
              - product_extraction
              - gold_table
            type: string
        - description: path 内の `resourcePublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: resourcePublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/MedallionCatalogBody.json
                    asset:
                      archivedAt: "2026-01-15T09:30:00Z"
                      byteSize: 1048576
                      createdAt: "2026-01-15T09:30:00Z"
                      displayName: customers.csv
                      layer: bronze
                      metadata: {}
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      resourceKind: drive_file
                      resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      rowCount: 1000
                      schemaSummary: {}
                      status: active
                      updatedAt: "2026-01-15T09:30:00Z"
                    downstream:
                      - example
                    pipelineRuns:
                      - example
                    upstream:
                      - example
              schema:
                $ref: "#/components/schemas/MedallionCatalogBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: resource public ID から Medallion catalog summary を返す
      tags:
        - Data & Datasets
  /api/v1/notifications:
    get:
      description: |-
        現在の user 宛の notifications を返す。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listNotifications
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: 検索語または filter query です。
          example: roadmap
          explode: false
          in: query
          name: q
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `readState` です。
          example: all
          explode: false
          in: query
          name: readState
          schema:
            enum:
              - all
              - unread
              - read
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `channel` です。
          example: in_app
          explode: false
          in: query
          name: channel
          schema:
            enum:
              - in_app
              - email
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `createdAfter` です。
          example: "2026-01-15T09:30:00Z"
          explode: false
          in: query
          name: createdAfter
          schema:
            format: date-time
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `cursor` です。
          example: example
          explode: false
          in: query
          name: cursor
          schema:
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    filteredCount: 3
                    items:
                      - body: You have a new notification.
                        channel: in_app
                        createdAt: "2026-01-15T09:30:00Z"
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        readAt: "2026-01-15T09:30:00Z"
                        status: queued
                        subject: Invitation
                        template: tenant_invitation
                        tenantId: 1
                        updatedAt: "2026-01-15T09:30:00Z"
                    nextCursor: example
                    readCount: 3
                    totalCount: 3
                    unreadCount: 3
              schema:
                $ref: "#/components/schemas/NotificationListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: 現在の user 宛の notifications を返す
      tags:
        - Tenant Workspace
  /api/v1/notifications/read:
    post:
      description: |-
        selected notifications を既読にする。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: markNotificationsRead
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  publicIds:
                    - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/MarkNotificationsReadBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - body: You have a new notification.
                        channel: in_app
                        createdAt: "2026-01-15T09:30:00Z"
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        readAt: "2026-01-15T09:30:00Z"
                        status: queued
                        subject: Invitation
                        template: tenant_invitation
                        tenantId: 1
                        updatedAt: "2026-01-15T09:30:00Z"
                    updatedCount: 3
              schema:
                $ref: "#/components/schemas/NotificationBulkReadBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: selected notifications を既読にする
      tags:
        - Tenant Workspace
  /api/v1/notifications/read-all:
    post:
      description: |-
        current filters に一致する unread notifications を全て既読にする。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: markAllNotificationsRead
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  channel: in_app
                  createdAfter: "2026-01-15T09:30:00Z"
                  q: roadmap
                  readState: all
            schema:
              $ref: "#/components/schemas/MarkAllNotificationsReadBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    updatedCount: 3
              schema:
                $ref: "#/components/schemas/NotificationReadAllBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: current filters に一致する unread notifications を全て既読にする
      tags:
        - Tenant Workspace
  /api/v1/notifications/{notificationPublicId}/read:
    post:
      description: |-
        notification を既読にする。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: markNotificationRead
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `notificationPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: notificationPublicId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    body: You have a new notification.
                    channel: in_app
                    createdAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    readAt: "2026-01-15T09:30:00Z"
                    status: queued
                    subject: Invitation
                    template: tenant_invitation
                    tenantId: 1
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/NotificationBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: notification を既読にする
      tags:
        - Tenant Workspace
  /api/v1/session:
    get:
      description: |-
        現在のセッションを返す。

        login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getSession
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    supportAccess:
                      expiresAt: "2026-01-15T09:30:00Z"
                      impersonatedUserDisplayName: Acme
                      impersonatedUserEmail: demo@example.com
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      reason: example
                      startedAt: "2026-01-15T09:30:00Z"
                      supportUserDisplayName: Acme
                      supportUserEmail: demo@example.com
                      tenantDisplayName: Acme
                      tenantSlug: acme
                    user:
                      displayName: Demo User
                      email: demo@example.com
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/SessionBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: 現在のセッションを返す
      tags:
        - Auth & Session
  /api/v1/session/refresh:
    post:
      description: |-
        セッションを再発行する。

        login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: refreshSession
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
          headers:
            Set-Cookie:
              schema:
                type: string
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: セッションを再発行する
      tags:
        - Auth & Session
  /api/v1/session/tenant:
    post:
      description: |-
        ログイン中の session に active tenant を設定します。tenant-scoped API はここで選択した tenant を既定の操作対象として扱います。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: selectTenant
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  tenantSlug: acme
            schema:
              $ref: "#/components/schemas/SelectTenantInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    activeTenant:
                      default: true
                      displayName: acme
                      id: 1
                      roles:
                        - todo_user
                      selected: true
                      slug: acme
              schema:
                $ref: "#/components/schemas/SelectTenantOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: 現在の session の active tenant を切り替える
      tags:
        - Tenant Workspace
  /api/v1/support/access/current:
    get:
      description: |-
        現在の support access を返す。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: getCurrentSupportAccess
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/SupportAccessOutputBody.json
                    access:
                      expiresAt: "2026-01-15T09:30:00Z"
                      impersonatedUserDisplayName: Acme
                      impersonatedUserEmail: demo@example.com
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      reason: example
                      startedAt: "2026-01-15T09:30:00Z"
                      supportUserDisplayName: Acme
                      supportUserEmail: demo@example.com
                      tenantDisplayName: Acme
                      tenantSlug: acme
                    active: true
              schema:
                $ref: "#/components/schemas/SupportAccessOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: 現在の support access を返す
      tags:
        - Tenant Administration
  /api/v1/support/access/end:
    post:
      description: |-
        support access を終了する。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: endSupportAccess
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: support access を終了する
      tags:
        - Tenant Administration
  /api/v1/support/access/start:
    post:
      description: |-
        support access を開始する。

        tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: startSupportAccess
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  durationMinutes: 1
                  impersonatedUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  reason: example
                  tenantSlug: acme
            schema:
              $ref: "#/components/schemas/StartSupportAccessBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    access:
                      expiresAt: "2026-01-15T09:30:00Z"
                      impersonatedUserDisplayName: Acme
                      impersonatedUserEmail: demo@example.com
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                      reason: example
                      startedAt: "2026-01-15T09:30:00Z"
                      supportUserDisplayName: Acme
                      supportUserEmail: demo@example.com
                      tenantDisplayName: Acme
                      tenantSlug: acme
                    active: true
              schema:
                $ref: "#/components/schemas/SupportAccessOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: support access を開始する
      tags:
        - Tenant Administration
  /api/v1/tenants:
    get:
      description: |-
        現在の user が利用できる tenants を返す。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTenants
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    activeTenant:
                      default: true
                      displayName: acme
                      id: 1
                      roles:
                        - todo_user
                      selected: true
                      slug: acme
                    defaultTenant:
                      default: true
                      displayName: acme
                      id: 1
                      roles:
                        - todo_user
                      selected: true
                      slug: acme
                    items:
                      - default: true
                        displayName: acme
                        id: 1
                        roles:
                          - todo_user
                        selected: true
                        slug: acme
              schema:
                $ref: "#/components/schemas/ListTenantsBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: 現在の user が利用できる tenants を返す
      tags:
        - Tenant Workspace
  /api/v1/todos:
    get:
      description: |-
        active tenant の TODO 一覧を返す。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: listTodos
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - completed: false
                        createdAt: "2026-01-15T09:30:00Z"
                        publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        title: Follow up with customer
                        updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TodoListBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の TODO 一覧を返す
      tags:
        - Tenant Workspace
    post:
      description: |-
        active tenant に TODO を作成する。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createTodo
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  title: Follow up with customer
            schema:
              $ref: "#/components/schemas/CreateTodoBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    completed: false
                    createdAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    title: Follow up with customer
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TodoBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant に TODO を作成する
      tags:
        - Tenant Workspace
  /api/v1/todos/{todoPublicId}:
    delete:
      description: |-
        active tenant の TODO を削除する。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
      operationId: deleteTodo
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `todoPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: todoPublicId
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の TODO を削除する
      tags:
        - Tenant Workspace
    patch:
      description: |-
        active tenant の TODO を更新する。

        active tenant と tenant 内 workspace の日常操作に使う endpoint です。

        ### 使い方

        - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: updateTodo
      parameters:
        - description: login または session refresh で発行される browser session Cookie です。
          example: sess_demo_1234567890
          in: cookie
          name: SESSION_ID
          schema:
            $ref: "#/components/schemas/Cookie"
        - description: Cookie session を使う state-changing request に必要な CSRF token です。
          example: csrf_demo_1234567890
          in: header
          name: X-CSRF-Token
          required: true
          schema:
            type: string
        - description: path 内の `todoPublicId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: todoPublicId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  completed: true
                  title: Follow up with customer
            schema:
              $ref: "#/components/schemas/UpdateTodoBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/TodoBody.json
                    completed: false
                    createdAt: "2026-01-15T09:30:00Z"
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    title: Follow up with customer
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/TodoBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - cookieAuth: []
      summary: active tenant の TODO を更新する
      tags:
        - Tenant Workspace
tags:
  - description: Authentication settings, login, logout, session refresh, and CSRF endpoints.
    name: Auth & Session
  - description: Tenant selection, tenant-scoped settings, notifications, invitations, and workspace utilities.
    name: Tenant Workspace
  - description: Tenant lifecycle administration, support access, and admin role management.
    name: Tenant Administration
  - description: Customer signal records, saved filters, and import jobs.
    name: Customer Signals
  - description: File metadata, dataset creation, work tables, query jobs, and export workflows.
    name: Data & Datasets
  - description: External integrations, machine clients, entitlements, and webhooks.
    name: Platform Integrations
  - description: Drive file, folder, item listing, search, metadata, and trash operations.
    name: Drive Files & Folders
  - description: Drive share links, invitations, groups, public access, and permission views.
    name: Drive Sharing & Permissions
  - description: Drive workspaces, collaborative editing, office sessions, sync, and offline operations.
    name: Drive Collaboration & Sync
  - description: Drive OCR, product extraction, AI summary, and AI classification operations.
    name: Drive AI & OCR
  - description: Drive encryption, HSM, legal hold, eDiscovery, clean room, and gateway controls.
    name: Drive Security & Compliance
  - description: Tenant-admin Drive policies, audit, search rebuild, OCR status, and operations health.
    name: Drive Admin & Governance
