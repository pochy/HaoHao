components:
  schemas:
    CreateDriveFolderBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateDriveFolderBody.json
          format: uri
          readOnly: true
          type: string
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          maxLength: 255
          type: string
        parentFolderPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        workspacePublicId:
          description: 対象 Drive workspace の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - name
      type: object
    CreateDriveShareBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateDriveShareBody.json
          format: uri
          readOnly: true
          type: string
        role:
          description: resource に対して付与する role または tenant role code です。
          enum:
            - owner
            - editor
            - viewer
          examples:
            - owner
          type: string
        subjectPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        subjectType:
          enum:
            - user
            - group
          examples:
            - user
          type: string
      required:
        - subjectType
        - subjectPublicId
        - role
      type: object
    DriveFileBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveFileBody.json
          format: uri
          readOnly: true
          type: string
        byteSize:
          examples:
            - 1048576
          format: int64
          type: integer
        contentType:
          examples:
            - text/markdown
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        deletedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        dlpBlocked:
          examples:
            - false
          type: boolean
        inheritanceEnabled:
          examples:
            - true
          type: boolean
        lockReason:
          examples:
            - example
          type: string
        locked:
          examples:
            - true
          type: boolean
        lockedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        originalFilename:
          examples:
            - spec.md
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        scanStatus:
          examples:
            - active
          type: string
        sha256Hex:
          examples:
            - 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        workspacePublicId:
          description: 対象 Drive workspace の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      required:
        - publicId
        - originalFilename
        - contentType
        - byteSize
        - sha256Hex
        - status
        - scanStatus
        - dlpBlocked
        - inheritanceEnabled
        - locked
        - createdAt
        - updatedAt
      type: object
    DriveFolderBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveFolderBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        deletedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        description:
          description: resource の説明文です。
          examples:
            - Quarterly planning documents.
          type: string
        inheritanceEnabled:
          examples:
            - true
          type: boolean
        name:
          description: 画面表示や検索で使う名前です。
          examples:
            - Project
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        workspacePublicId:
          description: 対象 Drive workspace の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
      required:
        - publicId
        - name
        - inheritanceEnabled
        - createdAt
        - updatedAt
      type: object
    DriveItemBody:
      additionalProperties: false
      properties:
        file:
          $ref: "#/components/schemas/DriveFileBody"
          examples:
            - example
        folder:
          $ref: "#/components/schemas/DriveFolderBody"
          examples:
            - example
        ownedByMe:
          examples:
            - true
          type: boolean
        ownerDisplayName:
          examples:
            - Acme
          type: string
        ownerUserPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        shareRole:
          examples:
            - viewer
          type: string
        sharedWithMe:
          examples:
            - true
          type: boolean
        source:
          description: record の発生元または permission の由来です。
          examples:
            - upload
          type: string
        starredByMe:
          examples:
            - true
          type: boolean
        tags:
          description: 検索や分類に使う tag 配列です。
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        type:
          examples:
            - file
          type: string
      required:
        - type
        - ownedByMe
        - sharedWithMe
        - starredByMe
      type: object
    DriveItemListOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveItemListOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/DriveItemBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    DriveShareBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DriveShareBody.json
          format: uri
          readOnly: true
          type: string
        createdAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
        createdByUserId:
          examples:
            - 1
          format: int64
          type: integer
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourcePublicId:
          description: 対象 resource の public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        resourceType:
          description: "`file` や `folder` など、対象 resource の種類です。"
          examples:
            - file
          type: string
        role:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - viewer
          type: string
        status:
          description: 現在の lifecycle status です。
          examples:
            - active
          type: string
        subjectPublicId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        subjectType:
          examples:
            - user
          type: string
        updatedAt:
          description: RFC3339 UTC の timestamp です。
          examples:
            - "2026-01-15T09:30:00Z"
          format: date-time
          type: string
      required:
        - publicId
        - resourceType
        - resourcePublicId
        - subjectType
        - subjectPublicId
        - role
        - status
        - createdByUserId
        - createdAt
        - updatedAt
      type: object
    ErrorDetail:
      additionalProperties: false
      properties:
        location:
          description: Where the error occurred, e.g. 'body.items[3].tags' or 'path.thing-id'
          examples:
            - example
          type: string
        message:
          description: Error message text
          examples:
            - example
          type: string
        value:
          description: The value at the given location
          examples:
            - example
      type: object
    ErrorModel:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ErrorModel.json
          format: uri
          readOnly: true
          type: string
        detail:
          description: A human-readable explanation specific to this occurrence of the problem.
          examples:
            - Property foo is required but is missing.
          type: string
        errors:
          description: Optional list of individual error details
          examples:
            - - example
          items:
            $ref: "#/components/schemas/ErrorDetail"
          type:
            - array
            - "null"
        instance:
          description: A URI reference that identifies the specific occurrence of the problem.
          examples:
            - https://example.com/error-log/abc123
          format: uri
          type: string
        status:
          description: HTTP status code
          examples:
            - 400
          format: int64
          type: integer
        title:
          description: A short, human-readable summary of the problem type. This value should not change between occurrences of the error.
          examples:
            - Bad Request
          type: string
        type:
          default: about:blank
          description: A URI reference to human-readable documentation for the error.
          examples:
            - https://example.com/errors/example
          format: uri
          type: string
      type: object
    ExternalMeBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ExternalMeBody.json
          format: uri
          readOnly: true
          type: string
        activeTenant:
          $ref: "#/components/schemas/TenantBody"
          examples:
            - example
        authorizedParty:
          examples:
            - "312345678901234568"
          type: string
        defaultTenant:
          $ref: "#/components/schemas/TenantBody"
          examples:
            - example
        groups:
          examples:
            - - external_api_user
          items:
            type: string
          type:
            - array
            - "null"
        provider:
          examples:
            - zitadel
          type: string
        roles:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - - todo_user
          items:
            type: string
          type:
            - array
            - "null"
        scopes:
          examples:
            - - external:read
          items:
            type: string
          type:
            - array
            - "null"
        subject:
          examples:
            - "312345678901234567"
          type: string
        tenants:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/TenantBody"
          type:
            - array
            - "null"
        user:
          $ref: "#/components/schemas/UserResponse"
          examples:
            - example
      required:
        - provider
        - subject
      type: object
    M2MSelfBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/M2MSelfBody.json
          format: uri
          readOnly: true
          type: string
        active:
          examples:
            - true
          type: boolean
        allowedScopes:
          examples:
            - - m2m:read
          items:
            type: string
          type:
            - array
            - "null"
        defaultTenant:
          $ref: "#/components/schemas/TenantBody"
          examples:
            - example
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - worker
          type: string
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 1
          format: int64
          type: integer
      required:
        - id
        - displayName
        - active
      type: object
    SCIMGroupBody:
      additionalProperties: false
      properties:
        value:
          examples:
            - example
          type: string
      required:
        - value
      type: object
    SCIMListResponseBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SCIMListResponseBody.json
          format: uri
          readOnly: true
          type: string
        Resources:
          examples:
            - - support
          items:
            $ref: "#/components/schemas/SCIMUserBody"
          type:
            - array
            - "null"
        itemsPerPage:
          examples:
            - 1
          format: int64
          type: integer
        schemas:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        startIndex:
          examples:
            - 1
          format: int32
          type: integer
        totalResults:
          examples:
            - 1
          format: int64
          type: integer
      required:
        - schemas
        - totalResults
        - startIndex
        - itemsPerPage
        - Resources
      type: object
    SCIMPatchInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SCIMPatchInputBody.json
          format: uri
          readOnly: true
          type: string
        Operations:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/SCIMPatchOperation"
          type:
            - array
            - "null"
        schemas:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - Operations
      type: object
    SCIMPatchOperation:
      additionalProperties: false
      properties:
        op:
          examples:
            - example
          type: string
        path:
          examples:
            - example
          type: string
        value:
          examples:
            - example
      required:
        - op
      type: object
    SCIMUserBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/SCIMUserBody.json
          format: uri
          readOnly: true
          type: string
        active:
          examples:
            - true
          type: boolean
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Acme
          type: string
        externalId:
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          type: string
        groups:
          examples:
            - - example
          items:
            $ref: "#/components/schemas/SCIMGroupBody"
          type:
            - array
            - "null"
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
        schemas:
          examples:
            - - example
          items:
            type: string
          type:
            - array
            - "null"
        userName:
          examples:
            - demo@example.com
          format: email
          type: string
      type: object
    TenantBody:
      additionalProperties: false
      properties:
        default:
          examples:
            - true
          type: boolean
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - acme
          type: string
        id:
          description: 内部 ID または protocol 上の識別子です。
          examples:
            - 1
          format: int64
          type: integer
        roles:
          description: resource に対して付与する role または tenant role code です。
          examples:
            - - todo_user
          items:
            type: string
          type:
            - array
            - "null"
        selected:
          examples:
            - true
          type: boolean
        slug:
          examples:
            - acme
          type: string
      required:
        - id
        - slug
        - displayName
        - default
        - selected
      type: object
    UserResponse:
      additionalProperties: false
      properties:
        displayName:
          description: 画面表示や検索で使う名前です。
          examples:
            - Demo User
          type: string
        email:
          description: user または invitee の email address です。
          examples:
            - demo@example.com
          format: email
          type: string
        publicId:
          description: client や URL path で参照する public UUID です。
          examples:
            - 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          format: uuid
          type: string
      required:
        - publicId
        - email
        - displayName
      type: object
  securitySchemes:
    bearerAuth:
      bearerFormat: JWT
      scheme: bearer
      type: http
    m2mBearerAuth:
      bearerFormat: JWT
      scheme: bearer
      type: http
info:
  description: |-
    HaoHao API は browser session API、tenant 管理 API、Drive API、external integration API をまとめた OpenAPI document です。

    ## 認証

    - browser API は `SESSION_ID` Cookie を使います。状態を変更する request では `X-CSRF-Token` も送ってください。
    - external / M2M / SCIM API は Bearer token を使います。必要な audience、scope、role は環境設定で制御されます。
    - tenant-scoped browser API は active tenant を前提にします。必要に応じて tenant select API で active tenant を切り替えます。

    ## Request の基本

    - UUID は OpenAPI 上では `format: uuid` として表します。
    - timestamp は RFC3339 UTC の `date-time` です。
    - list API の `limit` / `offset` は pagination に使います。
    - `Idempotency-Key` がある mutation API は、同じ key と同じ body の retry に対して保存済み response を返します。

    ## Error response

    error は Problem Details 形式です。validation error、認証エラー、認可エラー、not found、conflict、rate limit、server error は `application/problem+json` の `default` response として表します。
  title: HaoHao API
  version: 0.1.0
openapi: 3.1.0
paths:
  /api/external/v1/drive/files/{fileId}:
    delete:
      description: |-
        external bearer Drive file を削除する。

        browser Cookie に依存しない external client、M2M、SCIM integration 用 endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
      operationId: deleteExternalDriveFile
      parameters:
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: header
          name: X-Tenant-ID
          required: true
          schema:
            type: string
        - description: path 内の `fileId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: fileId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: external bearer Drive file を削除する
      tags:
        - External APIs
  /api/external/v1/drive/files/{fileId}/metadata:
    get:
      description: |-
        external bearer Drive file metadata を返す。

        browser Cookie に依存しない external client、M2M、SCIM integration 用 endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
      operationId: getExternalDriveFileMetadata
      parameters:
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: header
          name: X-Tenant-ID
          required: true
          schema:
            type: string
        - description: path 内の `fileId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: fileId
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    byteSize: 1048576
                    contentType: text/markdown
                    createdAt: "2026-01-15T09:30:00Z"
                    deletedAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    dlpBlocked: false
                    inheritanceEnabled: true
                    lockReason: example
                    locked: true
                    lockedAt: "2026-01-15T09:30:00Z"
                    originalFilename: spec.md
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    scanStatus: active
                    sha256Hex: 3a6eb0790f39ac87c94f3856b2dd2c5d110e6811602261a9a923d3bb23adc8b7
                    status: active
                    updatedAt: "2026-01-15T09:30:00Z"
                    workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveFileBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: external bearer Drive file metadata を返す
      tags:
        - External APIs
  /api/external/v1/drive/files/{fileId}/shares:
    post:
      description: |-
        external bearer Drive file share を作成する。

        browser Cookie に依存しない external client、M2M、SCIM integration 用 endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createExternalDriveFileShare
      parameters:
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: header
          name: X-Tenant-ID
          required: true
          schema:
            type: string
        - description: path 内の `fileId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: fileId
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  role: owner
                  subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  subjectType: user
            schema:
              $ref: "#/components/schemas/CreateDriveShareBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    createdByUserId: 1
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourcePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    resourceType: file
                    role: viewer
                    status: active
                    subjectPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    subjectType: user
                    updatedAt: "2026-01-15T09:30:00Z"
              schema:
                $ref: "#/components/schemas/DriveShareBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: external bearer Drive file share を作成する
      tags:
        - External APIs
  /api/external/v1/drive/folders:
    post:
      description: |-
        external bearer Drive folder を作成する。

        browser Cookie に依存しない external client、M2M、SCIM integration 用 endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: createExternalDriveFolder
      parameters:
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: header
          name: X-Tenant-ID
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  name: Acme
                  parentFolderPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
            schema:
              $ref: "#/components/schemas/CreateDriveFolderBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    createdAt: "2026-01-15T09:30:00Z"
                    deletedAt: "2026-01-15T09:30:00Z"
                    description: Quarterly planning documents.
                    inheritanceEnabled: true
                    name: Project
                    publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    updatedAt: "2026-01-15T09:30:00Z"
                    workspacePublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/DriveFolderBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: external bearer Drive folder を作成する
      tags:
        - External APIs
  /api/external/v1/drive/folders/{folderId}/children:
    get:
      description: |-
        external bearer Drive folder children を返す。

        browser Cookie に依存しない external client、M2M、SCIM integration 用 endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
      operationId: listExternalDriveFolderChildren
      parameters:
        - description: "tenant を識別する slug です。例: `acme`。"
          example: acme
          in: header
          name: X-Tenant-ID
          required: true
          schema:
            type: string
        - description: path 内の `folderId` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: folderId
          required: true
          schema:
            type: string
        - description: 返却件数の上限です。大量の結果は pagination で分割してください。
          example: 50
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    items:
                      - file: {}
                        folder: {}
                        ownedByMe: true
                        ownerDisplayName: Acme
                        ownerUserPublicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        shareRole: viewer
                        sharedWithMe: true
                        source: upload
                        starredByMe: true
                        tags:
                          - example
                        type: file
              schema:
                $ref: "#/components/schemas/DriveItemListOutputBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: external bearer Drive folder children を返す
      tags:
        - External APIs
  /api/external/v1/me:
    get:
      description: |-
        Bearer token で認証された external principal と tenant-aware context を返します。外部連携クライアントの認証確認に使います。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
      operationId: getExternalMe
      parameters:
        - description: tenant slug for tenant-aware bearer context
          example: acme
          in: header
          name: X-Tenant-ID
          schema:
            description: tenant slug for tenant-aware bearer context
            examples:
              - acme
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    activeTenant:
                      default: true
                      displayName: acme
                      id: 1
                      roles:
                        - todo_user
                      selected: true
                      slug: acme
                    authorizedParty: "312345678901234568"
                    defaultTenant:
                      default: true
                      displayName: acme
                      id: 1
                      roles:
                        - todo_user
                      selected: true
                      slug: acme
                    groups:
                      - external_api_user
                    provider: zitadel
                    roles:
                      - todo_user
                    scopes:
                      - external:read
                    subject: "312345678901234567"
                    tenants:
                      - default: true
                        displayName: acme
                        id: 1
                        roles:
                          - todo_user
                        selected: true
                        slug: acme
                    user:
                      displayName: Demo User
                      email: demo@example.com
                      publicId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
              schema:
                $ref: "#/components/schemas/ExternalMeBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: 現在の external bearer principal を返す
      tags:
        - External APIs
  /api/m2m/v1/self:
    get:
      description: |-
        現在の M2M machine client を返す。

        browser Cookie に依存しない external client、M2M、SCIM integration 用 endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
      operationId: getM2MSelf
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    active: true
                    allowedScopes:
                      - m2m:read
                    defaultTenant:
                      default: true
                      displayName: acme
                      id: 1
                      roles:
                        - todo_user
                      selected: true
                      slug: acme
                    displayName: worker
                    id: 1
              schema:
                $ref: "#/components/schemas/M2MSelfBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - m2mBearerAuth: []
      summary: 現在の M2M machine client を返す
      tags:
        - External APIs
  /api/scim/v2/Users:
    get:
      description: |-
        SCIM user を list する。

        browser Cookie に依存しない external client、M2M、SCIM integration 用 endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
      operationId: scimListUsers
      parameters:
        - description: 結果の絞り込みや pagination に使う query parameter `filter` です。
          example: example
          explode: false
          in: query
          name: filter
          schema:
            type: string
        - description: 結果の絞り込みや pagination に使う query parameter `startIndex` です。
          example: 1
          explode: false
          in: query
          name: startIndex
          schema:
            default: 1
            format: int32
            minimum: 1
            type: integer
        - description: 結果の絞り込みや pagination に使う query parameter `count` です。
          example: 3
          explode: false
          in: query
          name: count
          schema:
            default: 100
            format: int32
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    Resources:
                      - $schema: https://example.com/schemas/SCIMUserBody.json
                        active: true
                        displayName: Acme
                        externalId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        groups:
                          - example
                        id: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                        schemas:
                          - example
                        userName: demo@example.com
                    itemsPerPage: 1
                    schemas:
                      - example
                    startIndex: 1
                    totalResults: 1
              schema:
                $ref: "#/components/schemas/SCIMListResponseBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: SCIM user を list する
      tags:
        - External APIs
    post:
      description: |-
        SCIM user を作成または upsert する。

        browser Cookie に依存しない external client、M2M、SCIM integration 用 endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: scimCreateUser
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  active: true
                  displayName: Acme
                  externalId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  groups:
                    - value: example
                  schemas:
                    - example
                  userName: demo@example.com
            schema:
              $ref: "#/components/schemas/SCIMUserBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    active: true
                    displayName: Acme
                    externalId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    groups:
                      - value: example
                    id: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    schemas:
                      - example
                    userName: demo@example.com
              schema:
                $ref: "#/components/schemas/SCIMUserBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: SCIM user を作成または upsert する
      tags:
        - External APIs
  /api/scim/v2/Users/{id}:
    delete:
      description: |-
        SCIM user を deactivate する。

        browser Cookie に依存しない external client、M2M、SCIM integration 用 endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
      operationId: scimDeleteUser
      parameters:
        - description: path 内の `id` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: 操作に成功しました。response body はありません。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: SCIM user を deactivate する
      tags:
        - External APIs
    get:
      description: |-
        SCIM user を取得する。

        browser Cookie に依存しない external client、M2M、SCIM integration 用 endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
      operationId: scimGetUser
      parameters:
        - description: path 内の `id` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/SCIMUserBody.json
                    active: true
                    displayName: Acme
                    externalId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    groups:
                      - example
                    id: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    schemas:
                      - example
                    userName: demo@example.com
              schema:
                $ref: "#/components/schemas/SCIMUserBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: SCIM user を取得する
      tags:
        - External APIs
    patch:
      description: |-
        SCIM user を patch する。

        browser Cookie に依存しない external client、M2M、SCIM integration 用 endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: scimPatchUser
      parameters:
        - description: path 内の `id` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  Operations:
                    - op: example
                      path: example
                      value: example
                  schemas:
                    - example
            schema:
              $ref: "#/components/schemas/SCIMPatchInputBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/SCIMUserBody.json
                    active: true
                    displayName: Acme
                    externalId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    groups:
                      - example
                    id: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    schemas:
                      - example
                    userName: demo@example.com
              schema:
                $ref: "#/components/schemas/SCIMUserBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: SCIM user を patch する
      tags:
        - External APIs
    put:
      description: |-
        SCIM user を置換する。

        browser Cookie に依存しない external client、M2M、SCIM integration 用 endpoint です。

        ### 使い方

        - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
        - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
      operationId: scimReplaceUser
      parameters:
        - description: path 内の `id` を指定します。
          example: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            examples:
              default:
                description: ローカル demo tenant を想定した request body 例です。
                summary: Example request
                value:
                  active: true
                  displayName: Acme
                  externalId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                  groups:
                    - example
                  schemas:
                    - example
                  userName: demo@example.com
            schema:
              $ref: "#/components/schemas/SCIMUserBody"
        description: request body には操作に必要な field を JSON で指定します。必須 field、enum、文字数制限は schema を参照してください。
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                default:
                  description: 成功時に返る response body の例です。
                  summary: Example response
                  value:
                    $schema: https://example.com/schemas/SCIMUserBody.json
                    active: true
                    displayName: Acme
                    externalId: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    groups:
                      - example
                    id: 018f2f05-c6c9-7a49-b32d-04f4dd84ef4a
                    schemas:
                      - example
                    userName: demo@example.com
              schema:
                $ref: "#/components/schemas/SCIMUserBody"
          description: 操作に成功し、response body に結果を返します。
        default:
          content:
            application/problem+json:
              examples:
                validation:
                  description: request body、path、query、header の validation に失敗した場合の例です。
                  summary: Validation error
                  value:
                    detail: Request validation failed.
                    errors:
                      - location: body.name
                        message: expected value to be at least 1 characters
                        value: ""
                    status: 422
                    title: Unprocessable Entity
                    type: https://example.com/problems/validation-error
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Problem Details 形式の error response です。validation、authentication、authorization、conflict、rate limit、server error などで返ります。
      security:
        - bearerAuth: []
      summary: SCIM user を置換する
      tags:
        - External APIs
tags:
  - description: Bearer, M2M, SCIM, and external Drive integration APIs.
    name: External APIs
