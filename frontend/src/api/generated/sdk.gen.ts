// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { AcceptDriveOfficeWebhookData, AcceptDriveOfficeWebhookErrors, AcceptDriveOfficeWebhookResponses, AcceptDriveShareInvitationData, AcceptDriveShareInvitationErrors, AcceptDriveShareInvitationResponses, AcceptTenantInvitationData, AcceptTenantInvitationErrors, AcceptTenantInvitationResponses, AddDriveCleanRoomParticipantData, AddDriveCleanRoomParticipantErrors, AddDriveCleanRoomParticipantResponses, AddDriveGroupMemberData, AddDriveGroupMemberErrors, AddDriveGroupMemberResponses, AddDriveLegalCaseFileData, AddDriveLegalCaseFileErrors, AddDriveLegalCaseFileResponses, AddTenantAdminDataAccessGroupMemberData, AddTenantAdminDataAccessGroupMemberErrors, AddTenantAdminDataAccessGroupMemberResponses, ApproveDriveEDiscoveryExportData, ApproveDriveEDiscoveryExportErrors, ApproveDriveEDiscoveryExportResponses, ApproveDriveMarketplaceInstallationData, ApproveDriveMarketplaceInstallationErrors, ApproveDriveMarketplaceInstallationResponses, ApproveTenantAdminDriveShareApprovalData, ApproveTenantAdminDriveShareApprovalErrors, ApproveTenantAdminDriveShareApprovalResponses, ArchiveDatasetGoldPublicationData, ArchiveDatasetGoldPublicationErrors, ArchiveDatasetGoldPublicationResponses, BindDriveGatewayFileData, BindDriveGatewayFileErrors, BindDriveGatewayFileResponses, BindDriveHsmKeyData, BindDriveHsmKeyErrors, BindDriveHsmKeyResponses, CheckDriveMarketplaceScopeData, CheckDriveMarketplaceScopeErrors, CheckDriveMarketplaceScopeResponses, CheckTenantAdminDriveOperationsDriftData, CheckTenantAdminDriveOperationsDriftErrors, CheckTenantAdminDriveOperationsDriftResponses, ConnectIntegrationData, ConnectIntegrationErrors, ConnectIntegrationResponses, CopyDriveFileData, CopyDriveFileErrors, CopyDriveFileResponses, CopyDriveFolderData, CopyDriveFolderErrors, CopyDriveFolderResponses, CreateCustomerSignalData, CreateCustomerSignalErrors, CreateCustomerSignalImportData, CreateCustomerSignalImportErrors, CreateCustomerSignalImportResponses, CreateCustomerSignalResponses, CreateCustomerSignalSavedFilterData, CreateCustomerSignalSavedFilterErrors, CreateCustomerSignalSavedFilterResponses, CreateDataPipelineData, CreateDataPipelineErrors, CreateDataPipelineResponses, CreateDataPipelineRunData, CreateDataPipelineRunErrors, CreateDataPipelineRunResponses, CreateDataPipelineScheduleData, CreateDataPipelineScheduleErrors, CreateDataPipelineScheduleResponses, CreateDatasetData, CreateDatasetErrors, CreateDatasetGoldPublicationData, CreateDatasetGoldPublicationErrors, CreateDatasetGoldPublicationResponses, CreateDatasetLineageChangeSetData, CreateDatasetLineageChangeSetErrors, CreateDatasetLineageChangeSetResponses, CreateDatasetQueryJobData, CreateDatasetQueryJobErrors, CreateDatasetQueryJobResponses, CreateDatasetResponses, CreateDatasetScopedQueryJobData, CreateDatasetScopedQueryJobErrors, CreateDatasetScopedQueryJobResponses, CreateDatasetSyncJobData, CreateDatasetSyncJobErrors, CreateDatasetSyncJobResponses, CreateDatasetWorkTableExportData, CreateDatasetWorkTableExportErrors, CreateDatasetWorkTableExportResponses, CreateDatasetWorkTableExportScheduleData, CreateDatasetWorkTableExportScheduleErrors, CreateDatasetWorkTableExportScheduleResponses, CreateDriveAiJobData, CreateDriveAiJobErrors, CreateDriveAiJobResponses, CreateDriveCleanRoomData, CreateDriveCleanRoomErrors, CreateDriveCleanRoomResponses, CreateDriveE2EeEnvelopeData, CreateDriveE2EeEnvelopeErrors, CreateDriveE2EeEnvelopeResponses, CreateDriveE2EeFileKeyData, CreateDriveE2EeFileKeyErrors, CreateDriveE2EeFileKeyResponses, CreateDriveE2EeUserKeyData, CreateDriveE2EeUserKeyErrors, CreateDriveE2EeUserKeyResponses, CreateDriveEDiscoveryConnectionData, CreateDriveEDiscoveryConnectionErrors, CreateDriveEDiscoveryConnectionResponses, CreateDriveFileShareData, CreateDriveFileShareErrors, CreateDriveFileShareInvitationData, CreateDriveFileShareInvitationErrors, CreateDriveFileShareInvitationResponses, CreateDriveFileShareLinkData, CreateDriveFileShareLinkErrors, CreateDriveFileShareLinkResponses, CreateDriveFileShareResponses, CreateDriveFolderData, CreateDriveFolderErrors, CreateDriveFolderResponses, CreateDriveFolderShareData, CreateDriveFolderShareErrors, CreateDriveFolderShareInvitationData, CreateDriveFolderShareInvitationErrors, CreateDriveFolderShareInvitationResponses, CreateDriveFolderShareLinkData, CreateDriveFolderShareLinkErrors, CreateDriveFolderShareLinkResponses, CreateDriveFolderShareResponses, CreateDriveGroupData, CreateDriveGroupErrors, CreateDriveGroupResponses, CreateDriveHsmDeploymentData, CreateDriveHsmDeploymentErrors, CreateDriveHsmDeploymentResponses, CreateDriveLegalCaseData, CreateDriveLegalCaseErrors, CreateDriveLegalCaseResponses, CreateDriveLegalExportData, CreateDriveLegalExportErrors, CreateDriveLegalExportResponses, CreateDriveOcrJobData, CreateDriveOcrJobErrors, CreateDriveOcrJobResponses, CreateDriveOfficeSessionData, CreateDriveOfficeSessionErrors, CreateDriveOfficeSessionResponses, CreateDriveProductExtractionJobData, CreateDriveProductExtractionJobErrors, CreateDriveProductExtractionJobResponses, CreateDriveWorkspaceData, CreateDriveWorkspaceErrors, CreateDriveWorkspaceResponses, CreateMachineClientData, CreateMachineClientErrors, CreateMachineClientResponses, CreateTenantAdminDataAccessGroupData, CreateTenantAdminDataAccessGroupErrors, CreateTenantAdminDataAccessGroupResponses, CreateTenantAdminDriveLocalSearchRebuildData, CreateTenantAdminDriveLocalSearchRebuildErrors, CreateTenantAdminDriveLocalSearchRebuildResponses, CreateTenantAdminTenantData, CreateTenantAdminTenantErrors, CreateTenantAdminTenantResponses, CreateTenantDataExportData, CreateTenantDataExportErrors, CreateTenantDataExportResponses, CreateTenantInvitationData, CreateTenantInvitationErrors, CreateTenantInvitationResponses, CreateTodoData, CreateTodoErrors, CreateTodoResponses, CreateWebhookData, CreateWebhookErrors, CreateWebhookResponses, DeactivateTenantAdminTenantData, DeactivateTenantAdminTenantErrors, DeactivateTenantAdminTenantResponses, DeleteCustomerSignalData, DeleteCustomerSignalErrors, DeleteCustomerSignalResponses, DeleteCustomerSignalSavedFilterData, DeleteCustomerSignalSavedFilterErrors, DeleteCustomerSignalSavedFilterResponses, DeleteDatasetData, DeleteDatasetErrors, DeleteDatasetResponses, DeleteDatasetWorkTableData, DeleteDatasetWorkTableErrors, DeleteDatasetWorkTableResponses, DeleteDriveFileData, DeleteDriveFileErrors, DeleteDriveFileResponses, DeleteDriveFileShareData, DeleteDriveFileShareErrors, DeleteDriveFileShareResponses, DeleteDriveFolderData, DeleteDriveFolderErrors, DeleteDriveFolderResponses, DeleteDriveFolderShareData, DeleteDriveFolderShareErrors, DeleteDriveFolderShareResponses, DeleteDriveGroupData, DeleteDriveGroupErrors, DeleteDriveGroupMemberData, DeleteDriveGroupMemberErrors, DeleteDriveGroupMemberResponses, DeleteDriveGroupResponses, DeleteDriveShareLinkData, DeleteDriveShareLinkErrors, DeleteDriveShareLinkResponses, DeleteDriveWorkspaceData, DeleteDriveWorkspaceErrors, DeleteDriveWorkspaceResponses, DeleteFileData, DeleteFileErrors, DeleteFileResponses, DeleteIntegrationGrantData, DeleteIntegrationGrantErrors, DeleteIntegrationGrantResponses, DeleteMachineClientData, DeleteMachineClientErrors, DeleteMachineClientResponses, DeleteTodoData, DeleteTodoErrors, DeleteTodoResponses, DeleteWebhookData, DeleteWebhookErrors, DeleteWebhookResponses, DisableDataPipelineScheduleData, DisableDataPipelineScheduleErrors, DisableDataPipelineScheduleResponses, DisableDatasetWorkTableExportScheduleData, DisableDatasetWorkTableExportScheduleErrors, DisableDatasetWorkTableExportScheduleResponses, DownloadDatasetWorkTableExportData, DownloadDatasetWorkTableExportErrors, DownloadDatasetWorkTableExportResponses, DownloadDriveArchiveData, DownloadDriveArchiveErrors, DownloadDriveArchiveResponses, DownloadTenantDataExportData, DownloadTenantDataExportErrors, DownloadTenantDataExportResponses, EndDriveEditSessionData, EndDriveEditSessionErrors, EndDriveEditSessionResponses, EndSupportAccessData, EndSupportAccessErrors, EndSupportAccessResponses, EndTenantAdminDriveContentAccessSessionData, EndTenantAdminDriveContentAccessSessionErrors, EndTenantAdminDriveContentAccessSessionResponses, FinishIntegrationConnectData, FinishIntegrationConnectErrors, FinishIntegrationConnectResponses, FinishOidcLoginData, FinishOidcLoginErrors, FinishOidcLoginResponses, GetAuthSettingsData, GetAuthSettingsErrors, GetAuthSettingsResponses, GetCsrfData, GetCsrfErrors, GetCsrfResponses, GetCurrentSupportAccessData, GetCurrentSupportAccessErrors, GetCurrentSupportAccessResponses, GetCustomerSignalData, GetCustomerSignalErrors, GetCustomerSignalImportData, GetCustomerSignalImportErrors, GetCustomerSignalImportResponses, GetCustomerSignalResponses, GetDataPipelineData, GetDataPipelineErrors, GetDataPipelineResponses, GetDatasetData, GetDatasetErrors, GetDatasetGoldPublicationData, GetDatasetGoldPublicationErrors, GetDatasetGoldPublicationResponses, GetDatasetLineageChangeSetData, GetDatasetLineageChangeSetErrors, GetDatasetLineageChangeSetResponses, GetDatasetLineageData, GetDatasetLineageErrors, GetDatasetLineageResponses, GetDatasetQueryJobData, GetDatasetQueryJobErrors, GetDatasetQueryJobLineageData, GetDatasetQueryJobLineageErrors, GetDatasetQueryJobLineageResponses, GetDatasetQueryJobResponses, GetDatasetResponses, GetDatasetWorkTableData, GetDatasetWorkTableErrors, GetDatasetWorkTableExportData, GetDatasetWorkTableExportErrors, GetDatasetWorkTableExportResponses, GetDatasetWorkTableLineageData, GetDatasetWorkTableLineageErrors, GetDatasetWorkTableLineageResponses, GetDatasetWorkTablePreviewData, GetDatasetWorkTablePreviewErrors, GetDatasetWorkTablePreviewResponses, GetDatasetWorkTableResponses, GetDriveAiSummaryData, GetDriveAiSummaryErrors, GetDriveAiSummaryResponses, GetDriveE2EeEnvelopeData, GetDriveE2EeEnvelopeErrors, GetDriveE2EeEnvelopeResponses, GetDriveFileData, GetDriveFileErrors, GetDriveFileManifestData, GetDriveFileManifestErrors, GetDriveFileManifestResponses, GetDriveFilePermissionsData, GetDriveFilePermissionsErrors, GetDriveFilePermissionsResponses, GetDriveFileResponses, GetDriveFolderData, GetDriveFolderErrors, GetDriveFolderPermissionsData, GetDriveFolderPermissionsErrors, GetDriveFolderPermissionsResponses, GetDriveFolderResponses, GetDriveFolderTreeData, GetDriveFolderTreeErrors, GetDriveFolderTreeResponses, GetDriveGroupData, GetDriveGroupErrors, GetDriveGroupResponses, GetDriveOcrData, GetDriveOcrErrors, GetDriveOcrResponses, GetDriveStorageUsageData, GetDriveStorageUsageErrors, GetDriveStorageUsageResponses, GetDriveSyncDeltaData, GetDriveSyncDeltaErrors, GetDriveSyncDeltaResponses, GetMachineClientData, GetMachineClientErrors, GetMachineClientResponses, GetManagedDatasetWorkTableData, GetManagedDatasetWorkTableErrors, GetManagedDatasetWorkTablePreviewData, GetManagedDatasetWorkTablePreviewErrors, GetManagedDatasetWorkTablePreviewResponses, GetManagedDatasetWorkTableResponses, GetMedallionAssetData, GetMedallionAssetErrors, GetMedallionAssetResponses, GetMedallionResourceCatalogData, GetMedallionResourceCatalogErrors, GetMedallionResourceCatalogResponses, GetPublicDriveShareLinkData, GetPublicDriveShareLinkErrors, GetPublicDriveShareLinkResponses, GetSessionData, GetSessionErrors, GetSessionResponses, GetSystemJobData, GetSystemJobErrors, GetSystemJobResponses, GetTenantAdminDataAccessGroupData, GetTenantAdminDataAccessGroupErrors, GetTenantAdminDataAccessGroupResponses, GetTenantAdminDriveEncryptionPolicyData, GetTenantAdminDriveEncryptionPolicyErrors, GetTenantAdminDriveEncryptionPolicyResponses, GetTenantAdminDriveFileMetadataData, GetTenantAdminDriveFileMetadataErrors, GetTenantAdminDriveFileMetadataResponses, GetTenantAdminDriveOcrStatusData, GetTenantAdminDriveOcrStatusErrors, GetTenantAdminDriveOcrStatusResponses, GetTenantAdminDriveOpenFgaDriftData, GetTenantAdminDriveOpenFgaDriftErrors, GetTenantAdminDriveOpenFgaDriftResponses, GetTenantAdminDriveOperationsHealthData, GetTenantAdminDriveOperationsHealthErrors, GetTenantAdminDriveOperationsHealthResponses, GetTenantAdminDriveResidencyPolicyData, GetTenantAdminDriveResidencyPolicyErrors, GetTenantAdminDriveResidencyPolicyResponses, GetTenantAdminTenantData, GetTenantAdminTenantErrors, GetTenantAdminTenantResponses, GetTenantDataExportData, GetTenantDataExportErrors, GetTenantDataExportResponses, GetTenantSettingsData, GetTenantSettingsErrors, GetTenantSettingsResponses, GetWebhookData, GetWebhookErrors, GetWebhookResponses, GrantTenantAdminRoleData, GrantTenantAdminRoleErrors, GrantTenantAdminRoleResponses, HeartbeatDriveEditSessionData, HeartbeatDriveEditSessionErrors, HeartbeatDriveEditSessionResponses, InstallDriveMarketplaceAppData, InstallDriveMarketplaceAppErrors, InstallDriveMarketplaceAppResponses, LinkDatasetWorkTableData, LinkDatasetWorkTableErrors, LinkDatasetWorkTableResponses, ListCustomerSignalImportsData, ListCustomerSignalImportsErrors, ListCustomerSignalImportsResponses, ListCustomerSignalSavedFiltersData, ListCustomerSignalSavedFiltersErrors, ListCustomerSignalSavedFiltersResponses, ListCustomerSignalsData, ListCustomerSignalsErrors, ListCustomerSignalsResponses, ListDataPipelineRunsData, ListDataPipelineRunsErrors, ListDataPipelineRunsResponses, ListDataPipelineSchedulesData, ListDataPipelineSchedulesErrors, ListDataPipelineSchedulesResponses, ListDataPipelinesData, ListDataPipelinesErrors, ListDataPipelinesResponses, ListDatasetGoldPublicationsData, ListDatasetGoldPublicationsErrors, ListDatasetGoldPublicationsResponses, ListDatasetGoldPublishRunsData, ListDatasetGoldPublishRunsErrors, ListDatasetGoldPublishRunsResponses, ListDatasetLineageChangeSetsData, ListDatasetLineageChangeSetsErrors, ListDatasetLineageChangeSetsResponses, ListDatasetQueryJobLineageParseRunsData, ListDatasetQueryJobLineageParseRunsErrors, ListDatasetQueryJobLineageParseRunsResponses, ListDatasetQueryJobsData, ListDatasetQueryJobsErrors, ListDatasetQueryJobsResponses, ListDatasetRowsData, ListDatasetRowsErrors, ListDatasetRowsResponses, ListDatasetScopedQueryJobsData, ListDatasetScopedQueryJobsErrors, ListDatasetScopedQueryJobsResponses, ListDatasetScopedWorkTablesData, ListDatasetScopedWorkTablesErrors, ListDatasetScopedWorkTablesResponses, ListDatasetsData, ListDatasetsErrors, ListDatasetSourceFilesData, ListDatasetSourceFilesErrors, ListDatasetSourceFilesResponses, ListDatasetsResponses, ListDatasetSyncJobsData, ListDatasetSyncJobsErrors, ListDatasetSyncJobsResponses, ListDatasetWorkTableExportSchedulesData, ListDatasetWorkTableExportSchedulesErrors, ListDatasetWorkTableExportSchedulesResponses, ListDatasetWorkTableExportsData, ListDatasetWorkTableExportsErrors, ListDatasetWorkTableExportsResponses, ListDatasetWorkTablesData, ListDatasetWorkTablesErrors, ListDatasetWorkTablesResponses, ListDriveAiClassificationsData, ListDriveAiClassificationsErrors, ListDriveAiClassificationsResponses, ListDriveFileActivityData, ListDriveFileActivityErrors, ListDriveFileActivityResponses, ListDriveFolderActivityData, ListDriveFolderActivityErrors, ListDriveFolderActivityResponses, ListDriveFolderChildrenData, ListDriveFolderChildrenErrors, ListDriveFolderChildrenResponses, ListDriveGroupsData, ListDriveGroupsErrors, ListDriveGroupsResponses, ListDriveItemsData, ListDriveItemsErrors, ListDriveItemsResponses, ListDriveMarketplaceAppsData, ListDriveMarketplaceAppsErrors, ListDriveMarketplaceAppsResponses, ListDriveProductExtractionsData, ListDriveProductExtractionsErrors, ListDriveProductExtractionsResponses, ListDriveRecentData, ListDriveRecentErrors, ListDriveRecentResponses, ListDriveSharedWithMeData, ListDriveSharedWithMeErrors, ListDriveSharedWithMeResponses, ListDriveShareInvitationsData, ListDriveShareInvitationsErrors, ListDriveShareInvitationsResponses, ListDriveShareTargetsData, ListDriveShareTargetsErrors, ListDriveShareTargetsResponses, ListDriveStarredData, ListDriveStarredErrors, ListDriveStarredResponses, ListDriveTrashItemsData, ListDriveTrashItemsErrors, ListDriveTrashItemsResponses, ListDriveWorkspacesData, ListDriveWorkspacesErrors, ListDriveWorkspacesResponses, ListFilesData, ListFilesErrors, ListFilesResponses, ListIntegrationsData, ListIntegrationsErrors, ListIntegrationsResponses, ListMachineClientsData, ListMachineClientsErrors, ListMachineClientsResponses, ListMedallionAssetsData, ListMedallionAssetsErrors, ListMedallionAssetsResponses, ListNotificationsData, ListNotificationsErrors, ListNotificationsResponses, ListPublicDriveShareLinkChildrenData, ListPublicDriveShareLinkChildrenErrors, ListPublicDriveShareLinkChildrenResponses, ListSystemJobsData, ListSystemJobsErrors, ListSystemJobsResponses, ListTenantAdminDataAccessGroupsData, ListTenantAdminDataAccessGroupsErrors, ListTenantAdminDataAccessGroupsResponses, ListTenantAdminDataAccessResourcePermissionsData, ListTenantAdminDataAccessResourcePermissionsErrors, ListTenantAdminDataAccessResourcePermissionsResponses, ListTenantAdminDataAccessScopePermissionsData, ListTenantAdminDataAccessScopePermissionsErrors, ListTenantAdminDataAccessScopePermissionsResponses, ListTenantAdminDriveAuditEventsData, ListTenantAdminDriveAuditEventsErrors, ListTenantAdminDriveAuditEventsResponses, ListTenantAdminDriveInvitationsData, ListTenantAdminDriveInvitationsErrors, ListTenantAdminDriveInvitationsResponses, ListTenantAdminDriveLocalSearchIndexJobsData, ListTenantAdminDriveLocalSearchIndexJobsErrors, ListTenantAdminDriveLocalSearchIndexJobsResponses, ListTenantAdminDriveShareApprovalsData, ListTenantAdminDriveShareApprovalsErrors, ListTenantAdminDriveShareApprovalsResponses, ListTenantAdminDriveShareLinksData, ListTenantAdminDriveShareLinksErrors, ListTenantAdminDriveShareLinksResponses, ListTenantAdminDriveSharesData, ListTenantAdminDriveSharesErrors, ListTenantAdminDriveSharesResponses, ListTenantAdminSchemaMappingExamplesData, ListTenantAdminSchemaMappingExamplesErrors, ListTenantAdminSchemaMappingExamplesResponses, ListTenantAdminTenantsData, ListTenantAdminTenantsErrors, ListTenantAdminTenantsResponses, ListTenantDataExportsData, ListTenantDataExportsErrors, ListTenantDataExportsResponses, ListTenantEntitlementsData, ListTenantEntitlementsErrors, ListTenantEntitlementsResponses, ListTenantInvitationsData, ListTenantInvitationsErrors, ListTenantInvitationsResponses, ListTenantsData, ListTenantsErrors, ListTenantsResponses, ListTodosData, ListTodosErrors, ListTodosResponses, ListWebhookDeliveriesData, ListWebhookDeliveriesErrors, ListWebhookDeliveriesResponses, ListWebhooksData, ListWebhooksErrors, ListWebhooksResponses, LoginData, LoginErrors, LoginResponses, LogoutData, LogoutErrors, LogoutResponses, MarkAllNotificationsReadData, MarkAllNotificationsReadErrors, MarkAllNotificationsReadResponses, MarkNotificationReadData, MarkNotificationReadErrors, MarkNotificationReadResponses, MarkNotificationsReadData, MarkNotificationsReadErrors, MarkNotificationsReadResponses, ParseDatasetQueryJobLineageData, ParseDatasetQueryJobLineageErrors, ParseDatasetQueryJobLineageResponses, PermanentlyDeleteDriveFileData, PermanentlyDeleteDriveFileErrors, PermanentlyDeleteDriveFileResponses, PermanentlyDeleteDriveFolderData, PermanentlyDeleteDriveFolderErrors, PermanentlyDeleteDriveFolderResponses, PreviewDataPipelineDraftData, PreviewDataPipelineDraftErrors, PreviewDataPipelineDraftResponses, PreviewDataPipelineVersionData, PreviewDataPipelineVersionErrors, PreviewDataPipelineVersionResponses, PreviewDatasetGoldPublicationData, PreviewDatasetGoldPublicationErrors, PreviewDatasetGoldPublicationResponses, PromoteDatasetWorkTableData, PromoteDatasetWorkTableErrors, PromoteDatasetWorkTableResponses, ProvisionTenantInvitationIdentityData, ProvisionTenantInvitationIdentityErrors, ProvisionTenantInvitationIdentityResponses, PublishDataPipelineVersionData, PublishDataPipelineVersionErrors, PublishDataPipelineVersionResponses, PublishDatasetLineageChangeSetData, PublishDatasetLineageChangeSetErrors, PublishDatasetLineageChangeSetResponses, PutTenantAdminDataAccessResourcePermissionsData, PutTenantAdminDataAccessResourcePermissionsErrors, PutTenantAdminDataAccessResourcePermissionsResponses, PutTenantAdminDataAccessScopePermissionsData, PutTenantAdminDataAccessScopePermissionsErrors, PutTenantAdminDataAccessScopePermissionsResponses, QueryDriveRagData, QueryDriveRagErrors, QueryDriveRagResponses, RebuildTenantAdminDriveSearchIndexData, RebuildTenantAdminDriveSearchIndexErrors, RebuildTenantAdminDriveSearchIndexResponses, RebuildTenantAdminSchemaMappingSearchDocumentsData, RebuildTenantAdminSchemaMappingSearchDocumentsErrors, RebuildTenantAdminSchemaMappingSearchDocumentsResponses, RecordSchemaMappingExampleData, RecordSchemaMappingExampleErrors, RecordSchemaMappingExampleResponses, RefreshDatasetGoldPublicationData, RefreshDatasetGoldPublicationErrors, RefreshDatasetGoldPublicationResponses, RefreshDriveFileManifestData, RefreshDriveFileManifestErrors, RefreshDriveFileManifestResponses, RefreshSessionData, RefreshSessionErrors, RefreshSessionResponses, RegisterDatasetWorkTableData, RegisterDatasetWorkTableErrors, RegisterDatasetWorkTableResponses, RegisterDriveGatewayData, RegisterDriveGatewayErrors, RegisterDriveGatewayResponses, RegisterDriveSyncDeviceData, RegisterDriveSyncDeviceErrors, RegisterDriveSyncDeviceResponses, RejectDatasetLineageChangeSetData, RejectDatasetLineageChangeSetErrors, RejectDatasetLineageChangeSetResponses, RejectTenantAdminDriveShareApprovalData, RejectTenantAdminDriveShareApprovalErrors, RejectTenantAdminDriveShareApprovalResponses, RemoveTenantAdminDataAccessGroupMemberData, RemoveTenantAdminDataAccessGroupMemberErrors, RemoveTenantAdminDataAccessGroupMemberResponses, RenameDatasetWorkTableData, RenameDatasetWorkTableErrors, RenameDatasetWorkTableResponses, RepairTenantAdminDriveOpenFgaSyncData, RepairTenantAdminDriveOpenFgaSyncErrors, RepairTenantAdminDriveOpenFgaSyncResponses, RepairTenantAdminDriveOperationsData, RepairTenantAdminDriveOperationsErrors, RepairTenantAdminDriveOperationsResponses, ReplayDriveMobileOfflineOperationsData, ReplayDriveMobileOfflineOperationsErrors, ReplayDriveMobileOfflineOperationsResponses, RequestDriveCleanRoomExportData, RequestDriveCleanRoomExportErrors, RequestDriveCleanRoomExportResponses, RequestDriveEDiscoveryExportData, RequestDriveEDiscoveryExportErrors, RequestDriveEDiscoveryExportResponses, ResolveTenantInvitationData, ResolveTenantInvitationErrors, ResolveTenantInvitationResponses, RestoreDriveFileData, RestoreDriveFileErrors, RestoreDriveFileResponses, RestoreDriveFolderData, RestoreDriveFolderErrors, RestoreDriveFolderResponses, RetryWebhookDeliveryData, RetryWebhookDeliveryErrors, RetryWebhookDeliveryResponses, RevokeDriveE2EeEnvelopeData, RevokeDriveE2EeEnvelopeErrors, RevokeDriveE2EeEnvelopeResponses, RevokeDriveOfficeSessionData, RevokeDriveOfficeSessionErrors, RevokeDriveOfficeSessionResponses, RevokeDriveShareInvitationData, RevokeDriveShareInvitationErrors, RevokeDriveShareInvitationResponses, RevokeDriveSyncDeviceData, RevokeDriveSyncDeviceErrors, RevokeDriveSyncDeviceResponses, RevokeTenantAdminRoleData, RevokeTenantAdminRoleErrors, RevokeTenantAdminRoleResponses, RevokeTenantInvitationData, RevokeTenantInvitationErrors, RevokeTenantInvitationResponses, RotateWebhookSecretData, RotateWebhookSecretErrors, RotateWebhookSecretResponses, SaveDataPipelineVersionData, SaveDataPipelineVersionErrors, SaveDataPipelineVersionResponses, SaveDriveEditSessionContentData, SaveDriveEditSessionContentErrors, SaveDriveEditSessionContentResponses, SchemaMappingCandidatesData, SchemaMappingCandidatesErrors, SchemaMappingCandidatesResponses, SearchDriveDocumentsData, SearchDriveDocumentsErrors, SearchDriveDocumentsResponses, SearchDriveItemsData, SearchDriveItemsErrors, SearchDriveItemsResponses, SelectTenantData, SelectTenantErrors, SelectTenantResponses, SetupTenantInvitationIdentityData, SetupTenantInvitationIdentityErrors, SetupTenantInvitationIdentityResponses, StarDriveFileData, StarDriveFileErrors, StarDriveFileResponses, StarDriveFolderData, StarDriveFolderErrors, StarDriveFolderResponses, StartDriveEditSessionData, StartDriveEditSessionErrors, StartDriveEditSessionResponses, StartOidcLoginData, StartOidcLoginErrors, StartOidcLoginResponses, StartSupportAccessData, StartSupportAccessErrors, StartSupportAccessResponses, StartTenantAdminDriveContentAccessSessionData, StartTenantAdminDriveContentAccessSessionErrors, StartTenantAdminDriveContentAccessSessionResponses, StopSystemJobData, StopSystemJobErrors, StopSystemJobResponses, SubmitDriveCleanRoomDatasetData, SubmitDriveCleanRoomDatasetErrors, SubmitDriveCleanRoomDatasetResponses, TransferDriveFileOwnerData, TransferDriveFileOwnerErrors, TransferDriveFileOwnerResponses, TransferDriveFolderOwnerData, TransferDriveFolderOwnerErrors, TransferDriveFolderOwnerResponses, TruncateDatasetWorkTableData, TruncateDatasetWorkTableErrors, TruncateDatasetWorkTableResponses, UninstallDriveMarketplaceInstallationData, UninstallDriveMarketplaceInstallationErrors, UninstallDriveMarketplaceInstallationResponses, UnpublishDatasetGoldPublicationData, UnpublishDatasetGoldPublicationErrors, UnpublishDatasetGoldPublicationResponses, UnstarDriveFileData, UnstarDriveFileErrors, UnstarDriveFileResponses, UnstarDriveFolderData, UnstarDriveFolderErrors, UnstarDriveFolderResponses, UpdateCustomerSignalData, UpdateCustomerSignalErrors, UpdateCustomerSignalResponses, UpdateCustomerSignalSavedFilterData, UpdateCustomerSignalSavedFilterErrors, UpdateCustomerSignalSavedFilterResponses, UpdateDataPipelineData, UpdateDataPipelineErrors, UpdateDataPipelineResponses, UpdateDataPipelineScheduleData, UpdateDataPipelineScheduleErrors, UpdateDataPipelineScheduleResponses, UpdateDatasetLineageChangeSetGraphData, UpdateDatasetLineageChangeSetGraphErrors, UpdateDatasetLineageChangeSetGraphResponses, UpdateDatasetWorkTableExportScheduleData, UpdateDatasetWorkTableExportScheduleErrors, UpdateDatasetWorkTableExportScheduleResponses, UpdateDriveFileData, UpdateDriveFileErrors, UpdateDriveFileInheritanceData, UpdateDriveFileInheritanceErrors, UpdateDriveFileInheritanceResponses, UpdateDriveFileResponses, UpdateDriveFileShareData, UpdateDriveFileShareErrors, UpdateDriveFileShareResponses, UpdateDriveFolderData, UpdateDriveFolderErrors, UpdateDriveFolderInheritanceData, UpdateDriveFolderInheritanceErrors, UpdateDriveFolderInheritanceResponses, UpdateDriveFolderResponses, UpdateDriveFolderShareData, UpdateDriveFolderShareErrors, UpdateDriveFolderShareResponses, UpdateDriveGatewayStatusData, UpdateDriveGatewayStatusErrors, UpdateDriveGatewayStatusResponses, UpdateDriveGroupData, UpdateDriveGroupErrors, UpdateDriveGroupResponses, UpdateDriveHsmKeyStatusData, UpdateDriveHsmKeyStatusErrors, UpdateDriveHsmKeyStatusResponses, UpdateDriveShareLinkData, UpdateDriveShareLinkErrors, UpdateDriveShareLinkResponses, UpdateDriveWorkspaceData, UpdateDriveWorkspaceErrors, UpdateDriveWorkspaceResponses, UpdateMachineClientData, UpdateMachineClientErrors, UpdateMachineClientResponses, UpdateTenantAdminDataAccessGroupData, UpdateTenantAdminDataAccessGroupErrors, UpdateTenantAdminDataAccessGroupResponses, UpdateTenantAdminDriveEncryptionPolicyData, UpdateTenantAdminDriveEncryptionPolicyErrors, UpdateTenantAdminDriveEncryptionPolicyResponses, UpdateTenantAdminDriveKmsKeyStatusData, UpdateTenantAdminDriveKmsKeyStatusErrors, UpdateTenantAdminDriveKmsKeyStatusResponses, UpdateTenantAdminDriveResidencyPolicyData, UpdateTenantAdminDriveResidencyPolicyErrors, UpdateTenantAdminDriveResidencyPolicyResponses, UpdateTenantAdminSchemaMappingExampleSharingData, UpdateTenantAdminSchemaMappingExampleSharingErrors, UpdateTenantAdminSchemaMappingExampleSharingResponses, UpdateTenantAdminTenantData, UpdateTenantAdminTenantErrors, UpdateTenantAdminTenantResponses, UpdateTenantEntitlementsData, UpdateTenantEntitlementsErrors, UpdateTenantEntitlementsResponses, UpdateTenantSettingsData, UpdateTenantSettingsErrors, UpdateTenantSettingsResponses, UpdateTodoData, UpdateTodoErrors, UpdateTodoResponses, UpdateWebhookData, UpdateWebhookErrors, UpdateWebhookResponses, VerifyIntegrationAccessData, VerifyIntegrationAccessErrors, VerifyIntegrationAccessResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Drive Office provider webhook を受け取る
 *
 * Drive Office provider webhook を受け取る。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const acceptDriveOfficeWebhook = <ThrowOnError extends boolean = false>(options: Options<AcceptDriveOfficeWebhookData, ThrowOnError>) => (options.client ?? client).post<AcceptDriveOfficeWebhookResponses, AcceptDriveOfficeWebhookErrors, ThrowOnError>({
    url: '/api/office/webhooks/{provider}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * public Drive share link metadata を返す
 *
 * public Drive share link metadata を返す。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 */
export const getPublicDriveShareLink = <ThrowOnError extends boolean = false>(options: Options<GetPublicDriveShareLinkData, ThrowOnError>) => (options.client ?? client).get<GetPublicDriveShareLinkResponses, GetPublicDriveShareLinkErrors, ThrowOnError>({ url: '/api/public/drive/share-links/{token}', ...options });

/**
 * public Drive folder share link children を返す
 *
 * public Drive folder share link children を返す。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 */
export const listPublicDriveShareLinkChildren = <ThrowOnError extends boolean = false>(options: Options<ListPublicDriveShareLinkChildrenData, ThrowOnError>) => (options.client ?? client).get<ListPublicDriveShareLinkChildrenResponses, ListPublicDriveShareLinkChildrenErrors, ThrowOnError>({ url: '/api/public/drive/share-links/{token}/children', ...options });

/**
 * tenant admin 用の tenant 一覧を返す
 *
 * tenant admin 用の tenant 一覧を返す。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantAdminTenants = <ThrowOnError extends boolean = false>(options?: Options<ListTenantAdminTenantsData, ThrowOnError>) => (options?.client ?? client).get<ListTenantAdminTenantsResponses, ListTenantAdminTenantsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants',
    ...options
});

/**
 * tenant を作成する
 *
 * tenant を作成する。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createTenantAdminTenant = <ThrowOnError extends boolean = false>(options: Options<CreateTenantAdminTenantData, ThrowOnError>) => (options.client ?? client).post<CreateTenantAdminTenantResponses, CreateTenantAdminTenantErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * tenant を無効化する
 *
 * tenant を無効化する。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deactivateTenantAdminTenant = <ThrowOnError extends boolean = false>(options: Options<DeactivateTenantAdminTenantData, ThrowOnError>) => (options.client ?? client).delete<DeactivateTenantAdminTenantResponses, DeactivateTenantAdminTenantErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}',
    ...options
});

/**
 * tenant detail と membership を返す
 *
 * tenant detail と membership を返す。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getTenantAdminTenant = <ThrowOnError extends boolean = false>(options: Options<GetTenantAdminTenantData, ThrowOnError>) => (options.client ?? client).get<GetTenantAdminTenantResponses, GetTenantAdminTenantErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}',
    ...options
});

/**
 * tenant を更新する
 *
 * tenant を更新する。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateTenantAdminTenant = <ThrowOnError extends boolean = false>(options: Options<UpdateTenantAdminTenantData, ThrowOnError>) => (options.client ?? client).put<UpdateTenantAdminTenantResponses, UpdateTenantAdminTenantErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /api/v1/admin/tenants/{tenantSlug}/data-access/groups。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantAdminDataAccessGroups = <ThrowOnError extends boolean = false>(options: Options<ListTenantAdminDataAccessGroupsData, ThrowOnError>) => (options.client ?? client).get<ListTenantAdminDataAccessGroupsResponses, ListTenantAdminDataAccessGroupsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-access/groups',
    ...options
});

/**
 * POST /api/v1/admin/tenants/{tenantSlug}/data-access/groups。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createTenantAdminDataAccessGroup = <ThrowOnError extends boolean = false>(options: Options<CreateTenantAdminDataAccessGroupData, ThrowOnError>) => (options.client ?? client).post<CreateTenantAdminDataAccessGroupResponses, CreateTenantAdminDataAccessGroupErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-access/groups',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getTenantAdminDataAccessGroup = <ThrowOnError extends boolean = false>(options: Options<GetTenantAdminDataAccessGroupData, ThrowOnError>) => (options.client ?? client).get<GetTenantAdminDataAccessGroupResponses, GetTenantAdminDataAccessGroupErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}',
    ...options
});

/**
 * PATCH /api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateTenantAdminDataAccessGroup = <ThrowOnError extends boolean = false>(options: Options<UpdateTenantAdminDataAccessGroupData, ThrowOnError>) => (options.client ?? client).patch<UpdateTenantAdminDataAccessGroupResponses, UpdateTenantAdminDataAccessGroupErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * POST /api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}/members。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const addTenantAdminDataAccessGroupMember = <ThrowOnError extends boolean = false>(options: Options<AddTenantAdminDataAccessGroupMemberData, ThrowOnError>) => (options.client ?? client).post<AddTenantAdminDataAccessGroupMemberResponses, AddTenantAdminDataAccessGroupMemberErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}/members',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * DELETE /api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}/members/{userPublicId}。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const removeTenantAdminDataAccessGroupMember = <ThrowOnError extends boolean = false>(options: Options<RemoveTenantAdminDataAccessGroupMemberData, ThrowOnError>) => (options.client ?? client).delete<RemoveTenantAdminDataAccessGroupMemberResponses, RemoveTenantAdminDataAccessGroupMemberErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-access/groups/{groupPublicId}/members/{userPublicId}',
    ...options
});

/**
 * GET /api/v1/admin/tenants/{tenantSlug}/data-access/scope/permissions。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantAdminDataAccessScopePermissions = <ThrowOnError extends boolean = false>(options: Options<ListTenantAdminDataAccessScopePermissionsData, ThrowOnError>) => (options.client ?? client).get<ListTenantAdminDataAccessScopePermissionsResponses, ListTenantAdminDataAccessScopePermissionsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-access/scope/permissions',
    ...options
});

/**
 * PUT /api/v1/admin/tenants/{tenantSlug}/data-access/scope/permissions。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const putTenantAdminDataAccessScopePermissions = <ThrowOnError extends boolean = false>(options: Options<PutTenantAdminDataAccessScopePermissionsData, ThrowOnError>) => (options.client ?? client).put<PutTenantAdminDataAccessScopePermissionsResponses, PutTenantAdminDataAccessScopePermissionsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-access/scope/permissions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /api/v1/admin/tenants/{tenantSlug}/data-access/{resourceType}/{resourcePublicId}/permissions。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantAdminDataAccessResourcePermissions = <ThrowOnError extends boolean = false>(options: Options<ListTenantAdminDataAccessResourcePermissionsData, ThrowOnError>) => (options.client ?? client).get<ListTenantAdminDataAccessResourcePermissionsResponses, ListTenantAdminDataAccessResourcePermissionsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-access/{resourceType}/{resourcePublicId}/permissions',
    ...options
});

/**
 * PUT /api/v1/admin/tenants/{tenantSlug}/data-access/{resourceType}/{resourcePublicId}/permissions。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const putTenantAdminDataAccessResourcePermissions = <ThrowOnError extends boolean = false>(options: Options<PutTenantAdminDataAccessResourcePermissionsData, ThrowOnError>) => (options.client ?? client).put<PutTenantAdminDataAccessResourcePermissionsResponses, PutTenantAdminDataAccessResourcePermissionsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-access/{resourceType}/{resourcePublicId}/permissions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * tenant admin 用 schema mapping example 一覧を返す
 *
 * tenant admin 用 schema mapping example 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantAdminSchemaMappingExamples = <ThrowOnError extends boolean = false>(options: Options<ListTenantAdminSchemaMappingExamplesData, ThrowOnError>) => (options.client ?? client).get<ListTenantAdminSchemaMappingExamplesResponses, ListTenantAdminSchemaMappingExamplesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-pipelines/schema-mapping/examples',
    ...options
});

/**
 * schema mapping example の tenant 共有を切り替える
 *
 * schema mapping example の tenant 共有を切り替える。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateTenantAdminSchemaMappingExampleSharing = <ThrowOnError extends boolean = false>(options: Options<UpdateTenantAdminSchemaMappingExampleSharingData, ThrowOnError>) => (options.client ?? client).patch<UpdateTenantAdminSchemaMappingExampleSharingResponses, UpdateTenantAdminSchemaMappingExampleSharingErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-pipelines/schema-mapping/examples/{mappingExamplePublicId}/sharing',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * schema mapping search documents を rebuild する
 *
 * schema mapping search documents を rebuild する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const rebuildTenantAdminSchemaMappingSearchDocuments = <ThrowOnError extends boolean = false>(options: Options<RebuildTenantAdminSchemaMappingSearchDocumentsData, ThrowOnError>) => (options.client ?? client).post<RebuildTenantAdminSchemaMappingSearchDocumentsResponses, RebuildTenantAdminSchemaMappingSearchDocumentsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/data-pipelines/schema-mapping/search-documents/rebuild',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * tenant admin 用 Drive audit events を返す
 *
 * tenant admin 用 Drive audit events を返す。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantAdminDriveAuditEvents = <ThrowOnError extends boolean = false>(options: Options<ListTenantAdminDriveAuditEventsData, ThrowOnError>) => (options.client ?? client).get<ListTenantAdminDriveAuditEventsResponses, ListTenantAdminDriveAuditEventsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/audit-events',
    ...options
});

/**
 * Drive clean room を作成する
 *
 * Drive clean room を作成する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveCleanRoom = <ThrowOnError extends boolean = false>(options: Options<CreateDriveCleanRoomData, ThrowOnError>) => (options.client ?? client).post<CreateDriveCleanRoomResponses, CreateDriveCleanRoomErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/clean-rooms',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive clean room dataset を投入する
 *
 * Drive clean room dataset を投入する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const submitDriveCleanRoomDataset = <ThrowOnError extends boolean = false>(options: Options<SubmitDriveCleanRoomDatasetData, ThrowOnError>) => (options.client ?? client).post<SubmitDriveCleanRoomDatasetResponses, SubmitDriveCleanRoomDatasetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/clean-rooms/{roomPublicId}/datasets',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive clean room export を要求する
 *
 * Drive clean room export を要求する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const requestDriveCleanRoomExport = <ThrowOnError extends boolean = false>(options: Options<RequestDriveCleanRoomExportData, ThrowOnError>) => (options.client ?? client).post<RequestDriveCleanRoomExportResponses, RequestDriveCleanRoomExportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/clean-rooms/{roomPublicId}/exports',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive clean room participant を追加する
 *
 * Drive clean room participant を追加する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const addDriveCleanRoomParticipant = <ThrowOnError extends boolean = false>(options: Options<AddDriveCleanRoomParticipantData, ThrowOnError>) => (options.client ?? client).post<AddDriveCleanRoomParticipantResponses, AddDriveCleanRoomParticipantErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/clean-rooms/{roomPublicId}/participants',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive admin content break-glass session を開始する
 *
 * Drive admin content break-glass session を開始する。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const startTenantAdminDriveContentAccessSession = <ThrowOnError extends boolean = false>(options: Options<StartTenantAdminDriveContentAccessSessionData, ThrowOnError>) => (options.client ?? client).post<StartTenantAdminDriveContentAccessSessionResponses, StartTenantAdminDriveContentAccessSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/content-access-sessions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive admin content break-glass session を終了する
 *
 * Drive admin content break-glass session を終了する。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const endTenantAdminDriveContentAccessSession = <ThrowOnError extends boolean = false>(options: Options<EndTenantAdminDriveContentAccessSessionData, ThrowOnError>) => (options.client ?? client).delete<EndTenantAdminDriveContentAccessSessionResponses, EndTenantAdminDriveContentAccessSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/content-access-sessions/current',
    ...options
});

/**
 * Drive eDiscovery provider connection を作成する
 *
 * Drive eDiscovery provider connection を作成する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveEDiscoveryConnection = <ThrowOnError extends boolean = false>(options: Options<CreateDriveEDiscoveryConnectionData, ThrowOnError>) => (options.client ?? client).post<CreateDriveEDiscoveryConnectionResponses, CreateDriveEDiscoveryConnectionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/ediscovery/connections',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive eDiscovery export を request する
 *
 * Drive eDiscovery export を request する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const requestDriveEDiscoveryExport = <ThrowOnError extends boolean = false>(options: Options<RequestDriveEDiscoveryExportData, ThrowOnError>) => (options.client ?? client).post<RequestDriveEDiscoveryExportResponses, RequestDriveEDiscoveryExportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/ediscovery/exports',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive eDiscovery export を approve する
 *
 * Drive eDiscovery export を approve する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const approveDriveEDiscoveryExport = <ThrowOnError extends boolean = false>(options: Options<ApproveDriveEDiscoveryExportData, ThrowOnError>) => (options.client ?? client).post<ApproveDriveEDiscoveryExportResponses, ApproveDriveEDiscoveryExportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/ediscovery/exports/{exportPublicId}/approve',
    ...options
});

/**
 * break-glass session 中に Drive file metadata を返す
 *
 * break-glass session 中に Drive file metadata を返す。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getTenantAdminDriveFileMetadata = <ThrowOnError extends boolean = false>(options: Options<GetTenantAdminDriveFileMetadataData, ThrowOnError>) => (options.client ?? client).get<GetTenantAdminDriveFileMetadataResponses, GetTenantAdminDriveFileMetadataErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/files/{filePublicId}/metadata',
    ...options
});

/**
 * Drive on-prem gateway を登録する
 *
 * Drive on-prem gateway を登録する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const registerDriveGateway = <ThrowOnError extends boolean = false>(options: Options<RegisterDriveGatewayData, ThrowOnError>) => (options.client ?? client).post<RegisterDriveGatewayResponses, RegisterDriveGatewayErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/gateways',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive gateway status を更新する
 *
 * Drive gateway status を更新する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDriveGatewayStatus = <ThrowOnError extends boolean = false>(options: Options<UpdateDriveGatewayStatusData, ThrowOnError>) => (options.client ?? client).patch<UpdateDriveGatewayStatusResponses, UpdateDriveGatewayStatusErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/gateways/{gatewayPublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file を gateway object として bind する
 *
 * Drive file を gateway object として bind する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const bindDriveGatewayFile = <ThrowOnError extends boolean = false>(options: Options<BindDriveGatewayFileData, ThrowOnError>) => (options.client ?? client).post<BindDriveGatewayFileResponses, BindDriveGatewayFileErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/gateways/{gatewayPublicId}/objects',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive HSM key を file に bind する
 *
 * Drive HSM key を file に bind する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const bindDriveHsmKey = <ThrowOnError extends boolean = false>(options: Options<BindDriveHsmKeyData, ThrowOnError>) => (options.client ?? client).post<BindDriveHsmKeyResponses, BindDriveHsmKeyErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/hsm/bindings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive HSM deployment を作成する
 *
 * Drive HSM deployment を作成する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveHsmDeployment = <ThrowOnError extends boolean = false>(options: Options<CreateDriveHsmDeploymentData, ThrowOnError>) => (options.client ?? client).post<CreateDriveHsmDeploymentResponses, CreateDriveHsmDeploymentErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/hsm/deployments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive HSM key status を更新する
 *
 * Drive HSM key status を更新する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDriveHsmKeyStatus = <ThrowOnError extends boolean = false>(options: Options<UpdateDriveHsmKeyStatusData, ThrowOnError>) => (options.client ?? client).patch<UpdateDriveHsmKeyStatusResponses, UpdateDriveHsmKeyStatusErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/hsm/keys/{keyPublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * tenant admin 用 Drive invitations を返す
 *
 * tenant admin 用 Drive invitations を返す。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantAdminDriveInvitations = <ThrowOnError extends boolean = false>(options: Options<ListTenantAdminDriveInvitationsData, ThrowOnError>) => (options.client ?? client).get<ListTenantAdminDriveInvitationsResponses, ListTenantAdminDriveInvitationsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/invitations',
    ...options
});

/**
 * Drive legal case を作成する
 *
 * Drive legal case を作成する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveLegalCase = <ThrowOnError extends boolean = false>(options: Options<CreateDriveLegalCaseData, ThrowOnError>) => (options.client ?? client).post<CreateDriveLegalCaseResponses, CreateDriveLegalCaseErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/legal/cases',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive legal export を作成する
 *
 * Drive legal export を作成する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const createDriveLegalExport = <ThrowOnError extends boolean = false>(options: Options<CreateDriveLegalExportData, ThrowOnError>) => (options.client ?? client).post<CreateDriveLegalExportResponses, CreateDriveLegalExportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/legal/cases/{casePublicId}/exports',
    ...options
});

/**
 * Drive legal case に file hold を追加する
 *
 * Drive legal case に file hold を追加する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const addDriveLegalCaseFile = <ThrowOnError extends boolean = false>(options: Options<AddDriveLegalCaseFileData, ThrowOnError>) => (options.client ?? client).post<AddDriveLegalCaseFileResponses, AddDriveLegalCaseFileErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/legal/cases/{casePublicId}/files',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive marketplace app install を request する
 *
 * Drive marketplace app install を request する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const installDriveMarketplaceApp = <ThrowOnError extends boolean = false>(options: Options<InstallDriveMarketplaceAppData, ThrowOnError>) => (options.client ?? client).post<InstallDriveMarketplaceAppResponses, InstallDriveMarketplaceAppErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/marketplace/installations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive marketplace app install を uninstall する
 *
 * Drive marketplace app install を uninstall する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const uninstallDriveMarketplaceInstallation = <ThrowOnError extends boolean = false>(options: Options<UninstallDriveMarketplaceInstallationData, ThrowOnError>) => (options.client ?? client).delete<UninstallDriveMarketplaceInstallationResponses, UninstallDriveMarketplaceInstallationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/marketplace/installations/{installationPublicId}',
    ...options
});

/**
 * Drive marketplace app install を approve する
 *
 * Drive marketplace app install を approve する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const approveDriveMarketplaceInstallation = <ThrowOnError extends boolean = false>(options: Options<ApproveDriveMarketplaceInstallationData, ThrowOnError>) => (options.client ?? client).post<ApproveDriveMarketplaceInstallationResponses, ApproveDriveMarketplaceInstallationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/marketplace/installations/{installationPublicId}/approve',
    ...options
});

/**
 * Drive marketplace scope と OpenFGA permission を確認する
 *
 * Drive marketplace scope と OpenFGA permission を確認する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const checkDriveMarketplaceScope = <ThrowOnError extends boolean = false>(options: Options<CheckDriveMarketplaceScopeData, ThrowOnError>) => (options.client ?? client).get<CheckDriveMarketplaceScopeResponses, CheckDriveMarketplaceScopeErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/marketplace/installations/{installationPublicId}/scope-check',
    ...options
});

/**
 * tenant admin 用 Drive OCR runtime status を返す
 *
 * tenant admin 用 Drive OCR runtime status を返す。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getTenantAdminDriveOcrStatus = <ThrowOnError extends boolean = false>(options: Options<GetTenantAdminDriveOcrStatusData, ThrowOnError>) => (options.client ?? client).get<GetTenantAdminDriveOcrStatusResponses, GetTenantAdminDriveOcrStatusErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/ocr/status',
    ...options
});

/**
 * Drive OpenFGA drift dry-run を返す
 *
 * Drive OpenFGA drift dry-run を返す。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getTenantAdminDriveOpenFgaDrift = <ThrowOnError extends boolean = false>(options: Options<GetTenantAdminDriveOpenFgaDriftData, ThrowOnError>) => (options.client ?? client).get<GetTenantAdminDriveOpenFgaDriftResponses, GetTenantAdminDriveOpenFgaDriftErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/openfga-sync/drift',
    ...options
});

/**
 * Drive OpenFGA pending sync を修復する
 *
 * Drive OpenFGA pending sync を修復する。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const repairTenantAdminDriveOpenFgaSync = <ThrowOnError extends boolean = false>(options: Options<RepairTenantAdminDriveOpenFgaSyncData, ThrowOnError>) => (options.client ?? client).post<RepairTenantAdminDriveOpenFgaSyncResponses, RepairTenantAdminDriveOpenFgaSyncErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/openfga-sync/repair',
    ...options
});

/**
 * Drive OpenFGA drift dry-run を operation endpoint から実行する
 *
 * Drive OpenFGA drift dry-run を operation endpoint から実行する。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const checkTenantAdminDriveOperationsDrift = <ThrowOnError extends boolean = false>(options: Options<CheckTenantAdminDriveOperationsDriftData, ThrowOnError>) => (options.client ?? client).post<CheckTenantAdminDriveOperationsDriftResponses, CheckTenantAdminDriveOperationsDriftErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/operations/drift-check',
    ...options
});

/**
 * Drive / OpenFGA / storage operations health を返す
 *
 * Drive / OpenFGA / storage operations health を返す。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getTenantAdminDriveOperationsHealth = <ThrowOnError extends boolean = false>(options: Options<GetTenantAdminDriveOperationsHealthData, ThrowOnError>) => (options.client ?? client).get<GetTenantAdminDriveOperationsHealthResponses, GetTenantAdminDriveOperationsHealthErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/operations/health',
    ...options
});

/**
 * Drive OpenFGA pending sync repair を operation endpoint から実行する
 *
 * Drive OpenFGA pending sync repair を operation endpoint から実行する。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const repairTenantAdminDriveOperations = <ThrowOnError extends boolean = false>(options: Options<RepairTenantAdminDriveOperationsData, ThrowOnError>) => (options.client ?? client).post<RepairTenantAdminDriveOperationsResponses, RepairTenantAdminDriveOperationsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/operations/repair',
    ...options
});

/**
 * Drive residency policy を返す
 *
 * Drive residency policy を返す。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getTenantAdminDriveResidencyPolicy = <ThrowOnError extends boolean = false>(options: Options<GetTenantAdminDriveResidencyPolicyData, ThrowOnError>) => (options.client ?? client).get<GetTenantAdminDriveResidencyPolicyResponses, GetTenantAdminDriveResidencyPolicyErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/residency-policy',
    ...options
});

/**
 * Drive residency policy を更新する
 *
 * Drive residency policy を更新する。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateTenantAdminDriveResidencyPolicy = <ThrowOnError extends boolean = false>(options: Options<UpdateTenantAdminDriveResidencyPolicyData, ThrowOnError>) => (options.client ?? client).put<UpdateTenantAdminDriveResidencyPolicyResponses, UpdateTenantAdminDriveResidencyPolicyErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/residency-policy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive search index を rebuild する
 *
 * Drive search index を rebuild する。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const rebuildTenantAdminDriveSearchIndex = <ThrowOnError extends boolean = false>(options: Options<RebuildTenantAdminDriveSearchIndexData, ThrowOnError>) => (options.client ?? client).post<RebuildTenantAdminDriveSearchIndexResponses, RebuildTenantAdminDriveSearchIndexErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/search/index/rebuild',
    ...options
});

/**
 * Drive local search index job 一覧を返す
 *
 * Drive local search index job 一覧を返す。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantAdminDriveLocalSearchIndexJobs = <ThrowOnError extends boolean = false>(options: Options<ListTenantAdminDriveLocalSearchIndexJobsData, ThrowOnError>) => (options.client ?? client).get<ListTenantAdminDriveLocalSearchIndexJobsResponses, ListTenantAdminDriveLocalSearchIndexJobsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/search/local-index/jobs',
    ...options
});

/**
 * Drive local search index rebuild を enqueue する
 *
 * Drive local search index rebuild を enqueue する。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const createTenantAdminDriveLocalSearchRebuild = <ThrowOnError extends boolean = false>(options: Options<CreateTenantAdminDriveLocalSearchRebuildData, ThrowOnError>) => (options.client ?? client).post<CreateTenantAdminDriveLocalSearchRebuildResponses, CreateTenantAdminDriveLocalSearchRebuildErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/search/local-index/rebuilds',
    ...options
});

/**
 * Drive encryption policy を返す
 *
 * Drive encryption policy を返す。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getTenantAdminDriveEncryptionPolicy = <ThrowOnError extends boolean = false>(options: Options<GetTenantAdminDriveEncryptionPolicyData, ThrowOnError>) => (options.client ?? client).get<GetTenantAdminDriveEncryptionPolicyResponses, GetTenantAdminDriveEncryptionPolicyErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/security/encryption-policy',
    ...options
});

/**
 * Drive encryption policy を更新する
 *
 * Drive encryption policy を更新する。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateTenantAdminDriveEncryptionPolicy = <ThrowOnError extends boolean = false>(options: Options<UpdateTenantAdminDriveEncryptionPolicyData, ThrowOnError>) => (options.client ?? client).put<UpdateTenantAdminDriveEncryptionPolicyResponses, UpdateTenantAdminDriveEncryptionPolicyErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/security/encryption-policy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive KMS key status を更新する
 *
 * Drive KMS key status を更新する。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateTenantAdminDriveKmsKeyStatus = <ThrowOnError extends boolean = false>(options: Options<UpdateTenantAdminDriveKmsKeyStatusData, ThrowOnError>) => (options.client ?? client).patch<UpdateTenantAdminDriveKmsKeyStatusResponses, UpdateTenantAdminDriveKmsKeyStatusErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/security/kms-keys/{keyPublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * tenant admin 用 Drive share approvals を返す
 *
 * tenant admin 用 Drive share approvals を返す。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantAdminDriveShareApprovals = <ThrowOnError extends boolean = false>(options: Options<ListTenantAdminDriveShareApprovalsData, ThrowOnError>) => (options.client ?? client).get<ListTenantAdminDriveShareApprovalsResponses, ListTenantAdminDriveShareApprovalsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/share-approvals',
    ...options
});

/**
 * Drive external share approval を承認する
 *
 * Drive external share approval を承認する。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const approveTenantAdminDriveShareApproval = <ThrowOnError extends boolean = false>(options: Options<ApproveTenantAdminDriveShareApprovalData, ThrowOnError>) => (options.client ?? client).post<ApproveTenantAdminDriveShareApprovalResponses, ApproveTenantAdminDriveShareApprovalErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/share-approvals/{invitationPublicId}/approve',
    ...options
});

/**
 * Drive external share approval を拒否する
 *
 * Drive external share approval を拒否する。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const rejectTenantAdminDriveShareApproval = <ThrowOnError extends boolean = false>(options: Options<RejectTenantAdminDriveShareApprovalData, ThrowOnError>) => (options.client ?? client).post<RejectTenantAdminDriveShareApprovalResponses, RejectTenantAdminDriveShareApprovalErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/share-approvals/{invitationPublicId}/reject',
    ...options
});

/**
 * tenant admin 用 Drive share link state を返す
 *
 * tenant admin 用 Drive share link state を返す。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantAdminDriveShareLinks = <ThrowOnError extends boolean = false>(options: Options<ListTenantAdminDriveShareLinksData, ThrowOnError>) => (options.client ?? client).get<ListTenantAdminDriveShareLinksResponses, ListTenantAdminDriveShareLinksErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/share-links',
    ...options
});

/**
 * tenant admin 用 Drive share state を返す
 *
 * tenant admin 用 Drive share state を返す。
 *
 * tenant admin が Drive audit、policy、operations health、search index、content access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantAdminDriveShares = <ThrowOnError extends boolean = false>(options: Options<ListTenantAdminDriveSharesData, ThrowOnError>) => (options.client ?? client).get<ListTenantAdminDriveSharesResponses, ListTenantAdminDriveSharesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/drive/shares',
    ...options
});

/**
 * tenant entitlements を返す
 *
 * tenant entitlements を返す。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantEntitlements = <ThrowOnError extends boolean = false>(options: Options<ListTenantEntitlementsData, ThrowOnError>) => (options.client ?? client).get<ListTenantEntitlementsResponses, ListTenantEntitlementsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/entitlements',
    ...options
});

/**
 * tenant entitlements を更新する
 *
 * tenant entitlements を更新する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateTenantEntitlements = <ThrowOnError extends boolean = false>(options: Options<UpdateTenantEntitlementsData, ThrowOnError>) => (options.client ?? client).put<UpdateTenantEntitlementsResponses, UpdateTenantEntitlementsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/entitlements',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * tenant data export 一覧を返す
 *
 * tenant data export 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantDataExports = <ThrowOnError extends boolean = false>(options: Options<ListTenantDataExportsData, ThrowOnError>) => (options.client ?? client).get<ListTenantDataExportsResponses, ListTenantDataExportsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/exports',
    ...options
});

/**
 * tenant data export を request する
 *
 * tenant data export を request する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createTenantDataExport = <ThrowOnError extends boolean = false>(options: Options<CreateTenantDataExportData, ThrowOnError>) => (options.client ?? client).post<CreateTenantDataExportResponses, CreateTenantDataExportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/exports',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * tenant data export detail を返す
 *
 * tenant data export detail を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getTenantDataExport = <ThrowOnError extends boolean = false>(options: Options<GetTenantDataExportData, ThrowOnError>) => (options.client ?? client).get<GetTenantDataExportResponses, GetTenantDataExportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/exports/{exportPublicId}',
    ...options
});

/**
 * ready tenant data export を download する
 *
 * ready tenant data export を download する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const downloadTenantDataExport = <ThrowOnError extends boolean = false>(options: Options<DownloadTenantDataExportData, ThrowOnError>) => (options.client ?? client).get<DownloadTenantDataExportResponses, DownloadTenantDataExportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/exports/{exportPublicId}/download',
    ...options
});

/**
 * Customer Signals import job 一覧を返す
 *
 * Customer Signals import job 一覧を返す。
 *
 * customer signal の作成、検索、更新、import workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listCustomerSignalImports = <ThrowOnError extends boolean = false>(options: Options<ListCustomerSignalImportsData, ThrowOnError>) => (options.client ?? client).get<ListCustomerSignalImportsResponses, ListCustomerSignalImportsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/imports',
    ...options
});

/**
 * Customer Signals CSV import job を作成する
 *
 * Customer Signals CSV import job を作成する。
 *
 * customer signal の作成、検索、更新、import workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createCustomerSignalImport = <ThrowOnError extends boolean = false>(options: Options<CreateCustomerSignalImportData, ThrowOnError>) => (options.client ?? client).post<CreateCustomerSignalImportResponses, CreateCustomerSignalImportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/imports',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Customer Signals import job detail を返す
 *
 * Customer Signals import job detail を返す。
 *
 * customer signal の作成、検索、更新、import workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getCustomerSignalImport = <ThrowOnError extends boolean = false>(options: Options<GetCustomerSignalImportData, ThrowOnError>) => (options.client ?? client).get<GetCustomerSignalImportResponses, GetCustomerSignalImportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/imports/{importPublicId}',
    ...options
});

/**
 * tenant invitation 一覧を返す
 *
 * tenant invitation 一覧を返す。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenantInvitations = <ThrowOnError extends boolean = false>(options: Options<ListTenantInvitationsData, ThrowOnError>) => (options.client ?? client).get<ListTenantInvitationsResponses, ListTenantInvitationsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/invitations',
    ...options
});

/**
 * tenant invitation を作成する
 *
 * tenant に user を招待します。email と role codes を指定すると、招待 URL を含む invitation record が作成されます。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createTenantInvitation = <ThrowOnError extends boolean = false>(options: Options<CreateTenantInvitationData, ThrowOnError>) => (options.client ?? client).post<CreateTenantInvitationResponses, CreateTenantInvitationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/invitations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * tenant invitation を revoke する
 *
 * tenant invitation を revoke する。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const revokeTenantInvitation = <ThrowOnError extends boolean = false>(options: Options<RevokeTenantInvitationData, ThrowOnError>) => (options.client ?? client).delete<RevokeTenantInvitationResponses, RevokeTenantInvitationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/invitations/{invitationPublicId}',
    ...options
});

/**
 * pending tenant invitation の Zitadel identity 初期化メールを送る
 *
 * pending tenant invitation の Zitadel identity 初期化メールを送る。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const provisionTenantInvitationIdentity = <ThrowOnError extends boolean = false>(options: Options<ProvisionTenantInvitationIdentityData, ThrowOnError>) => (options.client ?? client).post<ProvisionTenantInvitationIdentityResponses, ProvisionTenantInvitationIdentityErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/invitations/{invitationPublicId}/provision-identity',
    ...options
});

/**
 * tenant local role を付与する
 *
 * tenant local role を付与する。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const grantTenantAdminRole = <ThrowOnError extends boolean = false>(options: Options<GrantTenantAdminRoleData, ThrowOnError>) => (options.client ?? client).post<GrantTenantAdminRoleResponses, GrantTenantAdminRoleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/memberships',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * tenant local role を無効化する
 *
 * tenant local role を無効化する。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const revokeTenantAdminRole = <ThrowOnError extends boolean = false>(options: Options<RevokeTenantAdminRoleData, ThrowOnError>) => (options.client ?? client).delete<RevokeTenantAdminRoleResponses, RevokeTenantAdminRoleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/memberships/{userPublicId}/roles/{roleCode}',
    ...options
});

/**
 * tenant settings を返す
 *
 * tenant settings を返す。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getTenantSettings = <ThrowOnError extends boolean = false>(options: Options<GetTenantSettingsData, ThrowOnError>) => (options.client ?? client).get<GetTenantSettingsResponses, GetTenantSettingsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/settings',
    ...options
});

/**
 * tenant settings を更新する
 *
 * tenant settings を更新する。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateTenantSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateTenantSettingsData, ThrowOnError>) => (options.client ?? client).put<UpdateTenantSettingsResponses, UpdateTenantSettingsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * webhook endpoint 一覧を返す
 *
 * webhook endpoint 一覧を返す。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listWebhooks = <ThrowOnError extends boolean = false>(options: Options<ListWebhooksData, ThrowOnError>) => (options.client ?? client).get<ListWebhooksResponses, ListWebhooksErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/webhooks',
    ...options
});

/**
 * webhook endpoint を作成する
 *
 * tenant admin API で webhook endpoint を登録します。作成後の delivery には署名 header が付き、secret は rotate API で更新できます。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createWebhook = <ThrowOnError extends boolean = false>(options: Options<CreateWebhookData, ThrowOnError>) => (options.client ?? client).post<CreateWebhookResponses, CreateWebhookErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/webhooks',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * webhook endpoint を削除する
 *
 * webhook endpoint を削除する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteWebhook = <ThrowOnError extends boolean = false>(options: Options<DeleteWebhookData, ThrowOnError>) => (options.client ?? client).delete<DeleteWebhookResponses, DeleteWebhookErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/webhooks/{webhookPublicId}',
    ...options
});

/**
 * webhook endpoint detail を返す
 *
 * webhook endpoint detail を返す。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getWebhook = <ThrowOnError extends boolean = false>(options: Options<GetWebhookData, ThrowOnError>) => (options.client ?? client).get<GetWebhookResponses, GetWebhookErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/webhooks/{webhookPublicId}',
    ...options
});

/**
 * webhook endpoint を更新する
 *
 * webhook endpoint を更新する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateWebhook = <ThrowOnError extends boolean = false>(options: Options<UpdateWebhookData, ThrowOnError>) => (options.client ?? client).put<UpdateWebhookResponses, UpdateWebhookErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/webhooks/{webhookPublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * webhook delivery log を返す
 *
 * webhook delivery log を返す。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listWebhookDeliveries = <ThrowOnError extends boolean = false>(options: Options<ListWebhookDeliveriesData, ThrowOnError>) => (options.client ?? client).get<ListWebhookDeliveriesResponses, ListWebhookDeliveriesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/webhooks/{webhookPublicId}/deliveries',
    ...options
});

/**
 * webhook delivery を retry する
 *
 * webhook delivery を retry する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const retryWebhookDelivery = <ThrowOnError extends boolean = false>(options: Options<RetryWebhookDeliveryData, ThrowOnError>) => (options.client ?? client).post<RetryWebhookDeliveryResponses, RetryWebhookDeliveryErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/webhooks/{webhookPublicId}/deliveries/{deliveryPublicId}/retry',
    ...options
});

/**
 * webhook secret を rotate する
 *
 * webhook secret を rotate する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const rotateWebhookSecret = <ThrowOnError extends boolean = false>(options: Options<RotateWebhookSecretData, ThrowOnError>) => (options.client ?? client).post<RotateWebhookSecretResponses, RotateWebhookSecretErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/tenants/{tenantSlug}/webhooks/{webhookPublicId}/rotate-secret',
    ...options
});

/**
 * OIDC callback を完了する
 *
 * OIDC callback を完了する。
 *
 * login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。
 */
export const finishOidcLogin = <ThrowOnError extends boolean = false>(options?: Options<FinishOidcLoginData, ThrowOnError>) => (options?.client ?? client).get<FinishOidcLoginResponses, FinishOidcLoginErrors, ThrowOnError>({ url: '/api/v1/auth/callback', ...options });

/**
 * OIDC login を開始する
 *
 * OIDC login を開始する。
 *
 * login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。
 */
export const startOidcLogin = <ThrowOnError extends boolean = false>(options?: Options<StartOidcLoginData, ThrowOnError>) => (options?.client ?? client).get<StartOidcLoginResponses, StartOidcLoginErrors, ThrowOnError>({ url: '/api/v1/auth/login', ...options });

/**
 * 現在の認証モード設定を返す
 *
 * 現在の認証モード設定を返す。
 *
 * login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。
 */
export const getAuthSettings = <ThrowOnError extends boolean = false>(options?: Options<GetAuthSettingsData, ThrowOnError>) => (options?.client ?? client).get<GetAuthSettingsResponses, GetAuthSettingsErrors, ThrowOnError>({ url: '/api/v1/auth/settings', ...options });

/**
 * CSRF token を再発行する
 *
 * CSRF token を再発行する。
 *
 * login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getCsrf = <ThrowOnError extends boolean = false>(options?: Options<GetCsrfData, ThrowOnError>) => (options?.client ?? client).get<GetCsrfResponses, GetCsrfErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/csrf',
    ...options
});

/**
 * Customer Signal saved filters を返す
 *
 * Customer Signal saved filters を返す。
 *
 * customer signal の作成、検索、更新、import workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listCustomerSignalSavedFilters = <ThrowOnError extends boolean = false>(options?: Options<ListCustomerSignalSavedFiltersData, ThrowOnError>) => (options?.client ?? client).get<ListCustomerSignalSavedFiltersResponses, ListCustomerSignalSavedFiltersErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/customer-signal-filters',
    ...options
});

/**
 * Customer Signal saved filter を作成する
 *
 * Customer Signal saved filter を作成する。
 *
 * customer signal の作成、検索、更新、import workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createCustomerSignalSavedFilter = <ThrowOnError extends boolean = false>(options: Options<CreateCustomerSignalSavedFilterData, ThrowOnError>) => (options.client ?? client).post<CreateCustomerSignalSavedFilterResponses, CreateCustomerSignalSavedFilterErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/customer-signal-filters',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Customer Signal saved filter を削除する
 *
 * Customer Signal saved filter を削除する。
 *
 * customer signal の作成、検索、更新、import workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteCustomerSignalSavedFilter = <ThrowOnError extends boolean = false>(options: Options<DeleteCustomerSignalSavedFilterData, ThrowOnError>) => (options.client ?? client).delete<DeleteCustomerSignalSavedFilterResponses, DeleteCustomerSignalSavedFilterErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/customer-signal-filters/{filterPublicId}',
    ...options
});

/**
 * Customer Signal saved filter を更新する
 *
 * Customer Signal saved filter を更新する。
 *
 * customer signal の作成、検索、更新、import workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateCustomerSignalSavedFilter = <ThrowOnError extends boolean = false>(options: Options<UpdateCustomerSignalSavedFilterData, ThrowOnError>) => (options.client ?? client).put<UpdateCustomerSignalSavedFilterResponses, UpdateCustomerSignalSavedFilterErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/customer-signal-filters/{filterPublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * active tenant の Customer Signals 一覧を返す
 *
 * active tenant の Customer Signals 一覧を返す。
 *
 * customer signal の作成、検索、更新、import workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listCustomerSignals = <ThrowOnError extends boolean = false>(options?: Options<ListCustomerSignalsData, ThrowOnError>) => (options?.client ?? client).get<ListCustomerSignalsResponses, ListCustomerSignalsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/customer-signals',
    ...options
});

/**
 * active tenant に Customer Signal を作成する
 *
 * active tenant に customer signal を作成します。重複送信を避けたい場合は `Idempotency-Key` を指定してください。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createCustomerSignal = <ThrowOnError extends boolean = false>(options: Options<CreateCustomerSignalData, ThrowOnError>) => (options.client ?? client).post<CreateCustomerSignalResponses, CreateCustomerSignalErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/customer-signals',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * active tenant の Customer Signal を soft delete する
 *
 * active tenant の Customer Signal を soft delete する。
 *
 * customer signal の作成、検索、更新、import workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteCustomerSignal = <ThrowOnError extends boolean = false>(options: Options<DeleteCustomerSignalData, ThrowOnError>) => (options.client ?? client).delete<DeleteCustomerSignalResponses, DeleteCustomerSignalErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/customer-signals/{signalPublicId}',
    ...options
});

/**
 * active tenant の Customer Signal detail を返す
 *
 * active tenant の Customer Signal detail を返す。
 *
 * customer signal の作成、検索、更新、import workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getCustomerSignal = <ThrowOnError extends boolean = false>(options: Options<GetCustomerSignalData, ThrowOnError>) => (options.client ?? client).get<GetCustomerSignalResponses, GetCustomerSignalErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/customer-signals/{signalPublicId}',
    ...options
});

/**
 * active tenant の Customer Signal を更新する
 *
 * active tenant の Customer Signal を更新する。
 *
 * customer signal の作成、検索、更新、import workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateCustomerSignal = <ThrowOnError extends boolean = false>(options: Options<UpdateCustomerSignalData, ThrowOnError>) => (options.client ?? client).patch<UpdateCustomerSignalResponses, UpdateCustomerSignalErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/customer-signals/{signalPublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * data pipeline schedule を disable する
 *
 * data pipeline schedule を disable する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const disableDataPipelineSchedule = <ThrowOnError extends boolean = false>(options: Options<DisableDataPipelineScheduleData, ThrowOnError>) => (options.client ?? client).delete<DisableDataPipelineScheduleResponses, DisableDataPipelineScheduleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipeline-schedules/{schedulePublicId}',
    ...options
});

/**
 * data pipeline schedule を更新する
 *
 * data pipeline schedule を更新する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDataPipelineSchedule = <ThrowOnError extends boolean = false>(options: Options<UpdateDataPipelineScheduleData, ThrowOnError>) => (options.client ?? client).patch<UpdateDataPipelineScheduleResponses, UpdateDataPipelineScheduleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipeline-schedules/{schedulePublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * selected node まで preview する
 *
 * selected node まで preview する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const previewDataPipelineVersion = <ThrowOnError extends boolean = false>(options: Options<PreviewDataPipelineVersionData, ThrowOnError>) => (options.client ?? client).post<PreviewDataPipelineVersionResponses, PreviewDataPipelineVersionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipeline-versions/{versionPublicId}/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * data pipeline version を publish する
 *
 * data pipeline version を publish する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const publishDataPipelineVersion = <ThrowOnError extends boolean = false>(options: Options<PublishDataPipelineVersionData, ThrowOnError>) => (options.client ?? client).post<PublishDataPipelineVersionResponses, PublishDataPipelineVersionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipeline-versions/{versionPublicId}/publish',
    ...options
});

/**
 * data pipeline manual run を request する
 *
 * data pipeline manual run を request する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const createDataPipelineRun = <ThrowOnError extends boolean = false>(options: Options<CreateDataPipelineRunData, ThrowOnError>) => (options.client ?? client).post<CreateDataPipelineRunResponses, CreateDataPipelineRunErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipeline-versions/{versionPublicId}/runs',
    ...options
});

/**
 * active tenant の data pipeline 一覧を返す
 *
 * active tenant の data pipeline 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDataPipelines = <ThrowOnError extends boolean = false>(options?: Options<ListDataPipelinesData, ThrowOnError>) => (options?.client ?? client).get<ListDataPipelinesResponses, ListDataPipelinesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipelines',
    ...options
});

/**
 * active tenant に data pipeline を作成する
 *
 * active tenant に data pipeline を作成する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDataPipeline = <ThrowOnError extends boolean = false>(options: Options<CreateDataPipelineData, ThrowOnError>) => (options.client ?? client).post<CreateDataPipelineResponses, CreateDataPipelineErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipelines',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * schema mapping 候補を返す
 *
 * schema mapping 候補を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const schemaMappingCandidates = <ThrowOnError extends boolean = false>(options: Options<SchemaMappingCandidatesData, ThrowOnError>) => (options.client ?? client).post<SchemaMappingCandidatesResponses, SchemaMappingCandidatesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipelines/schema-mapping/candidates',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * schema mapping の採用/却下履歴を記録する
 *
 * schema mapping の採用/却下履歴を記録する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const recordSchemaMappingExample = <ThrowOnError extends boolean = false>(options: Options<RecordSchemaMappingExampleData, ThrowOnError>) => (options.client ?? client).post<RecordSchemaMappingExampleResponses, RecordSchemaMappingExampleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipelines/schema-mapping/examples',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * data pipeline detail を返す
 *
 * data pipeline detail を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDataPipeline = <ThrowOnError extends boolean = false>(options: Options<GetDataPipelineData, ThrowOnError>) => (options.client ?? client).get<GetDataPipelineResponses, GetDataPipelineErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipelines/{pipelinePublicId}',
    ...options
});

/**
 * data pipeline の name / description を更新する
 *
 * data pipeline の name / description を更新する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDataPipeline = <ThrowOnError extends boolean = false>(options: Options<UpdateDataPipelineData, ThrowOnError>) => (options.client ?? client).patch<UpdateDataPipelineResponses, UpdateDataPipelineErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipelines/{pipelinePublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 未保存 draft graph の selected node まで preview する
 *
 * 未保存 draft graph の selected node まで preview する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const previewDataPipelineDraft = <ThrowOnError extends boolean = false>(options: Options<PreviewDataPipelineDraftData, ThrowOnError>) => (options.client ?? client).post<PreviewDataPipelineDraftResponses, PreviewDataPipelineDraftErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipelines/{pipelinePublicId}/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * data pipeline run history を返す
 *
 * data pipeline run history を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDataPipelineRuns = <ThrowOnError extends boolean = false>(options: Options<ListDataPipelineRunsData, ThrowOnError>) => (options.client ?? client).get<ListDataPipelineRunsResponses, ListDataPipelineRunsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipelines/{pipelinePublicId}/runs',
    ...options
});

/**
 * data pipeline schedules を返す
 *
 * data pipeline schedules を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDataPipelineSchedules = <ThrowOnError extends boolean = false>(options: Options<ListDataPipelineSchedulesData, ThrowOnError>) => (options.client ?? client).get<ListDataPipelineSchedulesResponses, ListDataPipelineSchedulesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipelines/{pipelinePublicId}/schedules',
    ...options
});

/**
 * data pipeline schedule を作成する
 *
 * data pipeline schedule を作成する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDataPipelineSchedule = <ThrowOnError extends boolean = false>(options: Options<CreateDataPipelineScheduleData, ThrowOnError>) => (options.client ?? client).post<CreateDataPipelineScheduleResponses, CreateDataPipelineScheduleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipelines/{pipelinePublicId}/schedules',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * data pipeline draft graph を保存する
 *
 * data pipeline draft graph を保存する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const saveDataPipelineVersion = <ThrowOnError extends boolean = false>(options: Options<SaveDataPipelineVersionData, ThrowOnError>) => (options.client ?? client).post<SaveDataPipelineVersionResponses, SaveDataPipelineVersionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/data-pipelines/{pipelinePublicId}/versions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * active tenant の dataset query job 一覧を返す
 *
 * active tenant の dataset query job 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetQueryJobs = <ThrowOnError extends boolean = false>(options?: Options<ListDatasetQueryJobsData, ThrowOnError>) => (options?.client ?? client).get<ListDatasetQueryJobsResponses, ListDatasetQueryJobsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-query-jobs',
    ...options
});

/**
 * active tenant の dataset SQL query job を作成する
 *
 * dataset に対する SQL query job を作成します。query は tenant-scoped work table に限定して実行されます。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDatasetQueryJob = <ThrowOnError extends boolean = false>(options: Options<CreateDatasetQueryJobData, ThrowOnError>) => (options.client ?? client).post<CreateDatasetQueryJobResponses, CreateDatasetQueryJobErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-query-jobs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * active tenant の dataset query job detail を返す
 *
 * active tenant の dataset query job detail を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDatasetQueryJob = <ThrowOnError extends boolean = false>(options: Options<GetDatasetQueryJobData, ThrowOnError>) => (options.client ?? client).get<GetDatasetQueryJobResponses, GetDatasetQueryJobErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-query-jobs/{queryJobPublicId}',
    ...options
});

/**
 * active tenant の dataset query job lineage graph を返す
 *
 * active tenant の dataset query job lineage graph を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDatasetQueryJobLineage = <ThrowOnError extends boolean = false>(options: Options<GetDatasetQueryJobLineageData, ThrowOnError>) => (options.client ?? client).get<GetDatasetQueryJobLineageResponses, GetDatasetQueryJobLineageErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-query-jobs/{queryJobPublicId}/lineage',
    ...options
});

/**
 * dataset query job の SQL を parse して lineage draft を作成する
 *
 * dataset query job の SQL を parse して lineage draft を作成する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const parseDatasetQueryJobLineage = <ThrowOnError extends boolean = false>(options: Options<ParseDatasetQueryJobLineageData, ThrowOnError>) => (options.client ?? client).post<ParseDatasetQueryJobLineageResponses, ParseDatasetQueryJobLineageErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-query-jobs/{queryJobPublicId}/lineage/parse',
    ...options
});

/**
 * dataset query job の lineage parse run 一覧を返す
 *
 * dataset query job の lineage parse run 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetQueryJobLineageParseRuns = <ThrowOnError extends boolean = false>(options: Options<ListDatasetQueryJobLineageParseRunsData, ThrowOnError>) => (options.client ?? client).get<ListDatasetQueryJobLineageParseRunsResponses, ListDatasetQueryJobLineageParseRunsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-query-jobs/{queryJobPublicId}/lineage/parse-runs',
    ...options
});

/**
 * Dataset に取り込める Drive CSV file 一覧を返す
 *
 * Dataset に取り込める Drive CSV file 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetSourceFiles = <ThrowOnError extends boolean = false>(options?: Options<ListDatasetSourceFilesData, ThrowOnError>) => (options?.client ?? client).get<ListDatasetSourceFilesResponses, ListDatasetSourceFilesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-source-files',
    ...options
});

/**
 * work table export schedule を更新する
 *
 * work table export schedule を更新する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDatasetWorkTableExportSchedule = <ThrowOnError extends boolean = false>(options: Options<UpdateDatasetWorkTableExportScheduleData, ThrowOnError>) => (options.client ?? client).patch<UpdateDatasetWorkTableExportScheduleResponses, UpdateDatasetWorkTableExportScheduleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-table-export-schedules/{schedulePublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * work table export schedule を disable する
 *
 * work table export schedule を disable する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const disableDatasetWorkTableExportSchedule = <ThrowOnError extends boolean = false>(options: Options<DisableDatasetWorkTableExportScheduleData, ThrowOnError>) => (options.client ?? client).post<DisableDatasetWorkTableExportScheduleResponses, DisableDatasetWorkTableExportScheduleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-table-export-schedules/{schedulePublicId}/disable',
    ...options
});

/**
 * work table export detail を返す
 *
 * work table export detail を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDatasetWorkTableExport = <ThrowOnError extends boolean = false>(options: Options<GetDatasetWorkTableExportData, ThrowOnError>) => (options.client ?? client).get<GetDatasetWorkTableExportResponses, GetDatasetWorkTableExportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-table-exports/{exportPublicId}',
    ...options
});

/**
 * ready work table export を download する
 *
 * ready work table export を download する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const downloadDatasetWorkTableExport = <ThrowOnError extends boolean = false>(options: Options<DownloadDatasetWorkTableExportData, ThrowOnError>) => (options.client ?? client).get<DownloadDatasetWorkTableExportResponses, DownloadDatasetWorkTableExportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-table-exports/{exportPublicId}/download',
    ...options
});

/**
 * active tenant の ClickHouse work table 一覧を返す
 *
 * active tenant の ClickHouse work table 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetWorkTables = <ThrowOnError extends boolean = false>(options?: Options<ListDatasetWorkTablesData, ThrowOnError>) => (options?.client ?? client).get<ListDatasetWorkTablesResponses, ListDatasetWorkTablesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables',
    ...options
});

/**
 * active tenant の ClickHouse work table detail を返す
 *
 * active tenant の ClickHouse work table detail を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDatasetWorkTable = <ThrowOnError extends boolean = false>(options: Options<GetDatasetWorkTableData, ThrowOnError>) => (options.client ?? client).get<GetDatasetWorkTableResponses, GetDatasetWorkTableErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/by-ref/{database}/{table}',
    ...options
});

/**
 * active tenant の ClickHouse work table preview rows を返す
 *
 * active tenant の ClickHouse work table preview rows を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDatasetWorkTablePreview = <ThrowOnError extends boolean = false>(options: Options<GetDatasetWorkTablePreviewData, ThrowOnError>) => (options.client ?? client).get<GetDatasetWorkTablePreviewResponses, GetDatasetWorkTablePreviewErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/by-ref/{database}/{table}/preview',
    ...options
});

/**
 * active tenant の ClickHouse work table を管理レコード化する
 *
 * active tenant の ClickHouse work table を管理レコード化する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const registerDatasetWorkTable = <ThrowOnError extends boolean = false>(options: Options<RegisterDatasetWorkTableData, ThrowOnError>) => (options.client ?? client).post<RegisterDatasetWorkTableResponses, RegisterDatasetWorkTableErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/register',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * managed work table を drop する
 *
 * managed work table を drop する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteDatasetWorkTable = <ThrowOnError extends boolean = false>(options: Options<DeleteDatasetWorkTableData, ThrowOnError>) => (options.client ?? client).delete<DeleteDatasetWorkTableResponses, DeleteDatasetWorkTableErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}',
    ...options
});

/**
 * managed work table detail を返す
 *
 * managed work table detail を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getManagedDatasetWorkTable = <ThrowOnError extends boolean = false>(options: Options<GetManagedDatasetWorkTableData, ThrowOnError>) => (options.client ?? client).get<GetManagedDatasetWorkTableResponses, GetManagedDatasetWorkTableErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}',
    ...options
});

/**
 * managed work table の export schedule 一覧を返す
 *
 * managed work table の export schedule 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetWorkTableExportSchedules = <ThrowOnError extends boolean = false>(options: Options<ListDatasetWorkTableExportSchedulesData, ThrowOnError>) => (options.client ?? client).get<ListDatasetWorkTableExportSchedulesResponses, ListDatasetWorkTableExportSchedulesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}/export-schedules',
    ...options
});

/**
 * managed work table の export schedule を作成する
 *
 * managed work table の export schedule を作成する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDatasetWorkTableExportSchedule = <ThrowOnError extends boolean = false>(options: Options<CreateDatasetWorkTableExportScheduleData, ThrowOnError>) => (options.client ?? client).post<CreateDatasetWorkTableExportScheduleResponses, CreateDatasetWorkTableExportScheduleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}/export-schedules',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * managed work table の export 一覧を返す
 *
 * managed work table の export 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetWorkTableExports = <ThrowOnError extends boolean = false>(options: Options<ListDatasetWorkTableExportsData, ThrowOnError>) => (options.client ?? client).get<ListDatasetWorkTableExportsResponses, ListDatasetWorkTableExportsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}/exports',
    ...options
});

/**
 * managed work table の export を request する
 *
 * managed work table の export を request する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDatasetWorkTableExport = <ThrowOnError extends boolean = false>(options: Options<CreateDatasetWorkTableExportData, ThrowOnError>) => (options.client ?? client).post<CreateDatasetWorkTableExportResponses, CreateDatasetWorkTableExportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}/exports',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * managed work table を Gold data mart として publish する
 *
 * managed work table を Gold data mart として publish する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDatasetGoldPublication = <ThrowOnError extends boolean = false>(options: Options<CreateDatasetGoldPublicationData, ThrowOnError>) => (options.client ?? client).post<CreateDatasetGoldPublicationResponses, CreateDatasetGoldPublicationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}/gold-publications',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * managed work table の lineage graph を返す
 *
 * managed work table の lineage graph を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDatasetWorkTableLineage = <ThrowOnError extends boolean = false>(options: Options<GetDatasetWorkTableLineageData, ThrowOnError>) => (options.client ?? client).get<GetDatasetWorkTableLineageResponses, GetDatasetWorkTableLineageErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}/lineage',
    ...options
});

/**
 * managed work table を dataset に紐付ける
 *
 * managed work table を dataset に紐付ける。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const linkDatasetWorkTable = <ThrowOnError extends boolean = false>(options: Options<LinkDatasetWorkTableData, ThrowOnError>) => (options.client ?? client).post<LinkDatasetWorkTableResponses, LinkDatasetWorkTableErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}/link',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * managed work table preview rows を返す
 *
 * managed work table preview rows を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getManagedDatasetWorkTablePreview = <ThrowOnError extends boolean = false>(options: Options<GetManagedDatasetWorkTablePreviewData, ThrowOnError>) => (options.client ?? client).get<GetManagedDatasetWorkTablePreviewResponses, GetManagedDatasetWorkTablePreviewErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}/preview',
    ...options
});

/**
 * managed work table を dataset 化する
 *
 * managed work table を dataset 化する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const promoteDatasetWorkTable = <ThrowOnError extends boolean = false>(options: Options<PromoteDatasetWorkTableData, ThrowOnError>) => (options.client ?? client).post<PromoteDatasetWorkTableResponses, PromoteDatasetWorkTableErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}/promote',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * managed work table を rename する
 *
 * managed work table を rename する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const renameDatasetWorkTable = <ThrowOnError extends boolean = false>(options: Options<RenameDatasetWorkTableData, ThrowOnError>) => (options.client ?? client).post<RenameDatasetWorkTableResponses, RenameDatasetWorkTableErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}/rename',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * managed work table を truncate する
 *
 * managed work table を truncate する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const truncateDatasetWorkTable = <ThrowOnError extends boolean = false>(options: Options<TruncateDatasetWorkTableData, ThrowOnError>) => (options.client ?? client).post<TruncateDatasetWorkTableResponses, TruncateDatasetWorkTableErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/dataset-work-tables/{workTablePublicId}/truncate',
    ...options
});

/**
 * active tenant の dataset 一覧を返す
 *
 * active tenant の dataset 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasets = <ThrowOnError extends boolean = false>(options?: Options<ListDatasetsData, ThrowOnError>) => (options?.client ?? client).get<ListDatasetsResponses, ListDatasetsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/datasets',
    ...options
});

/**
 * Drive CSV file から active tenant の dataset を作成する
 *
 * Drive CSV file を dataset として登録します。取り込み対象の file は active tenant の Drive 内に存在し、dataset 作成権限が必要です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDataset = <ThrowOnError extends boolean = false>(options: Options<CreateDatasetData, ThrowOnError>) => (options.client ?? client).post<CreateDatasetResponses, CreateDatasetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/datasets',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * active tenant の dataset を削除する
 *
 * active tenant の dataset を削除する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteDataset = <ThrowOnError extends boolean = false>(options: Options<DeleteDatasetData, ThrowOnError>) => (options.client ?? client).delete<DeleteDatasetResponses, DeleteDatasetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/datasets/{datasetPublicId}',
    ...options
});

/**
 * active tenant の dataset detail を返す
 *
 * active tenant の dataset detail を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDataset = <ThrowOnError extends boolean = false>(options: Options<GetDatasetData, ThrowOnError>) => (options.client ?? client).get<GetDatasetResponses, GetDatasetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/datasets/{datasetPublicId}',
    ...options
});

/**
 * active tenant の dataset lineage graph を返す
 *
 * active tenant の dataset lineage graph を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDatasetLineage = <ThrowOnError extends boolean = false>(options: Options<GetDatasetLineageData, ThrowOnError>) => (options.client ?? client).get<GetDatasetLineageResponses, GetDatasetLineageErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/datasets/{datasetPublicId}/lineage',
    ...options
});

/**
 * active tenant の dataset に紐づく query job 一覧を返す
 *
 * active tenant の dataset に紐づく query job 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetScopedQueryJobs = <ThrowOnError extends boolean = false>(options: Options<ListDatasetScopedQueryJobsData, ThrowOnError>) => (options.client ?? client).get<ListDatasetScopedQueryJobsResponses, ListDatasetScopedQueryJobsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/datasets/{datasetPublicId}/query-jobs',
    ...options
});

/**
 * active tenant の dataset に紐づく SQL query job を作成する
 *
 * active tenant の dataset に紐づく SQL query job を作成する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDatasetScopedQueryJob = <ThrowOnError extends boolean = false>(options: Options<CreateDatasetScopedQueryJobData, ThrowOnError>) => (options.client ?? client).post<CreateDatasetScopedQueryJobResponses, CreateDatasetScopedQueryJobErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/datasets/{datasetPublicId}/query-jobs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * active tenant の dataset rows を返す
 *
 * active tenant の dataset rows を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetRows = <ThrowOnError extends boolean = false>(options: Options<ListDatasetRowsData, ThrowOnError>) => (options.client ?? client).get<ListDatasetRowsResponses, ListDatasetRowsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/datasets/{datasetPublicId}/rows',
    ...options
});

/**
 * dataset の sync history を返す
 *
 * dataset の sync history を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetSyncJobs = <ThrowOnError extends boolean = false>(options: Options<ListDatasetSyncJobsData, ThrowOnError>) => (options.client ?? client).get<ListDatasetSyncJobsResponses, ListDatasetSyncJobsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/datasets/{datasetPublicId}/syncs',
    ...options
});

/**
 * work table 由来 dataset の再同期を request する
 *
 * work table 由来 dataset の再同期を request する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDatasetSyncJob = <ThrowOnError extends boolean = false>(options: Options<CreateDatasetSyncJobData, ThrowOnError>) => (options.client ?? client).post<CreateDatasetSyncJobResponses, CreateDatasetSyncJobErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/datasets/{datasetPublicId}/syncs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * dataset に紐づく managed work table 一覧を返す
 *
 * dataset に紐づく managed work table 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetScopedWorkTables = <ThrowOnError extends boolean = false>(options: Options<ListDatasetScopedWorkTablesData, ThrowOnError>) => (options.client ?? client).get<ListDatasetScopedWorkTablesResponses, ListDatasetScopedWorkTablesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/datasets/{datasetPublicId}/work-tables',
    ...options
});

/**
 * Drive mobile offline operation queue を replay する
 *
 * Drive mobile offline operation queue を replay する。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const replayDriveMobileOfflineOperations = <ThrowOnError extends boolean = false>(options: Options<ReplayDriveMobileOfflineOperationsData, ThrowOnError>) => (options.client ?? client).post<ReplayDriveMobileOfflineOperationsResponses, ReplayDriveMobileOfflineOperationsErrors, ThrowOnError>({
    url: '/api/v1/drive-mobile/offline/replay',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive sync delta を返す
 *
 * Drive sync delta を返す。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - Bearer token が必要です。token の audience、scope、role は環境設定に従って検証されます。
 */
export const getDriveSyncDelta = <ThrowOnError extends boolean = false>(options: Options<GetDriveSyncDeltaData, ThrowOnError>) => (options.client ?? client).get<GetDriveSyncDeltaResponses, GetDriveSyncDeltaErrors, ThrowOnError>({ url: '/api/v1/drive-sync/delta', ...options });

/**
 * Drive sync device を登録する
 *
 * Drive sync device を登録する。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const registerDriveSyncDevice = <ThrowOnError extends boolean = false>(options: Options<RegisterDriveSyncDeviceData, ThrowOnError>) => (options.client ?? client).post<RegisterDriveSyncDeviceResponses, RegisterDriveSyncDeviceErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive-sync/devices/register',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive sync device を revoke する
 *
 * Drive sync device を revoke する。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const revokeDriveSyncDevice = <ThrowOnError extends boolean = false>(options: Options<RevokeDriveSyncDeviceData, ThrowOnError>) => (options.client ?? client).post<RevokeDriveSyncDeviceResponses, RevokeDriveSyncDeviceErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive-sync/devices/{devicePublicId}/revoke',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive items を ZIP archive として download する
 *
 * Drive items を ZIP archive として download する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const downloadDriveArchive = <ThrowOnError extends boolean = false>(options: Options<DownloadDriveArchiveData, ThrowOnError>) => (options.client ?? client).post<DownloadDriveArchiveResponses, DownloadDriveArchiveErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/downloads/archive',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive E2EE public key を登録する
 *
 * Drive E2EE public key を登録する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveE2EeUserKey = <ThrowOnError extends boolean = false>(options: Options<CreateDriveE2EeUserKeyData, ThrowOnError>) => (options.client ?? client).post<CreateDriveE2EeUserKeyResponses, CreateDriveE2EeUserKeyErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/e2ee/user-keys',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file を削除する
 *
 * Drive file を削除する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteDriveFile = <ThrowOnError extends boolean = false>(options: Options<DeleteDriveFileData, ThrowOnError>) => (options.client ?? client).delete<DeleteDriveFileResponses, DeleteDriveFileErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}',
    ...options
});

/**
 * Drive file metadata を返す
 *
 * Drive file metadata を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDriveFile = <ThrowOnError extends boolean = false>(options: Options<GetDriveFileData, ThrowOnError>) => (options.client ?? client).get<GetDriveFileResponses, GetDriveFileErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}',
    ...options
});

/**
 * Drive file metadata を更新する
 *
 * Drive file の filename、description、tags、parent folder を更新します。ファイル本体の差し替えではなく metadata と配置の更新です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDriveFile = <ThrowOnError extends boolean = false>(options: Options<UpdateDriveFileData, ThrowOnError>) => (options.client ?? client).patch<UpdateDriveFileResponses, UpdateDriveFileErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file activity を返す
 *
 * Drive file activity を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveFileActivity = <ThrowOnError extends boolean = false>(options: Options<ListDriveFileActivityData, ThrowOnError>) => (options.client ?? client).get<ListDriveFileActivityResponses, ListDriveFileActivityErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/activity',
    ...options
});

/**
 * Drive AI classifications を返す
 *
 * Drive AI classifications を返す。
 *
 * Drive file の OCR、product extraction、AI summary/classification に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveAiClassifications = <ThrowOnError extends boolean = false>(options: Options<ListDriveAiClassificationsData, ThrowOnError>) => (options.client ?? client).get<ListDriveAiClassificationsResponses, ListDriveAiClassificationsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/ai/classifications',
    ...options
});

/**
 * Drive AI job を作成する
 *
 * Drive file に対する AI processing job を作成します。summary や classification のような後続取得 API と組み合わせて使います。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveAiJob = <ThrowOnError extends boolean = false>(options: Options<CreateDriveAiJobData, ThrowOnError>) => (options.client ?? client).post<CreateDriveAiJobResponses, CreateDriveAiJobErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/ai/jobs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive AI summary を返す
 *
 * Drive AI summary を返す。
 *
 * Drive file の OCR、product extraction、AI summary/classification に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDriveAiSummary = <ThrowOnError extends boolean = false>(options: Options<GetDriveAiSummaryData, ThrowOnError>) => (options.client ?? client).get<GetDriveAiSummaryResponses, GetDriveAiSummaryErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/ai/summary',
    ...options
});

/**
 * Drive file を copy する
 *
 * Drive file を copy する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const copyDriveFile = <ThrowOnError extends boolean = false>(options: Options<CopyDriveFileData, ThrowOnError>) => (options.client ?? client).post<CopyDriveFileResponses, CopyDriveFileErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/copy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive E2EE actor envelope を返す
 *
 * Drive E2EE actor envelope を返す。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDriveE2EeEnvelope = <ThrowOnError extends boolean = false>(options: Options<GetDriveE2EeEnvelopeData, ThrowOnError>) => (options.client ?? client).get<GetDriveE2EeEnvelopeResponses, GetDriveE2EeEnvelopeErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/e2ee/envelope',
    ...options
});

/**
 * Drive E2EE recipient envelope を作成する
 *
 * Drive E2EE recipient envelope を作成する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveE2EeEnvelope = <ThrowOnError extends boolean = false>(options: Options<CreateDriveE2EeEnvelopeData, ThrowOnError>) => (options.client ?? client).post<CreateDriveE2EeEnvelopeResponses, CreateDriveE2EeEnvelopeErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/e2ee/envelopes',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive E2EE recipient envelope を revoke する
 *
 * Drive E2EE recipient envelope を revoke する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const revokeDriveE2EeEnvelope = <ThrowOnError extends boolean = false>(options: Options<RevokeDriveE2EeEnvelopeData, ThrowOnError>) => (options.client ?? client).delete<RevokeDriveE2EeEnvelopeResponses, RevokeDriveE2EeEnvelopeErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/e2ee/envelopes/{recipientUserPublicId}',
    ...options
});

/**
 * Drive E2EE file key metadata を作成する
 *
 * Drive E2EE file key metadata を作成する。
 *
 * Drive の encryption、HSM、legal hold、eDiscovery、clean room、gateway を扱う compliance endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveE2EeFileKey = <ThrowOnError extends boolean = false>(options: Options<CreateDriveE2EeFileKeyData, ThrowOnError>) => (options.client ?? client).post<CreateDriveE2EeFileKeyResponses, CreateDriveE2EeFileKeyErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/e2ee/keys',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file edit session を開始する
 *
 * Drive file edit session を開始する。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const startDriveEditSession = <ThrowOnError extends boolean = false>(options: Options<StartDriveEditSessionData, ThrowOnError>) => (options.client ?? client).post<StartDriveEditSessionResponses, StartDriveEditSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/edit-sessions',
    ...options
});

/**
 * Drive edit session を終了する
 *
 * Drive edit session を終了する。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const endDriveEditSession = <ThrowOnError extends boolean = false>(options: Options<EndDriveEditSessionData, ThrowOnError>) => (options.client ?? client).delete<EndDriveEditSessionResponses, EndDriveEditSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/edit-sessions/{sessionPublicId}',
    ...options
});

/**
 * Drive edit session content を保存する
 *
 * Drive edit session content を保存する。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const saveDriveEditSessionContent = <ThrowOnError extends boolean = false>(options: Options<SaveDriveEditSessionContentData, ThrowOnError>) => (options.client ?? client).put<SaveDriveEditSessionContentResponses, SaveDriveEditSessionContentErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/edit-sessions/{sessionPublicId}/content',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file edit session lease を延長する
 *
 * Drive file edit session lease を延長する。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const heartbeatDriveEditSession = <ThrowOnError extends boolean = false>(options: Options<HeartbeatDriveEditSessionData, ThrowOnError>) => (options.client ?? client).post<HeartbeatDriveEditSessionResponses, HeartbeatDriveEditSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/edit-sessions/{sessionPublicId}/heartbeat',
    ...options
});

/**
 * Drive file inheritance を更新する
 *
 * Drive file inheritance を更新する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDriveFileInheritance = <ThrowOnError extends boolean = false>(options: Options<UpdateDriveFileInheritanceData, ThrowOnError>) => (options.client ?? client).patch<UpdateDriveFileInheritanceResponses, UpdateDriveFileInheritanceErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/inheritance',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file external share invitation を作成する
 *
 * Drive file external share invitation を作成する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveFileShareInvitation = <ThrowOnError extends boolean = false>(options: Options<CreateDriveFileShareInvitationData, ThrowOnError>) => (options.client ?? client).post<CreateDriveFileShareInvitationResponses, CreateDriveFileShareInvitationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/invitations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file document manifest を返す
 *
 * Drive file document manifest を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDriveFileManifest = <ThrowOnError extends boolean = false>(options: Options<GetDriveFileManifestData, ThrowOnError>) => (options.client ?? client).get<GetDriveFileManifestResponses, GetDriveFileManifestErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/manifest',
    ...options
});

/**
 * Drive file document manifest を再解析する
 *
 * Drive file document manifest を再解析する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const refreshDriveFileManifest = <ThrowOnError extends boolean = false>(options: Options<RefreshDriveFileManifestData, ThrowOnError>) => (options.client ?? client).post<RefreshDriveFileManifestResponses, RefreshDriveFileManifestErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/manifest/refresh',
    ...options
});

/**
 * Drive file OCR result を返す
 *
 * Drive file OCR result を返す。
 *
 * Drive file の OCR、product extraction、AI summary/classification に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDriveOcr = <ThrowOnError extends boolean = false>(options: Options<GetDriveOcrData, ThrowOnError>) => (options.client ?? client).get<GetDriveOcrResponses, GetDriveOcrErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/ocr',
    ...options
});

/**
 * Drive file OCR job を作成する
 *
 * Drive file の OCR job を作成します。画像/PDF から抽出された text は OCR result API で取得します。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const createDriveOcrJob = <ThrowOnError extends boolean = false>(options: Options<CreateDriveOcrJobData, ThrowOnError>) => (options.client ?? client).post<CreateDriveOcrJobResponses, CreateDriveOcrJobErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/ocr/jobs',
    ...options
});

/**
 * Drive Office edit session を作成する
 *
 * Drive Office edit session を作成する。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveOfficeSession = <ThrowOnError extends boolean = false>(options: Options<CreateDriveOfficeSessionData, ThrowOnError>) => (options.client ?? client).post<CreateDriveOfficeSessionResponses, CreateDriveOfficeSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/office/sessions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file owner を移譲する
 *
 * Drive file owner を移譲する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const transferDriveFileOwner = <ThrowOnError extends boolean = false>(options: Options<TransferDriveFileOwnerData, ThrowOnError>) => (options.client ?? client).post<TransferDriveFileOwnerResponses, TransferDriveFileOwnerErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/owner-transfer',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file を完全削除する
 *
 * Drive file を完全削除する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const permanentlyDeleteDriveFile = <ThrowOnError extends boolean = false>(options: Options<PermanentlyDeleteDriveFileData, ThrowOnError>) => (options.client ?? client).delete<PermanentlyDeleteDriveFileResponses, PermanentlyDeleteDriveFileErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/permanent',
    ...options
});

/**
 * Drive file permissions を返す
 *
 * Drive file permissions を返す。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDriveFilePermissions = <ThrowOnError extends boolean = false>(options: Options<GetDriveFilePermissionsData, ThrowOnError>) => (options.client ?? client).get<GetDriveFilePermissionsResponses, GetDriveFilePermissionsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/permissions',
    ...options
});

/**
 * Drive file product extraction items を返す
 *
 * Drive file product extraction items を返す。
 *
 * Drive file の OCR、product extraction、AI summary/classification に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveProductExtractions = <ThrowOnError extends boolean = false>(options: Options<ListDriveProductExtractionsData, ThrowOnError>) => (options.client ?? client).get<ListDriveProductExtractionsResponses, ListDriveProductExtractionsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/product-extractions',
    ...options
});

/**
 * Drive file product extraction job を作成する
 *
 * OCR 結果をもとに product extraction job を作成します。抽出結果は product extraction list API で確認します。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const createDriveProductExtractionJob = <ThrowOnError extends boolean = false>(options: Options<CreateDriveProductExtractionJobData, ThrowOnError>) => (options.client ?? client).post<CreateDriveProductExtractionJobResponses, CreateDriveProductExtractionJobErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/product-extractions/jobs',
    ...options
});

/**
 * Drive file を trash から復元する
 *
 * Drive file を trash から復元する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const restoreDriveFile = <ThrowOnError extends boolean = false>(options: Options<RestoreDriveFileData, ThrowOnError>) => (options.client ?? client).post<RestoreDriveFileResponses, RestoreDriveFileErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/restore',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file share link を作成する
 *
 * Drive file に public share link を作成します。password や expiry を設定すると、link access の安全性を高められます。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveFileShareLink = <ThrowOnError extends boolean = false>(options: Options<CreateDriveFileShareLinkData, ThrowOnError>) => (options.client ?? client).post<CreateDriveFileShareLinkResponses, CreateDriveFileShareLinkErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/share-links',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file share を作成する
 *
 * Drive file share を作成する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveFileShare = <ThrowOnError extends boolean = false>(options: Options<CreateDriveFileShareData, ThrowOnError>) => (options.client ?? client).post<CreateDriveFileShareResponses, CreateDriveFileShareErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/shares',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file share を解除する
 *
 * Drive file share を解除する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteDriveFileShare = <ThrowOnError extends boolean = false>(options: Options<DeleteDriveFileShareData, ThrowOnError>) => (options.client ?? client).delete<DeleteDriveFileShareResponses, DeleteDriveFileShareErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/shares/{sharePublicId}',
    ...options
});

/**
 * Drive file share role を更新する
 *
 * Drive file share role を更新する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDriveFileShare = <ThrowOnError extends boolean = false>(options: Options<UpdateDriveFileShareData, ThrowOnError>) => (options.client ?? client).patch<UpdateDriveFileShareResponses, UpdateDriveFileShareErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/shares/{sharePublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive file の star を外す
 *
 * Drive file の star を外す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const unstarDriveFile = <ThrowOnError extends boolean = false>(options: Options<UnstarDriveFileData, ThrowOnError>) => (options.client ?? client).delete<UnstarDriveFileResponses, UnstarDriveFileErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/star',
    ...options
});

/**
 * Drive file に star を付ける
 *
 * Drive file に star を付ける。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const starDriveFile = <ThrowOnError extends boolean = false>(options: Options<StarDriveFileData, ThrowOnError>) => (options.client ?? client).post<StarDriveFileResponses, StarDriveFileErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/files/{filePublicId}/star',
    ...options
});

/**
 * Drive folder tree を返す
 *
 * Drive folder tree を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDriveFolderTree = <ThrowOnError extends boolean = false>(options?: Options<GetDriveFolderTreeData, ThrowOnError>) => (options?.client ?? client).get<GetDriveFolderTreeResponses, GetDriveFolderTreeErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folder-tree',
    ...options
});

/**
 * Drive folder を作成する
 *
 * Drive に folder を作成します。`parentFolderPublicId` を省略すると root 配下に作成し、`workspacePublicId` を指定すると workspace 配下で管理します。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveFolder = <ThrowOnError extends boolean = false>(options: Options<CreateDriveFolderData, ThrowOnError>) => (options.client ?? client).post<CreateDriveFolderResponses, CreateDriveFolderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive folder を削除する
 *
 * Drive folder を削除する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteDriveFolder = <ThrowOnError extends boolean = false>(options: Options<DeleteDriveFolderData, ThrowOnError>) => (options.client ?? client).delete<DeleteDriveFolderResponses, DeleteDriveFolderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}',
    ...options
});

/**
 * Drive folder detail を返す
 *
 * Drive folder detail を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDriveFolder = <ThrowOnError extends boolean = false>(options: Options<GetDriveFolderData, ThrowOnError>) => (options.client ?? client).get<GetDriveFolderResponses, GetDriveFolderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}',
    ...options
});

/**
 * Drive folder を更新する
 *
 * Drive folder を更新する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDriveFolder = <ThrowOnError extends boolean = false>(options: Options<UpdateDriveFolderData, ThrowOnError>) => (options.client ?? client).patch<UpdateDriveFolderResponses, UpdateDriveFolderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive folder activity を返す
 *
 * Drive folder activity を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveFolderActivity = <ThrowOnError extends boolean = false>(options: Options<ListDriveFolderActivityData, ThrowOnError>) => (options.client ?? client).get<ListDriveFolderActivityResponses, ListDriveFolderActivityErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/activity',
    ...options
});

/**
 * Drive folder children を返す
 *
 * Drive folder children を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveFolderChildren = <ThrowOnError extends boolean = false>(options: Options<ListDriveFolderChildrenData, ThrowOnError>) => (options.client ?? client).get<ListDriveFolderChildrenResponses, ListDriveFolderChildrenErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/children',
    ...options
});

/**
 * Drive folder を copy する
 *
 * Drive folder を copy する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const copyDriveFolder = <ThrowOnError extends boolean = false>(options: Options<CopyDriveFolderData, ThrowOnError>) => (options.client ?? client).post<CopyDriveFolderResponses, CopyDriveFolderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/copy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive folder inheritance を更新する
 *
 * Drive folder inheritance を更新する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDriveFolderInheritance = <ThrowOnError extends boolean = false>(options: Options<UpdateDriveFolderInheritanceData, ThrowOnError>) => (options.client ?? client).patch<UpdateDriveFolderInheritanceResponses, UpdateDriveFolderInheritanceErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/inheritance',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive folder external share invitation を作成する
 *
 * Drive folder external share invitation を作成する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveFolderShareInvitation = <ThrowOnError extends boolean = false>(options: Options<CreateDriveFolderShareInvitationData, ThrowOnError>) => (options.client ?? client).post<CreateDriveFolderShareInvitationResponses, CreateDriveFolderShareInvitationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/invitations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive folder owner を移譲する
 *
 * Drive folder owner を移譲する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const transferDriveFolderOwner = <ThrowOnError extends boolean = false>(options: Options<TransferDriveFolderOwnerData, ThrowOnError>) => (options.client ?? client).post<TransferDriveFolderOwnerResponses, TransferDriveFolderOwnerErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/owner-transfer',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive folder を完全削除する
 *
 * Drive folder を完全削除する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const permanentlyDeleteDriveFolder = <ThrowOnError extends boolean = false>(options: Options<PermanentlyDeleteDriveFolderData, ThrowOnError>) => (options.client ?? client).delete<PermanentlyDeleteDriveFolderResponses, PermanentlyDeleteDriveFolderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/permanent',
    ...options
});

/**
 * Drive folder permissions を返す
 *
 * Drive folder permissions を返す。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDriveFolderPermissions = <ThrowOnError extends boolean = false>(options: Options<GetDriveFolderPermissionsData, ThrowOnError>) => (options.client ?? client).get<GetDriveFolderPermissionsResponses, GetDriveFolderPermissionsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/permissions',
    ...options
});

/**
 * Drive folder を trash から復元する
 *
 * Drive folder を trash から復元する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const restoreDriveFolder = <ThrowOnError extends boolean = false>(options: Options<RestoreDriveFolderData, ThrowOnError>) => (options.client ?? client).post<RestoreDriveFolderResponses, RestoreDriveFolderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/restore',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive folder share link を作成する
 *
 * Drive folder share link を作成する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveFolderShareLink = <ThrowOnError extends boolean = false>(options: Options<CreateDriveFolderShareLinkData, ThrowOnError>) => (options.client ?? client).post<CreateDriveFolderShareLinkResponses, CreateDriveFolderShareLinkErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/share-links',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive folder share を作成する
 *
 * Drive folder share を作成する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveFolderShare = <ThrowOnError extends boolean = false>(options: Options<CreateDriveFolderShareData, ThrowOnError>) => (options.client ?? client).post<CreateDriveFolderShareResponses, CreateDriveFolderShareErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/shares',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive folder share を解除する
 *
 * Drive folder share を解除する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteDriveFolderShare = <ThrowOnError extends boolean = false>(options: Options<DeleteDriveFolderShareData, ThrowOnError>) => (options.client ?? client).delete<DeleteDriveFolderShareResponses, DeleteDriveFolderShareErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/shares/{sharePublicId}',
    ...options
});

/**
 * Drive folder share role を更新する
 *
 * Drive folder share role を更新する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDriveFolderShare = <ThrowOnError extends boolean = false>(options: Options<UpdateDriveFolderShareData, ThrowOnError>) => (options.client ?? client).patch<UpdateDriveFolderShareResponses, UpdateDriveFolderShareErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/shares/{sharePublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive folder の star を外す
 *
 * Drive folder の star を外す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const unstarDriveFolder = <ThrowOnError extends boolean = false>(options: Options<UnstarDriveFolderData, ThrowOnError>) => (options.client ?? client).delete<UnstarDriveFolderResponses, UnstarDriveFolderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/star',
    ...options
});

/**
 * Drive folder に star を付ける
 *
 * Drive folder に star を付ける。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const starDriveFolder = <ThrowOnError extends boolean = false>(options: Options<StarDriveFolderData, ThrowOnError>) => (options.client ?? client).post<StarDriveFolderResponses, StarDriveFolderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/folders/{folderPublicId}/star',
    ...options
});

/**
 * Drive groups を返す
 *
 * Drive groups を返す。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveGroups = <ThrowOnError extends boolean = false>(options?: Options<ListDriveGroupsData, ThrowOnError>) => (options?.client ?? client).get<ListDriveGroupsResponses, ListDriveGroupsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/groups',
    ...options
});

/**
 * Drive group を作成する
 *
 * Drive group を作成する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveGroup = <ThrowOnError extends boolean = false>(options: Options<CreateDriveGroupData, ThrowOnError>) => (options.client ?? client).post<CreateDriveGroupResponses, CreateDriveGroupErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/groups',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive group を削除する
 *
 * Drive group を削除する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteDriveGroup = <ThrowOnError extends boolean = false>(options: Options<DeleteDriveGroupData, ThrowOnError>) => (options.client ?? client).delete<DeleteDriveGroupResponses, DeleteDriveGroupErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/groups/{groupPublicId}',
    ...options
});

/**
 * Drive group detail を返す
 *
 * Drive group detail を返す。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDriveGroup = <ThrowOnError extends boolean = false>(options: Options<GetDriveGroupData, ThrowOnError>) => (options.client ?? client).get<GetDriveGroupResponses, GetDriveGroupErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/groups/{groupPublicId}',
    ...options
});

/**
 * Drive group を更新する
 *
 * Drive group を更新する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDriveGroup = <ThrowOnError extends boolean = false>(options: Options<UpdateDriveGroupData, ThrowOnError>) => (options.client ?? client).patch<UpdateDriveGroupResponses, UpdateDriveGroupErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/groups/{groupPublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive group member を追加する
 *
 * Drive group member を追加する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const addDriveGroupMember = <ThrowOnError extends boolean = false>(options: Options<AddDriveGroupMemberData, ThrowOnError>) => (options.client ?? client).post<AddDriveGroupMemberResponses, AddDriveGroupMemberErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/groups/{groupPublicId}/members',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive group member を削除する
 *
 * Drive group member を削除する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteDriveGroupMember = <ThrowOnError extends boolean = false>(options: Options<DeleteDriveGroupMemberData, ThrowOnError>) => (options.client ?? client).delete<DeleteDriveGroupMemberResponses, DeleteDriveGroupMemberErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/groups/{groupPublicId}/members/{userPublicId}',
    ...options
});

/**
 * ログイン user 宛の Drive invitation を返す
 *
 * ログイン user 宛の Drive invitation を返す。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveShareInvitations = <ThrowOnError extends boolean = false>(options?: Options<ListDriveShareInvitationsData, ThrowOnError>) => (options?.client ?? client).get<ListDriveShareInvitationsResponses, ListDriveShareInvitationsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/invitations',
    ...options
});

/**
 * Drive invitation を受諾する
 *
 * Drive invitation を受諾する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const acceptDriveShareInvitation = <ThrowOnError extends boolean = false>(options: Options<AcceptDriveShareInvitationData, ThrowOnError>) => (options.client ?? client).post<AcceptDriveShareInvitationResponses, AcceptDriveShareInvitationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/invitations/{invitationPublicId}/accept',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive invitation を revoke する
 *
 * Drive invitation を revoke する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const revokeDriveShareInvitation = <ThrowOnError extends boolean = false>(options: Options<RevokeDriveShareInvitationData, ThrowOnError>) => (options.client ?? client).post<RevokeDriveShareInvitationResponses, RevokeDriveShareInvitationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/invitations/{invitationPublicId}/revoke',
    ...options
});

/**
 * Drive item 一覧を返す
 *
 * Drive item 一覧を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveItems = <ThrowOnError extends boolean = false>(options?: Options<ListDriveItemsData, ThrowOnError>) => (options?.client ?? client).get<ListDriveItemsResponses, ListDriveItemsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/items',
    ...options
});

/**
 * Drive marketplace catalog を返す
 *
 * Drive marketplace catalog を返す。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveMarketplaceApps = <ThrowOnError extends boolean = false>(options?: Options<ListDriveMarketplaceAppsData, ThrowOnError>) => (options?.client ?? client).get<ListDriveMarketplaceAppsResponses, ListDriveMarketplaceAppsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/marketplace/apps',
    ...options
});

/**
 * Drive Office edit session を revoke する
 *
 * Drive Office edit session を revoke する。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const revokeDriveOfficeSession = <ThrowOnError extends boolean = false>(options: Options<RevokeDriveOfficeSessionData, ThrowOnError>) => (options.client ?? client).delete<RevokeDriveOfficeSessionResponses, RevokeDriveOfficeSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/office/sessions/{sessionPublicId}',
    ...options
});

/**
 * Drive content を permission-filtered RAG で回答する
 *
 * Drive content を permission-filtered RAG で回答する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const queryDriveRag = <ThrowOnError extends boolean = false>(options: Options<QueryDriveRagData, ThrowOnError>) => (options.client ?? client).post<QueryDriveRagResponses, QueryDriveRagErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/rag/query',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Recent Drive item を返す
 *
 * Recent Drive item を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveRecent = <ThrowOnError extends boolean = false>(options?: Options<ListDriveRecentData, ThrowOnError>) => (options?.client ?? client).get<ListDriveRecentResponses, ListDriveRecentErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/recent',
    ...options
});

/**
 * Drive item を検索する
 *
 * Drive item を検索する。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const searchDriveItems = <ThrowOnError extends boolean = false>(options?: Options<SearchDriveItemsData, ThrowOnError>) => (options?.client ?? client).get<SearchDriveItemsResponses, SearchDriveItemsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/search',
    ...options
});

/**
 * Drive content index 付き検索結果を返す
 *
 * Drive content index 付き検索結果を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const searchDriveDocuments = <ThrowOnError extends boolean = false>(options?: Options<SearchDriveDocumentsData, ThrowOnError>) => (options?.client ?? client).get<SearchDriveDocumentsResponses, SearchDriveDocumentsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/search/documents',
    ...options
});

/**
 * Drive share link を無効化する
 *
 * Drive share link を無効化する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteDriveShareLink = <ThrowOnError extends boolean = false>(options: Options<DeleteDriveShareLinkData, ThrowOnError>) => (options.client ?? client).delete<DeleteDriveShareLinkResponses, DeleteDriveShareLinkErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/share-links/{shareLinkPublicId}',
    ...options
});

/**
 * Drive share link を更新する
 *
 * Drive share link を更新する。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDriveShareLink = <ThrowOnError extends boolean = false>(options: Options<UpdateDriveShareLinkData, ThrowOnError>) => (options.client ?? client).patch<UpdateDriveShareLinkResponses, UpdateDriveShareLinkErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/share-links/{shareLinkPublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive share target candidates を返す
 *
 * Drive share target candidates を返す。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveShareTargets = <ThrowOnError extends boolean = false>(options?: Options<ListDriveShareTargetsData, ThrowOnError>) => (options?.client ?? client).get<ListDriveShareTargetsResponses, ListDriveShareTargetsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/share-targets',
    ...options
});

/**
 * 自分に共有された Drive item を返す
 *
 * 自分に共有された Drive item を返す。
 *
 * Drive resource の共有、権限、share link、招待、group を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveSharedWithMe = <ThrowOnError extends boolean = false>(options?: Options<ListDriveSharedWithMeData, ThrowOnError>) => (options?.client ?? client).get<ListDriveSharedWithMeResponses, ListDriveSharedWithMeErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/shared-with-me',
    ...options
});

/**
 * Starred Drive item を返す
 *
 * Starred Drive item を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveStarred = <ThrowOnError extends boolean = false>(options?: Options<ListDriveStarredData, ThrowOnError>) => (options?.client ?? client).get<ListDriveStarredResponses, ListDriveStarredErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/starred',
    ...options
});

/**
 * Drive storage usage を返す
 *
 * Drive storage usage を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDriveStorageUsage = <ThrowOnError extends boolean = false>(options?: Options<GetDriveStorageUsageData, ThrowOnError>) => (options?.client ?? client).get<GetDriveStorageUsageResponses, GetDriveStorageUsageErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/storage',
    ...options
});

/**
 * Drive trash item 一覧を返す
 *
 * Drive trash item 一覧を返す。
 *
 * Drive の file/folder metadata、list、search、copy、trash などの基本操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveTrashItems = <ThrowOnError extends boolean = false>(options?: Options<ListDriveTrashItemsData, ThrowOnError>) => (options?.client ?? client).get<ListDriveTrashItemsResponses, ListDriveTrashItemsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/trash',
    ...options
});

/**
 * Drive workspace 一覧を返す
 *
 * Drive workspace 一覧を返す。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDriveWorkspaces = <ThrowOnError extends boolean = false>(options?: Options<ListDriveWorkspacesData, ThrowOnError>) => (options?.client ?? client).get<ListDriveWorkspacesResponses, ListDriveWorkspacesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/workspaces',
    ...options
});

/**
 * Drive workspace を作成する
 *
 * Drive workspace を作成する。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDriveWorkspace = <ThrowOnError extends boolean = false>(options: Options<CreateDriveWorkspaceData, ThrowOnError>) => (options.client ?? client).post<CreateDriveWorkspaceResponses, CreateDriveWorkspaceErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/workspaces',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drive workspace を削除する
 *
 * Drive workspace を削除する。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteDriveWorkspace = <ThrowOnError extends boolean = false>(options: Options<DeleteDriveWorkspaceData, ThrowOnError>) => (options.client ?? client).delete<DeleteDriveWorkspaceResponses, DeleteDriveWorkspaceErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/workspaces/{workspacePublicId}',
    ...options
});

/**
 * Drive workspace を更新する
 *
 * Drive workspace を更新する。
 *
 * Drive workspace、collaborative editing、office session、sync/offline workflow に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDriveWorkspace = <ThrowOnError extends boolean = false>(options: Options<UpdateDriveWorkspaceData, ThrowOnError>) => (options.client ?? client).patch<UpdateDriveWorkspaceResponses, UpdateDriveWorkspaceErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/drive/workspaces/{workspacePublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * active tenant の file metadata を返す
 *
 * active tenant の file metadata を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listFiles = <ThrowOnError extends boolean = false>(options?: Options<ListFilesData, ThrowOnError>) => (options?.client ?? client).get<ListFilesResponses, ListFilesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/files',
    ...options
});

/**
 * active tenant の file を soft delete する
 *
 * active tenant の file を soft delete する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteFile = <ThrowOnError extends boolean = false>(options: Options<DeleteFileData, ThrowOnError>) => (options.client ?? client).delete<DeleteFileResponses, DeleteFileErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/files/{filePublicId}',
    ...options
});

/**
 * active tenant の Gold data mart 一覧を返す
 *
 * active tenant の Gold data mart 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetGoldPublications = <ThrowOnError extends boolean = false>(options?: Options<ListDatasetGoldPublicationsData, ThrowOnError>) => (options?.client ?? client).get<ListDatasetGoldPublicationsResponses, ListDatasetGoldPublicationsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/gold-publications',
    ...options
});

/**
 * Gold data mart detail を返す
 *
 * Gold data mart detail を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDatasetGoldPublication = <ThrowOnError extends boolean = false>(options: Options<GetDatasetGoldPublicationData, ThrowOnError>) => (options.client ?? client).get<GetDatasetGoldPublicationResponses, GetDatasetGoldPublicationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/gold-publications/{goldPublicId}',
    ...options
});

/**
 * Gold data mart を archive する
 *
 * Gold data mart を archive する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const archiveDatasetGoldPublication = <ThrowOnError extends boolean = false>(options: Options<ArchiveDatasetGoldPublicationData, ThrowOnError>) => (options.client ?? client).post<ArchiveDatasetGoldPublicationResponses, ArchiveDatasetGoldPublicationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/gold-publications/{goldPublicId}/archive',
    ...options
});

/**
 * Gold data mart preview rows を返す
 *
 * Gold data mart preview rows を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const previewDatasetGoldPublication = <ThrowOnError extends boolean = false>(options: Options<PreviewDatasetGoldPublicationData, ThrowOnError>) => (options.client ?? client).get<PreviewDatasetGoldPublicationResponses, PreviewDatasetGoldPublicationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/gold-publications/{goldPublicId}/preview',
    ...options
});

/**
 * Gold data mart publish history を返す
 *
 * Gold data mart publish history を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetGoldPublishRuns = <ThrowOnError extends boolean = false>(options: Options<ListDatasetGoldPublishRunsData, ThrowOnError>) => (options.client ?? client).get<ListDatasetGoldPublishRunsResponses, ListDatasetGoldPublishRunsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/gold-publications/{goldPublicId}/publish-runs',
    ...options
});

/**
 * Gold data mart の full publish refresh を request する
 *
 * Gold data mart の full publish refresh を request する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const refreshDatasetGoldPublication = <ThrowOnError extends boolean = false>(options: Options<RefreshDatasetGoldPublicationData, ThrowOnError>) => (options.client ?? client).post<RefreshDatasetGoldPublicationResponses, RefreshDatasetGoldPublicationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/gold-publications/{goldPublicId}/refresh',
    ...options
});

/**
 * Gold data mart を unpublish する
 *
 * Gold data mart を unpublish する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const unpublishDatasetGoldPublication = <ThrowOnError extends boolean = false>(options: Options<UnpublishDatasetGoldPublicationData, ThrowOnError>) => (options.client ?? client).post<UnpublishDatasetGoldPublicationResponses, UnpublishDatasetGoldPublicationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/gold-publications/{goldPublicId}/unpublish',
    ...options
});

/**
 * downstream integration の接続状態を返す
 *
 * downstream integration の接続状態を返す。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listIntegrations = <ThrowOnError extends boolean = false>(options?: Options<ListIntegrationsData, ThrowOnError>) => (options?.client ?? client).get<ListIntegrationsResponses, ListIntegrationsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/integrations',
    ...options
});

/**
 * downstream integration consent callback を完了する
 *
 * downstream integration consent callback を完了する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const finishIntegrationConnect = <ThrowOnError extends boolean = false>(options: Options<FinishIntegrationConnectData, ThrowOnError>) => (options.client ?? client).get<FinishIntegrationConnectResponses, FinishIntegrationConnectErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/integrations/{resourceServer}/callback',
    ...options
});

/**
 * downstream integration consent を開始する
 *
 * downstream integration consent を開始する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const connectIntegration = <ThrowOnError extends boolean = false>(options: Options<ConnectIntegrationData, ThrowOnError>) => (options.client ?? client).get<ConnectIntegrationResponses, ConnectIntegrationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/integrations/{resourceServer}/connect',
    ...options
});

/**
 * downstream integration grant を削除する
 *
 * downstream integration grant を削除する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteIntegrationGrant = <ThrowOnError extends boolean = false>(options: Options<DeleteIntegrationGrantData, ThrowOnError>) => (options.client ?? client).delete<DeleteIntegrationGrantResponses, DeleteIntegrationGrantErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/integrations/{resourceServer}/grant',
    ...options
});

/**
 * downstream access token を backend 内で取得できるか検証する
 *
 * downstream access token を backend 内で取得できるか検証する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const verifyIntegrationAccess = <ThrowOnError extends boolean = false>(options: Options<VerifyIntegrationAccessData, ThrowOnError>) => (options.client ?? client).post<VerifyIntegrationAccessResponses, VerifyIntegrationAccessErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/integrations/{resourceServer}/verify',
    ...options
});

/**
 * tenant invitation を accept する
 *
 * tenant invitation を accept する。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const acceptTenantInvitation = <ThrowOnError extends boolean = false>(options: Options<AcceptTenantInvitationData, ThrowOnError>) => (options.client ?? client).post<AcceptTenantInvitationResponses, AcceptTenantInvitationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/invitations/accept',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * tenant invitation token の公開情報を返す
 *
 * tenant invitation token の公開情報を返す。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 */
export const resolveTenantInvitation = <ThrowOnError extends boolean = false>(options: Options<ResolveTenantInvitationData, ThrowOnError>) => (options.client ?? client).get<ResolveTenantInvitationResponses, ResolveTenantInvitationErrors, ThrowOnError>({ url: '/api/v1/invitations/resolve', ...options });

/**
 * tenant invitation の Zitadel identity 初期設定を開始する
 *
 * tenant invitation の Zitadel identity 初期設定を開始する。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const setupTenantInvitationIdentity = <ThrowOnError extends boolean = false>(options: Options<SetupTenantInvitationIdentityData, ThrowOnError>) => (options.client ?? client).post<SetupTenantInvitationIdentityResponses, SetupTenantInvitationIdentityErrors, ThrowOnError>({
    url: '/api/v1/invitations/{invitationPublicId}/setup-identity',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * system jobs を検索する
 *
 * system jobs を検索する。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listSystemJobs = <ThrowOnError extends boolean = false>(options?: Options<ListSystemJobsData, ThrowOnError>) => (options?.client ?? client).get<ListSystemJobsResponses, ListSystemJobsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/jobs',
    ...options
});

/**
 * system job detail を返す
 *
 * system job detail を返す。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getSystemJob = <ThrowOnError extends boolean = false>(options: Options<GetSystemJobData, ThrowOnError>) => (options.client ?? client).get<GetSystemJobResponses, GetSystemJobErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/jobs/{jobType}/{jobPublicId}',
    ...options
});

/**
 * system job を手動停止する
 *
 * system job を手動停止する。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const stopSystemJob = <ThrowOnError extends boolean = false>(options: Options<StopSystemJobData, ThrowOnError>) => (options.client ?? client).post<StopSystemJobResponses, StopSystemJobErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/jobs/{jobType}/{jobPublicId}/stop',
    ...options
});

/**
 * lineage change set 一覧を返す
 *
 * lineage change set 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listDatasetLineageChangeSets = <ThrowOnError extends boolean = false>(options?: Options<ListDatasetLineageChangeSetsData, ThrowOnError>) => (options?.client ?? client).get<ListDatasetLineageChangeSetsResponses, ListDatasetLineageChangeSetsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/lineage/change-sets',
    ...options
});

/**
 * lineage change set draft を作成する
 *
 * lineage change set draft を作成する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createDatasetLineageChangeSet = <ThrowOnError extends boolean = false>(options: Options<CreateDatasetLineageChangeSetData, ThrowOnError>) => (options.client ?? client).post<CreateDatasetLineageChangeSetResponses, CreateDatasetLineageChangeSetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/lineage/change-sets',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * lineage change set graph を返す
 *
 * lineage change set graph を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getDatasetLineageChangeSet = <ThrowOnError extends boolean = false>(options: Options<GetDatasetLineageChangeSetData, ThrowOnError>) => (options.client ?? client).get<GetDatasetLineageChangeSetResponses, GetDatasetLineageChangeSetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/lineage/change-sets/{changeSetPublicId}',
    ...options
});

/**
 * lineage change set draft graph を保存する
 *
 * lineage change set draft graph を保存する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateDatasetLineageChangeSetGraph = <ThrowOnError extends boolean = false>(options: Options<UpdateDatasetLineageChangeSetGraphData, ThrowOnError>) => (options.client ?? client).put<UpdateDatasetLineageChangeSetGraphResponses, UpdateDatasetLineageChangeSetGraphErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/lineage/change-sets/{changeSetPublicId}/graph',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * lineage change set draft を publish する
 *
 * lineage change set draft を publish する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const publishDatasetLineageChangeSet = <ThrowOnError extends boolean = false>(options: Options<PublishDatasetLineageChangeSetData, ThrowOnError>) => (options.client ?? client).post<PublishDatasetLineageChangeSetResponses, PublishDatasetLineageChangeSetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/lineage/change-sets/{changeSetPublicId}/publish',
    ...options
});

/**
 * lineage change set draft を reject する
 *
 * lineage change set draft を reject する。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const rejectDatasetLineageChangeSet = <ThrowOnError extends boolean = false>(options: Options<RejectDatasetLineageChangeSetData, ThrowOnError>) => (options.client ?? client).post<RejectDatasetLineageChangeSetResponses, RejectDatasetLineageChangeSetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/lineage/change-sets/{changeSetPublicId}/reject',
    ...options
});

/**
 * ログインして Cookie セッションを払い出す
 *
 * local password login が有効な環境で、email/password を検証して Cookie session を発行します。成功時は `SESSION_ID` Cookie が返り、以後の browser API はその Cookie と必要に応じて `X-CSRF-Token` を送ります。
 *
 * ### 使い方
 *
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const login = <ThrowOnError extends boolean = false>(options: Options<LoginData, ThrowOnError>) => (options.client ?? client).post<LoginResponses, LoginErrors, ThrowOnError>({
    url: '/api/v1/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * セッションを破棄する
 *
 * セッションを破棄する。
 *
 * login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const logout = <ThrowOnError extends boolean = false>(options: Options<LogoutData, ThrowOnError>) => (options.client ?? client).post<LogoutResponses, LogoutErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/logout',
    ...options
});

/**
 * machine client を list する
 *
 * machine client を list する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listMachineClients = <ThrowOnError extends boolean = false>(options?: Options<ListMachineClientsData, ThrowOnError>) => (options?.client ?? client).get<ListMachineClientsResponses, ListMachineClientsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/machine-clients',
    ...options
});

/**
 * machine client を作成する
 *
 * machine client を作成する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createMachineClient = <ThrowOnError extends boolean = false>(options: Options<CreateMachineClientData, ThrowOnError>) => (options.client ?? client).post<CreateMachineClientResponses, CreateMachineClientErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/machine-clients',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * machine client を無効化する
 *
 * machine client を無効化する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteMachineClient = <ThrowOnError extends boolean = false>(options: Options<DeleteMachineClientData, ThrowOnError>) => (options.client ?? client).delete<DeleteMachineClientResponses, DeleteMachineClientErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/machine-clients/{id}',
    ...options
});

/**
 * machine client を取得する
 *
 * machine client を取得する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getMachineClient = <ThrowOnError extends boolean = false>(options: Options<GetMachineClientData, ThrowOnError>) => (options.client ?? client).get<GetMachineClientResponses, GetMachineClientErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/machine-clients/{id}',
    ...options
});

/**
 * machine client を更新する
 *
 * machine client を更新する。
 *
 * integration、machine client、entitlement、webhook の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateMachineClient = <ThrowOnError extends boolean = false>(options: Options<UpdateMachineClientData, ThrowOnError>) => (options.client ?? client).put<UpdateMachineClientResponses, UpdateMachineClientErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/machine-clients/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * active tenant の Medallion catalog asset 一覧を返す
 *
 * active tenant の Medallion catalog asset 一覧を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listMedallionAssets = <ThrowOnError extends boolean = false>(options?: Options<ListMedallionAssetsData, ThrowOnError>) => (options?.client ?? client).get<ListMedallionAssetsResponses, ListMedallionAssetsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/medallion/assets',
    ...options
});

/**
 * Medallion catalog asset detail を返す
 *
 * Medallion catalog asset detail を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getMedallionAsset = <ThrowOnError extends boolean = false>(options: Options<GetMedallionAssetData, ThrowOnError>) => (options.client ?? client).get<GetMedallionAssetResponses, GetMedallionAssetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/medallion/assets/{assetPublicId}',
    ...options
});

/**
 * resource public ID から Medallion catalog summary を返す
 *
 * resource public ID から Medallion catalog summary を返す。
 *
 * file metadata、dataset、work table、query job、export の管理に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getMedallionResourceCatalog = <ThrowOnError extends boolean = false>(options: Options<GetMedallionResourceCatalogData, ThrowOnError>) => (options.client ?? client).get<GetMedallionResourceCatalogResponses, GetMedallionResourceCatalogErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/medallion/resources/{resourceKind}/{resourcePublicId}',
    ...options
});

/**
 * 現在の user 宛の notifications を返す
 *
 * 現在の user 宛の notifications を返す。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listNotifications = <ThrowOnError extends boolean = false>(options?: Options<ListNotificationsData, ThrowOnError>) => (options?.client ?? client).get<ListNotificationsResponses, ListNotificationsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/notifications',
    ...options
});

/**
 * selected notifications を既読にする
 *
 * selected notifications を既読にする。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const markNotificationsRead = <ThrowOnError extends boolean = false>(options: Options<MarkNotificationsReadData, ThrowOnError>) => (options.client ?? client).post<MarkNotificationsReadResponses, MarkNotificationsReadErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/notifications/read',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * current filters に一致する unread notifications を全て既読にする
 *
 * current filters に一致する unread notifications を全て既読にする。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const markAllNotificationsRead = <ThrowOnError extends boolean = false>(options: Options<MarkAllNotificationsReadData, ThrowOnError>) => (options.client ?? client).post<MarkAllNotificationsReadResponses, MarkAllNotificationsReadErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/notifications/read-all',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * notification を既読にする
 *
 * notification を既読にする。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const markNotificationRead = <ThrowOnError extends boolean = false>(options: Options<MarkNotificationReadData, ThrowOnError>) => (options.client ?? client).post<MarkNotificationReadResponses, MarkNotificationReadErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/notifications/{notificationPublicId}/read',
    ...options
});

/**
 * 現在のセッションを返す
 *
 * 現在のセッションを返す。
 *
 * login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getSession = <ThrowOnError extends boolean = false>(options?: Options<GetSessionData, ThrowOnError>) => (options?.client ?? client).get<GetSessionResponses, GetSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/session',
    ...options
});

/**
 * セッションを再発行する
 *
 * セッションを再発行する。
 *
 * login、session refresh、CSRF token 取得、認証設定確認に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const refreshSession = <ThrowOnError extends boolean = false>(options: Options<RefreshSessionData, ThrowOnError>) => (options.client ?? client).post<RefreshSessionResponses, RefreshSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/session/refresh',
    ...options
});

/**
 * 現在の session の active tenant を切り替える
 *
 * ログイン中の session に active tenant を設定します。tenant-scoped API はここで選択した tenant を既定の操作対象として扱います。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const selectTenant = <ThrowOnError extends boolean = false>(options: Options<SelectTenantData, ThrowOnError>) => (options.client ?? client).post<SelectTenantResponses, SelectTenantErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/session/tenant',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 現在の support access を返す
 *
 * 現在の support access を返す。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const getCurrentSupportAccess = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentSupportAccessData, ThrowOnError>) => (options?.client ?? client).get<GetCurrentSupportAccessResponses, GetCurrentSupportAccessErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/support/access/current',
    ...options
});

/**
 * support access を終了する
 *
 * support access を終了する。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const endSupportAccess = <ThrowOnError extends boolean = false>(options: Options<EndSupportAccessData, ThrowOnError>) => (options.client ?? client).post<EndSupportAccessResponses, EndSupportAccessErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/support/access/end',
    ...options
});

/**
 * support access を開始する
 *
 * support access を開始する。
 *
 * tenant admin 権限を持つ user が tenant lifecycle や support access を管理する endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const startSupportAccess = <ThrowOnError extends boolean = false>(options: Options<StartSupportAccessData, ThrowOnError>) => (options.client ?? client).post<StartSupportAccessResponses, StartSupportAccessErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/support/access/start',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 現在の user が利用できる tenants を返す
 *
 * 現在の user が利用できる tenants を返す。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTenants = <ThrowOnError extends boolean = false>(options?: Options<ListTenantsData, ThrowOnError>) => (options?.client ?? client).get<ListTenantsResponses, ListTenantsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/tenants',
    ...options
});

/**
 * active tenant の TODO 一覧を返す
 *
 * active tenant の TODO 一覧を返す。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const listTodos = <ThrowOnError extends boolean = false>(options?: Options<ListTodosData, ThrowOnError>) => (options?.client ?? client).get<ListTodosResponses, ListTodosErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/todos',
    ...options
});

/**
 * active tenant に TODO を作成する
 *
 * active tenant に TODO を作成する。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const createTodo = <ThrowOnError extends boolean = false>(options: Options<CreateTodoData, ThrowOnError>) => (options.client ?? client).post<CreateTodoResponses, CreateTodoErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/todos',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * active tenant の TODO を削除する
 *
 * active tenant の TODO を削除する。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 */
export const deleteTodo = <ThrowOnError extends boolean = false>(options: Options<DeleteTodoData, ThrowOnError>) => (options.client ?? client).delete<DeleteTodoResponses, DeleteTodoErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/todos/{todoPublicId}',
    ...options
});

/**
 * active tenant の TODO を更新する
 *
 * active tenant の TODO を更新する。
 *
 * active tenant と tenant 内 workspace の日常操作に使う endpoint です。
 *
 * ### 使い方
 *
 * - `SESSION_ID` Cookie が必要です。状態変更 request では `X-CSRF-Token` も送ります。
 * - request body は `application/json` を基本にし、必須 field と enum は schema に従って指定します。
 */
export const updateTodo = <ThrowOnError extends boolean = false>(options: Options<UpdateTodoData, ThrowOnError>) => (options.client ?? client).patch<UpdateTodoResponses, UpdateTodoErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'SESSION_ID',
            type: 'apiKey'
        }],
    url: '/api/v1/todos/{todoPublicId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
